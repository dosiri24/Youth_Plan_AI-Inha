# 실데이터 수집 라운드 계획

> **이 문서의 성격.** 유스플랜AI를 10명 규모 베타 테스트에서 **100~200명 실데이터 수집**으로 전환하기 위한 일회성 라운드 계획이다. 설계 계약의 SSOT는 루트 `PLAN.md`이며, 이 문서는 그 계약을 **이번 라운드에 한해 어떻게 운영하고 무엇을 바꾸는지**를 적는다. 라운드가 끝나면 계약 변경분만 `PLAN.md`에 반영하고 이 문서는 지운다(`PLAN.md` 10장 규칙).
>
> **자립성.** 이 문서는 **이 계획을 세운 대화를 전혀 모르는 새 세션이 단독으로 실행할 수 있게** 쓰여 있다. 결정과 이유, 실측값, 손댈 자리를 전부 담았다. 다만 `PLAN.md`를 대체하지는 않는다 — 0장의 읽는 순서를 따른다.
>
> **근거.** 2026-08-30 인천시 도시계획과 성시윤 주무관 인터뷰(`Interview2_workspace/성시윤 박사님 인터뷰 전사본.txt`), 같은 폴더의 시 설문 PDF, 2026-08-31 배포 상태 실측이다.
>
> **작성일.** 2026-08-31

---

## 0. 새 세션을 위한 안내

### 0.1 읽는 순서

1. **`CLAUDE.md`** — 개발 원칙과 역할 분담. 특히 YAGNI, 주석 정책, 위임 방식, UI 검증 방법.
2. **`PLAN.md`** — 설계 계약의 SSOT. 전체를 읽는다. 2장(하지 않기로 한 것)과 9장(미결)이 특히 중요하다.
3. **이 문서** — 이번 라운드에서 무엇을 바꾸고 왜 바꾸는지.
4. 필요할 때 `Interview2_workspace/` — 주무관 인터뷰 전사본, 시 설문 PDF, 지난 라운드의 수정 방향.

### 0.2 작업 방식

`CLAUDE.md`의 역할 분담을 그대로 따른다. **백엔드(`api/`)는 Codex MCP에 일회성으로 위임하고, 프론트엔드(`web/`)는 Claude 서브에이전트에 위임한다.** 위임 프롬프트는 자기완결적으로 쓰고, 브라우저 조작·서버 기동·`pnpm build`를 금지한다고 명시한다. 산출물은 PM이 diff로 직접 검토한다.

### 0.3 검증

`cd web && pnpm e2e`가 개발자 모드로 전 흐름을 헤드리스 완주하며 계약을 검사하고 `web/e2e/shots/`에 스크린샷을 남긴다. 약 45초다. **이번 라운드에서 기능을 추가하면 이 스크립트에 검사 항목을 함께 추가한다.** 백엔드는 `cd api && uv run pytest`와 `uv run ruff check`, 프론트는 `pnpm exec tsc --noEmit`과 `pnpm lint`다.

**알려진 실패 하나가 있다.** `api/tests/test_dev.py` 한 건이 저장소에 없는 `sion.json` 픽스처를 읽어 실패한다(`PLAN.md` 9.5). 게이트를 돌릴 때 이 1건은 알려진 실패로 취급한다.

### 0.4 이번 라운드에서 손대는 자리

| 작업 | 자리 |
|---|---|
| `end` 이벤트 증상 처방 | `api/app/interview.py`의 `_run` 예외 처리(`turn_failed` 로그 뒤) |
| 기기 토큰 | `api/app/main.py`의 `CreateSessionRequest`·`_submission_document`, `api/app/session.py`, `web/src/lib/api.ts`의 `createSession`, `web/src/app/page.tsx` |
| 연령 게이트 | `api/app/main.py`의 `CreateSessionRequest.birth_year` 제약, `web/src/app/page.tsx`의 `isValidBirthYear` |
| 만족도 문항 | `api/app/session.py`, `api/app/main.py`의 `_submission_document`, `web/src/components/result-screen.tsx`의 `report` 단계 |
| 경품 링크 | `web/src/components/result-screen.tsx`의 `Submitted`, `web/src/app/page.tsx` |
| 고지 문구 | `web/src/app/page.tsx` |
| 분석 팬아웃 상한 | `api/app/analysis.py`의 `supplement_missing` |
| 시설 16종 매핑 | `api/prompts/` 새 파일과 `api/app/analysis.py` |
| CSV 보강 | `web/src/app/admin/dashboard-data.ts`의 `CSV_COLUMNS`·`buildDemandCsv` |

---

## 1. 무엇을 왜 바꾸는가

### 1.1 주무관이 제시한 성공 기준

주무관은 표본 크기 자체를 성공으로 보지 않았다. 세 가지를 말했다.

- **사각지대를 채웠는가.** 기존 시민참여에서 비어 있던 10·20대를 채우는 것 자체가 가치라고 명시했다.
- **원자료가 시 스키마와 붙는가.** 대시보드보다 원자료를 중시했다. 시 데이터는 엑셀로 제공 가능하고, 붙이는 단위는 군·구(둘 다 있으면 동)다.
- **효과가 수치로 나오는가.** "AI를 썼다"는 것 자체는 효용이 아니다. **시간 단축, 비용 절감, 진입장벽 완화** 중 하나가 수치로 나와야 한다.

### 1.2 이번 라운드의 목표

**제출 200건**이 목표이고 **100건이 하한**이다. **하한에서도 논증이 성립하도록** 이중으로 잡는다. 미달했을 때 발표 논리가 통째로 무너지지 않게 하기 위해서다.

### 1.3 주 논증은 시 공식 설문과의 직접 비교다

**같은 계획을 위한 시의 공식 시민의견 수렴 창구가 지금 열려 있고, 그 실적을 우리가 직접 확인할 수 있다**(2장). 이것이 이번 라운드에서 가장 강한 논증이다. 같은 목적, 같은 계획(2045 도시기본계획), 같은 기간대에 두 창구의 실적을 나란히 놓을 수 있기 때문이다.

**진입장벽 (주무관의 세 효용 중 하나에 정확히 대응).**

- 시 설문은 **휴대폰 번호와 도로명·지번 주소를 필수**로 받고 로그인을 거친다. 우리는 **출생연도와 성별만** 받고 로그인이 없다.
- 주무관 본인이 저조한 참여의 원인을 로그인으로 진단했다("지금 들어온 사람이 80명밖에 안 되네요. 로그인을 하도록 만들어 놓았거든요").
- **참여자 수 대비**를 수집 종료 시점에 확인해 나란히 적는다(2.4).

**의견 수집량.**

- 시 설문은 1인당 주관식 1건이다. 우리는 1인당 요구 6~8건이 나온다. 200명이면 **1,200~1,600건**, 100명이어도 600~800건이다.
- 주무관은 "개별 의견 하나하나를 많이 끌어내는 것만으로도 충분히 의미가 있다"고 했다. 이 숫자가 그 기준에 직접 답한다.

**연령 사각지대.**

- **시 설문의 연령대 보기는 20대부터 시작한다. 10대를 받을 구조 자체가 없다**(2.2). 우리는 만 16세부터 받는다.
- 즉 10대 후반 응답은 **시 데이터에 존재하지 않는, 우리만 가진 자료**다. "사각지대를 채웠다"는 주장의 가장 깔끔한 증거다.

### 1.4 참여율 분모는 참고로만 쓴다

주무관은 인천 4년제 대학생을 약 5만 명으로 가정했다(신입생 연 1.2~1.3만 명 × 4개 학년). 이 분모를 쓴다. 참여자에게 소속을 묻지 않으므로(3.5) 이보다 좁은 분모는 데이터로 뒷받침되지 않는다.

환산하면 200건이 **0.40%**, 100건이 **0.20%**다.

**주 논증으로 쓰지 않는 이유가 둘이다.** 첫째, 하한에서 숫자가 약하다. 둘째, 모집 대상 기준이지 검증된 표본 구성이 아니다 — 참여자가 실제로 대학생인지 확인하지 않는다. **"인천 4년제 대학생 약 5만 명을 대상 모집단으로 삼아 N명을 모았다"고 써야 하고 "참여자 N명이 대학생이다"라고 쓰면 안 된다.**

**분모가 실제 도달 범위보다 넓다는 것은 오히려 유리하다.** 모집은 인하대 채널에 집중하므로(6.4) 5만에는 닿지 못한 사람이 포함되고, 따라서 참여율이 보수적으로 낮게 나온다. 과장이 아니라는 점에서 방어하기 쉽다.

### 1.5 이것이 "베타 테스트"가 아닌 이유

주무관은 "베타 테스트"라는 말이 참여자에게 시간을 많이 들여야 하는 일처럼 들린다고 했다. 온라인에 공개해 쉽게 들어오게 하고 응답을 많이 받는 편이 낫다는 것이다. 이 전환의 실질은 **대상자에게 검증을 부탁하는 일에서, 익명 다수에게 의견을 받는 일로 성격이 바뀌는 것**이다. 그래서 셋이 달라진다.

- 실패한 참여자를 다시 부를 수 없다. 테스터는 "다시 해주세요"가 통하지만 참여자는 그냥 나간다.
- 데이터가 시에 전달될 자료가 된다. 중복참여와 참여 자격을 "말할 수 있는" 수준으로는 다뤄야 한다.
- 수집 항목과 이용 목적 고지가 실제 사실과 맞아야 한다. 지금 고지는 "시범운영" 전제로 쓰여 있다.

---

## 2. 시 설문의 실제 계약

원본은 `Interview2_workspace/홈>시민참여>설문조사 | 인천은 소통이 가득.pdf`다. 우리 데이터가 붙어야 할 상대이므로 계약을 여기에 옮겨 둔다.

### 2.1 개요

- **제목**: 2045년 인천도시기본계획 및 생활권계획 시민의견 수렴
- **담당부서**: 도시계획과 (주무관의 부서)
- **설문기간**: 2026-08-18 ~ 2027-07-31 (약 1년)
- **참여 보상**: 300포인트(홀씨) 적립, 1일 1회
- **URL**: `https://www.incheon.go.kr/cool/COOL070002/apply?qestnarNo=1298`
- **참여자수**: PDF 캡처 시점(2026-08-26, 개시 9일차) **69명**. 주무관 인터뷰 시점(2026-08-30) 약 80명.

### 2.2 문항 7개

1. **[필수] 성별** — 남성 / 여성. **"기타" 없음.**
2. **[필수] 연령대** — 20대 / 30대 / 40대 / 50대 / 60대 / 70대 이상. **10대 보기 없음.**
3. **[필수] 휴대폰 번호**
4. **[필수] 거주지역** — 인천광역시 기준 도로명 또는 지번 주소
5. **[생활권 이용 현황]** 현재 거주지 기준 평소 가장 자주 이용하는 시설 (인천시민 대상, 최대 3개) — 아래 16종
6. **[생활권 시설 수요]** 현재 거주지 주변에 앞으로 가장 필요한 시설 (인천시민 대상, 최대 3개) — **5번과 같은 16종**
7. 거주 지역의 생활환경이나 필요 시설에 대한 자유 서술 (주관식, 선택)

### 2.3 시설 16종 (5·6번 공통 보기)

공원, 도서관, 문화시설, 체육시설, 대형마트, 주민센터(행정복지센터), 노인복지시설, 어린이복지시설, 공연장, 고등학교, 중학교, 초등학교, 공영주차장, 보건소, 파출소, 편의점

**이 목록을 인터뷰어에게 주지 않는다.** 주면 인터뷰어가 문구를 그대로 질문에 옮겨 보기 제시가 된다(`Interview2_workspace/종합_수정방향.md` 1장). 매핑은 수집이 끝난 뒤 분석 단계의 일이다(5.12).

### 2.4 두 데이터를 붙이는 단위

**정밀도는 아래로만 합칠 수 있다.** 항목마다 어느 쪽이 정밀한지가 다르다.

- **지역** — 시가 정밀하다(도로명·지번). 우리는 동네 이름까지만 받는다. **결합 단위는 군·구**이고, 시 주소를 동까지 집계할 수 있으면 동이다. 우리가 더 정밀해질 필요가 없다.
- **연령** — 우리가 정밀하다(출생연도 대 연령대 객관식). **결합 단위는 연령대**로 내린다. 시도 자기신고 연령대일 뿐 실제 나이를 검증하지 않으므로, 우리가 검증하지 않는 것도 같은 수준이다.
- **성별** — 시는 2지선다, 우리는 남성·여성·기타 3지선다다. **"기타"는 시 데이터에 대응 칸이 없어 우리만 가진 추가분**으로 다룬다.
- **시설** — 우리 요구를 16종에 사후 매핑해야 붙는다(5.12).
- **거주 자격** — 시 설문 5·6번은 "인천시민 대상"이다. 우리는 인천 밖 거주자(통학·통근·방문)도 받으므로 **그들의 데이터는 시 데이터와 붙는 것이 아니라 우리만 가진 추가분**이다. 거주자와 비거주자를 분석 단계에서 구분해 유지한다.

### 2.5 주의 — 4천 명과 69명은 다른 조사다

주무관은 대시보드를 보여주며 "2주 동안 약 4천 명이 참여했고 전체의 약 0.17%"라고 말했다. **그것은 이전에 운영한 다른 조사이고, 위의 2045 설문이 아니다.** 2045 설문은 2026-08-18에 시작해 9일차에 69명이다.

**우리가 직접 비교할 대상은 2045 설문이다.** 같은 계획, 같은 목적, 같은 기간대이기 때문이다. 4천 명 수치를 2045 설문의 실적처럼 쓰면 사실과 다르다.

**수집 종료 시점에 위 URL을 열어 참여자수를 다시 확인해 기록한다.** 페이지에 참여자수가 표시된다. 그 숫자와 우리 제출 건수를 나란히 놓는 것이 주 논증이다(1.3).

---

## 3. 확정된 결정

2026-08-31 팀 결정이다.

1. **수집 마감은 9월 20일경, 목표는 200건(하한 100건)이다.** 시민계획단 결과를 시장에게 제출하는 자리(추석 직전 월·화·수)에 맞춘다. 오늘부터 3주이며 개발 가용 시간은 실질적으로 1주 미만이다.
2. **중복참여는 막지 않고 기기 토큰으로 사후에 센다.** 실제 본인인증은 비용과 계약 주체 문제로 학생 팀이 3주 안에 붙일 수 없다. **없는 절차를 안내하던 목업 본인인증 버튼은 화면에서 걷어냈다**(5.7-2).
3. **모집은 온라인 분산이다.** 오프라인 부스는 승인 절차 때문에 제외한다. 팀원 3인의 직접 권유, 에브리타임 홍보글, 팀원 SNS, 교내 벽보 QR이 전부다.
4. **`end` 이벤트 누락은 증상 처방을 넣고 원인 조사를 병행한다.** `PLAN.md` 9.2의 "증상을 덮지 않는다"를 이번 라운드에 한해 뒤집는다. 이유는 5.1에 있다.
5. **인터뷰 길이는 목표 14턴을 유지한다.** 줄이면 빈 축이 늘고 요구가 얇아진다. 이탈은 모집량으로 감당한다.
6. **완주자 대상 스타벅스 쿠폰 10명 추첨.** 응모는 인터뷰 데이터와 완전히 분리한다(5.4).
7. **제출 직전에 5점 척도 만족도 두 문항을 받는다.** 선택 응답이다(5.5).
8. **연령 게이트는 만 16세 이상 29세 이하다.** 근거는 4.4에 있다.
9. **참여자의 소속은 받지 않는다.** 근거는 4.5에 있다.
10. **담당자 접근 코드를 24자 무작위 문자열로 바꾼다.** 네 자리로는 링크를 공개하는 순간 버티지 못한다. 근거는 7.3에 있다.
11. **참여자 입력에 길이 제한만 걸고 턴 상한은 두지 않는다.** 정상 참여자를 자르는 것이 더 비싸다는 판단이다(5.7-3).
12. **살아 있는 세션 수에 상한을 둔다.** 세기 전에 버려진 세션을 먼저 정리한다. 근거는 5.7-3에 있다.
13. **AI 크레딧은 한 번에 3만 원 정도씩 나눠 충전한다.** 방어가 아니라 손실 상한이다(3.1).

### 3.1 예산과 결제처

**AI API 총예산은 9만 원이다.** 결제처가 셋으로 갈리고 서로 대체되지 않는다.

- **Cloud Run·Firestore** — GCP 무료 크레딧 약 30만 원으로 덮인다. 시범 규모는 무료 티어 안이라 여유가 크다.
- **Gemini(채점·구조화·후처리·분석)** — 팀이 GCP와 별도로 충전한 크레딧을 쓴다. **GCP 크레딧이 남아 있어도 이쪽을 대신 막아 주지 않는다.**
- **Anthropic(인터뷰어)** — 또 다른 별도 결제다.

**건당 원가는 인터뷰어 Claude 약 300원에 Gemini 몫이 더 붙는다.** Gemini 몫은 매 턴 채점(14턴 기준 약 95원), 구조화 1회, 제출 후처리 3회를 합쳐 대략 150~180원으로 어림된다. 즉 **건당 총 450~480원**이고, 200건이면 약 9.4만 원이다.

**어림이므로 실측으로 고정한다.** 수집 첫날 몇 건의 실제 토큰 사용량을 API 대시보드에서 확인해 이 절의 숫자를 갱신한다. 로그에 턴별 토큰 사용량이 남는다.

**분석 실행은 필요할 때만 누른다.** 1회당 대형 호출 2건(축별 요약, 종합 브리핑)이 든다. 화면을 확인하려고 반복 실행하는 것이 예산을 가장 크게 갉아먹는다.

**충전은 한 번에 3만 원 정도씩 나눠 한다.** 남용이 있어도 손실이 그 금액에서 멈춘다. 다만 이것은 **방어가 아니라 손실 상한**이다. 잔액이 얇으면 바닥에 닿는 순간 채점이 조용히 실패하고 결과 생성이 죽어 **그때 인터뷰 중이던 사람들이 전부 날아가므로**, "돈이 많이 나가는 문제"가 "서비스가 멈추는 문제"로 바뀐다. 정상 사용이 9.4만 원쯤이니 바닥에 닿기 전에 채워 넣는 것이 전제다. **Gemini와 Anthropic 잔액을 따로따로 확인한다.**

**2026-08-19 사고의 원인은 크레딧 소진이 아니다.** 팀 확인 결과 잔액이 바닥난 적이 없다. 429는 잔액이 아니라 **분당 요청·토큰 할당량**에서도 나오므로 그날은 속도 제한에 걸린 것으로 본다. **이 구분이 대응을 가른다** — 충전으로는 막을 수 없고 동시 호출을 묶어야 막힌다. 분석 팬아웃 상한(5.6)과 동시 세션 상한(5.7-3)이 그 처방이며, 할당량은 청구 알림에 걸리지 않으므로 Cloud Logging의 429를 직접 본다(5.9).

---

## 4. 성공 기준과 측정

### 4.1 결과물

- 제출 200건(하한 100건).
- 요구 단위 원자료 CSV가 시 데이터와 **군·구 단위로 실제 결합된** 상태.
- 시 2045 설문과의 참여자 수·의견 건수·연령 범위 대비가 수치로 제시된 상태.
- 비용, 소요시간, 참여자 만족도가 수치로 제시된 상태.
- 표본 검수(밸리데이션)를 거친 상태.

### 4.2 이미 측정되고 있는 것

**퍼널 네 단계가 전부 기록된다.** 접속(`visit_participant`) → 인터뷰 시작(`interview_start`) → 결과 확인(`result_view`) → 제출(`submission`)이며 담당자 웹의 접속 현황에서 볼 수 있다. 추가 개발 없이 전환율을 낼 수 있다.

**인터뷰 소요시간은 저장된 대화록의 턴별 타임스탬프에서 도출된다.** 별도 필드는 없지만 데이터에는 있다.

**대화 심층도는 `sample_meta`가 산출한다.** 턴 수 평균·최소·최대와 빈 축 비율이다.

### 4.3 보고서용 정량 지표

주무관이 요구한 세 효용에 맞춰 묶는다.

**진입장벽 완화 (주 논증).**

- 시 2045 설문 대비 참여자 수(2.5의 방법으로 확인).
- 요구하는 개인정보 대비: 시는 휴대폰 번호와 도로명 주소 필수에 로그인까지, 우리는 출생연도와 성별뿐이고 로그인이 없다. **참여자가 직접 입력하는 항목 기준이라는 것을 밝힌다** — 우리도 기기 토큰과 접속 기록을 남기지만 참여자가 타이핑하는 것은 두 가지다.
- 만족도 두 문항의 평균과 분포(5.5).
- 퍼널 전환율 네 구간과 이탈 구간의 위치.

**의견 수집량.**

- 총 요구 건수와 시 설문 1인 1건 대비.
- 참여자당 요구 개수, 참여자 발화 총량, 빈 축 비율.
- 10대 후반 응답 건수 — **시 데이터에 존재하지 않는 연령층**이다(2.2).

**비용.**

- 건당 원가와 총비용. 실측으로 고정한다(3.1).
- 기존 방식 대비: 갤럽 등 조사기관 발주 최소 2천만 원, 원자료 미제공, 재질문 시 추가 발주.

**시간.**

- 인터뷰 소요시간 중앙값·최소·최대.
- 수집 시작부터 분석 결과 산출까지의 기간.

**참여율 (참고).**

- 인천 4년제 대학생 약 5만 명 대비. **모집 대상 기준이지 검증된 표본 구성이 아니라는 단서를 반드시 붙인다**(1.4).

### 4.4 연령 게이트 범위

**만 16세 이상 29세 이하로 한다.**

**하한 만 16세.** 개인정보 수집의 법정대리인 동의 문턱(만 14세)에 여유가 있고, 2045년의 자기 삶을 상상할 재료가 있는 나이다. **그리고 시 설문이 구조적으로 받지 못하는 연령층이라**(2.2) 여기서 얻는 응답은 전부 우리만 가진 자료가 된다. 19세 미만은 분석에서 별도 집단으로 구분한다.

**상한 만 29세.** 모집 채널이 전부 대학 내부라 30대 유입은 거의 없다. 열어 두는 실익이 없는 반면 참여율의 분자와 분모가 어긋난다. 좁히면 표본 성격이 "10대 후반과 20대"로 한 줄에 설명되고, 주무관이 사각지대로 지목한 10·20대와 정확히 겹친다.

**만 14세 미만은 어떤 경우에도 받지 않는다.** 개인정보 수집에 법정대리인 동의가 필요한데 3주 안에 그 절차를 만들 수 없고, 없이 받은 데이터는 시에 넘길 수 없다. 그리고 인터뷰가 2045년의 직업·주거·통근을 묻기 때문에 **초등·중학생에게는 답할 재료가 없어 대화가 성립하지 않는다.** 억지로 채워진 답은 표본을 오염시킨다.

**출생연도 기준으로 1997년~2010년이다. 2026-08-31 팀 확정이다.** 2026에서 출생연도를 뺀 값(연 나이)이 16~29가 되는 범위다. 만 나이가 아니라 연 나이를 쓰는 이유는 우리가 받는 값이 출생연도뿐이어서 생일 경과를 알 수 없기 때문이다. 경계를 확인하면, 2010년생은 생일 전이어도 만 15세라 만 14세 문턱에 한 살 여유가 있고, 1997년생은 생일이 지나도 만 29세라 상한을 넘지 않는다. **이번 라운드에 한정된 상수이므로 현재 연도에서 계산하지 않고 코드에 그대로 박는다.**

### 4.5 소속은 받지 않는다

**참여자의 소속을 묻지 않는다.** 시작 화면은 지금처럼 출생연도와 성별만 받는다.

**이유.** 항목을 하나 늘리면 진입장벽이 오르고, 수집 항목이 늘어 고지를 다시 써야 하며, 소속 대학은 준식별자라 비식별 기준에도 걸린다. 참여율을 이미 주 논증에서 내렸으므로(1.4) 그 비용을 치를 이득이 없다.

**시 데이터와의 정합성에는 문제가 없다.** 시 설문도 소속을 받지 않고 연령을 자기신고 연령대로만 받는다(2.2). 우리가 덜 수집하는 것이 아니라 같은 수준이며, 연령은 오히려 우리가 더 정밀하다.

**감수하는 것.** 주무관의 조언 — "우리는 인천에 있는 대학생이고, 인천 대학생의 정서와 답변 유도 노하우로 이 데이터를 모았다" — 을 데이터로 증명할 수 없다. **모집 경로에 대한 서술로 쓰고 참여자 구성을 확인했다고 쓰지 않는다.**

### 4.6 접속 현황 기록의 오염

개발 중 흔적이 접속 현황에 쌓여 있고 지우는 기능이 없다(`PLAN.md` 9.3-4). **수집 시작 시각을 기록해 두고 그 이후 이벤트만으로 퍼널을 계산한다.** 화면 총계는 오염된 채로 두고 지표 산출만 시각으로 자른다. 지우는 기능을 만드는 것은 이번 범위가 아니다.

---

## 5. 작업

### Phase 0 — 수집 시작 전 정리 (코드 수정 없음, 1일)

**무엇을.**

- `web` 서비스의 최대 인스턴스를 1에서 올린다.
- `api` 메모리를 512Mi에서 1Gi로 올린다.
- 저장소를 정리한다. 시험·튜닝 산출물이 실제 참여자와 같은 컬렉션에 섞이면 "200건"이라는 숫자 자체를 말할 수 없다. 담당자 웹에서 지우거나 Firestore 콘솔에서 정리한다.
- **Gemini와 Anthropic 잔액을 각각 확인하고 충전한다.** 서로 다른 결제처이고 GCP 크레딧이 어느 쪽도 대신 막아 주지 않는다. **한 번에 3만 원 정도씩 나눠 채운다**(3.1).
- 접속 QR을 확인한다(`docs/qr/`). 배포 주소가 바뀌지 않으므로 다시 만들 필요는 없다.
- 경품 응모용 구글폼을 만든다(5.4).
- **시 2045 설문 URL을 열어 현재 참여자수를 기록한다**(2.5). 수집 시작 시점의 기준값이다.

**왜 `web` max를 올리는가.** 프론트는 무상태라 올리는 데 아무 위험이 없다. max=1이면 정적 자산까지 인스턴스 하나가 내보내는데, 첫 접속 전송량이 2.9MB이고 그중 한글 서체가 2.06MB다(`PLAN.md` 9.5). 트래픽이 분산될 예정이라 급하진 않지만 설정만 바꾸면 되는 일이다.

### Phase 1 — 수집 전 코드 작업 (2~3일)

아래 일곱 항목을 마치고 **`api`를 한 번만 재배포한다.** 재배포는 인스턴스 교체를 뜻하고 인스턴스 교체는 진행 중인 세션을 전부 지우므로, 수집 시작 전에 한 번으로 몰아 끝낸다.

#### 5.1 `end` 이벤트 증상 처방과 원인 조사

**무엇을.** 인터뷰 스트림이 예외로 끝나는 경우에도 `end` 이벤트를 내보내 참여자가 "대화 종료하기" 버튼을 보게 한다. 현재 `api/app/interview.py`의 `_run`은 예외를 잡아 `turn_failed` 로그를 남긴 뒤 다시 올리고, 그래서 `end`가 나가지 않는다. **로그는 그대로 두고 그 뒤에 `end`를 내보낸다.**

**왜.** 이 결함은 인터뷰가 정상적으로 끝까지 진행된 뒤 마지막에 버튼이 나타나지 않는 방식으로 나타난다. 참여자는 무엇을 해야 할지 몰라 한 번 더 입력하고, 서버가 409를 돌려주고, 프론트가 그것을 오류로 올려 흐름을 끝낸다. **10분 넘게 타이핑한 내용이 제출 직전에 통째로 사라진다.** 여러 라운드에서 네 번 넘게 재발했고 재현 조건은 미상이다.

`PLAN.md` 9.2는 "원인을 모르는 채 증상만 덮으면 다음 재발이 로그에 남지 않는다"는 이유로 이 처방을 거부했다. **이번 라운드에서 뒤집는 이유는 잃는 대상이 달라졌기 때문이다.** 테스터는 다시 해주지만 참여자는 나간다. 모집 지렛대가 완주율뿐인 상황(6.3)에서 이미 끝까지 답해준 사람을 잃는 것이 가장 비싼 손실이다. **`turn_failed` 로그를 그대로 남기므로 진단 능력을 잃지 않는다** — 이것이 원래 거부 이유를 해소한다.

**주의.** 예외가 난 턴의 응답은 불완전할 수 있다. **완결되지 않은 턴은 대화록에 넣지 않고 직전 턴까지로 종료한다.** 반쪽짜리 발화가 채점과 보고서 구조화에 흘러 들어가는 것을 막기 위해서다.

**첫 턴이 실패하면 처방하지 않고 지금처럼 예외를 올린다.** 대화록이 비어 있으면 살릴 것이 없고, 빈 대화록으로 결과 생성에 보내면 어차피 죽는다. 이 처방이 지키려는 것은 이미 쌓인 발화이고, 도입 인사에서 실패한 참여자는 아직 아무것도 입력하지 않았다. 즉 처방은 `messages`가 비어 있지 않을 때만 작동한다.

**병행.** 수집 시작 전에 인터뷰를 몇 차례 완주시켜 재발을 유도하고 Cloud Logging의 `turn_failed`를 확인한다. 원인이 잡히면 제대로 고치고, 안 잡히면 증상 처방을 안고 수집에 들어간다.

#### 5.2 기기 토큰

**무엇을.** 브라우저에 영구 식별자를 두고 세션 생성 시 서버로 보내 제출본에 저장한다. 담당자 제출본 목록에 중복 토큰 건수를 한 숫자로 표시한다.

**분석 요약에는 넣지 않는다.** 사람이 이 숫자를 보는 자리는 담당자 화면 하나뿐이고, 같은 값을 분석 파이프라인에서 다시 계산하면 200건짜리 실행에 표면적만 늘어난다. 보고서에 쓸 숫자는 담당자 화면에서 읽어 적는다.

**왜.** 목업 인증은 중복참여를 강제하지 않는다. 시에 낼 자료에서 **"막았다"고 말할 수는 없더라도 "중복 의심 N건"이라고 말할 수는 있어야 한다.**

**막지 않는 이유.** 형제·친구의 휴대폰을 빌려 참여하거나 공용 기기를 쓰는 경우가 정상 사례로 존재한다. 막으면 그 참여자를 잃는데, 지금은 참여자 손실이 중복 데이터보다 비싸다. **저장하고 세기만 한다.**

**수집 항목이 늘어나므로 고지 문구에 반드시 반영한다**(5.7).

#### 5.3 연령 게이트

**무엇을.** 출생연도 입력 범위를 4.4에서 정한 범위로 좁힌다. 프론트와 백엔드 양쪽에서 검증한다. 현재는 프론트가 1900~2026, 백엔드가 `ge=1900, le=2026`으로 사실상 아무 값이나 통과한다.

**왜.** 만 14세 미만이 들어오면 법정대리인 동의가 필요해지고, 범위를 걸지 않으면 이 데이터를 "청년 데이터"라고 말할 근거가 없다.

#### 5.4 경품 응모 경로

**무엇을.** 제출 완료 화면에 응모용 구글폼 링크를 둔다. 시작 화면에는 완주 시 추첨한다는 안내를 넣는다.

**왜 데이터와 분리하는가.** 당첨자 연락에 연락처가 필요한데, 이것을 제출본에 넣으면 수집 항목이 늘고, 비식별 대상이 늘고, 고지를 다시 고쳐야 하고, 무엇보다 **시에 넘길 원자료에 개인정보가 섞인다.** 구글폼은 저장소가 아예 달라 인터뷰 데이터와 연결되지 않는다. 개발은 링크 하나다.

**시작 화면 안내가 필요한 이유.** 완주 동기가 되려면 들어오기 전에 알아야 한다. 제출 완료 화면에서 처음 알면 완주율에 기여하지 않는다.

**추첨 방식과 발표 시점.** 제출까지 마친 참여자 중 응모한 사람을 대상으로 10명을 무작위 추첨하고, 수집이 끝난 뒤 9월 말에 응모 시 남긴 연락처로 개별 안내한다. 무보상 참여를 전제로 설계된 서비스에 보상이 붙는 것이므로 조건이 명확해야 한다.

**구글폼 URL은 아직 없다(2026-08-31).** `web/src/lib/prize.ts`의 `PRIZE_FORM_URL` 한 줄이 유일한 자리다. 비어 있으면 시작 화면의 안내와 완료 화면의 응모 카드가 **둘 다 나오지 않는다** — 들어갈 방법이 없는 추첨을 약속하지 않기 위해서다. 판정은 같은 파일의 `PRIZE_DRAW_OPEN` 하나가 하므로 두 화면이 어긋날 수 없다. URL을 받으면 그 문자열만 채우면 된다.

#### 5.5 만족도 문항

**무엇을.** 결과 화면의 보고서 단계(`web/src/components/result-screen.tsx`의 `step === "report"`, 제출 버튼이 있는 화면)에 5점 척도 두 문항을 얹는다. 기존 "요구 수정하기" 카드와 같은 시각 언어로 카드 하나를 더한다. 응답값은 세션에 담았다가 제출 시 제출본에 함께 저장한다.

1. 이렇게 대화로 의견을 내는 방식이 편했나요?
2. 정리된 결과가 내 생각을 잘 담고 있나요?

**왜.** 주무관은 효용을 시간·비용·**진입장벽** 중 하나로 보여야 한다고 했다. 시간과 비용은 이미 측정되지만 **진입장벽을 낮췄다는 것은 물어봐야만 안다.** 퍼널과 소요시간은 행동 지표라서 "쉬웠는가"에 답하지 못한다. 그리고 `PLAN.md` 1장이 성공 조건으로 적어 둔 "참여자가 결과가 자기 생각을 반영한다고 느끼는 것"에 재는 수단이 없었는데, 두 번째 문항이 그것이다.

**왜 두 문항인가.** 셋 이상이면 설문처럼 느껴져 이탈이 붙는다. 자유 서술 칸은 두지 않는다 — 인터뷰 자체가 주관식인데 끝에서 또 쓰라면 부담이다.

**왜 선택 응답인가.** 필수로 하면 제출 직전에 관문이 하나 더 생긴다. 완주율이 유일한 지렛대인 상황(6.3)에서 제출 직전 이탈은 가장 비싼 손실이다. 절반만 답해도 100건이라 표본으로 충분하다.

**왜 제출 후가 아니라 제출 전인가.** 제출 시점에 세션이 폐기되므로 제출 후에 받으려면 참여자 API에 쓰기 경로를 새로 열어야 한다. 제출 전에 두면 세션에 담아 함께 저장되므로 새 엔드포인트도 그 경로의 보호 문제도 없다.

**세션에 담지 않고 제출 요청 본문으로 받는다.** 지금까지 본문이 없던 `POST /api/sessions/{id}/submit`에 `{ease, accuracy}`를 실어 그대로 제출본에 저장한다. 이 절이 막으려던 것은 새 엔드포인트와 그 경로의 보호 문제인데, 제출 본문에 실으면 그것도 생기지 않으면서 세션에 상태를 하나 더 두지 않아도 된다. 세션은 같은 요청 안에서 폐기되므로 거쳐 갈 이유가 없다.

**수집 항목이 늘어나므로 고지 문구에 반영한다**(5.7).

#### 5.6 분석 팬아웃 상한

**무엇을.** `api/app/analysis.py`의 `supplement_missing`에 동시 호출 상한을 둔다. **상한은 8이다.** 세 단계 사이의 순서(남은 요구 → 부문 라벨 → 비식별)는 `PLAN.md` 3.4의 계약이므로 그대로 두고, 각 단계 **안의** 팬아웃만 묶는다.

**왜.** 제출 직후 백그라운드 후처리가 실패하거나 지연되면 그 제출본은 후처리 값이 빈 채로 남고 분석 실행이 이를 보충한다. 현재 이 보충은 `asyncio.gather`로 전 제출본에 동시에 걸린다. **200건이 밀려 있으면 최대 600개 동시 호출이 나가 속도 제한에 걸려 통째로 무너진다.** 10건 규모에서는 드러나지 않던 구조다.

**CPU 스로틀링은 건드리지 않는다.** 백그라운드 후처리가 응답 이후에 돌기 때문에 Cloud Run 기본 설정에서 CPU가 조여진다(현재 해제 설정 없음, 8장). 해제하면 월 5만 원 이상이 추가로 드는데, 이 작업은 모델 응답을 기다리는 대기 위주라 조여져도 완료될 가능성이 높다. **상한만 걸어 두고 실제로 밀리는지는 수집 중 로그로 관찰한다**(5.9).

#### 5.7 고지 문구 확정

**무엇을.** 시작 화면(`web/src/app/page.tsx`)의 개인정보 수집·이용 고지를 실제 수집에 맞게 고친다. 수집 항목에 기기 토큰과 만족도 응답을 추가하고, 이용 목적을 "시범운영"이 아니라 실제 목적으로 쓰고, 보관 기간과 파기 시점을 실제 일정으로 적는다.

**왜.** `PLAN.md` 8장의 원칙은 "고지에 없는 항목을 받는 것은 동의를 받지 않고 수집하는 것"이다. 그리고 이 데이터는 실제로 인천시에 전달될 예정이므로 "시범운영이 끝나면 파기한다"는 현재 문구가 사실과 어긋난다.

**확정된 내용은 여섯이다(2026-08-31 팀 결정).**

- **수집 항목** — 출생연도, 성별, 인터뷰 대화 내용, 대화 중 언급한 별명·거주 지역·꿈 또는 직업, **기기 식별 토큰**(5.2), **만족도 응답 두 문항**(5.5), **접속 기록**(뒷자리를 가린 IP 주소와 기기·브라우저).
- **이용 목적** — 2045년 인천도시기본계획 수립을 위한 청년 의견 수집과 분석. **정리된 결과를 인천광역시에 전달하는 것까지 포함한다.** 전달을 적지 않으면 실제로 하는 일과 고지가 어긋난다.
- **보관 기간** — **2026년 12월 31일까지.** 9월 수집, 인천시 전달, 공모전 심사와 발표까지 원자료가 필요하고, 검수(5.14)에도 대화록 원문이 있어야 한다.
- **파기 시점** — 위 기간이 끝나면 파기한다. "시범운영이 끝나면"처럼 참여자가 시점을 알 수 없는 표현을 쓰지 않는다.
- **처리 주체** — 대화 내용이 **앤트로픽과 구글의 AI로 처리된다**는 것을 적는다.

**기기 토큰은 무엇에 쓰는지까지 적는다.** 중복참여를 세는 데만 쓰고 참여를 막거나 개인을 특정하는 데 쓰지 않는다는 것을 함께 적는다. 무엇을 받는지만 알리고 왜 받는지를 감추면 고지의 목적을 채우지 못한다.

**접속 기록은 두 가지를 정확히 쓴다.** IP는 뒷자리를 가려 저장하므로 **전체 주소를 갖고 있는 것처럼 읽히지 않게** 쓰고, 이 기록은 동의 버튼을 누르기 전 **화면을 여는 시점에 남으므로** 동의 후에만 일어나는 일처럼 쓰지 않는다.

**AI 처리는 회사 이름까지만 적고 국외 이전을 풀어 쓰지 않는다.** 누가 처리하는지는 밝히되 법률 고지의 어휘를 끌어오지 않는 선이다. **이 표현이 국외 이전 고지의 요건을 채우지는 못한다는 것을 아는 채로 택했다** — 침묵보다 분명히 낫고 프로토타입 범위에서 방어할 수 있지만 완전한 준수는 아니다.

**`PLAN.md` 9.4의 미결 항목이다.** 이번 라운드에서 닫는다.

#### 5.7-1 Phase 1 진행 기록 (2026-08-31)

**Phase 1의 일곱 항목이 전부 구현되어 게이트를 통과했다.** `api`는 `ruff check`·`ruff format --check` 통과, `pytest` 336건 통과에 알려진 실패 1건(0.3절)뿐이다. `web`은 `tsc --noEmit`·`eslint`·`prettier` 통과, `pnpm e2e` 179건 전부 통과다.

**e2e에 추가한 검사.** 연령 게이트의 경계 거부(1996·2011)와 안내 문구, 만족도 두 문항의 렌더와 응답, 미응답 상태에서 제출 버튼이 살아 있는 것, 답한 값과 답하지 않은 값이 제출 요청에 각각 실리는 것, 선택 칸이 인천시 파랑으로 차는 것이다.

**검토에서 잡아 고친 것 셋을 적어 둔다. 셋 다 게이트를 통과한 산출물에서 나왔다.**

- **분석 팬아웃 세마포어를 모듈 레벨에 두면 안 된다.** 세마포어는 처음 대기가 발생하는 순간의 이벤트 루프에 묶이는데, `pytest-asyncio`가 auto 모드라 테스트마다 새 루프를 만든다. 지금은 대기를 유발하는 테스트가 하나뿐이라 통과하지만 두 번째가 생기면 "다른 루프"라며 터진다. **`supplement_missing` 안에서 실행마다 만든다.**
- **제출본 상세 응답에 옛 문서용 기본값 채움이 붙어 있었다.** 그 두 값을 읽는 화면이 없어 쓰이지 않는 분기였다. 걷어냈다. 목록 응답의 `device_token` 기본값은 중복 집계가 실제로 읽으므로 남긴다.
- **만족도 척도의 `sr-only` 라디오가 화면을 통째로 밀어냈다.** `sr-only`는 `position: absolute`인데 위치 기준 조상이 없으면 `body`의 `overflow: hidden`에 잘리지 않는다. 보고서 화면처럼 스크롤이 긴 자리에서는 문서 자체가 스크롤 가능해져(844px 화면에 문서 4205px), 참여자가 척도를 누르는 순간 앱이 화면 밖으로 밀렸다. **라벨에 `relative`를 주어 잘리게 했고, e2e가 이 조건을 검사한다.** `PLAN.md` 2.4의 "화면이 사용자의 조작을 이기지 않는다"에 걸리는 결함이다.

**만족도 카드는 "요구 수정하기" 카드 아래에 둔다.** 두 번째 문항이 묻는 것은 제출될 판본이 내 생각을 담았는지인데, 수정 기회를 주기 전에 물으면 수정 전 판본을 평가하게 된다. 제출 안내 문구는 그대로 맨 끝에 남겨 버튼과 붙어 있게 한다.

**경품 문구가 시작 화면의 본인인증 버튼을 화면 밖으로 밀어냈다.** URL을 임시로 채워 렌더링을 확인하다 발견했다. 390×844에서 버튼 하단이 857px이 되어 첫 화면의 주요 행동 버튼이 스크롤해야 보였다. 도입 문단을 줄여 네 줄에서 세 줄로 만들어 해결했고(버튼 하단 829px), 마지막 줄에 "요." 한 글자만 남던 것도 함께 없어졌다. **경품 문구가 꺼져 있어도 이 문제는 URL을 채우는 순간 되살아나므로, URL을 넣는 사람이 레이아웃을 다시 볼 필요가 없도록 지금 고쳐 두었다.**

**배포는 `web` → `api` 순서로 한다.** 새 `api`는 기기 토큰이 없는 세션 생성 요청을 422로 막으므로, `api`를 먼저 올리면 그 사이 옛 화면을 열고 있던 사람이 시작조차 하지 못한다. 반대 순서는 안전하다 — 새 화면이 보내는 기기 토큰과 만족도를 옛 `api`는 모르는 필드로 무시할 뿐이다.

**2026-08-31에 Phase 1을 배포했다.** 리비전 `web-00019-ph6`·`api-00019-85p`다. 운영에서 확인한 것은 헬스체크 200, 1995·2011년생과 기기 토큰 누락이 각각 422, 경계인 1997·2010년생이 200, 새 고지 문구가 실려 있고 옛 "시범운영이 끝나면" 문구가 사라진 것이다. 배포 주소가 그대로이므로 `docs/qr/`의 QR은 다시 만들지 않는다.

**경품 URL은 `web`만 다시 배포하면 된다.** 상수가 `web/src/lib/prize.ts`에 있고 `web`은 무상태라 언제든 올려도 되므로(5.8), 진행 중 세션을 끊는 `api` 재배포를 다시 할 일이 없다.

**`api/scripts/regen_fixtures.py`도 함께 고쳤다.** `create_session`에 인자가 하나 늘어 이 스크립트가 깨졌다. 호출부에 고정 문자열 토큰을 넘긴다. 이 스크립트는 실행하지 않았다.

### Phase 2 — 수집 (9월 초 ~ 9월 20일경)

#### 5.8 배포 규칙

**수집 기간 중 `api` 재배포는 참여자가 없는 심야에만 한다.** 낮에는 하지 않는다. 세션이 프로세스 메모리에 있어 인스턴스가 교체되면 그 시점에 인터뷰 중이던 사람이 전부 사라지기 때문이다.

세션을 저장소에 영속화하면 이 제약이 없어지지만 **이번 라운드에서는 하지 않는다.** 핵심 구조를 건드리는 일이고 `PLAN.md` 2.3의 "중단 세션 복원을 하지 않는다"를 뒤집는 결정이며, 3주 일정에서 그만한 위험을 질 이유가 없다. 배포 동결이라는 운영 규칙으로 같은 효과를 얻는다.

`web`은 무상태이므로 언제든 배포해도 된다.

#### 5.9 매일 확인할 것

- 퍼널 네 단계 수치와 전환율.
- Cloud Logging의 후처리 실패 이벤트(`deidentify_failed` 등)와 `turn_failed`.
- Gemini·Anthropic 잔액.

#### 5.10 기준선 기록

**첫 20~30명 시점에 기준선을 기록한다.** 퍼널 전환율 네 구간과 소요시간 중앙값이다. 이후 모든 비교의 기준이 되고 보고서의 정량 지표도 여기서 나온다.

이탈이 특정 구간에 몰리는 것이 확인되면 그때 대응한다. **인터뷰 길이 관련 상수는 전부 환경변수라 재배포 없이 조정할 수 있다**(`interview_target_turns`, `interview_hint_turn`, `interview_wrapup_turn`, `interview_max_extra_turns`). 다만 3장의 결정에 따라 기본은 14턴 유지이고, 줄이는 것은 이탈이 실제로 심할 때의 대응 수단으로만 남긴다.

### Phase 3 — 분석과 전달 (9월 20일 ~ 23일)

#### 5.11 분석 실행

담당자 웹의 자료 업데이트를 실행한다. **200건에서 시간 초과가 날 수 있다.** `api`의 요청 타임아웃이 300초이고(8장) 분석은 그 요청 안에서 동기로 완료된다. 초과하면 타임아웃 값을 올린다(설정 변경이라 코드 수정이 아니다).

정성 요약 호출이 전 제출본의 요구와 인용을 한 번에 받는 구조라, 200건에서는 인용 후보가 수천 개가 되어 선정 품질이 떨어질 수 있다. **실제 결과를 보고 판단한다.** 나빠지면 인용 후보에 상한을 거는 것이 처방이며, 축별 요구 요약 자체는 표본이 커질수록 오히려 안정된다.

#### 5.12 시설 16종 매핑

**무엇을.** 저장된 요구를 시 설문 6번의 16종(2.3)에 사후 매핑한다. 수집이 끝난 뒤 한 번 돌린다.

**왜.** 주무관이 "그 질문과 여러분 시스템이 연계되게 하면 된다"고 콕 집은 지점이다. 우리 키워드는 자유 어휘라 그대로는 시 객관식 집계와 붙지 않는다.

**억지로 채우지 않는다.** 16종 어디에도 해당하지 않는 요구가 나오는 것이 정상이고 그것이 이 조사의 존재 이유다. **매핑되지 않은 요구를 "시 설문 보기에 없는 요구"로 따로 센다. 이 숫자 자체가 결과물이다** — 객관식 보기가 놓치는 수요가 얼마나 되는지를 보여준다.

**인터뷰어에게 16종을 주지 않는 결정은 유지한다**(2.3).

#### 5.13 CSV 보강

현재 CSV(`web/src/app/admin/dashboard-data.ts`)는 요구 단위로 참여자·거주 군구·거주지 원문·행정동·나이·부문·요구·키워드·언급 장소·언급 장소 군구·최우선 여부·정착 의향을 낸다. **주무관이 말한 "원자료"에 가장 가까운 자산이고 시 데이터와 붙일 축도 이미 있다.**

추가할 것은 성별, 도시유형, 대표 인용, 인터뷰 소요시간, 시설 16종 매핑 결과다. 성별은 시 데이터의 인구통계와 교차하는 데 필요하고, 인용은 주무관이 말한 "주관식 원자료"에 해당한다.

#### 5.14 밸리데이션

주무관은 주관식 분석 결과에 대해 "반드시 검증해야 한다"고 했고 관심 있으면 메일을 달라고 했다. 200건은 사람이 표본 검수할 수 있는 규모다.

**무엇을 검수하는가.** 부문 라벨이 요구에 맞게 붙었는가, 구조화된 요구가 참여자가 실제로 말한 것인가, 비식별 처리가 빠뜨린 것이 없는가 셋이다. **두 번째가 가장 중요하다** — `PLAN.md` 9.1에 표현 상향과 말하지 않은 가치 대비 창작이 살아 있는 결함으로 남아 있다.

**어떻게.** 표본 30건 정도를 대화록 원문과 대조한다. 검수 결과를 건수와 유형으로 집계해 보고서에 싣는다. **검수 방법 자체를 주무관에게 메일로 문의한다** — 본인이 제안한 일이고 행정에서 통용되는 검증 형식을 아는 사람이다.

#### 5.15 시 데이터 결합

주무관에게 시 시민참여 플랫폼 데이터를 엑셀로 받아 2.4의 단위 규칙대로 결합한다. 거주자와 비거주자를 분석 단계에서 구분해 유지한다.

---

## 6. 모집 계획

### 6.1 채널

팀원 3인의 직접 권유, 에브리타임 홍보글, 팀원 SNS 공유, 교내 벽보 QR이 전부다. 오프라인 부스는 승인 절차 때문에 제외한다.

### 6.2 숫자로 본 현실

- **직접 권유 50~80명.** 완주율이 가장 높다. 아는 사람의 부탁이라 끝까지 한다. 제출 40~60건을 기대한다.
- **에브리타임 홍보글.** 유입은 있지만 완주율이 크게 떨어진다. 8~12분짜리 타이핑 인터뷰이기 때문이다.
- **SNS 공유.** 도시유형 카드가 공유 소재로 설계돼 있어 완주자가 다음 참여자를 데려오는 경로다. 완주자가 늘어야 작동하므로 초반에는 기대하지 않는다.
- **벽보 QR.** 전환이 가장 낮다.

다 합쳐 **현실적인 도달 범위는 100~180건**이다. 200은 여유 있게 넘는 숫자가 아니라 빠듯하게 닿는 숫자다. 이것이 하한을 100건으로 함께 잡은 이유다(1.2).

### 6.3 완주율이 유일한 지렛대다

부스를 못 쓰면 인원을 늘릴 수단이 제한된다. 남는 것은 **들어온 사람을 끝까지 가게 하는 것**뿐이다. 그래서 이번 라운드의 개발 우선순위가 새 기능이 아니라 이탈 지점 제거로 정해졌다. `end` 이벤트 처방(5.1)이 최우선인 것도 같은 이유다.

### 6.4 대상은 인하대에 집중한다

인천대와 연세대 송도캠퍼스로 넓히지 않는다. 주무관의 조언이 명시적이었다. "모든 사람을 대상으로 하려고 하지 마세요", "인천 지역 20대의 감성에 대해서는 우리가 확실히 잘한다고 정하는 게 낫다".

**참여 자격 자체는 넓게 열려 있다.** 인천에 살거나, 학교·직장이 인천이거나, 인천을 자주 찾으면 참여할 수 있다. 부천에 살며 인하대에 통학하는 사람도 인천의 활동인구라는 것이 주무관의 판단이었다. **모집을 인하대에 집중하는 것과 자격을 좁히는 것은 다른 이야기다.**

---

## 7. 감수하는 것과 하지 않는 것

### 7.1 감수하는 것

- **중복참여를 막지 않는다.** 센다. 보고서에 한계로 명시한다.
- **표본이 인하대 채널에 편중된다.** 의도된 선택이다(6.4). 인천 청년 전체의 대표 표본이 아니라고 명시한다.
- **참여자 구성을 데이터로 확인할 수 없다.** 소속을 받지 않으므로(4.5) 모집 경로는 서술하되 구성은 단정하지 않는다.
- **인천 밖 거주자 데이터는 시 데이터와 결합되지 않는다.** 우리만 가진 추가분으로 따로 다룬다(2.4).
- **`api` 재배포 시 진행 중 세션이 사라진다.** 심야 배포 규칙으로 관리한다(5.8).
- **접속 현황 총계가 개발 흔적으로 오염돼 있다.** 지표는 시각으로 잘라 계산한다(4.6).

### 7.2 이번 라운드에서 하지 않는 것

`PLAN.md` 2.3의 목록에 더해 아래를 하지 않는다. **없는 이유가 있어서 없는 것이므로 "빠졌다"고 판단해 채우지 않는다.**

- 세션의 저장소 영속화와 중단 인터뷰 복원.
- `api`의 수평 확장.
- 실제 휴대폰 본인인증과 대학 이메일 인증.
- 참여자 소속 수집.
- Cloud Run CPU 스로틀링 해제.
- 관리자 권한 시스템, 다국어, 음성 입력.
- 접속 현황 기록 삭제 기능.
- 담당자 대시보드의 새 기능. 200건에서 읽히는지만 확인하고 문제가 있으면 그때 본다.
- 이슈 중심 저장 구조로의 전환.
- 한글 서체 동적 서브셋(`PLAN.md` 9.5). 이탈이 접속 단계에 몰리는지만 퍼널로 확인한다.

---

## 8. 현재 배포 실측값 (2026-08-31)

`gcloud run services describe`로 확인했다. **다시 조사하지 않아도 된다.**

- 프로젝트 `youthplan-ai-504608`, 리전 `asia-northeast3`
- `api` — `https://api-icvmeklqxa-du.a.run.app`, minScale 1, **maxScale 1**, 동시요청 80, CPU 1000m, **메모리 1Gi**, 타임아웃 300초, startup-cpu-boost 켜짐, **CPU 스로틀링 해제 설정 없음**
- `web` — `https://web-icvmeklqxa-du.a.run.app`, minScale 1, **maxScale 4**, 동시요청 80, CPU 1000m, 메모리 512Mi

`api`가 max=1인 것은 세션이 프로세스 메모리에 있기 때문이고 이번 라운드에서 바꾸지 않는다.

**Phase 0의 설정 변경은 2026-08-31에 끝냈다.** `web`의 maxScale을 1에서 4로, `api`의 메모리를 512Mi에서 1Gi로 올렸다(리비전 `web-00018-d65`·`api-00018-ssx`, 두 서비스 모두 헬스체크 200). **`web`을 4로 잡은 이유는 무상태라 올리는 데 위험이 없으면서도 크롤러나 폭주가 인스턴스를 무한정 늘리는 것은 막아야 하기 때문이다.** 200명 규모에서 동시요청 80짜리 인스턴스 하나로도 부족하지 않고, 올리는 실익은 첫 접속 2.9MB를 여러 인스턴스가 나눠 내보내는 데 있다.

---

## 9. 위험과 대응

- **크레딧 소진.** 채점과 결과 생성이 함께 죽고 결과 생성은 재시도가 없다. 실제로 겪은 사고다. 대응은 사전 충전과 매일 잔액 확인뿐이며 **결제처 둘을 따로 본다**(3.1).
- **목표 미달.** 100건 하한에서도 논증이 성립하도록 지표를 설계해 뒀다(1.2, 1.3).
- **후처리 누락 누적.** 상한을 걸어 두고 로그로 관찰한다(5.6, 5.9).
- **분석 시간 초과.** 타임아웃 설정을 올린다(5.11).
- **`end` 이벤트 원인 미규명.** 증상 처방으로 참여자는 지키고 로그는 계속 쌓는다(5.1).
- **첫 접속 지연.** 한글 서체 2.06MB 때문에 느린 회선에서 로딩 표시가 길게 돈다. 화면 자체는 1초 안에 뜨므로 실해는 제한적이지만 유입 경로가 모바일 링크뿐이라 이탈 요인이 될 수 있다. 고치지 않고 퍼널로 확인만 한다.

---

## 10. 주무관에게 보낼 메일에 담을 것

한 통에 묶는다.

1. 시 시민참여 플랫폼 데이터의 엑셀 제공 요청(5.15).
2. **주무관이 보여준 "2주 4천 명" 조사의 연령 분포 요청** — 20대 비중을 알아야 사각지대 논증이 완성된다. 그 조사가 무엇이었는지도 함께 확인한다(2.5).
3. 밸리데이션 방법 문의(5.14). 본인이 제안한 일이다.
4. 수집 결과 보고와 시민계획단 연계 논의.

---

## 11. `PLAN.md`에 반영할 항목

라운드가 끝난 뒤 계약 변경분만 옮긴다. 지금 적어 두는 것은 잊지 않기 위해서다.

- 수집 항목에 기기 토큰과 만족도 응답 추가(8장).
- 개인정보 고지 문구 확정 — 9.4의 미결이 닫힌다.
- 출생연도 범위 제한(3.1).
- `end` 이벤트 처방 — 9.2의 서술을 실제 처방에 맞게 고친다. 원인이 규명되면 9.2를 지운다.
- 분석 팬아웃 상한 — 3.4에 단계를 보완한다.
- 준식별자 처리 기준 — 9.4가 "베타 테스트에서 정한다"고 남겨 둔 항목이다. 이번 수집 데이터로 정한다.
- 부문 라벨링 실무 검수 — 9.4의 미결. 밸리데이션(5.14) 결과로 일부 답한다.
- 시설 16종 매핑 — 도입하면 3.4에 단계를 추가한다.
- **인터뷰어 이름 치환 금지 시점** — 9.5에 "실제 참여자의 대화록이 쌓인 뒤에는 같은 치환을 하지 않는다"고 적혀 있다. **이번 수집이 그 시점이다.**
