/**
 * Who is collecting the answers, named on the start screen itself rather than only inside
 * the consent notice: a participant spending their time and their data is owed the
 * collector's identity without having to open a disclosure. Ordered as on the poster,
 * which is also alphabetical, so no reading of rank is invited.
 */
export const RESEARCHERS =
  "인하대학교 강채연(행정학과)·이태수(공간정보공학과)·지수현(행정학과)";

export const CONTACT_EMAIL = "dosiri24@inha.edu";

/** Hides the collector line during the anonymous poster vote; the address names the school too, so it goes with it. Set false to restore. */
export const HIDE_TEAM = true;
