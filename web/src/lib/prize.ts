/** The external entry form, kept out of the submission so contact details never join the data the city receives. */
export const PRIZE_FORM_URL: string = "";

/** The only place the URL is tested, so no screen can promise a draw the others hide. */
export const PRIZE_DRAW_OPEN = PRIZE_FORM_URL !== "";
