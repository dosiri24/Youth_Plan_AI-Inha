# TUNING.md — AI 품질 튜닝 라운드

기준일 2026-08-11. 이 라운드가 끝나면 `PLAN.md`에 통폐합하고 삭제한다.

---

## 0. 먼저 읽을 것 — 지금 상태와 다음 할 일

**이 문서는 2026-08-11에 두 번 갱신됐다.** 오전에 12인 실험 결함 26건을 정리했고(3장), 오후에 **두 프론티어 모델을 전문가로 붙여 5라운드 토론을 시킨 결과**로 원인·처방·검증·순서를 확정했다.

**지금까지 한 것.** 결함마다 어느 파일 몇 행이 원인인지 확정했다. 프롬프트에 그대로 넣을 문장을 썼다. 착수 순서와 각 단계의 통과 판정을 정했다. `TUNING.md` 자신의 오진 11건을 찾아 정정했다. 새 결함 7건을 추가로 찾았다.

**아직 안 한 것. 코드와 프롬프트는 한 글자도 고치지 않았다.** 실호출 검증 없이 프롬프트를 바꾸면 지금 상태와 비교할 기준선이 사라진다. 적용 여부는 사람이 판단한다.

**다음에 할 일.**

1. 9장 착수 순서의 **0단계**부터 시작한다. 로그·모델·프롬프트 해시를 고정하고 검증 입력을 얼려야 이후 측정이 성립한다.
2. **0A단계(`structuring.md`)를 그다음에 한다.** 인터뷰어 프리픽스와 다른 파일이라 지면을 다투지 않고, **기존 12건 대화록으로 통제된 전후 비교가 가능한 유일한 구간이다.** 크레딧 12회로 완전한 A/B가 나온다.
3. 1단계에서 **계획 요약본을 인터뷰어 호출에서 뺀다.** 프리픽스의 40.2%가 한 번에 빈다. 이후 모든 수정의 지면이 여기서 나온다.

**전제 하나를 사람이 확인해야 한다.** 8장 [B8]과 3.5 (T)가 소요 시간 안내를 프롬프트에서 빼고 화면 한 곳에서 관리하기로 했다. **시작 화면에 이미 시간 안내가 있는지 확인이 필요하다.** 없으면 백엔드가 첫 턴에 주입해야 한다.

**토론 원문은 `docs/tuning-debate/`에 있다.** 아홉 건이며, 이 문서의 모든 결론에는 그쪽에 논증이 있다. 제로섬 회계, 40여 개 패치의 문장 전문, 실패 시나리오 예시는 `round4-A.md`와 `round5-B.md`가 상세하다.

---

## 1. 이 문서는 무엇인가

2026-08-10에 가상 인천 청년 12명이 배포 서버와 실제로 대화한 실험에서 나온 **AI 품질 결함**과, 그 해결책을 담는다. 원자료는 `docs/interview-lab/`에 있다(종합 `00-종합.md`, 개별 기록 12건).

### 1.1 의도적으로 빼놓은 것

과거 라운드에서 끝난 이야기를 다시 끌고 오지 않으려고 제외했다. 없는 것은 누락이 아니라 판단이다.

- **유도된 답의 증거 오염** — 2026-08-10 호출 분리로 해결됐다. 12인 실험에서 판정 쪽 재발은 없었다. 다만 **보고서 쪽에는 같은 배제가 없다**(3.4 (O)).
- **영종구 정규화** — 결함이 아니었다. 모델이 개편을 알고 옛 지명을 고친 것이다.
- **도시 유형화 개편(2026-07-29) 이전의 논의 일체.**
- **2026-08-11 팀 시험 사용의 플랫폼 UX 6건** — Phase 9로 배포까지 끝났다.
- **API로만 진행해서 생긴 착시** — 참여자가 웹 화면을 보지 못한 조건에서 나온 불만이다. `00-종합.md` 6절이 갈라 놓았고 여기는 **진짜로 판정된 것만** 옮겼다.

---

## 2. 건드리지 않기로 한 것

**채점 계산식과 유형화 자체는 이 라운드의 대상이 아니다.** 축 40개 중 35개(87.5%)가 설계한 인물상과 일치했고, 12명 중 7명은 네 글자가 전부 맞았다. `scoring.py`의 가중치 합산·동률 처리·강도 산출에 오류가 없다.

**다만 이 판단을 토론이 한 군데 정정했다.** "다섯 오판은 전부 태깅 문제이므로 `scoring.py`를 열지 않는다"는 처음 판단은 절반만 맞다. 계산식은 옳지만 **결과 계약에 근거 메타데이터가 없는 것**이 독립 결함이다. `scoring.py:47-55`가 증거 0건인 축에 51을 채우고, `:57-65`의 실제 산출 51과 **표면값이 같다.** 빈 축과 박빙이 구분되지 않는다. 계산식은 그대로 두고 `evidence_count`·양극별 가중치·`empty_axis`를 결과에 싣는다([B6]).

**`axes.md`의 가중치·태깅 규칙도 지금 고치지 않는다.** 이유는 11장에 있다.

---

## 3. 결함 목록 — 확정된 원인과 처방

발생 인원은 12인 중 몇 명에게 나타났는지다. 처방 ID는 8장에서 정의한다.

### 3.1 인터뷰어가 질문을 만드는 방식 — 본진

| 항목 | 결함 | 인원 | 확정된 원인 | 처방 |
|---|---|---|---|---|
| **(A)** | 참여자가 말한 적 없는 대립 구도를 전제로 깔고 그 안에서 답하게 한다 | 12/12 | `system.md:92-96`, `rubric.md:68,79-87` — **프롬프트가 가상 손실·대가·인과를 만들어도 된다고 명시한다** | S5, R5, T2 |
| **(B)** | 한 턴에 질문 두 개 이상, 앞 질문 유실 | 12/12 | `system.md:14`가 질문을 답칸으로 정의하지 않고, `rubric.md:45-46,50,52-53,67`이 한 불릿에 독립 답칸을 여럿 둔다 | S3, R4, B4 |
| **(C)** | 같은 주제를 표현만 바꿔 반복. 거절 뒤에도 재발 | 11/12 | `rubric.md:3`(자유 패러프레이즈) + `system.md:28`(무제한 축소 재질문, **포기 조항 없음**) + `system.md:115-122`·`rubric.md:24-26,97`(축 완주 의무) + `prompts.py`의 축명 보충 지시가 합성한다 | S3, S8, R1, R3, R5, B3 |
| **(D)** | 시민에게 정책 전문가의 답을 요구 | 8/12 | `system.md:34`, `rubric.md:46,51,78` — 시민에게 시설·사업·해법 **번역을 시킨다.** `system.md:3`의 "인천시가 만든"이 행정 페르소나로 이를 강화한다 | S1, S4, R4, R5 |
| **(E)** | 답의 후보를 만들어 내민다 | 10/12 | `system.md:40-96`의 긴 금지·허용 예문과 `rubric.md:85`의 모범 문장이 **문형을 복제시킨다** | S5, R5, T2 |
| **(F)** | 일상 어휘가 아닌 추상 표현 | 7/12 | `rubric.md:3,34,36,41,48,63` — 추상 씨앗과 **축을 암시하는 제목**을 자유 변형하게 한다 | R1, R4, B3 |

**(A)의 성격을 정정한다.** 이것은 모델의 위반이 아니라 **지시 준수**다. `system.md:44`가 "답이 될 만한 항목을 당신이 지어내어 참여자 앞에 놓지 마십시오"라고 원리를 정해 놓고, `system.md:94`의 "올바름" 예문이 그 원리를 위반한다 — 예문의 큰 마트·복합 쇼핑몰·주차 공간은 참여자가 말한 적이 없다. 그리고 "이것이 트레이드오프를 정당하게 얻는 방법입니다"라는 승인이 붙어 있다. 같은 템플릿이 `rubric.md`에 두 번 더 반복된다. **세 번 반복된 권장을 한 번의 원리 문장이 이길 수 없다.**

**(E)에서 `TUNING.md`의 원래 판단을 정정한다.** "참여자가 앞서 말한 항목을 정리해 고르게 하는 것은 대체로 괜찮았다"는 관측은 사용자 반응으로는 맞지만 기준으로는 틀렸다. **참여자 말이어도 둘만 골라 마주 세우면 인터뷰어가 답의 범위를 만든 것이다.** 전체를 열어 둔 우선순위 질문만 허용한다.

### 3.2 인터뷰어가 참여자의 말을 되돌려주는 방식

| 항목 | 결함 | 인원 | 확정된 원인 | 처방 |
|---|---|---|---|---|
| **(G)** | 하지 않은 말을 되짚고 없던 정서를 얹는다 | 8/12 | `system.md:15-18`이 **매 턴 생성 요약을 표준 구성으로 못박아** 동의어 승급·조건/극성 변경의 노출면을 만든다 | S3, V1, V2 |
| **(H)** | 사실을 임의로 채워 넣는다 | 4/12 | `system.md:104`의 환각 금지가 **"인천의 정책이나 계획에 대해"로 한정**되어 참여자 근무 형태 같은 것은 사정거리 밖이다. `rubric.md:93`은 긴 마무리 요약을 요구한다 | S3, V2 |
| **(I)** | 매 턴 요약 되풀이가 기계적이다 | 8/12 | `system.md:15-16`의 "되짚기 한 문장, 질문 한 문장" 템플릿 | S3, R4, V1 |

**(G)가 금지 규칙 아래에서도 살아남는 이유가 확인됐다.** `system.md:24`의 자기 점검이 **"이 말이 참여자의 발화를 확인하는가, 아니면 내가 평가를 덧붙이는가"**라는 2지선다인데, (G)의 실제 실패는 어느 쪽도 아니다. "사람들 얘기하는 소리"→"이웃들의 목소리"는 **동의어 승급**, "마감 있을 때만 몰아서 해요"→"바쁘게 지내고 계시는군요"는 **의미 반전**이다. 둘 다 "확인하는" 쪽으로 분류되어 통과한다. **금지가 겨눈 축(평가 여부)과 실패가 일어나는 축(의미 보존)이 어긋나 있다.**

### 3.3 증거 태깅과 강도

| 항목 | 결함 | 인원 | 확정된 원인 | 처방 |
|---|---|---|---|---|
| **(J)** | 강도가 극단으로 쏠린다 | 6/12 | `system.md:92-96`, `rubric.md:68,79-89`가 **참여자가 이미 기운 한 방향만 깊게 파게 해** 반대 극 발화 기회를 줄인다. 남는 태깅 누락분은 미확인 | S5, R5, B6, V4/V6 |
| **(K)** | 분명한 발언이 중립값으로 뭉개진다 | — | `scoring.py:47-55`가 무근거 축을 51로 만들고 `:57-65`의 산출 51과 표면값이 같다. `rubric.md:38`의 희석 기여는 **가설** | B6, R4, V4 |
| **(L)** | 축 사이 오귀속 (오판 5건 중 4건) | — | `rubric.md:44,66-67`이 거리·보존/변화 **대리변수를 공급**하고, `axes.md:107-115,187-192`가 같은 소재의 복수 축 태깅을 허용한다 | R4, T4, V4 후 재현 |
| **(M)** | 가중치 2·3 경계가 세 갈래 | — | **`axes.md:59-63`에서 실증됨.** "정책 수요, 우선순위, 또는 트레이드오프"로 열거한 뒤 다음 문단이 "우선순위나 트레이드오프를 스스로 꺼냈을 때만"으로 좁히며 **정책 수요를 빠뜨린다.** 여덟 극의 3점 앵커는 전부 명시적 포기를 요구한다 | 보류(11장) |

**(J)와 (K)의 분류를 바꾼다.** 이 둘은 반대되는 두 결함이 아니라 **"질문이 반대 극 어휘를 공급하는가"라는 한 변수의 양끝**이다. 공급하면 반대 극 증거가 인위 생성되어 51 쪽으로 눌리고(K), 안 하면 자발 발화에만 의존해 0건이 되어 100으로 간다(J). 원래 이 문서가 "가장 구조적인 관찰"이라고 적은 문장 — *"참여자가 대놓고 양쪽을 말하지 않는 한 강도는 극단으로 간다"* — 은 **태깅의 성질이 아니라 질문 설계의 귀결**이다.

따라서 **(J)(K)의 상당 부분은 3.1(질문 방식)에 속하고 문항 교체로 해결된다.** 태깅 소관으로 남는 잔여는 문항 교체 후에 다시 재야 크기를 알 수 있다.

### 3.4 보고서(구조화)

| 항목 | 결함 | 인원 | 확정된 원인 | 처방 |
|---|---|---|---|---|
| **(N)** | 참여자의 말보다 톤과 규모를 키운다 | 11/12 | `structuring.md:22,72,90,105,145`가 근거가 약해도 네 축을 비우지 못하게 하고 `:18-20`에 대상·규모·확신 보존 규칙이 없다. **상류의 (G)(H) 오염이 입력에 남아 있다** | T1, T3, T5, S3 |
| **(O)** | 인터뷰어가 먼저 꺼낸 소재가 시민 요구로 등재 | 4/12 | `structuring.md:18-22,72-90`에 **인용이 요구를 실제로 함의해야 한다는 계약과 단순 동의 배제 규칙이 없다** | T1, T2, S5 |
| **(P)** | 참여자가 명시적으로 부정한 내용이 요구로 | 2/12 | `structuring.md:18-22`에 명시적 부정·타인 사례 제외 규칙이 없다 | T2, T1 |
| **(Q)** | 강도 숫자의 근거를 보고서가 설명 못 한다 | 4/12 | `scoring.py:47-65`가 근거 건수·빈 축과 51의 두 의미를 내보내지 않는데 `structuring.md:54-66`은 네 축 이유를 쓰게 한다 | B6, T8 |
| **(R)** | 보고서 호출에 축 정의가 없다 | — | `report.py:193-205`가 대화록과 `type_result`만 보내고 `structuring.md:54-72`에 네 축·여덟 극 정의가 없다 | T4 |
| **(S)** | 서비스 불신 발화가 보고서에서 누락 | 1/12 | **`system.md:105`가 원인이다.** 조사 불신을 "주제 밖"으로 분류해 인터뷰어가 한 줄로 받고 넘어간다. 대화록에 한 줄만 남으니 하류에 규칙을 넣어도 살릴 것이 없다 | S6, T6 |

**(N)의 원인 배분을 정정한다.** 이 문서는 (N)을 구조화기 단독 결함으로 봤는데, **상류·하류 혼합**이다. 인터뷰어가 (G)에서 "이웃들의 목소리"를 발화하면 그 낱말이 대화록에 남고 구조화기가 그것을 참여자 어휘로 오인한다. (P)와 (O)는 하류 단독 방어가 가능하지만 **(N)은 되짚기를 먼저 고쳐야 효과를 잴 수 있다.**

**(Q)의 처방 방향을 뒤집는다.** 보고서가 설명하게 하면 안 된다. **빈 축은 설명할 근거가 존재하지 않는다.** 근거 없이 설명하라고 하면 모델이 지어내며, 이는 (R)에서 이미 관측된 행동이다. 코드가 사실을 채운다.

### 3.5 진행과 수집

| 항목 | 결함 | 인원 | 확정된 원인 | 처방 |
|---|---|---|---|---|
| **(T)** | 거주 지역을 아예 묻지 않은 회차 | 3/12 | `rubric.md:11-18`이 첫 응답에 설명·정답없음·시간·호칭·지역·현재일·꿈·2040년 나이를 **경쟁시킨다.** `system.md:16`의 "두세 문장"과 예산이 충돌해 매번 다른 항목이 유실된다 | R2, B8 |
| **(U)** | 별명을 스스로 정해 쓰고 저장하지 않음 | 1/12 | `rubric.md:18`의 **"자연스럽게 확보하십시오"**가 거절·결측보다 임의 호칭 생성을 우선하게 한다 | R2, S3 |
| **(V)** | 첫 인사에서 꿈을 묻는다 | 2/12 | `rubric.md:15,18`이 "앞으로 무엇을 하고 싶은지"와 "꿈"을 **필수 수집값으로** 만든다 | R2 |
| **(W)** | 마지막 질문 뒤 같은 응답에서 종료 | 4/12 | `rubric.md:91-95`에 턴 경계가 없고 `system.md:144-150`이 모델에게 종료를 결정하게 한다. `trailer.py:44-52`·`interview.py:49-61`은 **자격 판정 전에 문구를 스트리밍한다** | R6, S10, B1 |
| **(X)** | 서식 문자 누출 | 1/12 | `system.md:26`의 **열거**가 밑줄을 빠뜨렸고 `interview.py:49-61`에 결정적 스트립이 없다 | S3, B4 |
| **(Y)** | 옛 군·구 이름을 고치는 규칙이 없다 | — | `structuring.md:41`이 LLM에 정규화를 맡기고 **"특정할 수 없으면 발화된 표현을 그대로 두십시오"**라 미등록 값이 흔들린다 | B5, T7 |
| **(Z)** | 참여자 호칭 규칙 부재 | — | 호칭 거절 시 규칙이 없고, `system.md:3,5,7,42-44`의 지배적 2인칭 **"당신"이 출력으로 샌다** | R2, S3 |

**(W)를 코드 결함이 아닌 프롬프트 문제로 본 판단을 정정한다.** 종료 자체는 모델이 `[[END_INTERVIEW]]`로 결정하므로 프롬프트 문제가 맞다. 그러나 **처방은 프롬프트 한 문장으로 끝나지 않는다.** 물음표 가드는 `"마지막으로 덧붙이고 싶은 말을 들려주세요."`처럼 청유형에 뚫린다. 백엔드가 **종료 자격 비트**를 소유해야 한다.

**(Y)를 `structuring.md` 한 문장으로 막힌다고 본 판단도 정정한다.** 행정구역 별칭은 바뀌는 데이터이고 LLM에 맡길 일이 아니다. 버전 있는 별칭표와 `raw_region`/`normalized_region` 분리로 코드가 맡는다.

---

## 4. 새로 발견된 결함

토론에서 나온 것이며 12인 실험 종합에 없다.

1. **소요 시간 불일치.** 제품은 5~8분인데 `rubric.md:13`이 **"대략 10~15분쯤 걸린다"**를 안내하게 한다. 참여자들은 "예고된 시간보다 짧았다"고 적었다.
2. **공식성 허위 약속.** `system.md:3`이 **"인천시가 만든 AI 인터뷰어"**라고 선언하고 `:150`이 "2040년 인천을 준비하는 데 소중하게 쓰이겠습니다"라고 약속한다. 공모전 시연용 프로토타입이다. 사회적 바람직성 편향을 만들고 (S)의 불신을 키우며, **행정 페르소나가 (D) 정책 질문의 역할적 원인이다.**
3. **결핍 전제 질문.** `"지금은 왜 그렇게 안 되는지"`는 참여자가 불가능하다고 말한 적 없어도 현재에 결함이 있다고 전제한다. `"없다면 무엇이 생겨야 하는지"`는 해결을 **시설 신설로 한정**한다.
4. **블록 제목이 극 어휘를 공급한다.** 문항을 다 갈아도 제목이 남으면 같은 프레임이 유지된다.
   - `### 아침, 하루가 시작되는 **속도**` — '속도'는 생활 리듬 축의 조작적 정의다
   - `### **일하고 배우는** 낮과 **사람들과 있는** 저녁` — 우선 가치의 양극을 명명한다
   - `### 주말, **달라진 것과 그대로인 것**` — 발전 가치의 양극을 직접 명명한다
   **그런데 우선 가치는 유일한 10/10 만점 축이다.** 낮·저녁 제목만 양극을 **대칭으로** 명명하고 나머지는 비대칭이거나 한쪽 척도어다. **극 어휘 공급 자체보다 비대칭 공급이 해롭다**는 것이 여기서 나온다. 다만 대칭을 손으로 맞추는 것은 매번 성공을 보장하지 못하므로 **명명하지 않는 쪽**을 택한다.
5. **`rubric.md:38`이 생활 리듬 축에 반대 극을 인위 공급한다는 가설.** *"집을 나서서 처음 마주치는 거리는 얼마나 붐비고 있는지"* — '붐비다'는 활기 극의 어휘이고, 이 질문은 그 현상을 존재하는 연속량으로 전제한 뒤 정도를 채우게 한다. 차분 성향인 사람에게서도 활기 증거가 나온다. **(K)를 설명할 수 있다.** 검증은 크레딧 0이며 반증 조건은 10장에 있다.
6. **악성 응답 카운트를 모델이 센다.** `system.md:112-113`이 1차 경고·2차 중단을 지시하는데 백엔드 카운터가 없다. 12/12에서 발생 0건이라 검증된 적도 없다.
7. **`rubric.md:53`의 배치 오류.** 우선순위 문항이 셋째 블록에 있는데 55~61행이 "여러 가지를 말하지 않았다면 이 질문은 아직 이릅니다"라는 조건을 붙인다. **셋째 블록에 있으면서 넷째 블록 뒤에 물어야 한다.** 정리 절로 옮기면 조건이 자동 충족되고 **경고 일곱 줄이 사라진다.**

---

## 5. 이 문서 자신의 오진 — 정정 목록

3장 각 절에 반영했으나 한곳에 모은다. 다음 라운드에 같은 실수를 반복하지 않기 위해서다.

| # | 원래 판단 | 정정 |
|---|---|---|
| 1 | (E)에서 "참여자가 앞서 말한 항목이면 대체로 괜찮다" | 참여자 말이어도 **둘만 골라 마주 세우면 위반**이다. 전체를 열어 둔 것만 허용 |
| 2 | "오판은 전부 태깅 문제이므로 `scoring.py`를 열지 않는다" | 계산식은 옳으나 **결과 계약에 근거 메타데이터가 없는 것**이 독립 결함 |
| 3 | (J)를 순수 태깅 문제로 봄 | **상류(단극 심화 질문)와 하류(태깅 누락)의 혼합** |
| 4 | (N)을 구조화기 단독 결함으로 봄 | **상류·하류 혼합.** 되짚기가 심은 어휘를 구조화기가 이어받는다 |
| 5 | (Q)를 보고서의 설명 부족으로 봄 | **빈 축은 설명할 근거가 없다.** LLM 설명을 늘리지 말고 코드가 사실을 채운다 |
| 6 | (S)를 보고서 누락으로 봄 | **원인은 `system.md:105`**의 주제 밖 분류다. 상류부터 고쳐야 살릴 분량이 남는다 |
| 7 | (T)(U)를 수집 문제로 봄 | **원인은 `rubric.md:11-18`의 과적재와 "확보" 의무**다 |
| 8 | (W)를 "프롬프트 한 문장으로 막힌다"고 봄 | 청유형에 뚫린다. **백엔드 종료 자격 비트**가 필요하다 |
| 9 | (Y)를 "`structuring.md` 한 문장으로 막힌다"고 봄 | 바뀌는 데이터다. **코드의 별칭표**가 맡는다 |
| 10 | 첫 인사 길이·잔여 미화를 **모델 교체 대기**로 분류 | **지시 모순이다.** 더 순종적인 모델일수록 항목을 더 버리므로 모델을 바꾸면 악화될 수 있다 |
| 11 | 계획 요약본을 "관찰만 하는 항목"으로 분류 | **결정 사안이며 유해하다.** 프리픽스의 40.2%인데 관측 14회에서 인용 0회이고, 정책 어휘를 활성화해 (D)를 키우며, `system.md:69`("예시라는 이름으로 후보를 흘리는 질문")에 비추면 애초에 쓸 수 없는 자료다 |

---

## 6. 두 구조적 딜레마 — 결론

### 6.1 인터뷰어의 축 인지

**결론: 인터뷰어는 축·극·커버리지 임계를 영영 모른다.**

운영 지시가 축의 **표시 이름 하나만 실어도** 두 극이 역산되고, 실측에서는 "발전 가치"를 받고 성장이냐 분배냐를 묻는 **엉뚱한 축의 이지선다**까지 나왔다. 반대로 아무것도 안 주면 커버리지 책임을 이행할 수 없다. 축 이름과 침묵 사이의 제3의 선택지가 답이다.

**백엔드가 `ANSWER_KEY`(한 답칸의 식별자)와 검수를 통과한 질문 문장만 내려보낸다.** `axis_hint`는 백엔드 전용이며 프리픽스에 직렬화하지 않는다. 문맥이 맞지 않으면 그 키를 건너뛰고 다음 키를 고른다.

**축 커버리지는 질문 선택 신호일 뿐 종료 자격이 아니다.** 거절·무응답·빈 축을 정상 상태로 둔다. 이것이 (C) 11/12의 구조적 동력을 끊는다 — 모델이 반복 질문을 하는 이유는 빈 축을 실패로 믿기 때문이다.

`PLAN.md` 3.2의 커버리지 계약과 5.1의 "표시 이름에는 대비가 없어 안전하다"는 판단을 함께 고쳐야 한다.

### 6.2 루브릭 주말 블록의 유도성

**결론: 변화와 보존을 제목에서도 질문에서도 명명하지 않는다.**

원래 문항이 어려웠던 이유는 유도성만이 아니라 **판별력 부재**다. "무엇이 그대로 남았으면 하는가"에는 새로움 지향자도 무언가를 답하므로 보존 발언이 **보편적으로** 생산되고, 보편적으로 생산된 발화는 극을 가르지 못한다.

**대신 실제로 찾아간 인천 장소와 그 이유를 묻는다.** 이미 한 행동은 유도할 수 없다.

**다만 장소의 연식·종류는 발전 가치 근거가 아니다.** 새 공원에 간 이유가 조용해서면 선호 공간이고, 오래된 시장을 고른 이유가 싸서면 편의다. **참여자의 이유가 변화·대체·새로움 또는 이어짐·기억을 실제로 말할 때만** 발전 가치 근거로 센다. 아니면 빈 축을 허용한다.

**"네 축을 반드시 채운다"와 "유도하지 않는다"가 충돌하면 후자가 이긴다.** 질문에 극을 심어 계약을 만족시키는 것이 아니라 제품이 근거 부족을 표현해야 한다.

---

## 7. 설계 원칙 — 왜 어떤 규칙은 지켜지고 어떤 규칙은 안 지켜지는가

토론에서 나온 이론이며 모든 처방이 여기서 파생된다.

**지켜지는 규칙은 표면 신호로 검사 가능한 규칙이고, 안 지켜지는 규칙은 의미 판단을 요구하는 규칙이다.**

- **성공한 규칙**: 감탄 금지(`system.md:19-25`) — 12회차에서 **노골적 과찬 0회**. 목록이 완전해서가 아니라 `:24`가 일반화 축을 **'형용사'라는 품사 범주**로 명시했기 때문이다. 모델은 자기 출력에서 형용사를 식별할 수 있다.
- **실패한 규칙**: 선택지 금지 — 겨눈 것이 **문장 구조**라 내용어를 바꾸면 표면이 달라진다. `:44`의 일반화 조항은 "답이 될 만한 항목"이라는 의미 판단을 요구하고, 의미 판단은 모델이 자기에게 유리하게 한다.
- **실패한 규칙**: 서식 금지(`:26`) — 열거만 있고 일반화 축이 없어 밑줄이 샜다.

**따라서: 규칙은 표면 신호로 내리되, 신호가 닫힌 집합이거나 일반화 축이 품사·기능 범주로 명시되어야 한다. 둘 다 없는 열거는 반드시 샌다.**

**단서 하나.** 미탐 비용이 크고 오탐 비용이 0에 수렴하는 문맥에서는 열거를 써도 된다. 종료 턴이 그 자리다 — 오탐이 나면 한 턴 더 진행할 뿐이고 그것이 원하는 방향이다.

**그리고 금지가 규범을 압도하면 수렴점이 생긴다.** `system.md` 본문 대부분이 "~하지 마십시오"이고 긍정 지시는 둘뿐이다. 금지 목록만 받은 모델은 모든 금지를 통과하는 안전한 형태 하나를 찾아 반복한다. 역산하면 **"긴 정중한 문장 + 되짚기 템플릿 + 트레이드오프 + 추상적 수식"**이고, 관측된 (A)(F)(I)가 정확히 이것이다. **결함 26개는 무관한 26개가 아니라 하나의 수렴점을 여러 각도에서 본 것이다. 그러므로 금지를 더 넣지 않는다.**

---

## 8. 처방 명세

**패치 전문은 `docs/tuning-debate/round4-A.md`와 `round5-B.md`에 있다.** 여기는 ID와 요지, 그리고 값이 가장 큰 문안만 싣는다.

### 8.1 `system.md` — [S1]~[S10]

| ID | 대상 | 요지 |
|---|---|---|
| S1 | 3행, 150행 | 제작 주체 주장과 반영 약속 제거. "인천시가 만든"을 취지 서술로, "소중하게 쓰이겠습니다"를 "정리해 두겠습니다"로. **한 번의 교체로 공식성 편향·마무리 예문의 감탄 위반·(D)의 역할적 원인이 함께 닫힌다** |
| S2 | 26행 | 서식 금지를 열거에서 상위 범주로. 확정 차단은 [B4]가 맡는다 |
| S3 | 14~18행 | **질문 개수 3층 + 되짚기 규칙** (아래 상술) |
| S4 | 34행 | 경험만 묻기, 결핍 전제 금지, 중학생 어휘 |
| S5 | 40~96행 | **트레이드오프 권장 템플릿 전량 삭제.** 참여자가 말하지 않은 후보·예시·손실·대가·인과를 질문에 넣지 않는다 |
| S6 | 105행 | 조사 불신은 주제 밖이 아니다. 되돌리지 말고 한 겹 더 듣는다 |
| S7 | 28행 | **명시적 거절은 즉시 폐쇄, 단순 "모르겠다"는 한 번만 더 작게.** 비워 둔 채 끝나는 것이 정상이라고 명시 |
| S8 | 115~122행 | 커버리지 책임과 종료 금지 삭제 |
| S9 | 124~132행 | 운영 지시 계약. `axis_hint` 미노출 |
| S10 | 144~152행 | 종료 형식 |

**[S3]의 질문 개수 — 3층 구조.** 이것이 (B) 12/12의 처방이다.

- **1층 물음표 하나** — 결정적. 백엔드가 검사한다.
- **2층 주절 의문사 하나** — 어디·무엇·누구·언제·어떻게·어떤·왜·얼마나·몇. **닫힌 집합**이라 열거가 새지 않는다. 조건절(`~면`, `~때`) 안의 것은 세지 않는다.
- **3층 정보 단위 하나** — **최종 판정.** *"정보 단위는 참여자가 다른 부분과 독립해서 답하거나 거절할 수 있는 최소 답칸입니다. 한 부분에 완전히 답하고도 다른 부분이 미답으로 남을 수 있으면 두 질문입니다."*

1·2층은 **후보 추출기**이고 판정은 3층이 한다. 2층만으로는 `"주말 목적지·활동을 알려 주세요"`(의문사 0)를 놓치고, 3층만으로는 자기 점검이 무르다. 이 정의는 `"친구와 가고 싶은 곳은 어디인가요?"`를 정상으로 통과시킨다 — 친구는 답칸이 아니다.

**[S3]의 되짚기 — 낱말 층과 의미 층.**

- 빈도를 **두 턴에 한 번**으로. 관측은 "세 번째쯤부터" 기계적으로 느꼈다는 것이므로 존재가 아니라 빈도가 문제다.
- **명사와 동사는 참여자가 쓴 낱말 그대로.** 조사·어미·높임·어순은 자유. `들리다·보이다·있다·하다`와 `~라는 말씀이시군요`는 **잇는 용도로만** 허용하되 그 낱말로 없던 사실을 만들지 않는다("공원이 생기면 좋겠다"→"공원이 있다"는 위반).
- **대상·조건·범위·극성·정서·인과를 바꾸거나 보태지 않는다.** 형용사·부사 승급 금지(괜찮다→훌륭하다, 가끔→늘). `좀`·`가능하면`·`글쎄요`를 빼지 않는다.
- 자기 점검을 **"이 문장의 명사와 동사가 전부 참여자 입에서 나왔는가"**로 바꾼다. 2지선다가 통과시키던 동의어 승급이 여기서 걸린다.

### 8.2 `rubric.md` — [R1]~[R6]

| ID | 대상 | 요지 |
|---|---|---|
| R1 | 3행 | "바꿔서 물으십시오" 삭제. 16문항을 **질문 은행의 원본**으로 재정의해 `rubric.md`와 백엔드를 하나의 계약으로 묶는다 |
| R2 | 9~18행 | **도입 3턴 분할.** 1턴 호칭만, 2턴 거주지만, 3턴 근황+2040년 나이. 소요 시간·꿈 제거, 임의 호칭 금지, "당신"·"시민님" 금지 |
| R3 | 22~32행 | 커버리지 의무 삭제. 무엇을 얼마나 다룰지는 운영 지시가 정한다 |
| R4 | 34~70행 | **네 제목과 16문항 전면 교체** (아래) |
| R5 | 72~89행 | 가상 대가 템플릿·"강한 증거가 됩니다"·"보고서에도 판정에도 쓰이지 않습니다" 삭제 |
| R6 | 91~97행 | 정리 절 턴 분리. 우선순위 문항을 여기로 옮겨 선택과 이유를 나눈다 |

**[R4] 블록 제목** — 제목은 **언제·어디의 장면인지만** 알리고 무엇을 재는지는 알리지 않는다.

`### 아침` / `### 집 앞` / `### 낮과 저녁` / `### 인천의 장소`

**[R4] 최종 16문항.** 괄호는 `ANSWER_KEY`다.

**아침** — ① 2040년 쉬는 날 아침에는 몇 시쯤 눈을 뜨면 좋겠어요? (`morning_wake_time`) ② 요즘 쉬는 날에는 눈을 뜬 뒤 무엇을 하세요? (`morning_current_action`) ③ 2040년 아침에 창밖에서 무슨 소리를 듣고 싶으세요? (`morning_sound`) ④ 그런 아침을 바라는 이유는 무엇인가요? (`morning_reason`)

**집 앞** — ⑤ 2040년에 집을 나서면 눈앞에 무엇이 보이면 좋겠어요? (`space_visible_item`) ⑥ 쉴 때 주로 어디로 가세요? (`space_current_rest_place`) ⑦ 그곳에는 왜 가세요? (`space_place_reason`) ⑧ 2040년에 잠깐 시간이 비면 무엇을 하며 보내시겠어요? (`space_future_spare_action`)

**낮과 저녁** — ⑨ 요즘 낮에는 주로 어디에서 시간을 보내세요? (`day_current_place`) ⑩ 2040년에는 낮에 무슨 일을 하고 있으면 좋겠어요? (`day_future_action`) ⑪ 하루 일이 끝난 뒤에는 보통 무엇을 하세요? (`evening_current_action`) ⑫ 힘든 일이 있으면 보통 어떻게 하세요? (`hard_day_response`)

**인천의 장소** — ⑬ 인천에 온 지인과 함께 가 본 곳이 있다면 어디였어요? (`incheon_shared_visit_place`) ⑭ 그곳을 함께 간 까닭은 무엇인가요? (`incheon_shared_visit_reason`) ⑮ 최근 일부러 찾아간 인천의 장소는 어디였어요? (`incheon_recent_visit_place`) ⑯ 그곳을 일부러 찾아간 건 왜였어요? (`incheon_recent_visit_reason`)

**설계 근거.** 시간 태도는 **현재·과거 7 / 2040년 희망 5 / 이유 4**다. "참여자가 겪은 것만 묻는다"를 문항 층에서 구현하면서 미래 프레임은 다섯 문항이 유지한다. 문형은 시각 1 / 대상·행동 6 / 장소 4 / 방법 1 / 이유 4로 흩었다 — **주제만 바꾸고 표현을 고정하면 (I) 기계성이 되짚기에서 질문으로 자리를 옮길 뿐이다.** 질문 본문에 `가장` 0회, `먼저` 0회다. `가장 먼저`는 매 문항에 우선순위 틀을 씌우므로 `system.md:67`이 금지한 것의 약한 형태다.

⑬⑮에 경험이 없으면 `없음`이 정상 답이다. ⑦⑭⑯은 직전 장소 답이 있을 때만 적용하고 맞지 않으면 건너뛴다.

**질문 은행 자격 검사 10항목** (코드·문서에 두고 프리픽스 지면 0): ① 답칸 하나 ② 주절 의문사 하나 ③ 극 어휘 0 ④ 추상어 0(속도·간격·공기·풍경·분위기·결·감각) ⑤ 정책어 0(투자·기반·경쟁력·우선·재원) ⑥ 중학생 어휘 ⑦ 참여자에게 없는 후보·손실·인과 0 ⑧ `가장`·`먼저`·`우선` 프레임 0 ⑨ 사람·장소·정책을 답의 기본값으로 강제하지 않음 ⑩ 동일 문형의 기계적 반복 없음.

**⑦~⑩은 이번 라운드에 추가됐다.** 원래 6항목만으로 검사했을 때 `가장` 반복을 통과시켰기 때문이다. **자기 검사가 자기 결함을 못 보면 그 검사는 쓸모가 없다.**

### 8.3 백엔드 — [B1]~[B8]

| ID | 요지 |
|---|---|
| B1 | **종료 자격 비트 `termination_allowed`.** 기본 false. 추가 발언 확인을 내보낸 시점에도 false. **참여자 답을 한 번 받은 뒤에만 true.** 자격 없을 때의 `[[END_INTERVIEW]]`는 **문장 형태와 무관하게** 제거. 모델에게 알리지 않으므로 운영 지시 이행률에 의존하지 않는다 |
| B2 | **악성 카운터.** 판정은 모델이 신호로 내고 **카운트와 고정 응답은 백엔드**가 소유. 백엔드에 악성 분류기가 없으므로 판정까지 옮기면 기능이 사라진다 |
| B3 | **질문 은행 스키마.** `answer_key` / `axis_hint`(백엔드 전용, 절대 미직렬화) / `variants[]`(`text` + `condition`). 문맥 조건형 변형으로 야간 노동자에게 아침 출근길을 묻는 사고를 막는다 |
| B4 | 서식 스트립 + 복합 질문 후보 파서 |
| B5 | **지역 정규화.** 버전 있는 별칭표. `raw_region` 보존, `normalized_region`만 집계 |
| B6 | **강도 진단 기록.** `evidence_count`·양극별 가중치·`empty_axis`를 지금부터 기록 |
| B7 | 계획 요약본을 인터뷰어 프리픽스에서 제거. 운영 지시 생성기는 부족 축의 키를 고르되 **커버리지를 종료 조건으로 쓰지 않는다** |
| B8 | 소요 시간 안내를 화면 한 곳에서 관리 |

### 8.4 `structuring.md` — [T1]~[T8]

[T1] 요구마다 직접 인용과 턴 번호 연결, 연결 못 하면 쓰지 않음 · [T2] 인터뷰어 유래·부정·타인 예시 배제 · [T3] 대상·규모·주체·확신 확대 금지, 제한 표현 보존 · [T4] 코드가 `axes.md`에서 **축·극 정의만 뽑아** 덧붙임 · [T5] 근거 없는 축은 "확인하지 못함"으로 · [T6] 조사 신뢰·참여 조건 필드 신설 · [T7] 지역 원문 보존(정규화는 [B5]) · [T8] 강도 설명 금지, 코드가 사실을 채움.

---

## 9. 착수 순서와 통과 판정

| 단계 | 착수 | 통과 판정 |
|---|---|---|
| **0** | 로그·모델·프롬프트 해시 고정. 층화 30 표본과 2×2 입력 고정 | ID·시드·설정·라벨 지침 고정 |
| **0A** | [T1]~[T8], 보고서 스키마 | 기존 12건 대화록에서 **(P) 부정 반전 0, (O) 인터뷰어 유래 0, 인용 없는 요구 0**, 인용 원문 일치 100% |
| **1** | [B7] 계획 요약본 제거, [B1]~[B5][B8], [S8]~[S10], [R1]~[R3] | **프리픽스 문자 40% 이상 감소.** 축·극명 노출 0, 은행 문장 일치 100%, 무자격 종료 수용 0 |
| **2** | [S3]~[S5], [R4] 16문항과 제목, [R5][R6] | 단발 프로브에서 복합 답칸·새 후보/대가·거절 뒤 반복·정책 해법 요구·질문+종료가 **각각 0** |
| **3** | 되짚기 빈도·낱말/의미 규칙, [B4] | 층화 30에서 의미 왜곡·정도/빈도 승급 0, **κ≥0.80**, 형식 문자 노출 0 |
| **4** | 실호출 완주 2회 (말 많은 인물 / "글쎄요"만 하는 인물) | 한 답칸 100%, 세 번째 재질문 0, 마지막 답 기회 100%, 평균 응답 2문장 이하 |
| **5** | [B5][B6], [T4]~[T8], 구조화 2×2 | 빈 축 51 오표시 0, 근거 없는 요구 0, 오정규화 0 |
| **6** | 회귀 + 실모델 최소 1세션 완주 | 감탄 0, 축·코드 노출 0, 악성 경계·토큰 형식·역할 변경 테스트 100% |

**0A를 1단계보다 먼저 두는 이유**는 `structuring.md`가 인터뷰어 프리픽스와 다른 파일이라 지면을 다투지 않고, **입력이 고정이라 통제된 전후 비교가 가능한 유일한 구간**이기 때문이다. 인터뷰어 프롬프트를 고치면 대화 자체가 달라져 전후를 같은 조건으로 비교할 수 없다.

**착수 순서에 대한 원래 판단을 정정한다.** 이 문서는 질문 난이도(D·F)를 2순위로 뒀는데, **형식(A·B·C·E)이 먼저다.** 싸고(대부분 삭제로 끝난다), 원인이 확정적이고, 상류에서 (J)(N)(O)와 이탈 충동까지 함께 줄인다. 발생률도 형식 쪽이 높다((A)(B) 12/12, (C) 11/12, (E) 10/12 대 (D) 8/12, (F) 7/12).

**모델 자기 평가는 어느 단계의 게이트에도 쓰지 않는다.**

---

## 10. 검증 설계 — [V0]~[V6]

**[V0] 정적 검사(호출 0).** 프리픽스에 채점 어휘(증거·가중치·태깅·판정)·금지 예문·중복 지시·`axis_hint`가 남았는지. **프리픽스 문자 수를 수정 전과 대조한다 — 새 규칙을 넣었는데 줄지 않았으면 제로섬 재설계 실패다.**

**[V1] 파서 지표(호출 0, 기존 12건에 적용).** 12개 지표를 각 처방과 1:1로 대응시킨다. 핵심은 **5a — 인터뷰어 발화의 내용어 중 참여자가 대화 전체에서 한 번도 쓰지 않은 것의 개수**다. 직전 턴이 아니라 **누적** 대조여야 정상 되짚기의 오탐이 사라진다. 판정은 임계값이 아니라 **분포 이동**(중앙값·상위 10%)으로 한다.

**파서는 확정 판정과 후보 추출을 나눈다.** 확정: 제어 토큰 위치, 종료 턴 허용, 물음표 수, 서식 문자, 금지 채점 어휘, 필수 필드 공란. 후보만: 신규 내용어 비율, 질문 간 유사도, 추상어·정책어, 되짚기 종결형, 강도 분포.

**[V2] 층화 30 + 이중 라벨링.** 실패 직전 상태 30개를 고정하되 **파서 상위 10 + 무작위 10 + 파서 저값·0값 10**으로 층화한다. **저값 10이 거짓 음성을 잡는다** — `"마감 있을 때만 몰아서 해요"` → `"몰아서 해요"`는 신규 내용어가 0인데 조건절을 삭제했다. 상위만 보면 이 실패가 구조적으로 안 보인다.

버전을 가리고 두 평가자가 고정 표로 독립 라벨링한다. 항목별 일치율과 **Cohen's κ**를 기록하고 κ<0.80이면 지침을 고쳐 다시 블라인드로 돌린다. 최초 독립 라벨과 불일치 사유를 보존하고 같은 30개를 다음 버전에 재사용한다. 평가자당 약 한 시간, 조정까지 2~3인시로 **라운드마다 반복 가능하다.**

**"정보 단위 수" 항목의 κ가 가장 낮게 나올 것으로 예측한다.** 낮으면 지침에 [S3]의 의문사 계수를 조작적 정의로 넣는다. **같은 정의가 프롬프트 지시·라벨링 기준·은행 검수·루브릭 진단 네 곳에서 쓰인다.**

**[V3] 구조화 2×2 교차.** 대화록(기존/정화) × 구조화기(기존/수정) = 12건 × 4 = 48회. **정화본은 사람이 만들지 않는다** — 5a에 걸린 명사·동사를 참여자 원어로 되돌리고 되돌릴 대상이 없으면 삭제하는 규칙으로 생성한다. 사람이 정화하면 무엇이 '승급'인지 판정하는 것 자체가 측정 대상 판정이고, 실험자 편향이 조건 2·4에 직접 들어간다. **조건 2·4는 상류 수정의 상한이지 실제 효과가 아니라고 라벨한다.**

**[V4] `rubric.md:38` 가설의 반증 조건.** 전부 크레딧 0이다.

- **참이면**: 활기 극 증거 중 38행 계열 답에서 나온 것이 30% 이상 / 그 증거의 가중치가 1에 몰림 / 차분 판정자에게서도 이 문항 턴의 활기 증거가 나옴 / (K) 사례에서 그 증거를 빼고 재계산하면 51에서 유의하게 오름.
- **거짓이면**: 근거 객체에 활기 근거가 없고 `empty_axis=true` / 활기 근거가 38행 답이 아니라 자발 발화 / 38행 답이 혼잡을 부정했는데 활기로 태깅(원인은 극성 판독) / 38행 질문·답 쌍만 제거해 재채점해도 값이 같음.
- **반증되어도 문항 교체는 유지한다.** 인과 가설이 기각될 뿐, `"얼마나 붐비고"`가 한 극을 전제하는 것은 그대로이므로 은행 자격 검사를 여전히 통과하지 못한다.

**[V5] 단발 프로브(호출 24회).** 고정 히스토리 8종 × 3회. 실제 대화에서 4/12 빈도인 조건을 **100% 발생시키므로** 완주보다 판별력이 높다. 세션을 완주시키면 참여자 발화가 매번 달라 전후 비교가 성립하지 않는다.

**[V6] 실호출 완주 2회.** **"글쎄요"만 하는 인물이 더 중요하다** — (C)의 최악 조건이고 빈 축 허용이 실제로 작동하는지를 가르는 유일한 시험이다.

---

## 11. 보류와 미확인

### 안 고치는 것 — 근거 있는 보류

1. **`axes.md`의 가중치 정의와 중복 방지 규칙.** (M)의 세 갈래 문턱은 `axes.md:59-63`에서 실증됐지만 **지금 고치지 않는다.** 축 40개 중 35개가 맞는 체계이고, 7장 결론에 따르면 **문항 교체 뒤에 남는 것을 재야 범위를 알 수 있다.** 지금 고치면 과잉 수정이 된다. 무기한이 아니라 **선행 조건부**다 — (J)(K)(L)(M)의 특정 발화가 왜 누락·귀속됐는지 최소 재현을 만든 뒤에 손댄다.
2. **강도 표시 계약(`PLAN.md` 5.5).** 사용자 표시 변경은 프롬프트 A/B 뒤로 미룬다. 지금 바꾸면 수정 효과를 잴 기준선이 사라진다. **다만 내부 기록([B6])은 지금 시작한다** — 빈 축을 정상 51로 취급한 시연을 기준선으로 삼으면 안 된다. (이 보류에는 이견이 있었다. 알려진 오표시를 남기는 것이므로 내부 기록과 같은 단계에서 표시도 고쳐야 한다는 주장이 있었고, 사람이 판단할 사안이다.)
3. **`rubric.md:44`("어디까지 가서 해결하는지")의 (L) 상류 가설.** 문항 자체는 [R4]에서 교체되지만 **가설 검증은 38행과 동시에 하지 않는다.** 두 가설을 한 번에 바꾸면 무엇이 들었는지 알 수 없다.
4. **호출 분리의 정량 비교(토큰·캐시 적중률).** **포기한다.** 대조군이 지금 프리픽스를 전제하는데 계획 요약본 제거만으로 캐시 경계가 통째로 바뀐다. 목적이었던 증거 오염 차단은 이미 확인됐고, 남은 비용 지표는 튜닝 후 새 대조군으로 재는 것이 정확하다. **재생성이 함께 처리하려던 Phase 5 집계의 실데이터 검증은 [V6] 완주가 대신한다.**
5. **모델 교체(8.1).** 호출 계약을 고정한 뒤 비교한다. **다만 "첫 인사 길이"는 이 목록에서 내려온다** — 지시 모순이므로 모델 교체로 풀리지 않는다.
6. **인터뷰어가 만든 트레이드오프의 가중치 3.** 복구하지 않는다. 모델이 만든 대가에 대한 답은 강한 증거가 아니라 **강하게 유도된 증거**다. 3점 빈도가 낮아지는 것은 측정 실패가 아니라 의미 회복이다.

### 못 고친 것 — 미확인

- **의문사가 없으면서 답칸이 둘인 질문**(`"주말 이야기랑 아침 이야기를 들려주세요"`). 1·2층이 모두 통과시키고 3층 정의만 잡는다. **프롬프트만으로는 닫히지 않으며 사람 라벨링에 의존한다.** [V2]가 이 잔여의 크기를 잰다.
- **악성 판정의 정확도.** 12/12에서 악성 입력이 0건이었으므로 모델의 판정이 정확한지 검증된 적이 없다. [B2]는 카운트만 해결한다.
- **`rubric.md:38`·`44` 가설의 인과.** [V4]로 검증 예정.
- **새 프롬프트의 실호출 완주 결과.** 30턴 재생·48회 구조화·실제 Gemini 완주·UI 표시가 전부 미실행이다. **각 게이트를 통과하기 전에는 수정 완료라고 부르지 않는다.**

---

## 12. 제로섬 회계

원본은 `system.md` 152행 + `rubric.md` 97행 = **249행**이다.

**적용 후 162행, 순감 87행**을 배포 기준으로 삼는다. 두 전문가가 각각 순감 87행과 77행을 냈는데 산술은 양쪽 다 맞고, 차이는 **교체 경계와 유지 문장을 삽입으로 다시 세는 방식**의 차이였다. 완결 블록 교체 기준으로 통일했다.

**계획 요약본은 이 회계 밖이다.** `data/plan2040_summary.md` 30,598바이트(한글 약 10,200자, 현 프리픽스의 40.2%)가 `prompts.py:37-43`에서 별도 파일 내용으로 붙으므로 두 마크다운 파일의 행 회계에 들어가지 않는다. **인터뷰어 호출에서 빠지는 이 40.2%가 실제로는 가장 큰 감량이며, 위 87행과 합산하지 않는다.**

적용 전에는 예상치이고, 적용 뒤 `wc -l`과 diff로 확정한다.

---

## 13. 이 결론이 나온 과정

2026-08-11에 두 프론티어 모델에게 각각 AI 프롬프팅 전문가 역할을 주고 **5라운드 대립 토론**을 시켰다. 자료는 프로젝트 개요와 `system.md`·`rubric.md`·`TUNING.md`로 동일하게 제한했고, 서로의 산출물을 교차 전달해 반박·허점 공격·양보를 반복하게 했다.

**원문 아홉 건이 `docs/tuning-debate/`에 있다.** 이 문서의 모든 결론에는 그쪽에 논증과 실패 시나리오가 있다.

기록해 둘 것 셋.

- **인용은 매 라운드 전수 대조했다.** 양쪽 다 조작이 없었고 행 번호까지 정확했다. 코드로 확인한 것(`scoring.py`의 51 하한, `trailer.py`의 종료 경로, 계획 요약본 40.2%, `axes.md`의 세 갈래 문턱)은 이 문서에 사실로 반영했다.
- **양쪽이 독립적으로 같은 결론에 도달한 지점이 가장 신뢰할 만하다.** (A)의 원흉이 `system.md:92-96`이라는 것, 계획 요약본을 빼야 한다는 것, 커버리지를 백엔드로 옮겨야 한다는 것, 블록 제목을 시간·장소 표지로 바꿔야 한다는 것이 그렇다.
- **5라운드는 한쪽만 제출했다.** 다른 쪽이 세션 한도에 걸렸다. 다만 제출된 5라운드가 상대의 비판을 전부 흡수했고(문항의 `가장` 전량 제거, 현재/희망/이유 7·5·4 배분 채택, 검사 항목 확장, 회계 수렴) 실질 합의에 도달했으므로 그 시점에 종료했다. **미해결로 남은 대립은 11장 보류 2번 하나뿐이다.**
