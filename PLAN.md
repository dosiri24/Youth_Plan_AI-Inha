# 유스플랜AI 설계 문서

> **이 문서의 성격.** 구현이 아니라 **맥락**을 담는다. 무엇을 왜 만들고 있고, 무엇을 하지 않기로 했고, 어느 파일이 무엇의 원천인지를 적는다. API 필드, JSON 스키마, 채점 앵커 전문처럼 **코드와 프롬프트 자산에 이미 있는 것은 여기에 옮겨 적지 않는다.** 그것들의 원천은 7장에 있다.
>
> **상태.** 전 기능이 동작하는 배포된 프로토타입이다. 참여자 흐름(인터뷰 → 결과 → 수정 → 제출 → 공유)과 담당자 흐름(대시보드 → 상세 → 삭제 → CSV)이 모두 Cloud Run에서 외부 접속 가능하다. 3.7의 종합 브리핑까지 구현돼 있다. 그 밖에 남은 것은 실제 스마트폰 통주 테스트와 시연 리허설이며, 살아 있는 결함과 미결은 9장에 있다.
>
> **구현과 이 문서가 어긋날 때.** 동작에 관한 서술이 어긋나면 **구현이 최신이라고 보고 이 문서를 고친다.** 한 세션에 한 작업씩 진행하면서 사람이 그때그때 결정을 추가하기 때문에, 이 문서가 뒤처지는 것이 정상 상태다. 다만 **설계 원칙과 "하지 않기로 한 것"(2장)이 어긋나면 사람에게 확인한다.** 그쪽은 코드가 원천이 아니다.

## 1. 무엇을 왜 만드는가

유스플랜AI는 인천시 청년정책 아이디어 공모전에서 심사위원에게 시연할 프로토타입이다. 대규모 상용 서비스나 실제 정부 운영 시스템이 아니다. 팀명은 "청사진"이다.

도시기본계획은 장기 도시 발전 방향을 정하지만, 기존 공청회는 시간·정보·접근성 문제로 청년의 참여를 어렵게 한다. 유스플랜AI는 공청회를 대체하지 않고 보완한다.

참여자는 앱 설치 없이 모바일 웹에 접속해 AI 인터뷰어 **"바다"**와 1:1로 대화한다. 2045년 인천에서의 자기 하루를 상상하고, 꼬리질문을 통해 막연한 생각을 구체적인 정책 수요로 발전시킨다. 시스템은 그 대화를 구조화된 요구 보고서와 "내가 바라는 2045년 인천의 도시유형"으로 변환한다.

도시유형은 일상어로 정의한 4개 축을 조합한 16유형이다. 참여자는 4글자 코드 대신 유형별 일러스트와 별명, 일상어 라벨 배지를 받는다. 이것이 무보상 인터뷰의 유일한 완주 보상이자 SNS 공유 소재가 되고, 담당자에게는 축별 분포와 근거 발화를 함께 제공한다.

**핵심 설계 철학은 성격 검사가 아니라는 것이다.** 참여자가 어떤 사람인지가 아니라 어떤 도시를 원하는지를 진단한다. 참여자가 스스로 꺼낸 말에서만 증거를 찾는다. 답의 후보를 내밀지 않는 것은 인터뷰어 프롬프트가 지키고, 채점은 인터뷰어가 어떻게 물었는지를 판단하지 않는다 — 도시유형은 행정 데이터가 아니라 참여를 유도하는 완주 보상이기 때문이다(4.5).

모든 구현 판단에는 다음 우선순위를 적용한다. 핵심 기능의 큰 틀을 완성하고, 깔끔한 UX/UI를 만들고, 시연 중 안정적으로 동작하게 한다. 완벽함보다 끝까지 작동하는 전체 사용자 여정을 우선한다.

성공 조건은 시연에서 `접속 → 인터뷰 → 유형·보고서 확인/수정 → 제출 → 공유 카드 → 담당자 화면`이 끊김 없이 작동하는 것, 참여자가 결과가 자기 생각을 반영한다고 느끼는 것, 담당자가 축별 분포·평균 강도·정성 요약·근거 인용을 함께 보는 것이다.

## 2. 확정된 설계 결정

### 2.1 제품 원칙

- 핵심 MVP는 `AI 인터뷰 → 유형 판정·보고서 구조화 → 참여자 확인·제출`이다.
- 인터뷰·유형 판정·확인·제출은 실시간 구간이고, 다수 제출본의 비식별화·집계·정성 요약은 배치 구간이다. **두 구간의 경계는 제출 액션이다.**
- 유형 정량화에는 사전 정의된 4축을 쓴다. 임베딩 군집화는 통제와 해석이 어려우므로 쓰지 않는다.
- **유형은 성격 MBTI와 완전히 분리한다.** MBTI 글자 차용도 하지 않고, 성격 MBTI를 수집하거나 병렬 제시하거나 상관관계를 검증하지도 않는다.
- 프롬프트, 루브릭, 축 정의, 채점 앵커는 코드에 하드코딩하지 않는다.

**공식 표기는 "도시유형"이다.** 붙여 쓰고 띄우지 않는다. 검사 자체를 가리킬 때는 "도시유형 테스트", 한 사람의 판정 결과를 가리킬 때는 "도시유형"이다. MBTI를 두고 "MBTI 테스트"와 "MBTI 결과"를 함께 쓰는 것과 같은 용법이며, 두 표기가 아니라 한 표기의 두 쓰임이다. 참여자 화면·담당자 화면·프롬프트·문서 어디서나 이 표기를 쓴다.

### 2.2 기술 스택

백엔드는 Python 3.11, uv, FastAPI와 Uvicorn, google-genai SDK, pytest, ruff로 고정한다. 프론트엔드는 Node.js 22 LTS, pnpm, Next.js App Router와 TypeScript, Tailwind CSS, shadcn/ui, ESLint와 Prettier로 고정하고 모바일 우선 레이아웃을 쓴다. 데이터와 배포는 Cloud Firestore Native 모드, Google Cloud Run, 리전 `asia-northeast3`, `web`·`api` 두 서비스다.

### 2.3 명시적으로 하지 않는 것

이 목록은 코드에 없는 것을 적은 것이라 코드로 확인할 수 없다. **없는 이유가 있어서 없는 것이므로 "빠졌다"고 판단해 채우지 않는다.**

- 자체 AI 모델 개발, 임베딩 모델과 군집화, 별도 벡터 데이터베이스
- 성격 MBTI 수집·병렬 제시·상관 검증
- 자동저장과 중단 세션 복원
- 인터뷰 본편의 폼 입력이나 우회·폴백 화면
- 단계별 시스템 프롬프트 교체, 대화의 강제 턴 상한
- 참여자의 유형·강도·판정 근거 수정, 보고서 직접 편집
- 요약본과 원문을 나란히 대조하는 UI
- 제출 후 재작성·재제출
- Gemini explicit 캐시 API, Firestore 로컬 에뮬레이터
- 애플리케이션 수준의 추가 암호화
- 관리자 로그인·권한 시스템 (계정, 비밀번호 해싱, 세션, 역할 구분). 대신 6.2의 단일 접근 코드를 쓴다
- 악성 응답 전용 판정기와 그 데이터의 저장·전달
- 규칙 기반 개인정보 필터와 최소 인원 임계값
- 대화 원문·보고서 본문의 애플리케이션 로그 기록
- **실패한 인터뷰 턴의 자동 재시도**, Gemini 안전필터 오작동 대응. 응답이 아예 오지 않은 요청을 전송 계층에서 한 번 다시 보내는 것은 여기 해당하지 않는다(3.4) — 막으려는 것은 모델이 이미 만든 턴을 다시 만드는 일이다
- **언급 장소의 좌표 변환.** 소속 군·구를 확정하는 데서 멈춘다. 지도에 핀을 찍으려면 지오코딩이 필요하고, 그것은 이 도구가 답하려는 질문(어느 군·구가 얼마나 지목됐는가)에 필요하지 않다
- **참여자가 스스로 말한 해결 방법을 따로 구분해 저장·표시하는 것.** 인터뷰 안에서 대화하고 참여자의 승인을 거친 내용이므로 요구 서술에 그대로 포함한다
- **도시유형의 역할을 한정하는 경고 문구.** 유형은 참여자에 대한 완주 보상이자 의견을 범주화하는 수단이며, 담당자 화면의 서술은 이미 요구가 유형보다 앞에 온다. "참고 정보일 뿐"이라는 사족은 어디에도 넣지 않는다
- **참여자 요청의 속도 제한, CAPTCHA, 인터뷰 턴 수의 강제 상한.** 자유 입력에는 **길이 상한만** 둔다 — 정상 참여자가 절대 닿지 않을 만큼 넉넉히 잡아 붙여넣기 남용만 걷어내는 것이 목적이고, 그 이상은 정상 참여자를 먼저 막는다. 상한을 두지 않아 남는 노출은 감수한다
- 모델·프롬프트 버전 관리
- 다국어와 음성 입력
- 대규모 트래픽·수평 확장 대응, 현재 단계의 비용 분석

### 2.4 UI 디자인 원칙

참여자 화면은 **모바일 전용**이다. QR로 접속한 휴대폰이 유일한 실사용 환경이기 때문이다. 데스크톱 브라우저로 열면 모바일 폭을 유지한 채 가운데에 배치하고, 넓은 화면용 레이아웃을 따로 만들지 않는다. `/admin`은 예외이며 데스크톱에서 본다.

디자인 방향은 토스(Toss)의 결을 따른다.

- 한 화면에는 하나의 목표만 둔다. 참여자가 다음에 무엇을 해야 하는지 고민하지 않아야 한다.
- 여백을 넉넉히 쓰고 요소를 빽빽하게 채우지 않는다. 타이포그래피가 위계를 만든다.
- 주요 행동 버튼은 화면 폭을 꽉 채우고 충분히 높게 만들어 엄지로 누르기 쉽게 한다.
- 모서리는 부드럽게 굴리고 그림자는 절제한다. 경계선보다 배경 대비로 영역을 구분한다.
- 문구는 짧고 사람의 말처럼 쓴다. 행정 용어와 모호한 비유를 쓰지 않고, 지금 무엇을 보고 있는지 그대로 말한다.
- **화면이 사용자의 조작을 이기지 않는다.** 인터뷰 대화 영역은 참여자가 아래쪽에 있을 때만 새 글자를 따라 내려가고, 위로 스크롤해 지난 대화를 읽는 동안에는 화면을 끌어내리지 않는다.
- **되돌아가는 길은 앞으로 가는 길과 같은 자리에 둔다.** 주요 행동 버튼이 화면 아래에 있으면 이전 버튼도 그 옆에 있어야 한다. 화면 맨 위 구석의 작은 글씨는 실제 사용에서 발견되지 않았다.
- **기기의 뒤로가기는 화면의 "이전"과 같은 뜻이어야 한다.** 참여자는 화면 안의 버튼을 찾기 전에 휴대폰 뒤로가기부터 누른다. 그 조작이 서비스를 통째로 빠져나가게 만들면 진행하던 것을 전부 잃는다. 되돌아갈 곳이 있는 화면에서는 한 단계 뒤로 가고, 없는 화면에서는 아무 일도 일어나지 않는다.

색은 **인천광역시 전용색상만** 쓴다. 임의의 브랜드 색을 만들지 않는다. Incheon Blue `#005EB8`가 주조색(주요 버튼·강조·참여자 말풍선), Incheon Green `#00B2A9`가 보조색(완료와 긍정 상태, 유형 결과의 강조), Incheon Gray `#54585A`가 보조 텍스트와 비활성 상태다. 본문 먹색과 배경 회색처럼 이 3색으로 표현할 수 없는 중립색은 무채색에서 파생하되, 유채색 포인트는 3색을 벗어나지 않는다.

**예외는 없다.** 3색 밖의 유채색을 쓰는 화면이 하나도 없다. 테마에 `verify` 토큰이 남아 있으나 어느 화면도 참조하지 않으므로, 새 버튼을 만들 때 이것을 살아 있는 선택지로 오해하지 않는다.

한글 서체는 Pretendard Variable을 번들로 포함한다. 외부 폰트 CDN에 런타임 의존하지 않는다.

## 3. 전체 구조와 파트별 동작

Anthropic의 AI Interviewer 방법론을 세 단계로 적용한다. Planning(연구 질문과 탐색 주제로 인터뷰 루브릭 작성), Interviewing(참여자별 실시간 적응형 인터뷰), Analysis(원 방법론의 창발적 군집화 대신 사전 정의된 도시가치 축으로 정량화).

인터뷰의 공간 범위는 인천광역시 전체, 주제 범위는 인천시민 전체의 삶이다. 시범운영 참여자는 청년이다. 입력은 타이핑, 언어는 한국어로 한정한다.

데이터는 단방향으로 흐른다. 메모리 세션에 대화록과 증거를 누적하고 → 인터뷰 종료 → 코드가 유형을 산출하고 → 대화록과 유형으로 요구 보고서를 생성하고 → 참여자가 요구를 확인·수정하고 → 제출 → Firestore 영속화 → 백그라운드 비식별 사본 생성 → 분석 실행 시 코드 집계와 LLM 정성 요약 → 담당자 리포트.

**제출 전 데이터는 프로세스 메모리의 임시 상태다.** 서버 재시작, 참여자 이탈, 악성 종료로 사라져도 허용한다. 제출되지 않은 인터뷰는 저장하지 않으며, 악성 종료만 이벤트 로그 한 줄을 남긴다.

프론트는 백엔드 HTTP API만 호출한다. **LLM 키, 프롬프트, 축 정의, 도메인 지식은 백엔드에만 둔다.** 유형 별명·설명과 참여자 화면의 일상어 축 라벨만 표시용 프론트 정적 자산이다.

### 3.1 파트 A — 접속과 자격 확인

진입 순서는 QR 또는 링크 접속 → 시스템 개요 확인 → 개인정보 수집·이용 동의 → 인터뷰 시작이다. **대상은 인천과 인연이 있는 청년이다** — 인천에 살거나, 학교·직장이 인천이거나, 인천을 자주 찾는 사람. 시작 화면이 이를 한 줄로 안내한다. 화면은 안내만 하고 강제하지 않는다 — 인연 확인과 대상 밖 처리는 인터뷰 도입이 맡는다(3.2). 동의는 별도 화면이 아니라 시작 화면 안의 고지 문구로 통합했고, **시작 버튼을 누르는 것이 곧 동의다.** 체크박스는 두지 않는다 — 관문을 하나 줄이는 대신 무엇에 동의하는지를 버튼 바로 위에서 고지 문구와 같은 무게로 밝힌다. 별도 체크박스보다 동의의 증거력이 약하다는 것은 아는 채로 택한 것이다.

**고지는 화면을 밀어내지 않고 자기가 자란다.** 화살표를 누르면 고지 박스의 아래 끝은 그대로 둔 채 위쪽 끝만 0.3초에 걸쳐 올라가 출생연도·성별 위를 덮는다. 소속 표기는 덮지 않고 그 16px 아래에서 멈춘다 — 박스의 둥근 모서리가 그 줄을 스치면 글자가 삐져나온 것처럼 보인다. **새 판이 따로 뜨는 것이 아니라 같은 박스가 커지는 것이어야 한다** — 위에 다른 상자가 얹히는 연출은 눌린 것과 열린 것이 다른 물건으로 읽힌다. 그래서 박스는 흐름에서 빠져 아래 끝만 고정하고, 흐름에는 접힌 높이만큼의 빈 자리를 남겨 둔다. 그 빈 자리는 접힌 상태의 줄을 그대로 복사해 보이지 않게 놓아 만든다 — 문구가 줄바꿈되는 좁은 화면에서도 높이가 정확히 맞아야 하기 때문이다. **덮이는 것들은 자리를 지킨다** — 접었다 폈다 할 때마다 입력칸이 움직이면 화면이 술렁이고 반쯤 지워진 것처럼 읽힌다. 거기까지 차면 안의 글이 스크롤된다. 스크롤로 잘린 줄이 동의 문구에 달라붙지 않게 두는 사이 여백은 접히는 쪽에 둔다 — 고정된 자리에 두면 접힌 박스가 그만큼 두꺼워져 위의 성별 바에 닿는다. 시작 버튼은 어느 상태에서도 움직이지 않는다. **화살표는 하나뿐이고 동의 문구와 같은 줄에 선다.** 접힘·펼침에 따라 읽어 주는 이름만 '자세히 보기'와 '접기'로 바뀐다. 접힌 글은 문서에 남으므로 키보드와 스크린리더에서 닿지 않게 막는다.

**참여자 화면의 폭 상한은 데스크톱에만 건다.** 셸은 흐름이 넓은 화면에서 늘어지지 않도록 440px에서 멈추는데, 이 상한을 모든 폭에 걸면 페이지 확대·축소가 줄어든 폰에서 문제가 된다 — 브라우저가 보고하는 폭이 440px을 넘어 카드가 회색 액자에 갇힌 채 축소돼 그려진다. 실측에서 사파리 75% 상태의 아이폰이 정확히 이 모습이었다. **바탕색도 화면이 칠하는 색으로 맞춘다** — 모든 화면이 위쪽 끝을 흰색으로 칠하므로 문서 바탕과 브라우저 UI 색을 같은 흰색으로 두고, 셸이 떠 있는 회색 바탕은 데스크톱용으로 셸 자신에게만 남긴다.

**본인인증은 두지 않는다.** 실제 인증을 붙일 계획이 없는데 인증 절차를 안내하면 화면이 존재하지 않는 것을 설명하게 된다. 출생연도와 성별은 폼이 받는다.

**중복참여는 막지 않고 기기 토큰으로 세기만 한다.** 형제나 친구의 휴대폰을 빌려 참여하거나 공용 기기를 쓰는 것이 정상 사례로 존재하고, 지금은 참여자 손실이 중복 데이터보다 비싸기 때문이다. 브라우저가 만든 임의 문자열을 세션 생성 때 함께 보내 제출본에 저장하고, 담당자 목록이 둘 이상이 공유한 토큰의 건수를 보여 준다. **저장소를 거부하는 브라우저에서는 방문마다 새 값이 나오므로 그 참여자는 중복으로 잡히지 않는다** — 세는 일이 참여를 막는 일보다 뒤라는 뜻이고, 그래서 토큰을 만들지 못하는 것이 참여를 막는 이유가 되지 않는다. 시에 낼 자료에서 "막았다"고 말할 수는 없어도 "중복 의심 N건"이라고는 말할 수 있게 하는 것이 이 값의 전부다.

**출생연도에는 범위를 걸고 프론트와 백엔드가 같은 범위로 검사한다.** 범위를 걸지 않으면 이 데이터를 청년 데이터라고 부를 근거가 없고, 만 14세 미만이 들어오면 법정대리인 동의가 필요해지는데 그 절차는 만들지 않았다. 받는 값이 출생연도뿐이라 생일 경과를 알 수 없으므로 만 나이가 아니라 **연 나이 16세 이상 39세 이하**로 자른다(출생연도 1987~2010). 2026-09-02에 상한을 29에서 39로 넓혔다 — 근거와 이력은 `COLLECTION_PLAN.md` 4.4에 있다. **경계는 코드에 상수로 박고 현재 연도에서 계산하지 않는다** — 해마다 조용히 움직이는 경계보다 라운드와 함께 끝나는 상수가 낫다.

**성별도 이 화면에서 받는다.** 남성·여성·기타 셋이고 고르지 않으면 시작 버튼이 활성화되지 않는다. 밝히고 싶지 않은 참여자를 위한 자리가 "기타"이며, 그래서 이 값은 "그 외의 성별"과 "밝히지 않음"을 함께 담는다. 이 한계는 담당자 화면이 이 항목을 읽을 때 전제해야 한다.

성별 선택 바의 표시 규칙 두 가지에 이유가 있다. **선택 표시는 하나뿐이고 고른 칸으로 미끄러져 이동한다** — 칸마다 배경을 켜고 끄면 움직임이 아니라 점멸로 읽힌다. **첫 선택 전에는 표시자를 감춘다** — 어느 칸에든 미리 놓아 두면 고르지 않았는데 고른 것처럼 보이고, 시작 버튼이 왜 비활성인지 화면에서 설명되지 않는다. 이 바의 모서리를 거의 각지게 두는 것은 2.4에 대한 의도된 예외다. 위의 입력칸과 한 덩어리로 읽혀야 하고, 둘 다 크게 굴리면 두 개의 분리된 알약처럼 보인다.

참여는 열린 참여다. 인터뷰 데이터에 신원 정보를 결합하지 않는다. 자동저장은 없으며, 이어지는 범위는 브라우저가 페이지를 백그라운드에서 유지하는 정도에 따른다.

**동시에 살아 있는 세션 수에 상한이 있다.** 세션을 만들기 전에 유휴 세션을 먼저 정리하고, 그러고도 상한을 넘으면 503을 돌려준다. 유휴 기준은 60분이라 인터뷰가 5~10분인 참여자가 잠시 자리를 비워도 잃지 않으며, 상한은 `max_live_sessions` 환경변수여서 재배포 없이 조정한다. **정리를 먼저 하는 것이 계약이다** — 브라우저를 닫고 떠난 세션이 메모리에 남으므로, 정리 없이 세기만 하면 상한이 실제 동시 인터뷰가 아니라 누적 이탈자를 세게 되어 시간이 지날수록 멀쩡한 참여자를 막는다.

**이 상한이 막는 것은 참여자가 아니라 마비다.** 세션 생성에는 인증이 없고 턴 수에도 상한이 없어서, 스크립트가 세션을 대량으로 열고 동시에 굴리면 분당 모델 호출이 제공자 할당량을 넘긴다. 그러면 그 시간대에 인터뷰 중이던 사람들이 채점 증거를 잃거나 결과 생성에서 통째로 죽는다(9.5). **잔액이 아니라 속도의 문제라 청구 알림에는 잡히지 않는다.** 상한에 걸리면 `session_ceiling_reached` 이벤트가 남으므로, 발동 여부와 값 조정은 감이 아니라 로그로 판단한다.

### 3.2 파트 B — AI 인터뷰 엔진

참여자가 2045년 인천에서의 하루를 상상하도록 돕고, 꼬리질문으로 정책 수요를 구체화한다. 동시에 4개 도시가치 축을 판정할 수 있을 만큼 모든 축을 탐색한다.

출생연도는 세션 생성 시 백엔드로 전달하고, 백엔드가 연 단위 추정 나이(`2045 − 출생연도`)를 계산해 시스템 프롬프트에 주입한다. 별명·사는 곳·인천에서 자주 가는 곳은 폼으로 받지 않고 인터뷰어가 도입부에서 라포 형성을 겸해 묻는다. **요즘 무엇을 하며 지내는지와 지금의 하루는 묻지 않는다** — 참여자가 도시계획과의 관련을 느끼지 못하고 미래를 떠올리는 데도 도움이 되지 않는다는 것이 팀 합의다. 현재는 2045년의 바람이 추상적일 때 "지금은 어떤 게 불편해서요?"로 한 번 내려갔다 곧 돌아오는 구체화 도구로만 등장한다. 자주 가는 곳은 시 설문의 "자주 이용하는 시설"에 대응하는 발화이자, 인천 밖 거주자의 동네 장면 무대이자, 상상을 어려워하는 참여자를 위한 "참여자가 직접 꺼낸 사실 하나"다. **거주지는 군·구 단위를 강요하지 않고 "인천 어디에 사는지"로 묻는다** — 행정 단위를 대야 하는 질문은 답변 문턱을 올리고, 동네·섬 이름 답변은 보고서 구조화 단계가 현행 군·구로 정규화하기 때문이다. 정규화 프롬프트에 2026년 신설 구의 경계 힌트를 두고, 확정할 수 없으면 빈 값으로 남긴다 — 틀린 구가 저장되는 것보다 비는 쪽을 택한다.

**성별은 예외다. 인터뷰가 묻지 않고 시작 화면의 폼이 받는다.** 한 응답에 답칸을 하나만 두는 규칙 때문에 성별 질문이 도입에서 턴을 통째로 하나 먹는데, 턴 예산이 한정되어 생활 장면 하나와 맞바꾸는 셈이 된다. 그리고 성별은 라포로 알아낼 것이 아니라 폼에서 고르면 끝나는 값이다.

#### 한 턴은 두 번의 LLM 호출이다

인터뷰어 호출과 증거 태깅(채점) 호출이며, 참여자 발화가 도착하는 시점에 **동시에** 출발한다. 인터뷰어 호출은 Claude, 채점 호출은 Gemini로 간다(모델 배정의 근거는 5.1).

- **인터뷰어 호출**은 `rubric.md`, `system.md`, 참여자 나이를 시스템 지시로 받는다. `axes.md`도 도시기본계획 요약본도 받지 않는다. 출력은 참여자에게 보일 대화 텍스트와 제어 토큰뿐이다.
- **채점 호출**은 `axes.md`만 시스템 지시로 받는다.

**분리하는 이유는 억제로는 막히지 않기 때문이다.** 한 호출로 두 일을 시키면 인터뷰어에게 채점 기준 전문이 주어지고, 채점 기준을 읽은 인격은 그 대비를 질문으로 옮기고 싶어진다. 프롬프트가 "옮기지 마라"를 반복해 억눌러야 하는 구조 자체가 설계에서 나온다. 실측에서 억제는 유혹이 문장 형태로 떠오른 뒤에 걸러 내는 데만 기여했고 유혹의 발생 자체는 막지 못했으며, 실제 인터뷰에서 5.3이 가장 강하게 금지한 형태의 질문이 나갔고 그 답이 증거로 태깅되어 유형 판정까지 오염됐다. **읽지 않으면 옮길 수 없다는 것이 이 분리의 전부다.**

채점 호출은 참여자 발화만 있으면 되므로 인터뷰어 응답을 기다리지 않는다. 한 턴의 소요는 두 호출 중 느린 쪽이며 그것은 인터뷰어 호출이므로 **참여자 체감 지연은 늘지 않는다.** 첫 인사 턴에는 참여자 발화가 없으므로 채점 호출을 하지 않는다.

**채점 호출의 실패는 대화를 죽이지 않는다.** 그 턴의 증거를 0건으로 두고 실패 로그만 남기며, 인터뷰어 호출과 참여자 화면은 영향을 받지 않는다. 재시도와 폴백은 만들지 않는다.

#### 인터뷰어는 판별 차원까지만 안다

인터뷰어에게 주는 축 정보의 상한은 **판별 차원까지다.** 네 성질의 문구는 루브릭에 적지 않고 운영 지시가 빈 축이 있을 때만 하나씩 실어 준다. 루브릭은 성질이 운영 지시로 온다는 것과 그것을 장면으로 바꾸는 법만 알며, **극의 이름·앵커·채점 규칙은 주지 않고**, 두 방향을 나란히 놓고 고르게 하는 질문은 루브릭 문구로 금지한다.

처음에는 축 전체를 숨겼다("인터뷰어는 축을 영영 모른다"). 근거는 두 실측이었다 — 운영 지시가 축의 표시 이름 하나만 실어도 두 극이 역산되어 검사형 이지선다가 만들어졌고(12인 실험), 채점 기준을 읽은 인터뷰어가 극 대비를 질문으로 옮겨 그 답이 증거로 태깅되며 판정까지 오염됐다. 판별 차원 주입으로 완화한 근거는 세 가지다. 첫째, 그 사고 이후 `axes.md`에 인터뷰어가 답을 보기로 좁힌 질문의 답은 증거가 아니라는 규칙이 생겨, 이지선다가 새어 나가도 결과가 판정 오염이 아니라 턴 낭비로 격하됐다. 둘째, 인터뷰어가 지시 준수가 실측으로 확인된 상위 모델(5.1)로 바뀌었다. 셋째, 소재 문구만 주는 방식은 과녁을 모르는 채 쏘는 것이라, 힌트 질문에 참여자가 다른 축의 답을 해도 인터뷰어가 놓친 줄 모르고 지나가는 한계가 실측에서 확인됐다. 힌트 받은 턴이 이지선다형 질문으로 굳는지는 계속 관찰한다.

**커버리지는 성질 힌트의 형태로 쓴다.** 실측에서 하루 장면 중심의 2단계 인터뷰가 생활 리듬(거리의 붐빔)과 발전 가치(변화의 방식)를 구조적으로 건드리지 못해, 한 판이 끝나도 두 축이 증거 0건으로 남고 빈 축이 기본값 극으로 유형을 결정하는 문제가 확인됐다. 그래서 힌트 턴부터 마무리 턴 전까지, 증거가 없는 축이 있으면 운영 지시에 성질 문구 하나("동네와 거리가 얼마나 활발히 돌아가기를 바라는지" 같은)를 실어 그 성질이 드러날 장면을 물을 기회를 찾으라고 지시한다. 네 문구는 모두 단일 차원이다 — 양 끝을 나열한 문구는 루브릭이 금지한 문형을 백엔드가 보내는 것이 된다. **첫 힌트는 미룸이 기본으로 읽히게 쓴다.** 참여자가 방금 꺼낸 이야기나 루브릭의 장면 목록에 붙일 수 있을 때만 이번 응답에서 열고, 붙일 데가 없으면 참여자를 따라가며, 성질의 문구를 질문에 옮겨 적지 않는다. 격리 점검에서 의무형 어순("이번 응답에서는 … 물어볼 것")은 소재를 꺼내지 않는 참여자에게 성질 문구를 직역한 질문을 낳았고, 어순을 바꾸고 루브릭에 우회로(붙일 소재가 없으면 아직 묻지 않은 장면을 장면 목록에서 골라 연다)를 주자 직역이 사라졌다. 참여자가 이미 그 이야기를 꺼냈으면 따르지 않아도 된다는 단서도 남겨 같은 것을 되묻지 않게 한다. 힌트는 마무리 턴 전에는 축당 **최대 두 번, 최소 한 턴 간격**을 두고 보낸다 — 간격을 두는 것은 힌트 질문의 답이 다음 턴에서야 태깅되기 때문이다. 힌트 축은 빈 축들 중 시도 횟수가 가장 적은 축을 먼저 고른다 — 고정 축 순서로 고르면 뒤쪽 축(특히 발전 가치)이 기회를 얻지 못한 채 마무리에 닿는 것이 실사용에서 확인됐다. 커버리지 연장 구간(3.2 턴 일정)에서는 횟수 상한 없이 간격만 지키고, 단서 없는 더 강한 문구로 보낸다. **다만 연장 문구는 장면을 참여자가 세운 2045년의 무대 위에서 고르라고 못박는다** — 장면을 하나 골라 물으라는 지시만 주면 소재가 빈약할 때 인터뷰어가 없는 시설이나 현재 신분을 전제로 장면을 지어내는 것이 격리 테스트에서 확인됐다. **같은 축의 두 번째 이후 힌트는 재시도임을 명시한다** — 앞선 대화 맥락을 고려해, 앞서 쓴 장면을 되풀이하지 말고 참여자가 이미 꺼낸 소재에 붙여 다른 장면으로 물으라는 문구다. 같은 지시문을 되풀이하면 인터뷰어가 같은 장면을 판박이 문장으로 되묻는 것이 실측됐다. 정리 단계에는 마지막 안전망이 하나 있다. 증거가 0건인 축이 남아 있으면 정리 지시에 그 성질 문구를 얹어, 덧붙임 질문에서 그 이야기를 좀 더 듣고 싶다고 언급하고 열어 두게 한다. 못 들었다고 단정하게 하지 않는 이유는 실측이다 — 이미 여러 번 물었으나 증거가 안 잡힌 축에서 "못 여쭤봤다"는 말이 사실과 어긋났다. 덧붙임은 원래 수확률이 가장 낮은 질문이라 과녁을 줘도 잃는 것이 없고, 요약 검수와 참여자 의제라는 원래 역할은 열린 질문을 유지하는 것으로 지킨다. 힌트에는 축 이름도 극도 커버리지 상태도 실리지 않는다. 거절·무응답·빈 축은 여전히 정상 상태다. **발전 가치 힌트에는 여는 방법 한 문장이 따로 붙는다(정리 지시 제외).** 2026-09-03 실사용에서 이 성질 문구가 그대로 질문("인천이 달라져 가는 과정은 어떤 모습이면")으로 직역돼 "모르겠다"만 받은 것이 확인됐다. 다른 세 축은 장면 목록의 장면(집 앞 풍경, 저녁 거리, 출근길)에 붙일 수 있지만 변화의 방식은 하루 속 장면이 아니라서 우회로가 없었다. 그래서 이 축만 "루브릭의 '지금 거기 있는 것' 질문으로 연다"는 문장을 성질 문구 뒤에 붙인다. 문구 자체에 지시를 넣지 않는 이유는 continue 템플릿의 "성질의 문구를 옮겨 적지 말 것"과 정리 템플릿의 "언급만 하고 열어 둘 것"이 그 지시와 부딪히기 때문이다.

**인터뷰 품질은 프롬프트 엔지니어링에 국한한다.** 백엔드는 어떤 순서로 물을지, 어떤 문장으로 물을지에 관여하지 않는다. 그 전부를 `rubric.md`가 소유하며, 소재 힌트도 루브릭에 이미 있는 소재를 가리킬 뿐 문장을 내려보내지 않는다. 한때 백엔드가 검수 문장을 통째로 내려보내는 구조를 시도했으나, **인터뷰어가 이어 붙일 말을 잃어 딱딱해져** 되돌렸다. 검수된 문항은 `rubric.md`에 원본으로 남고, 인터뷰어는 그대로 물어도 되고 참여자가 방금 한 말에 맞게 말투와 어순을 다듬어도 된다. 다듬을 때 묻는 대상을 바꾸거나 답 후보·두 방향을 넣는 것은 여전히 금지다.

**백엔드에 남는 것은 진행 속도와 소재 힌트뿐이다.** 운영 지시는 마무리 턴 전에는 아직 마무리하지 말라고 알리고(빈 축이 있으면 소재 문구 하나를 덧붙이고), 마무리 턴부터는 정리로 넘어가라고 알린다. 단계 번호도 문장도 싣지 않는다.

#### 턴 일정과 진행률

한 턴은 `사용자 발화 1회 + AI 응답 1회`다. 목표는 14턴, 마무리는 10턴부터, 소재 힌트는 6턴부터이며 강제 상한은 없다. **힌트 시작을 5턴에서 6턴으로 늦춘 것은 인천 밖 거주자 격리 테스트의 결과다** — 거주지 확인과 2045년 갈래 선택에 도입 턴을 한둘 더 쓰기 때문에 5턴 시점에는 아직 2045년 무대가 서지 않았고, 무대 없이 도착한 힌트는 붙일 데가 없어 그대로 미룸으로 흘렀다. 세 값은 환경변수라 시범운영에서 재배포 없이 조정할 수 있다.

**2045년 진입은 루브릭이 소유한다.** 도입이 세 항목뿐이라 모델이 자기 위치를 셀 일이 없으므로 백엔드 전환 트리거를 두지 않는다. 진입 턴은 "타임워프" 같은 익숙한 말로 미래로 건너간다는 것을 살리고 그해의 나이를 한 번 알린 뒤, 2045년에 인천과 어떻게 지내고 있으면 좋겠는지를 묻는다(살고 있을 수도, 일하러 오고 있을 수도, 다른 곳일 수도 있다는 안내를 붙임). 이어 무엇을 하며 지내는지, 인천 어디에 지내는지를 한 턴에 하나씩 받아 무대를 만든다. 열린 하루 질문("아침에 눈을 뜨면…")으로 시작하지 않는다 — 무엇을 답해야 할지 모르는 질문이다. **인천에 살지 않겠다는 답은 종료가 아니라 갈래다.** 그 자리에서 대화의 길 두 가지를 열어 고르게 한다 — 2045년에도 인천에 살고 있으려면 인천이 어떻게 달라져 있어야 할지, 아니면 다른 곳에 살면서 인천에 들르는 날은 어떤 모습이면 좋을지. 답이 아니라 길의 선택이므로 후보 제시 금지에 걸리지 않고, 고른 길이 무대가 된다. 첫 번째 길이면 남기로 한 인천에서 하는 일·사는 곳을 이어 받고, 두 번째 길이면 도입의 자주 가는 곳이 그날의 장소다. **턴 번호 자체는 모델에게 알리지 않는다.** 의미 있는 페이싱 결정(힌트·연장·마무리)이 전부 백엔드 트리거가 되면 모델이 턴 수를 알아야 할 일이 없고, 번호를 주면 턴·단계 언급 금지 규칙에 대한 유출 표면만 늘어난다. 턴 수의 근거는 응답 규칙이다 — 한 응답에 답칸을 하나만 두어 턴당 정보 밀도를 일부러 낮췄으므로 턴 수로 보충하고, 참여자의 한 턴 답변 부담이 낮아 체감 소요는 같은 범위에 남는다. 도입·정리의 턴 수는 절차로 고정되어 있으므로 나머지가 2045년의 하루다. **빈 축이 남아 있으면 마무리를 최대 4턴(환경변수) 미룬다.** 실사용에서 발전 가치 축이 증거 0건인 채 인터뷰가 끝나 기본 극이 유형을 결정하는 일이 실제로 났기 때문이다. 연장 구간에서는 정리 지시 대신 빈 축의 성질 힌트를 계속 보낸다. 다만 상한 없이 늘리지는 않는다 — 몇 턴을 더 물어도 그 성질을 말하지 않는 참여자는 있고, 억지로 채운 답은 쓰이지 않는다. 상한에 닿으면 정리로 넘어가고 정리 3단계의 덧붙임 언급이 마지막 안전망이 된다. 조기 종료 방지는 안내일 뿐 강제가 아니며, 참여자가 그만하겠다고 말하면 마무리 턴 전이라도 종료한다.

**참여자에게는 진행 정도를 퍼센트 하나로 보여 준다.** 끝이 보이지 않는 대화는 중간 이탈을 부른다. 표시값은 현재 턴을 목표 턴으로 나눈 정수 퍼센트이며 종료 턴에는 100%다. **종료 전에는 99를 상한으로 클램프한다** — 커버리지 연장으로 목표 턴을 넘겨도, 끝나지 않은 대화에 100%가 떠 있으면 안 된다. **커버리지를 진행률에 섞지 않는다** — 빈 축이 정상 상태가 되면서 커버리지는 영영 100%에 닿지 않을 수 있고, 그것을 진행률의 하한으로 쓰면 남은 대화 내내 퍼센트가 멈춰 선다. **축 이름도 축의 개수도 참여자에게 노출하지 않는다.** 참여자는 축의 존재를 모른다.

#### 종료와 이탈

인터뷰어는 마무리 발화 뒤에 종료 제어 토큰을 출력하고, 백엔드가 토큰을 숨기고 세션을 종료 상태로 바꾼다.

**종료 가드는 백엔드가 소유한다.** 참여자가 그만두겠다고 말한 경우가 아니라면, 마무리 턴에 이르기 전이거나 **그 응답에 답을 요구하는 표현이 남아 있으면** 인터뷰어가 종료 토큰을 내도 문장 형태와 무관하게 제거하고 대화를 계속한다. 12인 실험에서 4/12가 마지막 질문과 종료를 같은 응답에 담았고, 물음표 가드는 청유형에 뚫렸다. **이 상태를 모델에게 알리지 않으므로 운영 지시 이행률에 의존하지 않는다.** 인터뷰 내용에 관여하지 않는 정합성 가드이므로 프롬프트로 옮기지 않는다.

**프론트는 종료 신호를 받아도 화면을 자동으로 넘기지 않는다.** 마무리 발화 아래에 "대화 종료하기" 버튼을 띄우고 참여자가 눌러야 결과 단계로 간다. 마무리 인사를 읽을 틈도 없이 화면이 넘어가면 참여자는 방금 무엇이 끝났는지 알지 못한 채 다음 화면을 마주한다. 대화가 이미 끝났는데 무엇을 해야 할지 몰라 한 번 더 입력하는 일도 막는다. 이 버튼은 별도 LLM 호출이 아니라 이미 존재하는 종료 신호에 반응한다.

화면 상단의 "인터뷰 그만두기"는 순수 이탈 경로다. 확인 모달 후 세션을 폐기하며 어떤 데이터도 저장하지 않는다. 인터뷰 화면에서 기기 뒤로가기를 누르면 이 모달이 열린다.

#### 되돌리기

**참여자는 직전 한 턴을 되돌릴 수 있다.** 잘못 보낸 답변이 그대로 남아 유형 판정과 보고서에 들어가는 것을 막는다. 경로는 둘이다.

- **생성 중 중단.** 답변이 나오는 도중에 중단하면 프론트가 스트림을 끊는다. 백엔드는 스트림이 끝난 뒤에 턴을 저장하므로 끊긴 턴은 세션에 아예 남지 않는다. **나오다 만 답변을 저장하는 방식은 쓰지 않는다** — 잘린 문장이 다음 턴의 맥락으로 들어가고 대화록에도 남는다.
- **완료된 턴의 수정.** 백엔드가 마지막 사용자·AI 메시지 쌍과 **그 턴 번호로 기록된 증거를 함께 지운다.** 증거를 남기면 참여자가 취소한 말이 유형 판정에 계속 반영된다.

되돌릴 수 있는 것은 **직전 한 턴뿐이다.** 더 거슬러 올라가면 인터뷰어의 이후 질문이 지워진 맥락 위에 서게 되고, 참여자가 대화를 재구성하는 도구가 된다. 인터뷰가 종료된 뒤에는 되돌릴 수 없다. 중단으로 채점 호출이 떠 있는 채 남지 않게, 클라이언트 연결이 끊기면 그 턴의 채점 호출을 취소한다.

#### 가드레일과 악성 응답

가드레일은 시스템 프롬프트로만 구현한다. 유도질문 금지, 특정 답 유도 금지, AI 편향 삽입 금지, 주제 이탈 시 복귀, 참여자 지시가 시스템 규칙을 덮어쓰지 못하게 하는 처리다.

악성 응답은 **판정과 카운트를 나눠서** 처리한다. 악성 여부는 의미 판단이라 모델만 할 수 있고, 몇 번째인지는 상태라 백엔드만 알 수 있다. 분업선을 그 사이에 둔다. 인터뷰어는 악성이라고 판단하면 경고나 종료 인사를 만들지 말고 제어 토큰만 단독으로 출력하고, 백엔드가 카운터를 올려 1회면 고정 경고 문구를, 2회면 고정 종료 문구를 내보내고 세션을 폐기한다. 어느 쪽이든 모델이 쓴 텍스트는 쓰지 않는다. 악성 판정 턴은 대화록에 저장하지 않고 그 턴의 채점 호출을 취소한다. **12인 실험에서 악성 입력이 0건이었으므로 모델의 악성 판정 정확도는 아직 검증된 적이 없다.**

**인천과 아무 관련이 없는 참여자만 정중히 안내하고 종료한다.** 사는 곳이 인천 밖이면 인천에 오는 곳(학교·직장·자주 가는 곳, 무엇이든)이 있는지 한 번 확인하고, 없다고 답할 때만 대상 밖이다. 자격을 거주로 한정하지 않는 이유는 실무 자문과 접근 경로에 있다 — 인천을 이용하는 사람은 활동인구이고(성시윤 주무관), 인하대 재학생의 상당수가 인천 밖에 산다. 경계를 세밀하게 그으려는 순간 판정이 흔들리므로 확인은 사실 질문 하나로 끝내고, 경계 사례는 참여자가 자주 온다고 하면 받는다. **데이터 오염은 자격이 아니라 무대 고정으로 막는다.** 부천 거주 통학생의 인터뷰에서 동네 장면 힌트가 부천 하천 요구를 수확해 인천 데이터로 태깅된 실측이 있는데, 그 본질은 부천 사람을 받은 것이 아니라 장면의 무대가 부천이 된 것이다. 그래서 도입에서 확보한 "인천에서 자주 가는 곳"을 인천 밖 거주자의 동네 장면 무대로 삼고, 2부가 모든 질문에 "2045년"을 넣듯 인천의 그 동네를 무대로 묻는다. **이 무대는 군·구나 지구 이름으로는 서지 않는다.** 격리 테스트에서 시흥 거주 참여자가 "송도 쪽으로요"라고 답하자 인터뷰어가 그것을 자주 가는 곳의 답으로 받고 넘어갔고, 이후 "그 동네 거리"와 "그 주차장"이 어디인지 특정되지 않은 채 인터뷰가 끝났다. 원인은 사는 곳 확인 질문과 자주 가는 곳 질문이 같은 형태의 답을 받는데 루브릭에 획득 기준이 없었던 것이다. 그래서 도입 3은 지구 이름을 받으면 그 안에서 주로 가는 곳을 한 번 더 묻고, 무대가 될 곳 하나를 받은 뒤에 2045년으로 넘어간다. 2045년에 인천에 살지 않을 것 같다는 답도 정상 진행한다(3.2 갈래). 처리는 악성 응답과 같은 분업이다. 판정은 모델이 하고(제어 토큰 단독 출력), 안내와 종료는 백엔드가 고정 문구로 한다 — 어느 쪽이든 모델이 쓴 작별 인사는 쓰지 않는다. 경고 단계 없이 1회로 종료하고, 그 턴은 저장하지 않으며 채점 호출을 취소하고 세션을 폐기한다. 거주지 답변 거절과 어느 도시인지 불명확한 답은 대상 밖 판정이 아니라 정상 진행이다 — 틀린 종료보다 비워 두는 쪽을 택하는 지역 정규화의 원칙과 같다.

**토큰 출력은 마무리 행위가 아니라 시스템에 보내는 판정 신호이며 매 턴 붙는 "계속할 것" 운영 지시보다 우선한다고 `system.md`에 명시한다.** 그 문장이 없던 문안은 실측에서 2회 연속 불이행됐다 — 모델이 토큰 출력을 마무리 행위로 해석해 계속하라는 최신 지시를 우선했다. **같은 충돌이 악성 판정 토큰에도 잠재하나 악성은 실측된 적이 없다**(3.2 가드레일의 미검증 상태 그대로다).

### 3.3 파트 C — 유형 판정, 보고서 구조화, 확인과 제출

인터뷰 종료 직후 두 트랙을 병렬로 시작한다. 유형 트랙은 누적 증거를 코드로 즉시 합산하고, 보고서 트랙은 원본 대화록과 유형 결과를 한 번의 LLM 호출로 구조화한다. **유형 합산에는 LLM을 호출하지 않는다.**

구조화 호출은 압축 없이 원본 대화록을 그대로 받는다. 인터뷰어 발화는 맥락으로만 쓰고 요구와 인용은 참여자 발화에서만 찾는다. 이 단계에서 전체 요약, 축별 요구, 축별 판정 이유, 별명, 거주지, 꿈 또는 직업이 나오고, 여기에 **행정동, 정착 의향(거주·근무·여가 각각 인천/타지), 마무리에서 고른 최우선 요구, 요구마다 참여자가 말한 장소**가 함께 나온다. 뒤의 넷은 명시적인 발화가 있을 때만 채우고 없으면 빈 값이며, 짐작해서 채우지 않는다.

**구조화 목록의 개수 상한은 프롬프트와 백엔드가 맡는다.** Gemini 응답 스키마는 항목의 모양과 필수 최소 조건만 표현하고 최대 개수를 넣지 않는다. 중첩된 bounded array의 최대 개수는 서로 곱해져 Gemini의 요청 한도를 넘기기 때문이다.

**요구 제목은 조치가 아니라 바람이 이뤄진 뒤의 모습으로 쓴다.** 한 제목에 요구 하나만 담고 두 항목을 잇지 않는다. 이 규칙을 금지어 목록으로 적었을 때는 모델이 동의어로 갈아타 실패했고, 문장의 모양을 제약하고 실제 출력에서 뽑은 예시쌍을 붙이자 줄었다.

**군·구는 모델의 기억이 아니라 표가 정한다.** 구조화 호출의 입력에 군·구별 동·읍·면 표가 실리고, 참여자가 동네 이름을 말했으면 모델은 그 표에서 군·구를 읽는다. 표에 있는 동네 이름이면 코드가 다시 표로 확정하므로 모델이 다르게 적어도 표가 이긴다. 표에 없는 장소 이름(랜드마크·하천·역)은 모델이 판정하되 현행 11개 목록으로 검증한다 — 표로는 원리적으로 풀 수 없고, 막으면 언급 장소 집계가 사실상 비기 때문이다. 참여자가 말한 원문(`raw_region`, `dong`)은 표에 맞춰 고치지 않고 그대로 둔다.

보고서 생성은 15초 안팎이 걸린다. 이 시간 동안 참여자를 정지된 문구 앞에 세워 두지 않는다. 서버가 실제 진행률을 알려 주지 않으므로 기다리는 동안의 진행은 예상 소요를 근거로 한 연출이며, **이 구간에서는 진행바가 100%에 닿지 않는다.** 끝나지 않았는데 끝난 것처럼 보이면 안 되기 때문이다. **응답이 실제로 도착하면 진행바를 100%까지 채우고 잠깐 머문 뒤 넘어간다.** 이때의 100%는 사실이다. 다 채우지 않은 채로 넘어가면 참여자는 무언가 덜 끝났다고 느낀다. **생성이 실패해도 막다른 오류 화면으로 보내지 않는다**(9.2).

**결과는 세 단계로 나눈다.** 한 화면에 모두 담으면 스크롤이 지나치게 길어져 참여자가 무엇을 해야 하는지 알기 어렵다. 각 화면은 하나의 목적만 갖는다.

1. **내 유형 확인** — 유형 카드(일러스트, 별명, 한 줄 설명, 일상어 라벨 배지 4개, 축별 강도 바), 공유·저장 버튼, 축별 판정 이유
2. **내 이야기와 요구 확인** — 인터뷰 전체 요약, 축별 요구 보고서, 수정 진입점
3. **요구 수정** — 요구 문장 선택과 참여자 의견 입력, 전체 요약과 4축 요구 재생성

세 단계 모두 그 화면의 행동을 **아래에 고정된 바**에 둔다. 1단계는 다음 버튼 하나가 폭을 채우고, 2단계는 이전과 제출을 나란히, 3단계는 이전과 재생성을 나란히 둔다. **하단 바에는 그 화면의 주된 행동 하나와 되돌아가는 길만 둔다** — 셋을 넣으면 무엇이 주된 행동인지 흐려진다. 요구 수정 진입점은 본문에 남긴다. **헤더의 작은 이전 버튼은 두지 않는다** — 시험 사용에서 참여자가 그 버튼을 찾지 못하고 휴대폰 뒤로가기를 눌러 서비스를 통째로 빠져나갔다. 기기 뒤로가기는 각 단계의 이전 버튼과 같게 동작한다.

**판정 근거로 참여자의 발화를 다시 인용하지 않는다.** 대신 각 축에 대해 왜 그 글자로 판정했는지를 한두 문장으로 설명한다. 참여자가 알고 싶은 것은 자기가 한 말이 아니라 그 말이 어떻게 이 판정으로 이어졌는가다. 다만 **증거가 한 건도 없는 축에는 설명할 근거가 존재하지 않으므로 코드가 고정 문장을 넣는다** — 근거 없이 설명하게 하면 모델이 지어낸다.

**요구에는 참여자의 말과 AI의 정리를 나란히, 그러나 갈라서 보여 준다.** 결과 2단계가 요구마다 인용 원문과 정리 문장을 이름표와 배경 대비로 가른다. 인용은 수정 선택 대상이 아니고 턴 번호도 노출하지 않는다. 18인 관측에서 참여자가 가장 자주 반복한 요구가 "어디까지가 내 말이고 어디부터가 AI의 해석인가"였고, 그것을 판정 근거의 인용으로 되돌리는 대신 요구 쪽에서 표시로 가르는 것이 답이었다.

**저장되는 모든 인용은 실제 발화인지 코드가 검사한다.** 구조화·수정 응답의 인용을 그 턴의 참여자 발화에 대한 부분 문자열 검사로 거르고, 실패한 인용은 버리며 인용이 0개가 된 요구는 요구째 버린다. 인용에 턴 번호가 붙어 있어 검사 대상이 발화 하나로 좁혀지므로 규칙 기반으로 충분하다. 검사는 비식별화가 텍스트를 바꾸기 전의 원문 기준으로 돌고, 결과는 어떤 화면에도 내보내지 않고 로그로만 남긴다. **이 검사가 위 표시의 전제다** — 참여자에게 "당신의 말"이라고 보여 주는 것이 실제 발화가 아니면 그 표시가 거짓말이 된다.

판정 이유는 유형 코드와 강도가 코드로 결정된 뒤에 생성되므로 판정과 어긋나지 않는다. 참여자는 수정할 수 없고 요구를 수정해도 바뀌지 않는다.

**제출 직전에 만족도 두 문항을 받는다.** 결과 2단계에 5점 척도 두 개를 두고, 대화로 의견을 내는 방식이 편했는지와 정리된 결과가 자기 생각을 담았는지를 묻는다. 시간과 비용은 로그와 청구서가 이미 재지만 **진입장벽을 낮췄는지는 물어봐야만 알고**, 1장이 성공 조건으로 적어 둔 "참여자가 결과가 자기 생각을 반영한다고 느끼는 것"에도 재는 수단이 없었다. 자유 서술 칸은 두지 않는다 — 인터뷰 자체가 주관식인데 끝에서 또 쓰라면 부담이다.

**둘 다 선택 응답이고 제출 버튼은 이 답을 기다리지 않는다.** 필수로 하면 제출 직전에 관문이 하나 더 생기는데, 그 자리의 이탈이 가장 비싸다. **수정 진입점보다 아래에 둔다** — 두 번째 문항이 묻는 것은 제출될 판본이므로 수정 기회를 주기 전에 물으면 수정 전 판본을 평가하게 된다. 응답은 세션에 담지 않고 제출 요청 본문으로 받아 그대로 제출본에 저장한다. 세션은 같은 요청 안에서 폐기되므로 거쳐 갈 이유가 없고, 새 엔드포인트도 그 경로를 보호할 문제도 생기지 않는다.

**참여자가 직접 수정하는 영역은 축별 요구뿐이다.** 유형 코드, 축별 강도, 판정 근거는 고정한다. 참여자가 요구 문장을 하나 이상 고르고 의견을 입력하면 LLM이 **전체 요약과 4축 요구 전체를 동일한 규칙으로 재생성**하고, 서버가 판정 근거와 자기보고 정보 등 나머지를 기존 보고서에서 복사해 조립한다. 요약은 수정된 요구와 어긋나지 않게 다시 쓰지만, 요약을 포함해 사용자가 텍스트를 직접 편집하는 UI는 두지 않는다. 수정 횟수에는 제한이 없고, 15회에 도달하면 제출을 권하는 문구만 표시한다.

제출 시 원본 대화록·증거 로그·유형 결과·최종 보고서·메타데이터를 Firestore에 영속화하고 메모리 세션을 제거한다. 한 흐름은 1회 제출로 끝나며 재작성과 재제출을 허용하지 않는다.

**공유와 이미지 저장은 제출 전 결과 1단계에서도 제공한다.** 유형 카드는 참여자가 가장 먼저 보고 싶어 하는 것이고, 그것을 보기 위해 제출을 먼저 요구하면 마찰이 생긴다. 제출하지 않고 카드만 공유한 뒤 이탈할 수 있다는 뜻이지만, 제출을 인질로 삼는 것보다 낫다고 본다. 공유 카드는 프론트가 결과 단계에서 받아둔 상태로 렌더링하며 서버에서 다시 조회하지 않는다. **새로고침하거나 재접속하면 결과를 다시 볼 수 없다는 안내를 함께 표시한다.**

**결과 1단계의 순서는 유형 카드 → 공유·저장 버튼 → 별명과 한 줄 설명 → 축별 강도 바 → 축별 판정 이유다.** 유형 이야기와 궁합은 별명 아래 펼침 안에 있다(4.7). 별명·설명·강도 바가 카드 밖에 있는 이유는 카드가 내보내기 이미지 그 자체이기 때문이다(6.4). 제출 완료 화면은 카드 → 공유·저장 버튼 → 별명과 설명까지만 두고 강도 바와 판정 이유는 싣지 않는다 — 그 화면의 목적은 공유와 응모다.

### 3.4 파트 D — 후처리 분석 파이프라인

담당자 웹의 "자료 업데이트" 버튼이 파이프라인을 한 번 실행한다. 참여자 흐름과 분리된 배치 작업이며, 약 10건 규모의 admin HTTP 요청 안에서 동기 완료한다. 실행 단계는 (1) 제출본별 후처리 중 빠진 것을 보충, (2) 코드가 축별 극 인원수·극별 평균 강도·16유형 분포·대시보드 집계를 계산, (3) Gemini가 축별 요구 경향을 요약하고 대표 인용을 선정, (4) Gemini가 대시보드 해석 문장과 종합 브리핑을 한 번에 생성하는 순서다.

**제출본별 후처리 세 호출은 제출 응답이 끝난 뒤 백그라운드에서 이 순서로 돈다** — 남은 요구 추출, 부문 라벨링, 비식별화다. 각 단계는 자기 예외를 잡아 로그만 남기고 제출과 다음 단계를 깨뜨리지 않으며, 분석 실행이 같은 순서로 누락분을 보충한다. 순서가 계약인 이유는 뒤가 앞을 전제하기 때문이다 — 남은 요구가 새로 나오면 그 요구를 담지 못한 부문 라벨과 비식별 사본이 함께 무효가 된다.

**보충의 팬아웃에는 상한이 있다.** 단계 사이의 순서는 그대로 두고 각 단계 **안에서** 동시에 나가는 호출 수만 묶는다. 상한이 없으면 제출본 수에 비례해 동시 호출이 늘어 표본이 커질수록 속도 제한에 통째로 걸린다 — 열 건 규모에서는 드러나지 않고 수백 건에서 드러나는 종류의 구조다. **세마포어는 실행마다 새로 만든다** — 모듈 수준에 두면 처음 대기가 발생한 이벤트 루프에 묶여, 루프가 바뀌는 자리에서 다른 루프라며 깨진다. **개발자 모드 묶음집에서 온 세션은 이 세 값을 그 묶음집에서 복사하고 백그라운드 작업을 걸지 않는다**(6.1). 규칙 기반 비식별 필터, 최소 인원 임계값, 임베딩, 군집화는 쓰지 않는다.

**비식별 호출은 텍스트만 바꾼다.** 축·요구 ID·턴 번호는 모델에게 다시 만들게 하지 않고 서버가 원본에서 복사하며, 구조나 개수가 입력과 어긋나면 그 제출본의 비식별화를 실패로 처리한다. 대화록 전체나 요약은 비식별 대상이 아니다 — 담당자 리포트와 대표 인용이 쓰는 것이 요구 텍스트와 인용 발화뿐이기 때문이다.

**후처리 결과는 제출본 문서의 최상위에 두고 `report` 안은 건드리지 않는다.** 남은 요구, 부문 라벨, 비식별 사본이 각각 제 자리를 갖는다. `report`는 참여자가 확인하고 제출한 기록이므로 나중에 만든 값을 그 안에 섞지 않는다. **부문 라벨이 없는 요구는 부문 집계에서 빠지며 폴백을 두지 않는다.** 부문은 요구 하나하나를 보고 골라야 갈리는 것이라 다른 분류 체계에서 기계적으로 옮겨올 수 없다. 같은 소재라도 미세먼지 요구는 환경의 보전과 관리이고 산책로 요구는 공원 및 녹지다.

**부문 라벨링 호출이 요구마다 키워드도 함께 뽑는다.** 부문 열 개는 담당자가 소관을 찾기에는 맞지만 요구가 무엇에 대한 것인지 보여주기에는 굵다 — 교통 열두 건이 배차인지 주차인지 자전거도로인지 구분되지 않는다. 키워드가 부문 아래 한 겹을 만든다. **어휘를 미리 정해 두지 않는 것이 계약이다** — 계획서 목차에서 뽑은 목록으로 세면 계획이 예상하지 못한 요구는 정의상 걸리지 않고, 이 조사의 존재 이유가 바로 그것을 듣는 데 있다. 대신 형태만 묶는다. 명사형, 띄어쓰기 없음, 한국어, 여덟 자 이하이며 요구당 최대 세 개이고 억지로 채우지 않는다. 소재만 쓰고 `불편`·`개선`·`확충` 같은 평가어는 쓰지 않는다 — 아무 요구에나 붙어서 세어도 아무것도 나오지 않는다. **사람·직장·학교를 특정하는 고유명사도 쓰지 않는다** — 비식별화가 라벨링 다음에 돌기 때문에 키워드는 비식별을 거치지 않는다. 형태를 어긴 키워드는 코드가 그것만 버리며 폴백을 두지 않는다.

**같은 소재가 여러 이름으로 갈리는 것은 감수한다.** `버스`와 `배차간격`이 따로 서는 것을 막으려면 이미 쓰인 키워드 목록을 모델에게 보여 주거나 사후에 대표어로 묶어야 하는데, 앞은 먼저 제출한 사람이 그 소재의 이름을 정하게 만들고 뒤는 같은 데이터에서 실행마다 다른 순위를 낸다. 어제 본 숫자와 오늘 본 숫자가 다른 화면은 담당자의 신뢰를 잃는다. 버블 표시(3.6)가 이 흩어짐에 관대한 것도 이 선택을 받쳐 준다 — 순위표와 달리 큰 것만 눈에 들어오고 한 건짜리는 작게 있어도 화면을 망가뜨리지 않는다.

**키워드 집계는 사람 수 하나뿐이다.** 한 참여자 안에서 같은 키워드가 몇 번 나와도 1로 센다. 한 사람이 내는 요구가 최대 여덟 개라 언급을 그대로 더하면 표본이 작을 때 한 사람이 가장 큰 값을 혼자 만들고, 담당자는 그것을 여러 명이 말한 것으로 읽는다. 반복이 뜻하는 시급함은 최우선 요구와 축 강도가 이미 따로 센다.

**인용 ID는 코드가 부여한다.** 서버가 모든 인용에 ID를 붙이고, 요약 호출에는 ID와 비식별 발화만 담은 후보 목록이 들어가며, 모델은 그중에서 고르기만 한다. 후보에 없는 ID를 돌려주면 그 항목만 버린다. ID 생성을 모델에게 맡기면 형식이 어긋나 추적이 끊기고, 애초에 코드가 결정할 수 있는 일이다.

**요약 호출은 실행당 한 번이며 네 축을 함께 처리한다.** 축마다 나누면 같은 시스템 지시를 네 번 보내면서 축 사이의 대비를 볼 수 없다.

**빈 축은 그 축의 집계와 요약 어디에도 넣지 않는다.** 빈 축의 판정 글자는 기본 극으로 채워진 값이라 실제 선호가 아니다. 그것을 인원수에 섞으면 분포가 기본 극 쪽으로 기울고, 요약에 섞으면 그 요구가 근거 없이 기본 극의 경향으로 서술된다. 유형 분포만 코드 기준이므로 모든 제출본을 센다. **평균 강도에는 5.4의 표시 클램프를 적용하지 않는다** — 클램프는 한 사람의 강도 바가 단정적으로 읽히는 것을 막는 표시 규칙이고, 여러 명을 평균한 값은 그 문제를 갖지 않는다.

**제출본이 하나도 없으면 분석을 실행하지 않는다.** 집계할 것이 없는 실행 기록을 남기지 않기 위해서다.

**모든 Gemini 호출에 요청당 시간 제한과 재시도 한 번이 걸려 있다.** SDK 기본값이 무한 대기에 재시도 없음이라, 응답이 오지 않는 호출 하나가 담당자의 요청을 영원히 붙잡을 수 있었다. 제한값은 두 번의 시도가 Cloud Run 요청 제한 안에 들어가도록 잡는다. 재시도 조건에는 시간 초과와 429·5xx가 들어가므로 보충 단계가 제출본 수만큼 동시에 나갈 때의 한도 초과도 여기서 흡수된다.

**요약 호출과 브리핑 호출은 각자 실패를 가둔다.** 어느 쪽이 터져도 코드가 계산한 집계는 그대로 저장되고 그 호출의 서술만 비어서 화면에 나온다. 요약 호출의 입력은 제출본 수에 비례해 커지는 유일한 입력이라 표본이 늘 때 가장 먼저 위험해지는 자리다 — 여기서 예외가 밖으로 나가면 실행 전체가 죽고 아무것도 저장되지 않는다.

### 3.5 파트 E — 담당자 웹

같은 Next.js 앱의 `/admin` 경로다. 실제 작동 화면이어야 하지만 로그인이나 권한 시스템은 만들지 않고, 링크를 공개하지 않는 것과 6.2의 접근 코드로 통제한다.

담당자 리포트에는 정확히 다음 다섯 요소가 있어야 한다. 축별 분포와 극별 평균 강도(**화면과 브리핑에서는 "강도"라는 낱말을 쓰지 않고 퍼센트로만 적는다** — 담당자에게 설명 없는 척도 이름은 해석 혼동만 만든다), 16유형 분포, 축별·극별 정성 요구 요약, 인용 ID가 붙은 비식별 대표 인용, 축과 유형의 의미를 설명하는 담당자용 정적 해설이다. 그 밖에 제출본 목록, 제출본 상세(유형 결과·개인 보고서·원본 대화록), 자료 업데이트 버튼이 있다.

종합 리포트와 대표 인용은 비식별본을 쓰고, 인용 ID로 제출본까지 근거를 추적할 수 있게 한다. 담당자가 간접 정보를 조합해 개인을 추적하지 않는다고 전제하며, 그 이상의 익명성 검증은 구현 범위에 넣지 않는다.

**접속 현황이 `/admin/activity`에 있다.** 참여자 화면 방문, 담당자 화면 방문, 인터뷰 시작, 결과 확인, 제출의 다섯 가지를 이벤트 단위로 Firestore에 남긴다. **결과 확인은 결과 생성 성공 시점에 백엔드가 기록한다** — 세션 상태 가드가 세션당 1회를 보장하므로 별도 중복 방지가 없다. 이 이벤트의 용도는 퍼널이다. 결과 확인 대비 제출의 갭이 유형만 보고 제출하지 않은 이탈률이며, 유형 공개를 제출 뒤로 미루자는 논의(9.4)를 감이 아니라 숫자로 판정하기 위해 두었다. 갭 계산 화면은 만들지 않고 누적 건수로 본다. 화면은 누적 건수 다섯 개를 위에 두고, 그 아래에 **개별 접속을 최신순 로그 표로 나열한다** — 시각(Asia/Seoul, 초 단위), 이벤트 종류, 접속 IP, 기기, 브라우저 다섯 열이다. 목록이 길어지면 담당자 셸이 그대로 세로로 스크롤된다. 날짜별 집계는 로그가 대체했으므로 두지 않는다.

**IP는 마지막 자리를 가려 저장한다.** IPv4는 마지막 옥텟을, IPv6는 앞 네 그룹 뒤를 `*`로 바꾸며, **원본 IP는 어디에도 저장하지 않는다** — 마스킹은 조회 시점이 아니라 기록 시점에 한다. Cloud Run 뒤에 있으므로 실제 클라이언트 주소는 `X-Forwarded-For`의 첫 값에서 얻는다. 기기와 브라우저는 User-Agent 원문을 저장해 두고 조회 시점에 짧은 라벨(iPhone·Android·Windows 등, 삼성인터넷·Chrome·Safari 등)로 분류한다. **대화 내용도 참여자를 특정할 식별자도 담지 않는다.** 이 기능이 생기기 전에 쌓인 기록에는 IP·기기 정보가 없어 빈칸으로 표시된다. 방문 기록 엔드포인트는 참여자 화면도 치므로 접근 코드 게이트 밖에 있고, 조회는 admin 경로라 게이트 안에 있다. 기록 실패는 본 흐름(세션 생성·제출·화면 진입)을 깨지 않는다 — 3.4의 비식별화 실패 처리와 같은 원칙이다.

**제출본 목록에는 삭제 모드가 있다.** 시범운영에서 시험 삼아 만든 대화와 실제 참여자의 대화가 한 목록에 섞이므로 담당자가 시연 전에 정리할 수단이 필요하다. 삭제 모드는 토글이고, 꺼져 있으면 체크상자도 삭제 버튼도 보이지 않는다. 삭제 직전에 **접근 코드를 다시 입력받고** 되돌릴 수 없다는 것을 그 자리에서 알린다. 코드는 서버가 다시 검사한다 — 헤더의 코드는 화면에 이미 들어와 있는 값이라 삭제 의사를 확인하지 못하기 때문이다. 지운 뒤 이미 저장된 분석 결과는 고치지 않고, 집계는 다음 자료 업데이트에서 새로 계산된다.

### 3.6 파트 F — 담당자 대시보드

세로로 긴 종합 리포트를 **한 화면 대시보드**로 교체했다. 담당자가 계획서를 쓰면서 옆에 띄워 두는 화면이고, 스크롤로 답을 찾는 것이 아니라 **한눈에 보고 궁금한 것을 눌러 파고드는** 방식이어야 한다. 확정 디자인은 저장소 루트의 `dashboard-mock.html`이며 이것이 화면 계약이다.

화면은 1920×1080을 기준으로 만들고 브라우저 크기에 맞춰 통째로 축소한다. 담당자 화면은 회의실 스크린이나 노트북에서 **전체가 한 번에 보여야** 하고, 반응형으로 재배치하면 카드 간의 비교 관계가 깨진다. 2.4의 모바일 우선 원칙은 여기에 적용하지 않는다.

구성은 상단에 요약 수치 줄과 AI 분석모드 토글·버튼들, 왼쪽 열에 군·구별 분포 지도·연령 구성·조사 개요, 가운데 열은 세 줄이며 위에서부터 요구 키워드, 계획 부문별 요구와 그 옆의 최우선 요구·정착 의향, 연령대별 교차표와 도시가치 4축이다. 오른쪽 열에 상세 보기 패널이다. 오른쪽 패널은 아무것도 고르지 않았으면 비워 둔다.

**요구 키워드가 가운데 열 맨 위에서 폭을 다 쓴다.** 담당자가 계획서에 그대로 옮겨 쓸 수 있는 것은 부문 이름이 아니라 청년이 실제로 말한 소재이고, 부문 열 개는 그 소재가 어느 과의 일인지만 알려 준다. 그래서 무엇을 말했는지를 먼저 보이고 소관 분류를 그 아래에 둔다. **최우선 요구는 부문 카드 바로 옆에 둔다.** 둘 다 부문으로 세는 값이라 나란히 놓아야 "많이 말한 것"과 "먼저 꼽은 것"이 비교된다. **정착 의향은 백분율을 쓰지 않는다** — 말한 사람만 세므로 막대의 분모는 답한 사람 수이고 전체 참여자 수를 함께 적는다. **여기서 여가는 막대로 두지 않는다.** 마무리 질문이 사는 것만 직접 묻기 때문에 여가는 참여자가 지나가듯 말했을 때만 잡히고, 실측에서 여덟 명 중 한 명이었다. 데이터 추출과 원자료 CSV 열은 그대로 두어 질문을 손보면 되살릴 수 있게 한다.

**가운데 열 마지막 카드는 도시유형 분포가 아니라 요구 키워드 버블이다.** 여덟 명이 열여섯 칸에 흩어지면 대부분 0이고, 유형 코드는 설명 없이 담당자에게 아무 뜻이 없다. 계획서를 쓰는 사람에게 "배차간격 여섯 명"은 그대로 한 문장이 되지만 유형 두 명은 되지 않는다. **도시가치 4축 카드는 그대로 둔다** — 개발이냐 보전이냐 같은 방향을 주고 브리핑의 쟁점 섹션이 그 위에 선다. 유형은 참여자 개인 결과와 담당자 리포트, 종합 브리핑의 도시유형 섹션에 그대로 남으므로 대시보드에서 카드 하나가 자리를 내주는 것뿐이다.

**버블은 크기가 사람 수이고 색이 그 키워드가 가장 많이 붙은 부문의 색이다.** 무엇을 말했는지와 그것이 어느 소관인지가 한 그림에 들어온다. 버블을 누르면 오른쪽 상세 패널에 그 키워드가 붙은 요구와 인용이 펼쳐지며, 이는 부문 카드가 이미 쓰는 문법이다. **카드에 다 들어가지 않으므로 상위 몇 개만 그리고 그것이 상위 몇 개인지 밝힌다** — 잘라 놓고 말하지 않으면 화면에 있는 것이 전부인 것처럼 읽힌다. 전수는 원자료 CSV에 있다. **버블 개수의 상한을 정하는 것은 배치가 아니라 읽힘이다.** 이 카드 크기에서는 어떤 표본이든 다 들어가지만, 개수가 늘수록 반지름이 줄어 마흔 개를 넘기면 작은 원이 제 이름도 담지 못하는 크기로 내려가고 카드가 이름 없는 점들의 밭이 된다. **연결 그래프는 쓰지 않는다** — 요구당 키워드가 셋뿐이라 함께 나오는 짝이 거의 생기지 않아 조각난 점들이 되고, 힘기반 배치는 실행마다 자리가 달라져 담당자가 지난 화면과 견주지 못한다.

**AI 분석모드는 토글이다.** 켜면 카드마다 "AI 해석"이 나타나고 누르면 그 카드의 수치가 무엇을 시사하는지 오른쪽 패널에 펼친다. 켜져 있는 동안 상단에 AI 생성물이라는 주의 문구를 띄운다. 해석 문장은 화면을 열 때가 아니라 **분석 실행 때 한 번 생성해 저장한다** — 화면마다 호출하면 담당자를 기다리게 하고 같은 수치에 다른 문장이 나온다.

**원자료 내려받기는 요구 전수를 CSV로 내보낸다.** 담당자가 계획서에 붙이거나 엑셀에서 다시 세는 것이 실제 업무이고, 화면이 그 길을 막으면 안 된다. **열 구성이 계약이다** — 참여자, 중복 기기, 거주 군·구, 거주지 원문, 행정동, 나이, 성별, 도시유형, 정착 의향 셋(거주·근무·여가), 만족도 둘, 인터뷰 소요시간(초), 부문, 요구, 키워드, 언급 장소 원문, 언급 장소 군·구, 최우선 여부의 스무 열이다. **참여자 단위 열을 왼쪽에 모두 모으고 요구 단위 열을 오른쪽에 모은다** — 담당자가 두 행을 견주지 않고도 어느 열이 한 사람 안에서 반복되고 어느 열이 행마다 바뀌는지 한눈에 가르기 위해서다. **중복 기기 열에는 기기 토큰을 그대로 싣지 않는다** — 둘 이상이 공유하는 토큰에만 내보내기 시점의 그룹 번호를 붙이고 나머지는 비운다. 시에 필요한 사실은 "이 행들이 한 기기에서 왔다"는 것뿐이고, 브라우저를 계속 가리키는 식별자를 넘길 이유는 없다. **답하지 않은 만족도와 잴 수 없었던 소요시간은 0이 아니라 빈 칸이다** — 0을 쓰면 엑셀에서 실제 값으로 평균과 최솟값에 섞여 든다. 한 요구의 키워드 여럿은 한 칸 안에서 잇는다. 요구 하나가 한 행이고 축 밖 요구도 같은 자격으로 행을 받는다. 한 요구가 장소를 여럿 말했으면 행을 나누지 않고 한 칸 안에서 잇되 두 장소 열의 개수와 순서를 맞춘다.

지도는 2026년 7월 개편 경계를 단순화한 형상을 쓴다. 옹진군은 본토에서 멀리 떨어져 있어 같은 축척에 넣으면 본토가 작아지므로 왼쪽 아래 별도 상자에 넣는다.

**지도는 참여자 수와 언급 수를 토글로 갈아 끼운다.** 두 값은 같은 도형 위에 있지만 다른 것을 센다 — 하나는 그 군·구에 사는 참여자이고 다른 하나는 요구가 그 군·구를 가리킨 횟수다. 처음에는 참여자 수를 색으로 칠하고 언급 수를 이름 아래 상자로 함께 얹었는데, 군·구 열한 곳에 이름·수치·상자가 겹쳐 380px 폭의 카드가 읽히지 않았다. 지금은 부문 카드의 정렬 토글과 같은 문법으로 **한 번에 한 값만** 칠하고, 색 띠도 그 값의 색과 단위로 함께 바뀐다(참여자는 파랑·명, 언급은 청록·건 — 화면이 이미 쓰는 두 색을 그대로 나눠 갖는다). 두 값을 한 번에 보고 싶을 때는 군·구를 가리키면 설명풍선이 둘 다 준다. **색을 나누는 이유는 같은 도형의 같은 진하기가 다른 것을 뜻하기 때문이다** — 한 색으로 두 값을 칠하면 모드를 바꾼 것을 눈치채지 못한 담당자가 참여자가 늘었다고 읽는다.

**카드는 제 높이를 다 쓴다.** 고정 무대에서는 카드가 남긴 여백이 그대로 화면의 빈 띠로 보인다. 그래서 아래 두 규칙을 둔다. 첫째, **줄 수가 고정된 목록은 그 줄이 다 들어가는 높이를 카드에 준다** — 계획 부문 열 줄이 8px 모자라 스크롤바가 생겼고, 손가락 한 마디도 움직이지 않는 스크롤바는 담당자에게 목록이 더 있다고 거짓말을 한다. 둘째, **줄 수가 표본에 따라 변하는 카드는 줄이 카드 높이를 나눠 갖는다** — 줄 높이를 못 박으면 표본이 작을 때 카드 아래가 비고, 크면 스크롤이 생긴다. 줄은 제 글자 높이 아래로는 줄어들지 않고, 거기까지 갔는데도 넘치면 그때 스크롤한다. 연령대별 교차표는 여기에 더해 **요구가 없는 부문도 0으로 줄을 세운다** — 줄이 빠지면 그 부문이 아예 없는 것처럼 읽히고, 바로 위 카드가 같은 열 개를 세우고 있어 두 카드가 어긋나 보인다.

**차트는 화면에 들어올 때마다 2초 안에 채워지며 등장한다.** 완성된 그림이 툭 나타나면 담당자는 무엇이 갱신됐는지 알 수 없고, 시연에서는 화면이 정지 화상처럼 보인다. 재생 시점은 첫 로드, 자료 업데이트 직후, 다른 화면에서 돌아왔을 때 셋이다. **정렬 토글이나 항목 선택처럼 담당자가 조작할 때는 재생하지 않는다** — 조작할 때마다 다시 차오르면 금세 성가시고 읽기를 방해한다. 대신 정렬을 바꾸면 막대는 새 길이로 늘어나고 행은 새 순위 자리로 미끄러진다. **조작에 대한 응답은 등장 연출보다 짧아야 한다.** 같은 길이로 맞추면 화면이 굼떠 보인다. 운영체제에서 동작 줄이기를 켠 담당자에게는 연출 없이 완성 상태를 곧바로 보여준다.

### 3.7 파트 G — 종합 브리핑

담당자 대시보드와 별개의 `/admin/report` 라우트다. 대시보드는 회의 중 한 화면에서 즉답을 얻는 화면이고, 브리핑은 담당자가 계획서를 쓰면서 근거로 인용하는 세로로 긴 문서다. 3.6이 못 박은 고정 스테이지와, 좁은 글과 넓은 데이터를 교차하는 읽기 리듬은 같은 화면에 공존할 수 없으므로 화면을 나눈다. **대시보드는 이 개편에서 손대지 않는다.** 카드별 "AI 해석" 칩도 그대로 두고 브리핑 진입점만 추가한다.

참조 원형은 Anthropic의 「81,000명이 AI에 바라는 것」이고 그 구조 분석은 저장소 루트 `website_design_analysis.md`에 있다. 원형에서 가져오는 것은 한 섹션이 한 질문에 답하는 골격, 수치 옆에 개인의 인용을 붙이는 배치, 양극을 좌우로 미러링하는 대비 막대, 방법론과 한계를 본문에서 밝히는 태도, 서사는 세리프·수치는 산세리프로 역할을 나누는 타이포다.

**섹션 골격은 열한 개로 고정하고 내부 서술만 실행마다 바뀐다.** 표지, 이번 분석의 핵심, 어떻게 조사했나, 무엇을 가장 많이 말했나(계획 부문), 어떤 도시를 바라나(4축), 엇갈리는 요구(쟁점), 누가 무엇을 말했나(연령·성별 교차), 바라는 도시유형, 확인이 필요한 것, 청년들의 목소리(인용 전수 탐색), 자료 순이다. 이 순서가 설득 구조 자체이므로 실행 결과에 따라 섹션을 늘리거나 빼지 않는다.

**쟁점 섹션이 이 파트를 만드는 이유다.** 카드 하나의 수치를 보고 쓰는 해석과, 네 축·부문·유형을 동시에 놓고 서로 충돌하는 요구를 찾는 것은 다른 작업이다. 후자는 대시보드의 카드별 칩으로는 원리상 나올 수 없고, 계획서를 쓰는 담당자에게 가장 필요한 것이다. 쟁점마다 양쪽 인용을 나란히 놓아 대립이 실제 발화에 있음을 보인다.

**표지 지도는 참여자 한 명을 점 하나로 찍는다.** 원형의 지구본 scrollytelling은 만들지 않는다 — 스크롤 고정 컨테이너에 애니메이션 라이브러리 세 개가 얹힌 구성이고, 무엇보다 담당자가 첫 수치에 닿기까지 스크롤을 열 번 굴려야 한다. 대신 3.6의 인천 지도를 다시 쓰고 군·구 도형 안에 점을 배치한다. 인용 시퀀스는 스크롤에 묶어 옮긴다(아래). **점 하나는 정확히 한 명이다.** 원형은 점 하나를 응답자 4명으로 묶었지만 우리 규모에서 묶으면 표본을 부풀리게 된다. 점 마흔 개면 마흔 명으로 보여야 한다.

**원형 인트로의 연출은 지구본이 아니라 이 지도로 옮겼다.** 표지에서 군·구가 겹치지 않는 인용 다섯 개가 **스크롤에 묶여** 지나간다. **지도는 화면에 붙어 제자리를 지키고, 바뀌는 것은 말과 점의 색뿐이다.** 지도 크기는 16:9 창에서 높이가 제약이므로 뷰포트 높이에 묶어 키운다. **지도의 두 치수는 한 길이에서 계산해 못 박는다** — 폭만 정하고 높이를 `aspect-ratio`에 맡겼더니 Safari에서 인용이 바뀔 때마다 지도가 줄었다 커졌다 했다. 사파리는 flex 안에서 이 조합의 크기를 늦게 확정한다. 원형에서 지구본이 자리를 지키고 서사만 그 위를 지나가던 구조 그대로다. **인용은 sticky 무대 안에 함께 고정되고, 스크롤은 어느 인용을 보여줄지만 정한다.** 처음에는 인용 블록 자체를 스크롤에 태웠는데, 그러면 읽는 사람이 멈춘 자리에 따라 글이 화면 위쪽이나 아래쪽 아무 데나 걸렸다. 지금은 스크롤 길이만 담당하는 빈 간격자를 두고, 그중 화면 한가운데를 지나는 것이 활성 장면이 되며, 글은 언제나 무대 중앙에 있다. 간격자 하나는 화면 높이의 76%이고 마지막 뒤에 여유를 두어 끝 인용이 잠깐 머물다 풀린다. 활성 장면의 인용은 0.7초 뒤부터 초당 65자로 캐럿과 함께 타이핑되며, **한 번 다 쓰인 문장은 되돌아와도 다시 쓰지 않는다.** 그 발화를 한 참여자의 점이 강조되고 그가 사는 군·구가 켜지며, 나머지 군·구의 점과 라벨은 뒤로 물러나 글이 그 위에서 읽힌다.

**자동 순환과 화살표·진행점은 쓰지 않는다.** 처음에는 7초 자동 순환에 좌우 버튼과 진행점을 두었으나, 원형의 구조가 아니었고 읽는 사람이 버튼을 눌러 가며 봐야 했다. 스크롤이 진행을 맡으면 컨트롤이 필요 없다.

**한 제스처는 한 군·구까지만 전진한다.** 트랙패드 한 번의 델타가 화면 몇 개 분량이어서, 잡아 두지 않으면 시퀀스를 통째로 지나쳐 지도 아래로 빠져 버린다. 앞으로 가는 휠·터치는 **현재 위치보다 앞에 있는 첫 장면의 중앙까지만** 허용하고 그 너머는 거절한다. 시퀀스에 들어오기 전의 큰 제스처도 같은 규칙에 걸려 첫 장면에 내려앉는다. CSS의 `scroll-snap-stop: always`만으로는 큰 델타를 막지 못해 실측에서 그대로 통과했다.

**문장이 도착하는 동안은 아래로 넘어가지 않는다.** 트랙패드 한 번이 한 장면을 통째로 덮어서, 잡아 두지 않으면 인용이 읽히기도 전에 지나간다. 타이핑이 끝날 때까지 아래 방향 휠과 터치만 거절한다. **위로 되돌아가기, 건너뛰기, 키보드, 스크롤바는 그대로 열어 둔다** — 잠깐 붙드는 것과 가두는 것은 다르다. 타이핑이 끝났다고 보고되지 않는 경우를 대비해 예상 소요에 여유를 더한 상한을 두고, 상한에 걸리면 문장을 전문으로 채우면서 함께 풀린다. 동작 줄이기에서는 타이핑이 없으므로 잠금도 없다.

**시퀀스에는 빠져나갈 길을 둔다.** 원형의 `Jump to story`에 해당하는 "바로 요약 보기"를 화면 하단에 고정한다. 이 연출은 담당자가 수치에 닿기 전에 네 화면 넘게 스크롤하게 만들고, 그것을 강제하면 안 된다.

**점 색은 지금 말하고 있는 인용의 축으로 전체를 다시 칠한다.** 생활 리듬 인용이 나오는 동안에는 참여자 전원이 활기·여유로 갈려 보이고, 다음 장면이 선호 공간이면 같은 점들이 도시·자연으로 다시 갈린다. 그래서 장면 선정은 군·구가 겹치지 않게 고르되 **네 축을 돌아가며** 뽑는다. 한 축에만 머무르면 지도가 얼어붙어 이 연출의 이유가 사라진다. 무엇이 무슨 색인지는 장면마다 달라지므로 범례를 지도 밑에 함께 띄운다.

**두 극의 색은 여기서도 파랑 한 계열의 명도 대비다.** 원형은 초록과 주황을 썼지만 그 색들은 기대와 우려라는 좋음·나쁨 축에 붙은 것이고, 우리 4축의 양극은 대등한 선호다.

**동작 줄이기에서는 타이핑을 끄고 인용을 전문으로 보여준다.** 진행은 스크롤이 하므로 그대로 남는다.

**점의 위치는 무작위처럼 보이되 결정적이다.** 제출본 ID를 시드로 군·구 도형 안에서 위치를 뽑으므로 같은 참여자는 언제나 같은 자리에 있고, 자료 업데이트로 참여자가 늘면 기존 점은 그대로 있고 새 점만 나타난다. 매번 다시 뽑으면 담당자가 화면을 열 때마다 점이 옮겨 다니고 업데이트 전후를 비교할 수 없다. **점의 색은 전부 같다** — 유형이나 축으로 색을 가르면 16유형은 색으로 구분되지 않고, 네 축 중 하나만 고르면 그 축이 특별대우가 된다. 점은 "한 명"만 뜻하고 개별 정보는 가리켰을 때 나온다.

**AI 서술은 대시보드 카드 해석과 같은 호출에서 나온다.** 브리핑용으로 별도 호출을 두면 같은 집계에 서로 다른 서술이 생기고, 담당자가 칩과 본문의 어긋남을 발견하는 순간 둘 다 못 믿게 된다. 3.4가 축별 분할 호출을 금지한 것과 같은 이유이며, 쟁점은 애초에 축을 가로질러 봐야 나온다. 카드용 서술은 짧고 브리핑용은 리드문과 해석 두 자리이므로 길이와 역할이 달라 각각 자리를 갖되, 한 호출 안에 있어 어긋날 수 없다.

**데이터 섹션마다 서술 자리를 둘 준다.** 차트 앞의 리드문은 이 섹션에서 무엇을 볼 것인가를, 차트 뒤의 해석은 무엇이 보이고 그것이 계획에 무엇을 시사하는가를 쓴다. 원형이 좁은 본문과 넓은 차트를 교차시켜 얻는 리듬이 이 두 자리에서 나온다. 확인이 필요한 것 섹션은 정책을 확정해 제안하지 않고 후속 확인 질문의 형태로만 쓴다.

**방법론 섹션은 고정층과 가변층으로 나눈다.** 인터뷰 방식, 증거 기반 판정 원리, 비식별 처리, 4축 정의, 표본이 자발적 참여라는 성격은 실행마다 바뀔 이유가 없는 사실 서술이므로 정적 텍스트다. 반면 **이번 표본이 실제로 어땠는가는 AI가 쓴다** — 참여자 수와 군·구 분포, 평균 대화 턴 수, 수집된 요구 건수, 증거가 없어 기본 극으로 채워진 축이 몇 건이었는지, 단극 관측이 몇 건이었는지, 그래서 어느 축의 해석을 얼마나 조심해야 하는지다. 방법론 전체를 AI에게 맡기면 사실 오류가 섞이고 실행마다 표현이 달라져 담당자가 발표 자료에 인용할 수 없다. 반대로 전부 정적으로 두면 이번 실행의 실제 데이터 성격을 말하지 못한다. **4축 정의는 반드시 정적이다** — AI가 매번 다시 쓰면 축 계약이 흔들리고, 그 원천은 `admin_guide.json`이다.

**표본이 작다는 사실을 숨기지 않는 것이 이 파트의 방어선이다.** 원형은 참여자가 8만 명이어서 지역 12개 비교와 버블 지도가 성립한다. 우리는 시범운영이고 군·구가 11곳이라 군·구당 참여자가 한 자릿수다. 그래서 다음을 규칙으로 둔다.

- **군·구 간 요구 성향 비교는 만들지 않는다.** 지도는 참여 분포만 보여주고, 군·구 서술은 어느 군·구 청년이 무엇을 원한다가 아니라 **모집 경로의 편중**을 읽는 데만 쓴다.
- **백분율로 서술하지 않는다.** "인천 청년의 30%"가 아니라 "참여한 47명 중 14명"으로 쓴다. 원형이 발행 다음 날 낸 정정이 정확히 이것이었다 — 전 세계 사람의 67%를 인터뷰 대상자의 67%로 고쳤다. 우리는 처음부터 그렇게 쓴다.
- **연령대 비교는 절대 건수로만 한다.** 비율 비교는 하지 않는다.
- 모든 집계에 표본 수를 병기하고, 막대 폭이 최댓값 대비 상대 폭이라는 것을 캡션에 밝힌다.

**AI 서술을 생성하지 못한 실행은 수치만 보여준다.** 브리핑 자리를 비워 두고 그 사실을 화면에 적으며, 담당자가 자료 업데이트를 다시 누르는 것이 복구 수단이다. 부분 재시도나 이전 실행의 서술을 끌어오는 것은 하지 않는다 — 다른 표본의 해석이 이번 수치 옆에 붙는 것이 비어 있는 것보다 나쁘다.

**인용 전수 탐색은 축·부문·군·구·연령으로 걸러 보는 한 겹의 레이어다.** 비식별본을 쓰고 인용 ID로 제출본까지 추적된다. 원형의 문제는 되풀이하지 않는다 — 인용 데이터는 담당자가 레이어를 열 때 받아 오고, 카드는 클릭 가능한 `div`가 아니라 버튼이며, 레이어에는 포커스 관리가 있다.

**색은 원형의 기대-녹색·우려-파랑 이원화를 쓰지 않는다.** 우리 4축의 양극은 좋음과 나쁨이 아니라 대등한 선호인데 색을 가르면 한쪽이 부정적으로 읽힌다. 축은 인천 브랜드 블루의 명도 대비로 표현하고, 대비색은 쟁점 섹션에서만 쓴다. 그 섹션에서만 두 요구가 실제로 대립한다.

**읽기 축은 가운데 하나다.** 글과 데이터는 왼쪽 끝이 아니라 **가운데 선을 공유한다.** 폭은 세 겹으로 좁혀지며 모두 같은 중심에 놓인다 — 본문 680px, 차트를 설명하는 제목·캡션 780px, 차트 자체 1120px이다. **넓은 열 안에 있어도 산문이면 글 폭을 지킨다.** 이 상한이 없으면 캡션 한 줄이 1120px를 다 써서 읽히지 않는다. 표지 지도와 그 제목도 같은 폭에 맞춰 왼쪽 끝을 맞추고, 고른 참여자는 지도 아래가 아니라 옆에 둔다. **왼쪽 정렬로 통일하는 안은 시도했다가 되돌렸다** — 축은 하나가 되지만 넓은 화면에서 글이 왼쪽에 몰리고 오른쪽이 비어 문서로 읽히지 않았다.

**서체는 역할로 나눈다.** 서사·인용·표지 문장은 세리프, 제목·수치·레이블·컨트롤은 산세리프다. 한글 세리프는 라틴보다 행간이 더 필요하므로 본문 행간을 넉넉히 잡고, 문서 전체에 tabular 숫자를 걸어 열끼리 비교되는 수치의 자릿수가 흔들리지 않게 한다.

**연출은 스크롤에 따라 한 번만 재생한다.** 섹션은 처음 도달할 때 떠오르고, 막대는 그때 0에서 자라며, 표지 수치는 세어 올라간다. **되돌아올 때 다시 재생하지 않는다** — 보고서가 슬라이드쇼처럼 읽힌다. 담당자가 조작하는 응답(행 펼침, hover, 레이어 열기)은 등장 연출보다 짧다. 3.6이 대시보드에 둔 것과 같은 원칙이다. **동작 줄이기를 켠 담당자에게는 지속시간을 0으로 만드는 것으로 끝내지 않고 블록을 처음부터 보이게 한다** — 연출을 끄면서 등장 여부까지 관찰자에 맡기면 관찰자가 실패했을 때 내용이 사라진다.

## 4. 도시유형 4축과 판정 원칙

### 4.1 성격과 표기

진단 대상은 참여자의 성격이 아니라 참여자가 바라는 2045년 인천의 수요와 지향이다. 심리측정 검사가 아니라 참여 흥미와 정책 전달력을 높이기 위한 진단 장치다. 각 축은 **하나의 질문 차원**만 갖는다. "현재 유형"이 아니라 "바라는 유형"을 표현한다.

**4글자 유형 코드는 어느 화면에도 노출하지 않는다.** 결과 화면, 공유 카드, 저장 이미지, 파일명, 공유 시트 제목은 물론 담당자 대시보드·제출본 목록·제출본 상세에도 코드와 개별 극 글자를 표시하지 않는다. 코드는 내부 판정·저장·집계에만 쓰는 식별자다. 화면에서 코드가 하던 식별자 역할은 유형 별명과 일상어 라벨 배지 4개가 대신한다. 담당자에게도 코드를 숨기는 이유는 공무원 역시 축 글자를 외우고 있지 않기 때문이다 — 해석표를 함께 주더라도 알파벳 네 글자를 먼저 읽게 하면 해석 부담만 늘어난다.

### 4.2 4축

| 순서 | 축 ID | 축 이름 | 판별 차원 | 극 1 | 극 2 | 동률 기본 극 |
|---|---|---|---|---|---|---|
| 1 | `AC` | 생활 리듬 | 도시의 활동 수준 | `A` 번화한 | `C` 한적한 | C |
| 2 | `UN` | 선호 공간 | 눈앞의 풍경 | `U` 도시적인 | `N` 자연과 가까운 | N |
| 3 | `OW` | 우선 가치 | 무엇을 먼저 챙기는가 | `O` 기회가 많은 | `W` 서로 돌보는 | W |
| 4 | `FH` | 발전 가치 | 변화를 받아들이는 방식 | `F` 새로움을 여는 | `H` 이야기를 이어가는 | H |

여덟 극의 **표시 이름**은 `번화`·`한적` / `도시`·`자연` / `경쟁`·`포용` / `개발`·`보존`이다. `axes.md`의 JSON 블록이 그 원천이고 담당자 화면의 배지와 브리핑 서술이 이 이름을 글자 그대로 쓴다. **표시 이름은 판별 차원보다 좁을 수 있으며, 태깅은 이름이 아니라 판별 차원과 앵커만 본다** — `경쟁`은 O의 이름이지만 통근·행정의 부담 완화가 O에 들어가고, `개발`은 F의 이름이지만 건설을 말하지 않는 기술·제도·문화의 새로움도 F다. 참여자 화면의 긴 라벨(`활기찬 도시`, `기회가 많은 도시` 등)은 유형 일러스트와 짝을 이루므로 판별 차원 쪽 표현을 그대로 쓴다.

유형 코드는 네 자리 순서 고정으로 `AUOF`~`CNWH`, 16종이다. 여덟 극 글자가 서로 겹치지 않으므로 코드에 모호성이 없고, 이 불변식은 코드가 검증한다.

각 축이 **다루지 않는 것**이 축 설계의 핵심이다.

- **생활 리듬**은 동네와 거리의 붐빔·활동 수준에 대한 바람만 다룬다. 시설의 종류나 근접성은 선호 공간, 새것의 등장은 발전 가치가 맡는다. **참여자 개인이 무엇을 하며 쉬는지는 이 축이 아니다.** 통근·환승·야근의 피로는 `O`로, 안심을 바라는 발화는 `W`로 간다.
- **선호 공간**은 물리적 환경과 근접성만 다룬다. 그 시설을 얼마나 자주 쓰는지는 생활 리듬, 새것인지 옛것인지는 발전 가치가 맡는다.
- **우선 가치**는 성장·기회·효율과 복지·돌봄·관계 사이의 배타적 자원 배분을 다룬다. 시간·비용·번거로움을 줄여 달라는 발화는 산업이나 복지를 언급하지 않아도 `O` 증거이고, 이웃·단골·아지트처럼 사람 사이의 연결을 말하는 발화는 복지 제도를 언급하지 않아도 `W` 증거다. **이 배타성은 채점자가 증거를 가르는 기준이지 참여자에게 던질 프레임이 아니다.**
- **발전 가치**는 변화를 받아들이는 방식만 다룬다. 변화의 결과로 하루가 얼마나 붐비는지는 생활 리듬, 그 공간이 도심인지 자연인지는 선호 공간, 그 변화가 누구의 이익을 위한 것인지는 우선 가치가 맡는다.

**AC의 판별 차원은 한 번 교체됐고 그 경험이 축 설계의 교훈이다.** 옛 정의("하루의 템포")는 개인의 여가 행위(카페에서 쉬기, 혼자 밥 먹기, 산책)를 `C` 증거로 빨아들여 `C`가 판정이 아니라 쓰레기통이 됐다. 앵커를 하나씩 열거해 막는 방식은 열거한 표면형만 막고 재발했고, **판별 차원 자체를 다시 정의하자 한 번에 멈췄다.** 일반화하면 이렇다 — **앵커를 열거해 막는 방식은 열거한 표면형만 막고, 판별 차원을 다시 정의하거나 문항을 신설하는 것은 유형 전체를 움직인다.** 발화 경로가 없으면 채점 규칙만으로는 증거가 생기지 않는다는 것도 같은 실험에서 나왔다.

### 4.3 축 계보 (심사 대응)

이것이 없으면 심사위원에게 "감성 테스트"로 읽힌다. 담당자 해설과 발표 자료에 기록한다.

- 생활 리듬 ← 구 E/I (개방·교류·활력 vs 정주·정온·내밀)
- 발전 가치 ← 구 S/N (현실·기존 자원 vs 미래·혁신·첨단)
- 우선 가치 ← 구 T/F (효율·기능·경쟁력 vs 관계·돌봄·정서)
- 선호 공간 ← 신설. 인천 계획의 해양·녹지·수변 의제를 반영
- 구 J/P (계획·정돈 vs 유연·자생)는 **의도적으로 제외**했다. 실측에서 태깅이 가장 희소했고 행정 개념이라 일상어 인터뷰와 궁합이 나쁘다. 운영 방식에 대한 수요는 부문 라벨과 축별 요구 보고서로 잡는다.

**MBTI 글자 차용을 폐기한 이유**는 자기모순이었다. "성격 MBTI와 완전히 분리한다"면서 MBTI 글자를 쓰고 있었고, 그 때문에 참여자 화면에서 축 라벨을 일상어로 가리는 우회를 이미 하고 있었다. 개편은 그 우회를 정식 설계로 승격시킨 것이다. 구 축의 극 정의가 정책 추상어("관문·글로벌", "성장관리")여서 자유 발화에서 태깅이 어려웠던 문제도 함께 해소했다.

### 4.4 채점 앵커 설계 원칙

축 교체의 성패는 앵커에 달려 있다. `axes.md`는 다음 다섯 원칙을 지킨다.

1. **축 간 상호배타.** 각 축의 앵커는 그 축의 판별 차원에만 반응한다. 같은 장면이 두 축의 앵커에 나타나면 안 된다. 소재가 겹칠 때는 참여자가 그것을 **어떤 문제로 말했는지**로 가른다.
2. **양극 대칭.** 여덟 극 모두 가중치 1·2·3 앵커를 정확히 하나씩 갖는다. 구 축의 T/F 쏠림은 F에만 앵커가 많던 비대칭에서 나왔다.
3. **부정 앵커 포함.** 축마다 "증거가 아닌 발화" 예시를 3건씩 둔다. 판정 일관성은 긍정 예시보다 경계 예시에서 갈린다.
4. **교차 조합 앵커 포함.** 상관 쏠림을 끊기 위해 활기+자연, 여유+도심 발화 예시를 명시한다. "활기찬 = 도시적인"이 아님을 채점 LLM에게 보여 주는 장치다.
5. **지명 의존 축소.** 취지가 같으면 지명이 다르거나 없어도 동일하게 태깅한다.

`H` 극에 "기존 주민이 떠나지 않도록 배려함"을 두지 않는다. 이 장면은 `W` 극의 "소외되는 사람이 없도록 배려"와 정면으로 겹쳐 원칙 1이 깨진다. 대신 "밀려나지 않는 동네"를 취지에 따라 `W`와 `H`로 가르는 판별 규칙을 둔다.

**축 문안을 고칠 때는 `axes.md`를 먼저 고치고 담당자 해설(`admin_guide.json`)이 따라오게 한다.** 반대 방향은 채점을 바꾸지 못한 채 설명만 바꾸는 것이 된다.

### 4.5 증거 기반 판정

대화록 전체를 한 번에 평가하는 홀리스틱 LLM 판정은 실행마다 기준이 흔들리므로 쓰지 않는다. 개방형 대화에서 미시 증거를 추출하고 코드가 이를 결정적으로 합산한다.

**인터뷰어가 답의 후보를 만들어 내밀지 않는다.** 이것이 가장 강한 금지다. 축 이름을 말하지 않아도, 두 개든 세 개든, 마지막을 열어 두었든, 인터뷰어가 지어낸 항목 안에서 참여자가 답하는 순간 그 말은 참여자의 생각이 아니라 인터뷰어가 만든 틀에 대한 반응이 된다. 최소한 세 형태로 나타난다 — 두 극을 나란히 놓는 질문, **우선순위나 예산 배분을 두 항목으로 제시하는 질문**(문장이 길고 정중해도, 후보를 참여자가 앞서 한 말에서 가져왔더라도 같은 위반이다), 예시라는 이름으로 후보를 흘리는 질문. 우선순위는 참여자가 이미 꺼낸 것들 **전체** 안에서 스스로 고르게 하고, 트레이드오프도 참여자가 이미 말한 바람 하나를 붙들고 그 대가를 감수하겠는지 묻는 형태여야 한다.

**이 위험을 다루는 주된 수단은 금지 문구가 아니라 호출 분리다**(3.2). 인터뷰어가 극 이름·정의문의 대비·앵커 문장을 아예 받지 않으므로 옮길 것이 없다. `system.md`의 후보 제시 금지는 인터뷰 규범으로 남기되, 읽지 않는 문서를 옮기지 말라는 지시는 존재 이유가 없으므로 `axes.md`에서 걷어냈다.

**참여자가 스스로 꺼낸 현재의 불편은 바람이 붙어 있지 않아도 증거다.** 구체화 사다리가 현재의 불편을 한 번 끌어오므로 증거 일부가 바람이 아니라 불편의 형태로 나온다. 다만 반대 상태의 바람을 추론해 채우는 것이 아니다. 불편이 가리키는 문제가 어느 한 극의 판별 차원과 그대로 일치할 때만 그 극으로 태깅하고, 둘 이상의 극으로 읽히면 증거를 만들지 않는다. 가중치는 2가 상한이다 — 우선순위와 트레이드오프는 지향을 말한 발화에서만 성립한다. 같은 소재가 뒤에서 2045년의 장면이나 바람으로 다시 나오면 반복이 아니라 별개의 증거다. 문제의 확인과 미래에 대한 요구는 서로 다른 근거이기 때문이다.

**채점은 증분으로 한다.** 매 턴 마지막 참여자 발화 하나만 태깅하며 대화 전체를 다시 채점하지 않는다. 이유는 셋이다. 매 턴 전면 재채점은 이 절이 배제한 홀리스틱 판정을 매 턴 반복하는 것에 가깝고, 전면 갱신이면 각 증거의 턴 번호를 모델이 지정해야 하는데 그것은 백엔드가 결정할 일이며, 매 턴 판정이 달라져도 관측되는 것은 마지막 상태뿐이라 흔들림을 관찰할 방법이 없다. **증분의 대가도 기록해 둔다** — 어떤 턴에서 인용이 한 번 느슨해지면 그것이 누적 증거에 남아 이후 모든 턴의 선례로 작동한다.

**누적 대화 기록을 채점자에게 함께 넘기는 이유**는 발화 하나만 떼어 놓으면 채점할 수 없는 경우가 많기 때문이다. **누적 증거를 넘기는 목적은 중복 태깅 방지 하나로 한정하고 그 한정을 `axes.md`에 명시한다.** 누적 증거를 본 채점자가 "이 축은 한쪽 극만 있으니 반대쪽도 찾아보자"고 균형을 맞추기 시작하면 채점이 오염된다. 단극 관측은 참여자가 실제로 일관되게 말했다는 신호여야 하며, 채점자가 인위적으로 지우면 표시 강도 보정과 시범운영 지표가 함께 무의미해진다. 실측에서 이 충동은 실재했고 이 한정 문단이 막았다.

**채점은 인터뷰어가 어떻게 물었는지를 판단하지 않는다.** 유도질문의 예방은 인터뷰어 프롬프트의 몫이고, 채점기는 참여자 발화만 본다. 엄격한 채점이 빈 축을 남기고, 빈 축을 채우려는 힌트가 소재 없는 참여자에게 직역 질문으로 나가 인터뷰를 딱딱하게 만드는 고리가 격리 점검에서 확인됐으며, 도시유형은 참여 유도용이므로 채점을 느슨하게 했다. 가중치 3은 참여자 발화에 우선순위나 트레이드오프가 드러날 때이며, 정리 1단계 "딱 하나만 먼저 이뤄진다면"의 답은 참여자 자신의 우선순위이므로 가중치 3이다. 지향이 암시된 현재의 선호 서술(자주 가는 곳, 좋아하는 곳)과 유보된 짧은 답은 가중치 1이다. 축 정의와 라우팅(개인 여가는 여유가 아니다, 노동·안전 동기), 같은 취지 반복 금지, 인용 원문은 그대로다. 대가는 강도 숫자의 뜻이 약해지는 것이며, 표시는 4.6이 이미 감당한다.

**턴 번호는 백엔드가 저장 시점에 부여하고 LLM에게 요구하지 않는다.** 모델이 세어 낸 값은 어차피 백엔드 값으로 덮이는데, 그 필드의 형식이 어긋나면 나머지가 멀쩡한 증거까지 통째로 폐기되기 때문이다. 축과 극의 대응은 스키마로 표현할 수 없으므로 백엔드가 항목마다 검증하고, 실패한 항목만 버리고 나머지는 살린다.

합산 규칙은 코드가 소유한다. 극 점수는 그 극 증거의 가중치 총합이고, 점수가 큰 극이 판정 글자다. 강도는 `승자 점수 ÷ 축 총점 × 100`을 반올림해 51~100으로 클램프한 값이다. 동률이면 증거 개수가 많은 극, 개수도 같으면 기본 극으로 정한다. **증거가 하나도 없는 축은 기본 극 51%로 채우고 로그를 남긴다.**

**축 결과에는 증거 건수와 빈 축 여부가 함께 실린다.** 빈 축의 51과 박빙의 51은 표면값이 같아 구분되지 않았고, 그 구분이 없으면 프롬프트 A/B의 기준선 자체가 오염된다. **빈 축 여부는 참여자 응답에도 실린다** — 화면이 두 51을 갈라 표시해야 하기 때문이다. 극 점수와 증거 목록은 서버 전용으로 남는다.

증거 태깅 자체는 LLM 판단이므로 완전히 재현되지는 않는다. 다만 한 번의 큰 판단 대신 여러 미시 판단을 합산하고, 합산·반올림·동률·빈 축 처리를 코드로 고정해 변동 범위를 줄인다.

### 4.6 표시 강도 보정

두 축이 같은 이유로 잘못 읽힌다. 한쪽은 강도가 위로 튀고 다른 쪽은 근거 없는 값이 근거 있는 값처럼 보인다. **판정·저장값(51~100)은 불변이고 표시만 보정한다.**

**단극 관측.** 한쪽 극 증거가 0건이면 강도가 100%로 계산된다. 공식상 옳지만 단정적으로 읽힌다. 참여자·담당자 화면 공통으로 표시 강도를 95로 클램프하며, 96~100 표시는 양극이 모두 관측된 축에서만 가능하다. 담당자 제출본 상세에는 "단극 관측" 표기를 붙여 데이터의 성격을 알 수 있게 한다.

**빈 축.** 증거가 0건인 축에는 백분율을 보이지 않는다. 그 축의 51은 계산 결과가 아니라 기본 극을 채운 값이고, 숫자를 그리는 순간 없는 근거를 있는 것처럼 만든다. 참여자 유형 카드는 강도 바와 두 백분율을 그리지 않고 "이번 대화에서는 이 이야기가 나오지 않았어요"만 둔다. **극 라벨 배지 네 개는 그대로 둔다** — 유형은 네 글자로 성립하고 기본 극도 판정 결과이기 때문이다. 담당자 상세는 강도 자리에 "강도 없음"을 두고 그 값이 집계에서 제외된다는 것을 함께 적는다. 판정 이유 자리는 코드가 채우고 축별 요구는 빈 배열이 된다. 표시 세 곳이 같은 사실을 말한다.

### 4.7 산출물

참여자 산출물은 유형별 정형 일러스트, 유형 별명, 한 줄 설명, 유형 이야기와 궁합 두 유형, 일상어 라벨 배지 4개, 축별 강도 바, 축별 판정 이유, 축별 요구 보고서, 저장·공유할 수 있는 유형 카드다. **4글자 코드는 어디에도 포함하지 않는다.**

라벨 배지 8종은 **번화 · 한적 · 도시 · 자연 · 경쟁 · 포용 · 개발 · 보존**이다. W극을 "포용"으로 정한 이유는 "복지"가 제도로 좁고 "돌봄"이 돌봄 서비스로 좁게 읽히는 반면, 포용이 제도·이웃 관계·소외 없는 배려를 한 단어로 덮기 때문이다.

**2026-09-02에 배지 다섯 개를 교체했다**(활기→번화, 여유→한적, 기회→경쟁, 새로움→개발, 이야기→보존). 근거는 인천연구원 2차 인터뷰다 — "기회와 포용, 새로움과 이야기, 서로 상반되는 축으로 바꿀 수 있을까요"라는 지적이었고, 요구는 쉬운 말이 아니라 **두 극이 대립쌍으로 읽히는 것**이었다. 지적된 두 축만 바꾸면 나머지 두 축과 결이 어긋나므로 AC도 함께 옮겼고, AC 교체는 `여유`가 개인 성향어라 `axes.md`가 방어 문구로 막고 있던 낱말이라는 점에서 판별 차원에 오히려 가깝다. **판별 차원과 채점 앵커는 바꾸지 않았다.** 대가로 표시 이름이 판별 차원보다 좁아진 곳이 둘 생겼고(O의 효율·부담 완화, F의 비물리적 새로움), 이는 4.2의 총칙과 `axes.md`·`admin_guide.json`의 단서로 막는다. `admin_guide.json`에 O극 과소 태깅이 실측된 이력이 있으므로 재태깅 회귀에서 O·F 태깅 건수를 확인한다.

**별명 검수 기준.** 별명만 보고 4축 극을 역산할 수 있어야 한다. 다만 카드 전면의 배지 4개가 이미 정확한 식별자이므로 역산 부담을 별명이 혼자 지지 않는다. 16개가 같은 문법 구조를 공유하는 기계적 조합이어서는 안 된다. 무보상 18턴 인터뷰의 유일한 완주 보상이자 SNS 공유 소재이기 때문이다.

**유형마다 긴 이야기와 궁합 두 유형을 함께 둔다.** 2026-09-02에 추가했다. 근거는 팀 회의다 — 일반적인 유형 테스트가 별명 옆에 그 유형의 설명과 잘 맞는·안 맞는 유형을 함께 보여 주고, 그것이 완주 보상의 재미를 만든다는 지적이었다. 원천은 `city_types.json`의 `story`·`match`·`mismatch`이고, 화면에서는 별명과 한 줄 설명 아래에 **접힌 채로 두었다가 참여자가 펼칠 때만** 보인다 — 250자 남짓이라 항상 펼쳐 두면 결과 화면의 첫 인상이 글자 벽이 된다. **궁합 상대는 별명이 아니라 코드로 저장하고 화면에서 별명으로 되돌린다** — 별명으로 참조하면 별명을 다듬는 순간 짝이 조용히 끊긴다. 코드가 데이터에만 있고 화면에 나오지 않는 것은 4.1 그대로다. **이야기의 주어는 참여자가 아니라 참여자가 바라는 도시다** — 4.1이 이 진단을 성격 검사가 아니라고 못 박았으므로 "당신은 이런 사람입니다"로 쓸 수 없고, "당신이 바라는 2045년 인천은 이런 도시입니다"로 쓴다.

**축 강도 바는 축마다 한 칸으로 묶는다.** 팀 시험 사용에서 어느 제목이 어느 막대의 것인지 매칭되지 않는다는 지적이 나왔다. 원인은 제목과 자기 막대 사이의 간격이 축과 축 사이의 간격만큼 벌어져 있던 것이다. 내용과 순서는 그대로 두고 축 안의 간격을 좁히고 축 사이에 실선을 넣어, 제목이 자기 막대와 한 칸 안에 들어 있음이 선으로 읽히게 했다.

**일러스트 칸은 세로 규격으로 고정한다.** 최종 일러스트는 세로 방향 우표 시트 형태이며 기준 크기는 835×1024다. 이 수치의 원천은 `web/src/lib/city-types.ts`의 상수 한 쌍이고, 카드의 칸 비율과 `next/image` 치수가 모두 여기서 나온다. 칸을 비율로 고정하는 이유는 자산을 갈아끼울 때 카드 높이가 흔들리지 않게 하기 위해서다. **자산은 잘리지 않고 전체가 들어간다** — 우표 시트는 가장자리가 잘리는 순간 테두리를 잃기 때문이며, 비율이 어긋나 여백이 생기면 그 여백은 카드 배경색을 유지해 띠로 읽히지 않게 한다. 2026-08-30에 임시 일러스트 16종이 들어오면서 플레이스홀더 SVG는 삭제했고, 2026-09-02에 최종본 16종으로 교체했다.

일러스트 16종은 `web/public/city-types/<코드>.jpeg`에 있고 `city_types.json`의 `image`가 이를 가리킨다. 16종 모두 시트 상단 제목이 현재 별명과 일치한다. 자산을 갈아끼울 때는 같은 파일명으로 덮어쓰기만 하면 된다 — 경로도 JSON도 손댈 필요가 없다. 실제 픽셀 비율이 835×1024를 벗어나면 `city-types.ts`의 상수 두 줄만 고치고, `pnpm e2e`로 검증한다. 스모크가 **자산 자체의 자연 크기 비율**과 내보내기 카드의 높이를 상수와 대조하므로 규격을 벗어난 자산을 그 자리에서 잡아낸다. 화면 카드는 더 이상 일러스트를 직접 그리지 않으므로(6.4) 렌더된 칸은 카드 전체 비율로 검사한다.

**웹에 두는 자산은 일부러 작게 유지한다.** 835×1024는 원본을 절반 남짓으로 줄인 것이고, 이유는 결과 화면의 첫 인상이 이 그림 한 장을 받는 시간에 달려 있기 때문이다. 16종 합이 6MB 아래에 머문다. **대신 공유 카드는 이 크기를 따라가지 않는다** — 자산 폭을 카드 폭으로 쓰면 QR 모듈이 함께 줄어 스캐너가 가장 예민한 자리에서 무너진다. 카드는 `share-card.ts`의 `CARD_WIDTH`가 정하는 1697px으로 고정하고 우표만 그 폭에 맞춰 확대한다. 띠 안의 QR·막대·문구는 캔버스가 직접 그리므로 확대의 영향을 받지 않는다.

**이미지 로딩에 실패하면 결과 화면은 별명 타이포그래피 카드로 폴백하고 흐름을 끝내지 않는다.** 공유 카드 래스터화 시에는 서체에 적용 중인 검증 규칙(6.4)을 이미지에도 동일 적용한다.

담당자 산출물은 축별 인원 분포, 극별 평균 강도, 16유형 분포, 축별·극별 정성 요약, 비식별 대표 인용, 그리고 축과 유형의 의미 설명·축 계보·구 J/P 제외 사유다.

## 5. LLM 자산

### 5.1 모델

모델은 둘이다. **인터뷰어 응답 생성만 Claude(`claude-sonnet-5`)를 쓰고**, 나머지 모든 LLM 작업 — 증거 태깅, 개인 보고서 구조화, 비식별화, 축별 정성 요약과 대표 인용 선정, 대시보드 해석 문장, 개발용 묶음집의 가상 참여자 — 은 `gemini-3.7-flash`를 쓴다. 모델명은 코드에 하드코딩하지 않고 설정값(`claude_model`, `gemini_model`)으로 주입하며, 인터뷰어는 `interview_provider` 설정으로 Gemini로 되돌릴 수 있다. Claude 인터뷰어는 adaptive thinking을 켜되 **생각 깊이를 `output_config.effort`로 `medium`에 고정한다**(설정값 `interview_effort`). 기본값 `high`로 돌던 2026-09-03 시범운영에서 출력 토큰의 86%가 thinking이었고(20턴 실측 10,873토큰 중 9,336), 후반 턴이 2,000토큰을 넘기며 한 턴에 20~30초가 걸렸다. 그 대기 직후 참여자가 그만두는 것이 접속 기록에서 확인됐다. **끝까지 오지 못한 인터뷰는 자료가 0이므로 응답 품질보다 완주율을 우선한다.** `low`는 함께 시험해 기각했다 — 격리 점검에서 도입 고지와 2045년 전환을 통째로 빠뜨리고, 축 힌트를 받을 때마다 두 극을 나란히 놓은 이지선다 질문을 만들어(3회 중 3회) 루브릭이 금지한 검사형 문항이 나왔다. `medium`은 같은 점검에서 도입·전환·힌트 처리를 모두 규정대로 수행했으나 **마무리와 종료 토큰 구간은 아직 실측되지 않았다.**

인터뷰어만 상위 모델을 쓰는 근거는 실측이다. 같은 프롬프트에서 저가 모델(Gemini Flash, Claude Haiku)은 받아주기 생략, 중복 질문, 페이싱 붕괴, 지역 사실 오답(동네를 엉뚱한 군·구로 확언)을 반복했고, thinking을 켜도 사라지지 않았다. Sonnet에서 이 문제들이 사라졌다. 격리 점검(모델당 두 판)에서도 Haiku 4.5는 판마다 채점에 걸릴 유도 형태의 질문 1건과 참여자가 밝힌 사실과 어긋난 전제 1건이 재발했고, 그 규칙이 파일에 있어도 회상되지 않았다. 절감은 인터뷰어 몫의 절반이라 Sonnet을 유지한다. 채점과 후처리는 입출력 형식이 강하게 규정된 작업이라 Flash로 충분하다. **유형 합산과 축별 통계 집계는 LLM이 아니라 코드가 한다.** 임베딩 모델은 쓰지 않는다.

**모델을 바꾸면 지금까지의 AI 품질 실측은 기준선을 잃는다.** 프롬프트 문장과 채점 앵커, 특히 AC 판별 차원 교체는 특정 모델이 어떻게 오태깅했는지를 보고 만든 처방이다. 모델이 바뀌면 그 처방이 여전히 필요한지도 과교정인지도 알 수 없다. 교체할 때는 고정 페르소나 1회를 새 모델로 돌려 판정이 유지되는지 확인한 뒤 진행한다. 채점은 Flash에 남았으므로 앵커의 기준선은 유지되지만, 인터뷰어 프롬프트의 처방 다수는 Gemini 실측에서 나온 것이라 Sonnet에서 과교정이 된 규칙을 걷어내는 것이 다음 튜닝 라운드의 후보다.

### 5.2 프롬프트 파일 아홉 개

모든 LLM 자산은 `api/prompts/` 아래 마크다운으로 두고 런타임에 로드한다. 파일은 정확히 아홉 개다.

- **`system.md`** — 인터뷰어 페르소나(이름 "바다"), 존댓말, 상상 보조, 금지사항과 가드레일, 악성 응답 처리, 인천과 무관 판정(사는 곳이 인천 밖이면 인천에 오는 곳이 있는지 한 번 확인하고 없다고 답할 때만 토큰), 종료 제어 토큰 규칙. `[운영 지시: ...]` 블록이 참여자 발화가 아니라 백엔드 운영 지시이며 참여자 의견이나 증거로 해석하면 안 된다는 규칙. **2045년의 장면은 전환 뒤에 세운 2045년의 무대 위에 세운다는 규칙** — 도입에서 들은 지금의 신분·소속·일정은 무대를 만드는 재료이지 그대로 2045년으로 옮겨지지 않으며, 무엇이 새로 생겨 있으면 좋을지도 참여자가 답할 몫이다. 없는 시설을 전제로 까는 것과 현재 신분을 이월하는 것을 한 규칙으로 묶은 것은 둘이 같은 위반이기 때문이고, 인천 밖 참여자가 학생·통근자처럼 신분에 매인 접점으로 들어오므로 후자가 더 자주 일어난다. **증거 형식 규정은 두지 않는다** — 인터뷰어는 증거를 만들지 않는다.
- **`rubric.md`** — 도입(첫 인사에 대화 소개·정답 없음·데이터 사용 고지·호칭, 사는 곳(인천 밖이면 인천에 오는 곳 확인), 인천에서 자주 가는 곳. **성별과 요즘 하는 일은 묻지 않는다**), 2045년 진입(타임워프·나이 고지·인천과 어떻게 지내고 있으면 좋겠는지 → 하는 일 → 사는 곳, 인천에 살지 않겠다는 답의 두 길), 2045년의 하루(바람 꼴 질문, 없었으면 하는 일과 생겨 있으면 하는 것을 서로 다른 턴에, "지금 거기 있는 것" 질문 한 번, 예측형 답 되묻기 한 번, 참여자가 밝힌 사실과 어긋난 전제 금지), 정리(딱 하나만 → 이유 → 인천에서 지내는 마음 → 요약과 검증형 덧붙임 → 마침). **2부 질문은 전망이 아니라 바람을 묻는 꼴로 낸다** — 어떻게 될 것 같냐고 물으면 참여자가 실현 가능성을 따져 "현실적인 인천"을 답하는 것이 실사용에서 확인됐다. 루브릭은 이를 금지 조항이 아니라 바람 꼴의 예시 문장들과 응대 예시로 싣는다. 추상 바람이 나오면 지금의 불편으로 한 번 내려갔다 곧 2045년으로 돌아오는 구체화 사다리가 예시로 실린다. **"지금 거기 있는 것" 질문은 변화의 방식을 여는 유일한 장치라 대상 순서와 문구를 고정한다.** 대상은 도입의 자주 가는 곳이 기본이고, 학생의 학교처럼 2045년의 삶과 끊긴 것이 분명할 때만 2045년의 하루에서 꺼낸 장소로 바꾸며, 둘 다 없으면 묻지 않는다. 참여자가 지금은 없는 물리적 시설이 생기길 바란다고 말했고 어디인지 알 수 있으면 다음 턴에 그 시설의 자리를 대상으로 삼는다(이유 질문은 그 턴에 생략). 2026-09-03 실사용에서 자주 가는 곳이 "학교"인 참여자가 2045년엔 공무원이라 이 질문이 무너져 축이 비었고, 대체 대상이 없어 인터뷰어가 추상 질문을 지어낸 것이 원인이었다. 문구는 "그곳이 어떻게 되어 있으면"이 아니라 "지금 거기 있는 모습은 어떻게 되어 있으면"으로 묻는다 — 앞 문형은 시설·자연·활기(다른 세 축) 답으로 흘러 변화의 방식이 나오지 않는다. "어우러지다"·"조화"·"자리 잡다"는 지키는 쪽을 미리 놓으므로 금지다. 이동·민원 같은 일상 방식을 대상으로 삼는 갈래는 검토 끝에 뺐다 — 그 답은 채점 기준상 부담 완화(우선 가치의 기회 극)로 잡히지 변화의 방식이 아니다. **예측형 답("어우러지기 힘들 것 같은데")은 한 번만 바람으로 되묻는다.** 예측은 바람이 아니라 태깅되지 않으므로, 실현 가능성 답 처리와 같은 규칙으로 묶고 되물은 소재의 이유 질문을 생략해 턴을 보전한다. 되묻기는 운영 지시의 장면보다 먼저지만 정리 지시가 왔으면 하지 않는다. **막힘 처방과 힌트 붙이는 법은 조작 단위의 긍정문으로 쓴다.** 막히면 앞서 참여자가 이름을 댄 것 하나를 골라 2045년에 그것을 하고 있는 순간의 행동 하나를 묻는다 — 연도를 앞당기지 말라는 금지 대신 연도가 박힌 조작을 주는 방식이며, 격리 테스트에서 첫 막힘에 처방이 없어 인터뷰어가 "졸업하고 나면"이라는 중간 미래를 만들어 전망을 물은 것이 근거다. 힌트도 마찬가지로 붙이는 방법 둘을 먼저 쓴다 — 참여자가 방금 말한 장소나 시간대를 그대로 무대로 두거나(불편·불만도 무대가 된다), 붙일 데가 없으면 장면 목록에서 아직 열지 않은 장면 하나를 연다. 최신 모델에는 금지 규칙보다 긍정형 지시와 퓨샷이 효과적이라는 판단이며, 대조용 avoid는 실측된 문장 하나씩만 남긴다.

극 이름·앵커·성질 문구는 등장하지 않는다 — 성질은 운영 지시가 실어 주고, 루브릭은 그것을 장면으로 바꾸는 법과 붙일 소재가 없을 때 장면 목록(사는 집, 집 주변과 동네의 풍경, 저녁이나 쉬는 날 동네 거리, 오가는 길과 일하는 시간)에서 고르는 우회로만 안다. 참고 문항 목록은 두지 않는다 — 질문 문장 목록은 인터뷰어가 그대로 옮기는 재료가 되고, 그 내용은 장면 목록에 흡수됐다.
- **`axes.md`** — **채점 호출 전용이며 인터뷰어는 받지 않는다.** 4축과 극의 정의, 축마다 다루지 않는 것과 그것이 어느 축으로 가는지, 축·극·강도별 채점 앵커, 소재가 겹칠 때의 판별 규칙, 증거가 아닌 발화, 채점 호출의 입출력 형식, 누적 증거의 용도 한정. 인터뷰어의 질문 형태를 판단하는 규칙은 두지 않는다(4.5). **기계 판독 JSON 블록**이 백엔드 축 상수의 단일 원천이며 `api/app/axes.py`가 파싱한다. 파서가 문서의 **첫 번째** JSON 코드펜스를 읽으므로 이 블록보다 앞에 다른 JSON 펜스를 만들면 조용히 깨진다.
- **`structuring.md`** — 원본 대화록을 개인 보고서로 구조화. 인터뷰어 발화는 맥락으로만 쓰고 요구·인용은 참여자 발화에서만 추출. 자기보고 정보 추출(**군·구는 입력으로 실린 동·읍·면 표에서 읽고 성별은 추출하지 않는다**. 표에 없으면 빈 값이다). 축별 요구 생성, 요구 제목의 형태 규칙, 행정동·정착 의향·최우선 요구·언급 장소 추출. **기타·미분류라는 개념을 언급하지 않는다** — 언급하면 모델이 고민 없이 그리로 던진다. 수정 시 전체 요약과 `axis_demands` 4축 전체를 재생성.
- **`extra.md`** — 대화록과 최종 요구 목록을 받아 **축에 담기지 못한 요구**를 찾는다. 출력은 축 요구와 같은 모양(제목·설명·인용·장소)이며, 이미 잡힌 요구와 겹치는 것은 내지 않는다. 담당자 전용이라 참여자 화면과 수정 기능에는 연결하지 않는다.
- **`sectors.md`** — 요구의 제목과 설명만 보고 **도시기본계획 부문**을 하나 붙이고 **키워드**를 최대 세 개 뽑는다. 대화록을 받지 않는다. 부문 고정 어휘는 `api/app/sectors.py`가 원천이며 이 파일과 글자 그대로 같아야 한다. 키워드는 어휘를 정해 두지 않고 형태 규칙만 준다(3.4).
- **`deidentify.md`** — 요구에서 이름과 연락처를 블라인드. 텍스트만 바꾸고 구조와 개수는 유지. 블라인드 이외의 표현 수정·요약·보완 금지.
- **`aggregate.md`** — 네 축의 극별 요구 경향 요약(참여자가 없는 극은 빈 배열)과 주어진 후보에서 대표 인용 선정. **ID를 새로 만들지 않는다.**
- **`briefing.md`** — 대시보드 카드별 해석 문장과 종합 브리핑(3.7)의 모든 서술을 **한 호출로** 만든다. 핵심 발견, 섹션별 리드문과 해석, 쟁점과 그 근거 인용, 후속 확인 질문, 이번 표본의 성격 서술이 한 응답에 들어간다. 입력에 있는 집계 수치만 근거로 삼고, **백분율로 쓰지 않으며**(모수와 인원으로 쓴다), 군·구는 참여 편중으로만 해석하고, 연령대는 절대 건수로만 비교하며, 축·극 이름을 새로 짓지 않고, 표본이 대표성을 갖지 않는다는 한계를 반영한다. 쟁점의 인용은 주어진 후보에서만 고르고 **ID를 새로 만들지 않는다.** 카드 해석과 브리핑을 한 호출에 둔 이유는 3.7에 있다.

**극 이름은 프롬프트가 아니라 입력이 나른다.** 브리핑 payload의 각 극과 각 인용 후보에 `name`이 실리고, 모델은 그 이름을 글자 그대로 쓴다. 그 이름의 원천은 `axes.md`의 기계 판독 블록이며 화면 배지(`city-axes.ts`)와 글자 그대로 같아야 한다 — 브리핑은 배지 바로 옆에서 극을 길게 서술하므로 어긋나면 담당자가 같은 극을 두 이름으로 보게 된다. **이 방식은 프롬프트로 금지해 보고 실패한 뒤에 택한 것이다.** 산문으로 "다른 낱말로 부르지 말라"고 적었을 때 모델은 `포용`을 `돌봄`, `자연`을 `자연친화`, `새로움`을 `신규 개발`로 계속 바꿔 썼다. 일반화하면 **모델에게 대응표를 외우게 하지 말고 코드가 답을 입력에 실어 준다.** 인용 ID를 코드가 부여하는 것(3.4)과 같은 판단이다.

별도 `typing.md`는 만들지 않는다. 유형 채점 기준은 `axes.md`에 통합한다.

### 5.3 프리픽스 구성과 캐싱

인터뷰 도중과 종료 후 모두 대화록을 압축하지 않는다. 인터뷰어 호출의 프리픽스는 **`rubric.md` → `system.md` → 참여자 나이** 순이고 그 뒤에 누적 대화 기록과 현재 발화·운영 지시가 붙는다. 세 자산은 각각 `<rubric>`, `<instructions>`, `<participant_info>` XML 태그로 감싼다 — 여러 종류의 내용이 섞인 프롬프트에서 경계를 명확히 하라는 Anthropic 권장 구조이며, 감싸는 것은 조립 코드가 하므로 마크다운 원문은 태그를 모른다.

이 순서에는 이유가 있다. 매 턴 반드시 지켜야 하는 행동 규범과 응답 형식이 담긴 `system.md`는 생성 직전에 오도록 자산 중 마지막에 둔다. 참여자 나이는 참여자마다 다른 유일한 값이므로 반드시 맨 뒤를 유지한다. 그래야 앞선 고정 자산 전체가 참여자 간 캐시를 공유한다. **이 배치가 의도대로 작동한다는 직접 증거가 있다** — 격리 점검에서 모델이 응답 형식을 지킨 이유로 "이 절이 문서 맨 끝이라 가장 최근에 읽었기 때문"을 들었고 "문서 중간에 있었다면 틀렸을 것"이라고 답했다.

채점 호출의 시스템 지시는 `axes.md` 단독이고, 이 호출도 시스템 지시가 참여자 간 완전히 동일하므로 캐시를 공유한다. 캐싱 방식은 프로바이더를 따른다. 인터뷰어(Claude)는 시스템 지시에 명시적 `cache_control` 표시를 붙인다 — Anthropic은 표시가 있어야 캐시가 적용되고, 매 턴 프리픽스 전문이 반복 전송되므로 실익이 크다. 채점(Gemini)은 implicit caching을 쓰고 explicit 캐시 API는 쓰지 않는다 — Flash 요율에서 시간당 저장비가 절감분을 상쇄한다.

**프리픽스 분량의 방향에 주의한다.** 호출 분리와 요약본 제거로 한때 원본 대비 79.5%까지 줄었으나, 이후 튜닝 라운드가 규칙을 얹으면서 다시 늘었다. 얹힌 규칙은 하나하나 실측 근거가 있지만 **5.4가 제로섬으로 관찰한 방향과는 반대로 움직였다.** 다음 라운드의 첫 후보는 새 규칙이 아니라 이미 닫힌 항목의 규칙을 걷어내는 쪽이다.

### 5.4 프롬프트 변경의 판단 기준

프롬프트 자산을 고칠 때 상시 적용한다. 앞선 라운드들이 실측으로 세웠다.

**기준 1. 코드가 결정할 수 있는 것은 프롬프트에 두지 않는다.** 프롬프트를 줄이는 방향이 항상 우선한다.

**기준 2. 정보 결손과 행동 튜닝을 가른다.** 모델이 알 수 없는 정보가 없어서 지어내는 것은 결손이며 반드시 채운다. 정보는 있는데 다르게 행동하는 것은 튜닝이며 얹을수록 희석된다. 결손은 즉시 채우고, 튜닝은 실측 재발이 두 번 이상일 때만 손댄다.

**기준 3. 예방적 패치를 넣지 않는다.** 실측 근거 없이 "이럴 수도 있으니" 넣지 않는다.

**기준 4. 효과를 재는 라운드에서는 다른 프롬프트 내용을 함께 바꾸지 않는다.** 다음 재생성에서 무엇이 무엇을 고쳤는지 가릴 수 없게 된다.

**프롬프트는 제로섬에 가깝다.** 한 줄을 넣으면 그 규칙은 정확해지지만 나머지 전부가 조금씩 흐려진다는 것이 실측으로 확인됐다. 기준 2와 3이 여기서 나온다. 반대로 분량을 줄이면 남은 규칙 전부의 준수율이 오른다.

**다만 금지 규칙의 중복 배치는 줄이지 않는다.** 반복의 효과가 실측으로 확인됐다. 격리 점검에서 모델이 운영 지시 블록 규칙이 또렷하게 남은 이유로 그것이 두 곳에 중복돼 있다는 점을 꼽았고, 반대로 한 번만 적힌 선언은 응답 열 개 중 두 번만 회상됐다. 기준 1의 "줄이는 방향 우선"은 **불필요해진 규칙**을 걷어내라는 뜻이지 살아 있는 규칙의 반복을 지우라는 뜻이 아니다.

**모델의 자기점검은 신뢰하지 않는다.** 시뮬레이션들이 자기 산출물을 지시와 대조하는 단계에서 위반을 거의 잡지 못했다. 축 세 개가 뒤바뀐 결과물을 "지어낸 내용 없음, 축 순서 준수"로 통과시킨 사례가 있다. 다만 "무엇이 흐려졌는가", "규칙이 없었다면 무엇을 했겠는가" 같은 **내성 질문에 대한 답은 유용했다.** 자기 산출물의 옳고 그름을 판정하게 하지 말고 자기 상태를 보고하게 한다.

**작은 모델은 지적을 받으면 심각도를 부풀린다.** 한 검토가 사실관계 정정을 받고 같은 항목을 문제없음에서 가장 심각한 설계 오류로 뒤집었는데 새 근거는 없었다. 튜닝 판단을 모델 의견이 아니라 실측에 두어야 하는 이유다.

### 5.5 프롬프트 격리 점검 절차

프롬프트를 고칠 때마다 함께 돌린다. **저장소 전체를 읽은 검토는 프롬프트의 결손을 놓친다.** 다른 파일에서 얻은 정의를 무의식적으로 채워 넣기 때문이다. 실제로 보고서 호출의 축 정의 결손은 저장소를 읽은 검토 세 건이 모두 놓쳤고 프롬프트만 준 점검에서 즉시 드러났다.

1. 실제 호출에 들어가는 시스템 지시를 그대로 조립해 임시 파일로 쓴다. 사용자 턴도 코드가 실제로 만드는 문자열 그대로 넣는다.
2. 에이전트에 그 파일만 읽게 하고 저장소 접근을 금지한다. **코드가 이 텍스트를 어떻게 처리하는지 추측하는 것도 금지한다.** 이 금지가 없으면 코드 흐름을 낙관적으로 추정해 엉뚱한 결론을 낸다.
3. **비평이 아니라 실행을 시킨다.** 인터뷰라면 첫 인사와 참여자 발화 몇 개에 대한 응답을 형식 그대로 실제로 쓰게 하고, 채점이라면 실제 증거 배열을 만들게 한다.
4. 만든 결과를 지시와 대조해 스스로 위반을 짚게 하되, 이 자기점검은 신뢰하지 않고 사람이 직접 대조한다.
5. **"규칙이 없었다면 하고 싶었을 행동"을 적게 한다.** 금지 규칙이 실제로 작동하는지 확인하는 가장 직접적인 방법이다.
6. **분량 소감을 묻는다.** 어느 규칙이 또렷하게 남고 어느 규칙이 흐려졌는지가 배치 판단의 근거가 된다.

입력에는 함정을 심는다. 근거가 얇은 축을 하나 두어 네 축을 모두 채우라는 지시와 지어내지 말라는 지시가 부딪치게 만들고, 지역명을 동네 이름으로 주어 정규화를 시험하고, 운영 지시의 특정 분기를 일부러 도착시켜 그 분기를 검사한다. 채점 쪽에는 답을 보기로 좁힌 질문과 그에 대한 답을 심어 배제 규칙을 시험하고, 참여자의 앞선 소재를 예시로 되받은 질문의 답을 심어 가중치 1 상한을 시험한다.

### 5.6 도시기본계획 지식

2040 인천 도시기본계획 12개 보고서 687쪽을 전사·QA해 `data/plan2040/`에 정본으로 두었고, 출처 포인터가 붙은 파생 요약본도 만들었다. 해당 계획이 채택된 역사적 배경은 구현에 반영하지 않는다. 인터뷰의 기준 연도는 2045년이다 — 시의 현행 시민 조사가 2045년 도시기본계획용이고, 질문이 전망이 아니라 바람을 묻는 꼴이라 수립 중인 계획을 전제로 하지 않아도 성립한다. 2040 계획 정본과 부문 10종은 인천 맥락 자료이자 담당자 라벨 어휘의 원천으로 유지한다.

**요약본을 인터뷰어 프리픽스에 항상 넣던 방식은 폐지했다.** 관측 14회에서 인용이 한 번도 없었고, 그 정책 어휘가 시민에게 행정의 답을 요구하는 질문을 키웠다. 프리픽스의 40.2%를 차지하던 자료였다.

전체 마크다운을 Gemini File Search Tool에 올려 매 턴 검색하게 하는 경로는 **아직 활성화하지 않았다**(9.4). File Search는 Google이 청킹·임베딩·저장·검색을 관리하는 RAG 도구이며 별도 벡터 저장소를 만들지 않는다. 스토어는 GCP 프로젝트당 하나만 쓰고 이름은 환경변수로 주입한다.

## 6. 개발·배포·운영

### 6.1 개발자 모드

결과 화면·보고서·유형 카드·담당자 리포트를 확인하려고 그때마다 인터뷰를 완주하면 한 번에 십수 턴이 든다. UI를 반복해 다듬는 동안 이 비용을 감당할 수 없으므로 인터뷰 중간을 건너뛰는 개발자 모드를 둔다. 이 기능의 이름은 어디서나 "개발자 모드"로 통일한다.

개발자 모드는 **세션을 새로 만들지 않고 이미 만들어진 세션에 대화를 주입한다.** 참여자와 똑같이 시작 화면을 거쳐 인터뷰에 들어온 뒤 원하는 시점에 완성된 대화를 불러온다. 그래야 시작 화면과 세션 생성 경로가 개발자 모드 때문에 갈라지지 않는다. 불러온 묶음집은 이미 완주된 인터뷰이므로 **세션을 곧바로 종료 상태로 두어** "대화 종료하기" 버튼이 바로 나타나게 한다. 주입된 세션은 실제로 진행한 세션과 구별되지 않으므로 이후 경로를 특별 취급 없이 그대로 탄다.

진입점은 인터뷰 화면 안의 눈에 띄지 않는 작은 버튼이다. 시작 화면에는 두지 않는다 — 참여자 흐름의 첫 화면이 개발용 요소로 오염되지 않게 하기 위해서다.

**대화 묶음집은 손으로 지어내지 않는다.** 실제 인터뷰를 완주시킨 결과를 그대로 저장한다. 참여자 역할은 재생성 스크립트의 가상 참여자(같은 Gemini 호출)이거나 프론티어 모델이 배정된 인물로 직접 대화하는 것 둘 중 하나다. 재현성은 전자가 낫고 발화의 자연스러움과 결함 발견력은 후자가 낫다. 어느 쪽이든 **지어낸 대화로 만든 결과는 실제 결과와 다르므로 검증에 쓸 수 없다.** 성향이 대비되는 묶음집을 여러 개 두고 **축이 함께 묶여 보이는 착시를 검사할 교차형 인물을 최소 하나 포함한다.**

**묶음집에서 온 세션은 결과와 제출까지 그 묶음집으로 처리한다.** 결과 생성이 구조화 호출을 돌지 않고 저장된 예시 제출본의 유형·보고서를 그대로 쓰며, 제출도 같은 파일의 부문 라벨·남은 요구·비식별 사본을 넣고 후처리를 걸지 않는다. 화면 배선을 확인하는 것이 목적인 자동 검사가 매번 모델 값을 치르지 않게 하기 위해서다. 개발자 라우터가 개발자 모드에서만 붙으므로 이 경로는 운영에서 도달할 수 없다. **대신 묶음집을 불러와 결과를 눌러 보는 것으로는 구조화 프롬프트를 시험할 수 없다** — 그 시험은 아래 재실행 스크립트로 한다.

**구조화 프롬프트만 바뀌었을 때도 대화를 다시 하지 않는다.** `api/scripts/restructure_fixtures.py`가 저장된 대화록을 현재 구조화 호출에 다시 통과시켜 묶음집 파일을 갱신하고, `api/scripts/restructure_submissions.py`가 같은 일을 저장소 문서에 한다. 둘 다 저장된 유형 결과는 다시 계산하지 않고(참여자가 이미 본 값이다) 파생 값 셋을 비워 다음 분석 실행이 채우게 하며, 제목을 전후로 찍어 사람이 문구 변화를 바로 읽게 한다. 제출본당 구조화 호출 한 번을 쓴다.

**축 정의만 바뀌었을 때는 대화를 다시 하지 않고 증거만 다시 붙인다.** `api/scripts/retag_fixtures.py`가 저장된 대화록을 그대로 두고 참여자 발화를 현재 채점 호출에 다시 통과시켜 증거를 새로 만들고 예시 제출본까지 갱신한다. 실제로 완주된 인터뷰는 다시 만들 수 없는 자산이므로 축이 바뀔 때마다 버리지 않는다. 인터뷰 호출은 한 번도 쓰지 않고 채점 호출만 턴 수만큼 쓴다.

```
cd api && PYTHONPATH=. uv run python scripts/retag_fixtures.py
```

인물 정의나 대화 자체를 새로 만들어야 할 때만 전체 재생성(`api/scripts/regen_fixtures.py`)을 쓴다. **실제 Gemini 크레딧을 소모한다.** 개발 중에는 이름을 인자로 주어 한 명만 돌린다 — 프롬프트 변화가 인터뷰어 행동에 미치는 영향은 한 명으로도 관찰되고, 여러 명을 필요로 하는 것은 담당자 화면의 집계뿐이다.

**축 정의나 채점 앵커를 바꾸면 기존 묶음집의 증거는 무효가 된다.** 합산 함수가 알 수 없는 축의 증거를 조용히 걸러내므로 에러 없이 모든 축이 기본 극·강도 51로 나온다. 축을 바꾼 뒤에는 반드시 재태깅을 돌린다.

묶음집 파일은 출생연도 옆에 **성별을 선언한다.** 실제 참여자의 성별은 시작 화면에서 세션에 실리는데 묶음집은 세션 없이 예시 제출본을 만들므로 그 자리를 파일이 채운다. 군·구는 대화에서 나오므로 선언하지 않는다.

담당자 웹에도 같은 성격의 지름길이 있다. 예시 제출본 문서를 store에 적재하는 경로이며, 고정 ID를 쓰므로 반복 호출해도 중복이 쌓이지 않는다. 예시 제출본 역시 손으로 지어내지 않고 **묶음집을 실제 파이프라인에 한 번 통과시켜 저장된 문서를 그대로 파일로 떠 둔 것이다.**

개발자 모드는 `dev_mode` 환경변수가 켜져 있을 때만 경로를 등록한다. **배포에서도 켠다** — 팀이 배포된 서비스로 계속 시험하기 때문이다. 아무나 쓰지 못하게 막는 것은 이 스위치가 아니라 접근 코드다.

### 6.2 접근 코드

개발자 모드와 담당자 웹은 **하나의 접근 코드**로 막는다. 계정도 세션도 만들지 않는다.

코드는 `access_code` 환경변수 하나다. 자릿수와 문자 종류를 코드가 강제하지 않으므로 늘리고 싶으면 값만 바꾸면 된다.

**화면의 입력칸도 형식을 강제하지 않는다.** 2026-09-02까지 이 칸이 숫자 네 자리로 고정돼 있었고, 입력 필터가 그 형식에 맞지 않는 글자를 버렸다. 실제 값은 긴 임의 문자열이므로 붙여넣어도 칸에 아무것도 들어가지 않았다 — 화면은 붙여넣기가 막힌 것처럼 보였지만 실은 형식 검사가 값을 통째로 버리고 있었다. 지금은 앞뒤 공백만 떼고 나머지를 그대로 서버로 보낸다. 모바일에서 숫자 키패드·자동 대문자·자동 수정을 끄는 것도 같은 이유다 — 대소문자를 구분하는 값을 브라우저가 말없이 바꾼다. 스모크가 이 칸에 숫자가 아닌 값을 넣어 그대로 남는지 검사한다. **값이 비어 있으면 게이트가 없다** — 자동화 테스트가 코드를 몰라도 돌게 하기 위해서다. 값이 있으면 `/api/dev/*`와 `/api/admin/*`의 모든 요청에 헤더가 있어야 하고, 없거나 다르면 403이다. **참여자 API는 막지 않는다** — 참여자는 코드를 모르며 알아서도 안 된다.

**검사는 반드시 백엔드가 한다.** 프론트에서만 확인하면 아무 보호가 되지 않는다. 담당자 API를 직접 호출하면 대화록 원문이 그대로 나오기 때문이다. 화면의 코드 입력은 편의일 뿐이고 실제 방어선은 서버다. 검사는 HTTP 미들웨어가 하므로 나중에 두 접두사 아래에 라우트를 더 붙여도 자동으로 보호된다. 프론트도 호출을 한 곳으로 모아 경로를 보고 헤더를 붙이므로 어느 화면도 붙이는 것을 잊을 수 없다.

통과한 코드는 `sessionStorage`에만 두고 403이 오면 지운다. **코드 확인 전용 엔드포인트를 만들지 않는다** — 각 화면이 원래 하던 첫 호출이 403이면 코드가 틀린 것이다.

**이 코드가 막는 것과 막지 못하는 것을 분명히 해 둔다.** 값은 충분히 긴 임의 문자열이므로 자동화된 시도는 막는다. **주소를 감추는 것은 방어가 아니다** — `/admin`은 참여자가 QR로 여는 화면과 같은 출처에 있어서, 접속 주소를 아는 사람은 누구나 이 화면에 닿는다. 실제 방어선은 값의 길이 하나뿐이다.

막지 못하는 것은 코드를 아는 사람이다. **코드 하나를 모두가 나눠 쓰므로 누가 열람했는지 남지 않고 한 사람만 회수할 수도 없다.** 값이 샜다고 판단되면 값을 바꾸는 것이 유일한 대응이며, 그러면 코드를 아는 모두가 새 값을 다시 받아야 한다. 계정과 권한을 만들지 않기로 한 대가가 이것이다(2.3).

### 6.3 배포와 환경변수

리전은 서울 `asia-northeast3`이고 `web`·`api` 두 서비스를 배포한다. 각 서비스가 Dockerfile을 갖고 이미지는 Artifact Registry에 둔다. **`api`는 프로세스 메모리 세션 때문에 `min=1`, `max=1`로 고정한다.** 수평 확장은 하지 않으며, `min=1`로 콜드스타트를 줄여 시연 안정성을 확보한다.

**빌드는 `cloudbuild.api.yaml` / `cloudbuild.web.yaml`을 거친다.** `gcloud run deploy --source`가 아니다. `web`은 API 주소를 빌드 타임에 주입한다. 도메인은 사지 않기로 확정했으므로 Cloud Run 기본 주소를 그대로 쓰고, 접속용 QR은 `docs/qr/`에 있다. **배포 주소가 바뀌면 QR을 다시 만들어야 한다.**

`api`가 읽는 환경변수는 Gemini 키(필수), Claude 키, 인터뷰어 프로바이더와 두 모델명, File Search 스토어 이름, GCP 프로젝트 ID, 목표 턴·마무리 턴·소재 힌트 턴·마무리 연장 상한, 인터뷰어 생각 깊이(`interview_effort`), `dev_mode`, `access_code`다. 루트 `.env`에서 로드하고 Cloud Run에서는 서비스 환경변수로 주입한다. 값이 비어 있어도 기동은 되며 해당 기능을 쓰는 시점에 필요하다. **턴 상수와 생각 깊이를 환경변수로 두는 이유는 시범운영에서 인터뷰 길이와 응답 속도를 재배포 없이 조정하기 위해서다.** 생각 깊이의 이름에 `claude_`를 쓰지 않는다 — 개발 기기에 이미 있는 `CLAUDE_EFFORT`가 대소문자 구분 없이 설정을 덮는 것이 확인됐다.

`web`은 API 주소 하나만 읽는다. **LLM 키, 프롬프트, 접근 코드는 프론트에 두지 않는다.**

Firestore는 Native 모드를 쓴다. 대화록과 보고서가 JSON 문서 구조에 적합하고 시범 규모는 무료 티어로 충분하다. 로컬 개발도 동일 GCP 프로젝트의 Firestore를 쓰고 **에뮬레이터는 쓰지 않는다.** 저장소 구현은 GCP 프로젝트 ID가 있으면 Firestore, 없으면 인메모리로 갈라지므로 자격증명이 없는 환경에서도 개발과 테스트가 돈다. 별도 스위치를 만들지 않았다. **개발 서버를 띄우면 실제 Firestore에 쓴다** — 지저분해지면 사람이 콘솔이나 담당자 화면에서 지운다. 자동 정리는 없다.

로컬 개발은 루트의 `./dev.sh` 한 줄로 api(8000)와 web(3000)을 함께 띄운다. UI를 확인할 때는 인터뷰를 직접 타이핑하지 말고 개발자 모드를 쓴다.

### 6.4 공유 카드

**화면에 보이는 카드가 곧 이 이미지다.** 2026-09-02에 결과 화면의 카드를 DOM으로 다시 그리는 대신 캔버스가 만든 PNG를 그대로 얹도록 바꿨다. 저장 버튼을 누르지 않고 결과 화면을 스크린샷으로 찍어 공유하는 사례가 흔하다는 것이 팀 시험 사용에서 확인됐는데, 그때 화면에는 QR도 사업명도 없어 스크린샷을 본 사람이 들어올 길이 없었다. 화면과 저장본을 같은 그림으로 만드는 길은 한쪽을 다른 쪽에 맞추는 것뿐이고, 두 벌을 유지하면 언젠가 어긋난다. **카드는 결과가 도착한 뒤 한 번만 만들고 화면 표시·공유·저장이 그 파일을 함께 쓴다** — 공유 버튼을 누른 뒤의 대기가 사라지고 화면과 저장본이 같은 그림임이 구조적으로 보장된다. 결과 연출은 카드 생성이 끝난 뒤에 걷히므로 자리를 비워 둘 필요가 없다. **생성이 실패하면 별명 타이포그래피 카드로 떨어지고 흐름은 계속된다**(4.7). 대가로 카드 안의 글자는 이미지가 되어 확대나 시스템 글자 크기 설정에 반응하지 않는다 — 읽어야 할 수치는 카드 밖의 축 강도 바가 맡으므로 감수한다.

**띠의 조판은 이 변경에서 건드리지 않았다.** 1697px 기준 조판을 350px 폭 화면에 얹으면 띠 글자가 7px이 된다는 계산으로 확대를 검토했으나, 실제 기기에서 저장본이 읽힌다는 확인이 먼저 있었으므로 계산을 근거로 되돌리지 않았다.

프론트에서 **캔버스에 공유 카드를 직접 조립한다.** 일러스트 원본을 그 픽셀 그대로 얹고 그 아래 가로폭의 19%만큼 인천시 파랑 띠를 그린 뒤, 띠 왼쪽에 서비스명과 사업명을, 오른쪽에 **참여 링크 QR과 그것이 무엇인지 알리는 한 줄**을 앉힌다. 화면 카드(강도 바·배지·설명)는 내보내지 않는다 — 세로로 긴 이미지는 SNS에서 잘려 공유 소재로 쓰기 어렵기 때문이다.

**DOM을 그대로 이미지화하지 않는 이유가 있다.** `html-to-image` 계열은 카드 DOM을 SVG `foreignObject`에 담아 캔버스에 그리는데, **WebKit은 `foreignObject` 안에 중첩된 래스터 이미지를 오류 없이 버린다.** 실측하면 같은 카드가 Chromium에서는 일러스트가 그려지고 WebKit에서는 그 자리가 백지로 나오며, 자산을 가로 200px까지 줄여야 겨우 그려진다. 플레이스홀더가 2KB SVG였을 때는 드러나지 않다가 실제 JPEG를 넣는 순간 Safari 사용자에게만 일러스트 없는 카드가 저장됐다. 캔버스 직접 조립에는 이 제약이 없다.

한글 서체를 번들하므로 **띠의 글자를 그리기 전에 그 서체가 실제로 로드됐는지 확인한다.** 서체가 준비되지 않으면 시스템 서체로 조용히 대체되어 카드가 망가지므로, 그 경우에는 조용히 넘어가지 않고 그 공유·저장 시도를 실패로 끝낸다. 다만 **공유·저장 실패는 결과 흐름 전체를 종료하지 않는다.** 참여자는 결과 화면에 그대로 남아 다시 시도하거나 공유 없이 진행할 수 있다. 부수 기능의 실패로 제출 전 인터뷰 결과 전체가 사라지지 않게 하기 위함이다.

모바일에서는 Web Share API로 공유 시트를 열고, 파일 공유를 지원하지 않는 환경에서는 이미지를 내려받는다. **공유 시트에는 이미지와 함께 참여 링크를 싣는다.**

**QR은 정적 파일을 얹지 않고 `window.location.origin`에서 그때그때 생성해 캔버스에 직접 그린다.** 이유가 둘이다. 페이지가 서비스되는 주소에서 만들므로 배포 주소가 바뀌어도 따로 다시 만들 일이 없고 — `docs/qr/`의 포스터용 QR이 지고 있는 부담을 카드는 지지 않는다 — 1697px짜리 카드에 45mm PNG를 확대해 얹으면 스캐너가 가장 예민한 자리에서 흐려지기 때문이다. **모듈 크기를 정수 픽셀로 내림한 뒤 그 값에서 판 크기를 거꾸로 구한다.** 반대로 하면 모듈이 소수 경계에 걸린다. 오류정정은 화면을 사진으로 찍는 용도라 `M`을 쓰고, 어두운 모듈은 파랑 위에 바로 그리지 않고 흰 판 위에 올린다.

**링크와 QR을 둘 다 두는 이유는 카드가 가는 길이 둘이기 때문이다.** 메신저로 보내면 받는 사람이 누르는 것은 링크이고, 이미지로 저장해 스토리에 올리면 링크가 아예 없어 QR만이 유일한 유입 경로다.

### 6.5 로깅, 에러 처리, 품질 도구

**구조화 JSON 로그를 표준출력에 남기고** Cloud Run이 Cloud Logging으로 수집하게 한다. 프로세스가 표준출력에 쓰는 모든 줄이 한 줄짜리 JSON이며 uvicorn과 서드파티 로그도 같은 포맷으로 통일했다. 필드는 시각·레벨·`session_id`·이벤트명·토큰 사용량이고, 이벤트에는 세션 생성, 턴 진행, 종료, 이탈, 악성 중단, 결과 생성, 수정, 제출, 분석 실행이 포함된다. **대화 원문과 보고서 본문은 로그에 남기지 않는다.**

에러는 존재하지 않는 세션이 404, 세션 상태에 맞지 않는 호출이 409, 그 외가 500이다. **자동 재시도와 복구 로직은 만들지 않는다.** API 오류가 나면 고정 문구와 처음 화면으로 가는 버튼 하나만 있는 오류 화면을 표시하고 흐름을 종료한다. 그 외 복구 안내나 복구 UI는 만들지 않고, 시연 직전에 서비스 상태를 점검하는 것으로 대응한다.

**핵심 로직의 pytest는 기능 구현보다 먼저 작성한다.** 대상은 세션 관리, 제어 토큰 파싱, 프롬프트 조립, 스키마 검증, 유형 가중치·반올림·동률·빈 축 처리, 축별 집계 계산이다. **테스트는 로컬 개발 자산으로만 유지하고 공개 저장소에 포함하지 않는다.** 저장소에는 시스템 구동에 필요한 코드만 둔다. 테스트를 작성하지 않는다는 뜻이 아니라 공개 범위에서 제외한다는 뜻이며, 선행 작성 원칙은 그대로 지킨다.

UI 검증은 `cd web && pnpm e2e`가 개발자 모드로 전 흐름을 헤드리스로 완주하며 계약을 검사하고 단계별 스크린샷을 남긴다. 인터뷰 대화를 타이핑하지 않으므로 Gemini 호출이 최소다. 기능을 추가하면 이 스크립트에 검사 항목을 함께 추가한다. 정적 검사는 Python이 ruff, 프론트가 ESLint와 Prettier다.

**e2e에 대해 알아 둘 두 가지가 있다.** 악성 응답 검사는 스트림을 가로채 만든다 — 모델이 어떤 발화를 악성으로 판정할지가 검증된 적이 없어 실제 호출로는 재현할 수 없기 때문이며, 고정 문구를 백엔드에서 정규식으로 읽으므로 문구가 바뀌면 이 검사가 깨진다. 그만두기 검사는 모달을 열고 취소만 한다 — 확정하면 그 실행이 이어서 써야 할 세션이 사라진다.

## 7. 저장소 구조와 원천

모노레포는 네 영역으로 나눈다.

- **`web/`** — Next.js 프론트엔드. 진입과 동의, 대화 UI, 유형 카드와 보고서 확인·수정, 공유 카드, `/admin`.
- **`api/`** — FastAPI 백엔드. 세션과 LLM 호출, 증거 수집과 유형 합산, 저장, 분석 파이프라인, 담당자 API, `api/prompts/`의 LLM 자산 아홉 개, `api/app/data/`의 군·구별 동·읍·면 표.
- **`data/`** — `plan2040/`의 전처리된 도시기본계획 정본과 출처 포인터가 포함된 파생 요약본.
- **`docs/`** — 사례조사 등 보조 문서, `qr/`의 참여자 접속 QR, `interview-lab/`의 12인 인터뷰 실험 자료, `tuning-debate/`의 설계 토론 기록.

**무엇이 어디서 나오는지.**

| 대상 | 단일 원천 |
|---|---|
| 축·극·기본 극·표시명 상수 | `api/prompts/axes.md`의 기계 판독 JSON → `api/app/axes.py`가 파싱 |
| 채점 앵커와 판별 규칙 | `api/prompts/axes.md` |
| 인터뷰 진행 방식과 문항 | `api/prompts/rubric.md` |
| 인터뷰어 행동 규범 | `api/prompts/system.md` |
| API 요청·응답 필드 | `api/app/main.py`와 `web/src/lib/api.ts` |
| 저장 문서 스키마 | `api/app/store.py`와 관련 모델 |
| 유형 합산·반올림·동률·빈 축 | `api/app/scoring.py` |
| 도시기본계획 부문 고정 어휘 | `api/app/sectors.py` (`sectors.md`와 글자 그대로 같다) |
| 부문의 화면 색과 나열 순서 | `web/src/app/admin/dashboard-data.ts` (부문 이름과 순서가 `api/app/sectors.py`와 같아야 한다) |
| 군·구 목록과 동·읍·면 소속 | `api/app/regions.py`와 `api/app/data/incheon_dong.csv` |
| 16유형 별명·설명·이미지 | `web/src/data/city_types.json` |
| 참여자 화면의 일상어 축 라벨 | `web/src/lib/city-axes.ts` |
| 담당자용 축·유형 해설 | `web/src/data/admin_guide.json` (`axes.md`를 따라온다) |
| 일러스트 규격 상수 | `web/src/lib/city-types.ts`의 `TYPE_IMAGE_WIDTH`·`TYPE_IMAGE_HEIGHT` |
| 담당자 대시보드 화면 계약 | 루트의 `dashboard-mock.html` |

**축 이름이나 극 글자를 다른 파일에 리터럴로 두지 않는다.** 백엔드 모듈은 모두 `axes.py`만 참조한다.

**사람에 대한 사실은 그 사람의 제출본 문서에만 둔다.** 별명에서 성별을 유추하는 표를 코드에 두었던 적이 있는데 잘못된 설계였다. 이유는 셋이다. 공개 저장소에 참여자 이름과 성별이 그대로 실리고, 같은 별명을 쓴 다른 참여자가 오면 조용히 틀리고, 표본이 늘 때마다 코드를 고쳐야 하므로 데이터가 있어야 할 자리를 코드가 대신하게 된다.

**여기서 배운 것 하나를 남긴다. 화면에 빈칸이 보일 때 그 빈칸을 코드에서 메우려 들면 거의 언제나 잘못된 설계가 된다.** 빈칸은 그 데이터를 수집하지 않았다는 사실의 정직한 표시이고, 고칠 자리는 수집 경로이거나 저장된 문서다.

## 8. 개인정보와 안전

수집 항목은 출생연도, 성별, 별명, 거주 지역, 꿈 또는 현재 직업, 인터뷰 대화록, 기기 토큰, 만족도 응답 둘, 파생된 유형 결과와 보고서다. **여기에 접속 기록이 더해진다** — 화면을 열면 뒷자리를 가린 IP와 기기·브라우저가 남는다(3.5).

**결과 생성이 실패했을 때 참여자가 오류 기록 보내기를 누르면 같은 항목이 제출본과 별도로 저장된다**(9.2). 새로 받는 항목이 없고 저장 시점과 자리만 다르므로 고지 문구는 그대로 두며, 대화가 함께 간다는 것은 버튼 옆에서 밝힌다. 원인을 규명하면 지운다.

**접힌 고지는 무엇에 동의하는 것인지만 남기고, 항목은 펼친 전문이 진다.** 2026-09-03에 접힌 상태의 요약 한 줄을 걷어냈다 — 요약과 전문이 번갈아 나타나는 것이 고지가 자라는 연출을 어색하게 만들었고, 요약·전문·동의 문구가 한 박스에 겹쳐 담기면서 접힌 박스도 두꺼워졌다. 지금 접힌 상태에는 동의 문구와 화살표 한 줄만 있고, 받는 항목·목적·보관 기간·파기 시점은 화살표 하나를 눌러 열리는 전문에 있다. 동의의 대상이 무엇인지는 접힌 채로도 읽히고 그 내용은 한 번의 조작으로 닿는다는 것이 이 배치의 근거이며, 요약이 전문과 어긋날 여지가 사라지는 것은 덤이다.

**시작 화면의 수집 고지 문구에 이 항목들을 함께 적는다.** 고지에 없는 항목을 받는 것은 동의를 받지 않고 수집하는 것이다. **참여자가 동의하는 대상은 문서가 아니라 화면이므로, 항목이 늘면 화면을 먼저 고치고 이 목록을 거기에 맞춘다.** 고지와 시작 버튼이 같은 화면에 있으므로 참여자는 무엇을 넘기는지 보면서 누른다. 진입 화면에는 수집 항목, 이용 목적, 보관 기간, 파기 시점을 고지하고, **이용 목적에는 정리된 결과를 인천광역시에 전달한다는 것까지 적는다** — 전달을 적지 않으면 실제로 하는 일과 고지가 어긋난다.

**받는 사람이 누구인지는 고지 안이 아니라 시작 화면 본문에 적는다.** 진행자 소속·실명과 문의처는 안내 박스 아래, 고지를 펼치지 않아도 보이는 자리에 한 줄로 둔다. 고지 안에만 두면 형식은 갖추지만 목적은 못 채운다 — 처음 들어온 사람이 누가 받는지 모른 채 수상하다고 판단해 나가는 것을 막지 못하고, 접힌 고지를 펼쳐 읽는 사람은 소수다. 시간과 정보를 내주는 쪽이 받는 쪽을 알 수 있어야 한다는 것이 이 배치의 이유다. **같은 내용을 고지 전문에 겹쳐 싣지 않는다** — 한 화면에 있으므로 동의의 맥락은 유지되고, 겹쳐 실으면 그만큼 전문이 길어져 짧은 화면에서 먼저 잘린다. 고지가 자라도 이 줄은 덮이지 않고 자리도 지킨다(2.2). 고지 전문은 한 문단으로 쓴다 — 문단을 나누면 그만큼 박스가 길어지고, 세로가 짧은 화면에서 먼저 잘리는 것이 이 글이다. **공모전 명칭과 팀명은 쓰지 않는다** — 이 수집물은 공모전 심사가 아니라 인천광역시에 전달할 자료이고, 소속과 실명이 그 자리에서 더 강한 신뢰 근거다. 이름 순서는 포스터와 같게 두며 그것이 곧 가나다순이라 서열로 읽히지 않는다. 연락처는 학교 메일을 쓴다. 무료 메일 주소는 실명·학과 표기가 벌어 놓은 신원 보증을 도로 깎는다.

**기기 토큰은 무엇에 쓰는지까지 적는다.** 중복참여를 세는 데만 쓰고 참여를 막거나 개인을 특정하는 데 쓰지 않는다는 것을 함께 밝힌다. 무엇을 받는지만 알리고 왜 받는지를 감추면 고지의 목적을 채우지 못한다.

**대화 내용이 외부 모델 제공자에게 전달된다는 사실도 고지에 적는다.** 인터뷰어는 앤트로픽, 채점·구조화·분석은 구글의 모델을 쓴다. **누가 처리하는지를 밝히는 데서 멈추고 국외 이전 고지의 형식은 갖추지 않는다** — 침묵보다는 분명히 낫지만 완전한 준수는 아니라는 것을 아는 채로 택한 것이다.

분석 전 Gemini가 이름과 연락처를 블라인드 처리하고, 담당자 종합 리포트와 대표 인용은 비식별본을 쓴다. **비식별 대상은 요구 텍스트와 인용 발화뿐이고 대화록 원문은 그대로 남는다**(3.4). 제출본 상세를 여는 사람은 원문을 보므로, 이 처리는 집계와 리포트를 위한 것이지 원문 접근을 막는 수단이 아니다.

Firestore에는 애플리케이션 수준의 추가 암호화 없이 저장한다. **원본 접근을 막는 수단은 접근 코드 하나뿐이다** — `/admin`은 참여자 화면과 같은 출처에 있어 주소가 통제 수단이 되지 못한다(6.2). 코드는 서버가 검사하므로 화면을 우회해도 원문에 닿지 못한다. 계정과 권한 시스템은 만들지 않는다.

프롬프트 인젝션은 시스템 프롬프트 지시로만 방어한다. Gemini 안전필터의 오작동은 개발 단계에서 다루지 않는다.

## 9. 미결 사항

### 9.1 AI 품질 — 살아 있는 결함

관측 원자료는 저장소에 없다(로컬 아카이브). 목록만 여기 남긴다.

**`structuring.md`에 남은 것.** 요구 제목의 사업명투가 실측 열에 하나쯤 남으며, 소재 자체가 조치인 요구("노후 건물 관리")에 몰린다. 표현 상향, 참여자가 말하지 않은 가치 대비 창작과 **그 창작이 축 판정 근거로 실리는 것**, 사실 단정, 희망을 확정으로 서술, 장면 합성, 부수 언급의 필수 격상은 처방이 들어갔으나 재발 여부를 실측으로 확인하지 않았다.

**채점(`axes.md`)에 남은 것.** `FH`가 언급과 재확인을 가르지 못해 사실상 미판정이다(`H` 17 대 `F` 1). `OW`의 월세 소재가 두 극으로 갈라진다. `UN`의 성립 기준이 `AC`보다 느슨하다는 비대칭이 있다. 강도가 증거량과 무관해 보인다는 지적(18/18)과 얇은 근거 1건이 축을 확정하는 사례도 반복됐다.

**채점 완화(4.5)의 재태깅 격리 점검은 아직 돌리지 않았다.** 저장된 대화록에 새 `axes.md`를 다시 적용해 기존 태깅과 비교하는 절차(재태깅 스크립트, 크레딧 소량)가 남아 있다. 격리 점검(Sonnet 채점기 2판)에서는 완화 규칙이 의도대로 작동했고, 한 발화가 두 축에 걸칠 때 한 축만 잡는 경계 판단(U/W, N/H)이 관찰됐다.

**표시.** 범례가 없다(18/18). 퍼센트 숫자의 비교 기준이 설명되지 않는다. 사후 절차 안내와 소관 구분 요구도 있었다.

**데이터.** `dream_or_job`에 꿈이 아니라 현재 직업이 들어온다(17/18). 도서 지역에서 정규화 단위와 요구 단위가 어긋난다.

**격리 점검에서 남은 관찰(각 1회, 재발 2회까지 관찰):** 참여자가 밝힌 사실(2045년의 가족 나이)과 어긋난 전제를 깐 질문, 질문의 "2045년" 누락 1~2턴(2026-09-03 인천 밖 거주자 격리 테스트에서 2턴 연속 재발, 처방은 아직 두지 않음), 참여자가 말하지 않은 감정·전제 덧붙임, 요약 길이 초과. 판박이 힌트 질문과 정리 단계의 "못 들었다" 언급은 재시도 인지형 힌트와 문구 중립화로 처방했고 격리 점검 4판에서 재발하지 않았다.

**빈 축의 계약.** 빈 축에도 판정 글자·강도·유형 코드 자리가 강제되고, 동점이 표시에서 소거되며, 단극 관측이 100으로 계산된다. 셋 다 4.5·4.6이 의도한 계약이지만, 통제 실험에서 **빈 축이 정직한 판정으로 나온 사례**가 생기면서 "증거 0건인 축의 글자가 네 글자 코드에 들어가는 것"이 관측 가능한 상태가 됐다. 계약을 바꿀지 표시를 더 가를지는 미결이다.

### 9.2 백엔드 — 종료 이벤트 누락

`api/app/interview.py`의 스트림 처리가 도중에 예외를 만나 턴이 완결되지 않는 사례가 여러 라운드에서 반복 관측됐다. **재현 조건을 특정하지 못했고 원인은 여전히 미상이다.**

**원인 기록은 구현돼 있다.** 예외의 종류와 턴 번호를 로그 한 줄로 남긴다(대화 원문은 남기지 않으며, 참여자가 스스로 끊은 경우는 정상 경로라 제외한다). 네 번을 관측하고도 판정 불가로 끝난 것은 그 기록이 없었기 때문이므로, **다음 재발 때는 로그가 남는다.**

**실패한 턴을 `end`로 닫아 준다.** 대화록이 비어 있지 않으면 세션을 `ended`로 바꾸고 종료 이벤트를 내보내 참여자가 종료 버튼을 보게 한다. 첫 인사에서 실패하면 살릴 대화록이 없으므로 지금처럼 예외를 올린다. 원래 이 처방을 거부한 이유는 증상을 덮으면 재발이 로그에 남지 않는다는 것이었는데, **`turn_failed` 로그를 그대로 남기므로 진단 능력을 잃지 않는다.** 잃는 대상이 달라진 것도 이유다 — 테스터는 다시 해 주지만 익명 참여자는 그냥 나가고, 10분 넘게 타이핑한 내용이 제출 직전에 통째로 사라진다.

**결과 생성 실패도 막다른 길이 아니다.** `/result`가 실패해도 서버 세션은 `ended`로 살아 있으므로 화면이 다시 시도를 제안하고, 참여자가 누를 때만 다시 호출한다. 자동 재시도는 하지 않는다. 실패는 `result_failed` 이벤트로 남으며 예외 종류·상위 상태코드·메시지 수·턴 수·페이로드 길이를 담고 **대화 내용은 담지 않는다.** 2026-08-28에 두 번 관측된 Gemini `400 INVALID_ARGUMENT`가 이 기록이 없어 원인을 좁히지 못한 사례다.

**대화까지 보낼지는 참여자가 정한다.** 위 필드로 좁혀지지 않는 실패가 남는데, 무엇을 묻고 무엇을 답했는지를 모르면 더 볼 것이 없기 때문이다. 실패 화면의 두 번째 버튼이 그 세션의 대화록·증거 로그·실패 정보를 Firestore의 `failure_reports`에 한 건으로 저장한다. 실패 정보에는 예외 종류와 메시지, 스택, 상위 상태코드, 메시지 수, 턴 수, 페이로드 길이가 들어가며, 예외를 잡는 자리에서 세션에 붙여 두었다가 이 요청이 꺼내 쓴다. **참여자가 누를 때만 저장하고 자동으로는 보내지 않는다** — 대화 원문을 남기지 않는다는 6.5의 선을 사람이 넘기로 정한 경우에만 넘는다.

**읽는 곳은 Firestore 콘솔이고 담당자 웹에는 싣지 않는다.** 이 기록을 읽는 사람은 인천시 담당자가 아니라 개발자이고, 드물게 나야 정상인 것을 위해 만든 화면이 가장 먼저 낡는다. 자주 들여다보게 되면 그때 화면을 붙인다. **세션이 만료됐거나 인스턴스가 새로 떴으면 이 버튼도 다시 시도와 함께 실패한다** — 세션이 메모리에 있다는 같은 전제 위에 있으므로 조건이 같고, 그 경우 살릴 방법은 없다.

### 9.3 남은 작업

1. **종료 이벤트 누락의 원인을 규명한다.** 원인 기록은 들어갔으므로 다음 재발의 로그를 기다린다.
2. **실제 스마트폰으로 QR 접속부터 공유 카드까지 통주한다.** 지금까지의 검증은 전부 헤드리스 브라우저였다.
3. **최종 일러스트 16종을 받아 삽입한다**(4.7의 교체 절차).
4. **시연 전 저장소를 정리한다.** 시험 삼아 만든 대화와 예시 제출본이 실제 참여자와 한 목록에 섞인다. 접속 현황 기록에 쌓인 개발 중 흔적은 **`api/scripts/reset_activity.py`로 백업 후 일괄 삭제한다**(2026-09-03에 1,532건을 지웠다). 담당자 화면에는 초기화 버튼을 두지 않는다 — 심사자에게 보이는 화면에 파괴적 조작을 노출하지 않기 위해서이고, 이 정리는 운영자가 저장소에서 하는 일회성 작업이다. **Firestore 시점 복구가 꺼져 있어 삭제는 되돌릴 수 없으므로** 스크립트가 백업 파일을 먼저 쓰고, 그 경로가 이미 있으면 실행을 거부한다.
5. **발표 자료의 2040 표기를 2045로 맞춘다.** 코드와 프롬프트 자산은 이미 맞췄다. **참고 자료의 출처를 가리키는 2040은 사실이므로 그대로 둔다** — 부문 10종은 2040 계획 제4편의 장이고(`briefing.md`·`sectors.md`), AC축 계보도 2040 계획의 도시가치 대립에서 도출했다.
6. **재태깅 격리 점검**(9.1).
7. **시연 리허설.**

**이관해 둔 것.** 판단은 끝났고 이번 범위에 넣지 않기로 한 작업들이다 — 증거 없는 축의 기본값 채움 방식 재설계, 현재 직업과 장래 희망의 분리, 담당자 검수 완료 표시, 준식별자까지 다루는 비식별 처리, 참여자 수정 이력 보관, 이슈 중심 저장 구조로의 전환. 마지막 것은 지금 축 중심 구조에 얹힌 신설 필드가 전부 요구 단위·제출본 단위라 옮길 때 기계적 이동이 되도록 설계해 두었다.

### 9.4 확정되지 않은 것

- 16유형 **한 줄 설명**과 담당자 해설(`admin_guide.json`) **문구**는 팀 재검수 대상이다. 별명 16종과 배지 8종은 확정됐다.
- **부문 라벨링 결과의 실무 담당자 검수**는 아직 받지 않았다. 어휘 자체는 도시기본계획 목차라 확정이지만, 요구가 그 장에 맞게 붙었는지는 소관 부서가 봐야 안다.
- **특정 회사·세부 지역 같은 준식별자 처리 기준**은 베타 테스트에서 정한다.
- **도시기본계획 File Search 활성화.** 정본과 요약본은 준비됐다. 업로드 스크립트를 실행하고 스토어 이름을 `.env`에 넣으면 코드 변경 없이 켜진다. 켜면 매 턴 검색 청크가 추가되므로 그 시점에 비용을 다시 본다.
- **도입의 데이터 사용 고지를 시작 화면으로 옮길지.** 지금은 인터뷰어가 도입에서 한 문장으로 알린다. 화면으로 옮기면 도입 턴이 가벼워지지만 시작 화면의 고지 문구와 역할이 겹친다.
- **유형 공개를 제출 뒤로 미룰지.** 팀에서 제기됐고 잠정 결론은 미루지 않는 것이다 — 제출은 시에 의견을 전달한다는 능동적 동의인데, 유형을 그 뒤에 두면 제출이 결과 확인의 통행료가 되어 제출 수가 전달 의사를 재지 못하게 된다. 접속 현황의 결과 확인 대비 제출 갭(3.5)을 실측한 뒤 재논의한다.
- **README**는 아직 한 줄짜리다. 마지막에 작성하기로 했다.

### 9.5 알고만 있는 것

- **느린 회선에서 첫 접속이 오래 걸린다.** 전송량 약 2.9MB 중 한글 서체 하나가 2.06MB로 70%를 차지한다. 9Mbps에서는 2.2초, 1.6Mbps에서는 11.3초였다. 서버는 원인이 아니다. **화면 자체는 두 회선 모두 1초 안에 뜨고**, 서체가 늦게 도착해 글자가 바뀌면서 브라우저 로딩 표시가 계속 도는 것이 지연으로 느껴진다. 고치려면 동적 서브셋으로 바꾸는 방법이 있으나 공유 카드의 서체 임베드 검증(6.4)과 부딪칠 수 있어 함께 설계해야 한다. **시연장 회선이 혼잡할 때 재발할 수 있다는 것만 알아 둔다.**
- **`api/scripts/personas.json`의 인물 목록과 실제 묶음집이 어긋나 있다.** 인물 정의가 없는 묶음집과 묶음집이 없는 인물이 섞여 있다. 재태깅에는 인물 정의가 필요 없으므로 지금 당장 막는 것은 없고, **전체 재생성을 돌릴 일이 생기면 선행 정리가 필요하다.**
- **`api/tests/test_dev.py` 한 건이 저장소에 없는 `sion.json` 픽스처를 읽어 실패한다.** 다른 작업의 게이트를 돌릴 때 이 1건은 알려진 실패로 취급한다. 픽스처를 복원하거나 테스트를 현행 픽스처에 맞출 때까지 남는다.
- **`web/src/app/globals.css`와 `web/src/app/admin/report/quote-layer.tsx`가 `prettier --check`에 걸린다.** 게이트인 `ruff check`와 `eslint`는 통과한다.
- **인터뷰어 이름이나 표기를 바꿀 때 코드만 고치면 절반만 바뀐다.** 이름은 인터뷰어가 대화 중에 실제로 말한 것이라 이미 저장된 대화록에 남는다. 지금까지는 저장된 것이 전부 가상주민과 통제 실험의 산출물이었으므로 함께 치환했다. **실제 참여자의 대화록이 쌓인 뒤에는 같은 치환을 하지 않는다** — 사람이 실제로 들은 이름을 사후에 바꾸는 것은 원자료를 고치는 일이다. 그 시점 이후로 이름을 또 바꾸려면 옛 이름이 남은 대화록을 그대로 두는 쪽을 택한다.
- **Gemini가 429를 돌려주면 태깅과 결과 생성이 함께 죽는다.** 2026-08-19 가상 참여자 인터뷰 중 태깅 호출이 13턴부터 429로 전부 실패했고, 같은 이유로 결과 생성(`/result`)도 500을 반환해 참여자가 결과를 받지 못했다. **원인은 크레딧 소진이 아니다** — 팀 확인 결과 잔액이 바닥난 적이 없다. 429는 잔액이 아니라 분당 요청·토큰 할당량에서도 나오므로 그날은 **속도 제한**에 걸린 것으로 본다. 충전으로는 막을 수 없고 동시 호출을 묶어야 막힌다는 점에서 대응이 다르다. 태깅 실패는 설계대로 조용히 넘어가지만 그만큼 증거가 유실되어 빈 축이 늘고, 결과 생성은 참여자가 다시 시도를 누를 때만 재호출한다(9.2).
- **튜닝 실험 산출물은 지우지 않기로 했다.** 담당자 대시보드를 시험하려면 쌓인 표본이 필요하고, 통제 실험의 반복 인물이 참여자수를 부풀리는 것은 시연 표본의 성격을 아는 상태에서 감수한다.

## 10. 문서 유지 규칙

- **이 문서는 맥락 문서다.** 결정과 그 이유, 하지 않기로 한 것, 원천의 위치를 적는다. API 필드·JSON 스키마·채점 앵커 전문처럼 코드와 프롬프트에 이미 있는 것은 옮겨 적지 않고 7장의 원천을 가리킨다.
- **현재형으로만 쓴다.** 과거에 무엇이었다가 무엇으로 바뀌었는지는 적지 않는다. 지금의 결정과 그 이유만 적는다. 라운드 기록, 실측 로그, Phase 진행 이력은 이 문서에 넣지 않는다 — 필요하면 git 이력에 있다.
- **결정이 뒤집히면 옛 결정을 취소선으로 남기지 않고 지운다.** 뒤집힌 이유가 앞으로도 판단에 쓰이면 새 결정의 이유 문단으로 흡수한다.
- **미결은 9장에만 둔다.** 닫히면 지운다. 계약이 바뀌었으면 해당 절을 고친다.
- **진행 중인 라운드 문서를 이 문서로 통폐합하지 않는다.** 라운드가 끝나면 계약 변경분만 해당 절에 반영하고 그 문서는 지운다.
- 구현과 이 문서가 어긋날 때의 처리는 문서 맨 앞에 적었다.
