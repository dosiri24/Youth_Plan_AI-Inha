# 유스플랜AI 구현 계획서

> 문서 성격: 아무 맥락이 없는 코딩 LLM이 이 문서 하나만 읽고 구현할 수 있도록 만든 프로젝트 최상위 설계 문서이자 단일 진실 원천(SSOT).
>
> 상태: **Phase 0~11 구현 완료. 두 서비스가 Cloud Run에 떠 있고 배포본이 전 흐름 검증을 통과했다.** 남은 것은 도메인 연결, QR 생성, 외부 스마트폰 통주 테스트, 시연 리허설이다. 참여자 진입부터 담당자 대시보드까지 전 구간이 실제 Gemini로 동작하고, 제출본과 분석 결과가 Firestore에 영속화되어 서버를 재기동해도 남는다. 인터뷰·채점 호출 분리만 실호출 재생성 검증이 남았다.
>
> **2026-08-12에 AI 품질 튜닝 라운드를 적용했다.** 12인 실험에서 나온 결함 26건의 처방이 코드와 프롬프트에 들어갔고, 계약이 바뀐 곳은 이 문서의 해당 절에 반영했다(요약은 15장). **결함 목록·원인 분석·처방 근거의 SSOT는 루트의 `TUNING.md`다.**
>
> 최종 기준일: 2026-08-12.

## 1. 프로젝트 성격과 목표

유스플랜AI는 인천시 청년정책 아이디어 공모전에서 심사위원에게 시연할 프로토타입이다. 대규모 상용 서비스나 실제 정부 운영 시스템이 아니다.

모든 구현 판단에는 다음 우선순위를 적용한다.

- 핵심 기능의 큰 틀을 완성한다.
- 깔끔한 UX/UI를 만든다.
- 시연 중 안정적으로 동작하게 한다.
- 완벽함보다 끝까지 작동하는 전체 사용자 여정을 우선한다.
- 과도한 보안·암호화, 대규모 트래픽 대응, 완벽한 예외 처리는 구현하지 않는다.

도시기본계획은 장기 도시 발전 방향을 정하지만, 기존 공청회는 시간·정보·접근성 문제로 청년의 참여를 어렵게 한다. 유스플랜AI는 공청회를 대체하지 않고 보완한다.

참여자는 앱 설치 없이 모바일 웹에 접속해 LLM과 1:1로 대화한다. 2040년 인천에서의 자기 하루를 상상하고, AI의 꼬리질문을 통해 막연한 생각을 구체적인 정책 수요와 문제의식으로 발전시킨다. 시스템은 대화를 구조화된 보고서와 "내가 바라는 2040년 인천의 도시 유형"으로 변환한다.

도시 유형은 일상어로 정의한 4개 축을 조합한 16유형이다. 참여자는 4글자 코드 대신 유형별 일러스트와 별명, 일상어 라벨 배지를 받는다. 이것이 참여 보상이자 SNS 공유 소재가 되고, 담당자에게는 축별 분포와 근거 발화를 함께 제공한다. 자체 AI 모델은 개발하지 않고 상용 Gemini API를 사용한다.

성공 조건은 다음과 같다.

- 시연에서 `접속 → 인터뷰 → 유형·보고서 확인/수정 → 제출 → 공유 카드 → 담당자 화면`이 끊김 없이 작동한다.
- 참여자가 모바일 웹에서 인터뷰를 완주하고 결과가 자기 생각을 반영한다고 느낀다.
- 담당자가 축별 분포, 평균 강도, 정성 요약, 근거 인용을 함께 볼 수 있다.
- 7~8월 약 10명의 시범운영에서 질문 구성, 응답 품질, 축 커버리지, 유형 납득도, 요약 정확도, 비식별화를 실측한다.

## 2. 확정 결정 요약

### 2.1 제품 및 설계 원칙

- 핵심 MVP는 `AI 인터뷰 → 유형 판정·보고서 구조화 → 참여자 확인·제출`이다.
- 시스템과 백엔드·프론트엔드의 틀을 먼저 완성하고, 프롬프트·축·유형 문구는 외부 파일 튜닝으로 정련한다.
- 인터뷰·유형 판정·확인·제출은 실시간 구간이다.
- 다수 제출본의 비식별화·집계·정성 요약은 제출 후 실행하는 배치 구간이다.
- 실시간 구간과 배치 구간의 경계는 제출 액션이다.
- 유형 정량화에는 사전 정의된 4축을 사용한다.
- 임베딩 군집화는 통제와 해석이 어려우므로 사용하지 않는다.
- 유형은 심리검사가 아니며 성격 MBTI와 완전히 분리한다. MBTI 글자 차용도 하지 않는다.
- 성격 MBTI를 수집하거나, 도시 유형과 병렬 제시하거나, 상관관계를 검증하지 않는다.
- 프롬프트, 루브릭, 축 정의, 채점 앵커는 코드에 하드코딩하지 않는다.
- 기능 변경은 먼저 이 문서에 반영한다.

### 2.2 기술 스택

백엔드는 다음 조합으로 고정한다.

- Python 3.11
- uv
- FastAPI와 Uvicorn
- google-genai SDK
- pytest
- ruff

프론트엔드는 다음 조합으로 고정한다.

- Node.js 22 LTS
- pnpm
- Next.js App Router와 TypeScript
- Tailwind CSS
- shadcn/ui
- ESLint와 Prettier
- 모바일 우선 레이아웃

데이터와 배포는 다음과 같이 고정한다.

- Cloud Firestore Native 모드
- Google Cloud Run
- 리전 `asia-northeast3`
- `web`과 `api` 두 서비스
- 각 서비스를 Dockerfile로 컨테이너화
- Artifact Registry에 이미지 저장
- `api` 인스턴스는 `min=1`, `max=1`
- Gemini 키, 모델명, File Search 스토어 이름 등은 Cloud Run 환경변수로 주입
- 참여자에게 `web` URL을 링크와 QR코드로 배포

### 2.3 명시적으로 하지 않는 것

- 자체 AI 모델 개발
- 임베딩 군집화와 임베딩 모델 사용
- 구 계획의 `gemini-embedding-001` 사용
- 성격 MBTI 수집·병렬 제시·상관 검증
- 자동저장과 중단 세션 복원
- 인터뷰 본편의 폼 입력이나 우회·폴백 화면
- 단계별 시스템 프롬프트 교체
- 대화의 강제 턴 상한
- 참여자의 유형·강도·판정 근거 수정
- 참여자의 보고서 직접 편집
- 요약본과 원문을 나란히 대조하는 UI
- 제출 후 재작성·재제출
- 별도 벡터 데이터베이스
- Gemini explicit 캐시 API
- Firestore 로컬 에뮬레이터
- 애플리케이션 수준의 추가 암호화
- 관리자 로그인·권한 시스템 (계정, 비밀번호 해싱, 세션, 역할 구분. 대신 6.5의 단일 접근 코드를 쓴다)
- 악성 응답 전용 판정기와 데이터 저장·개발자 전달 (경고 카운터는 3.2에서 백엔드가 소유한다)
- 규칙 기반 개인정보 필터와 최소 인원 임계값
- 대화 원문과 보고서 본문의 애플리케이션 로그 기록
- API 장애 자동 재시도
- Gemini 안전필터 오작동 대응
- 모델·프롬프트 버전 관리
- 다국어와 음성 입력
- 대규모 트래픽·수평 확장 대응
- 현재 단계의 비용 분석
- 프로젝트 전체 세부 파일 구조의 선행 확정

### 2.4 UI 디자인 원칙

참여자 화면은 **모바일 전용**으로 설계한다. QR로 접속한 휴대폰이 유일한 실사용 환경이기 때문이다. 데스크톱 브라우저로 열면 모바일 폭을 유지한 채 화면 가운데에 배치한다. 반응형으로 넓은 화면용 레이아웃을 따로 만들지 않는다. `/admin`은 예외이며 데스크톱에서 본다.

디자인 방향은 토스(Toss)의 결을 따른다. 구체적으로 다음을 뜻한다.

- 한 화면에는 하나의 목표만 둔다. 참여자가 다음에 무엇을 해야 하는지 고민하지 않아야 한다.
- 여백을 넉넉히 쓴다. 요소를 빽빽하게 채우지 않는다.
- 제목은 크고 굵게, 본문은 담백하게 쓴다. 타이포그래피가 위계를 만든다.
- 주요 행동 버튼은 화면 폭을 꽉 채우고 충분히 높게 만들어 엄지로 누르기 쉽게 한다.
- 모서리는 부드럽게 굴리고 그림자는 절제한다. 경계선보다 배경 대비로 영역을 구분한다.
- 문구는 짧고 사람의 말처럼 쓴다. 행정 용어를 쓰지 않는다. 모호한 비유로 에두르지 말고, 지금 무엇을 보고 있는지 그대로 말한다.
- 화면이 사용자의 조작을 이기지 않는다. 인터뷰 대화 영역은 참여자가 아래쪽에 있을 때만 새 글자를 따라 내려가고, 참여자가 위로 스크롤해 지난 대화를 읽는 동안에는 화면을 끌어내리지 않는다. 답변은 그동안에도 아래에서 계속 생성된다.
- **되돌아가는 길은 앞으로 가는 길과 같은 자리에 둔다.** 주요 행동 버튼이 화면 아래에 있으면 이전 버튼도 그 옆에 있어야 한다. 화면 맨 위 구석의 작은 글씨는 실제 사용에서 발견되지 않았다(2026-08-11 팀 시험 사용).
- **기기의 뒤로가기는 화면의 "이전"과 같은 뜻이어야 한다.** 참여자는 화면 안의 버튼을 찾기 전에 휴대폰의 뒤로가기부터 누른다. 그 조작이 서비스를 통째로 빠져나가게 만들면 진행하던 것을 전부 잃는다. 되돌아갈 곳이 있는 화면에서는 한 단계 뒤로 가고, 없는 화면에서는 아무 일도 일어나지 않는다.

색은 **인천광역시 전용색상만** 사용한다. 임의의 브랜드 색을 만들지 않는다.

- Incheon Blue, Pantone 300C, `#005EB8`: 주조색. 주요 버튼, 강조, 참여자 말풍선.
- Incheon Green, Pantone 326C, `#00B2A9`: 보조색. 완료와 긍정 상태, 유형 결과의 강조.
- Incheon Gray, Pantone 425C, `#54585A`: 보조 텍스트와 비활성 상태.

본문 텍스트의 진한 먹색과 배경의 옅은 회색처럼 위 3색으로 표현할 수 없는 중립색은 무채색 계열에서 파생해 쓰되, 유채색 포인트는 위 3색을 벗어나지 않는다.

예외는 하나뿐이다. **본인인증 버튼에만 `#EB4D51`을 쓴다.** 본인인증은 인천시의 서비스가 아니라 통신사 인증 절차로 넘어가는 행동이고, 국내 이용자에게 이 계열의 붉은색이 본인인증의 관례적 신호로 통하기 때문이다. 이 색은 그 버튼 밖으로 번지지 않는다.

한글 서체는 Pretendard Variable을 애플리케이션에 번들로 포함해 사용한다. 외부 폰트 CDN에 런타임 의존하지 않는다.

## 3. 방법론과 전체 아키텍처

Anthropic의 AI Interviewer 방법론을 다음 세 단계로 적용한다.

1. Planning: 연구 질문과 탐색 주제로 인터뷰 루브릭을 만든다.
2. Interviewing: 참여자별 실시간 적응형 인터뷰를 진행한다.
3. Analysis: 원 방법론의 창발적 군집화 대신 사전 정의된 도시가치 축으로 정량화한다.

시스템은 참여자 여정에 따라 다섯 파트로 나눈다. 인터뷰의 공간 범위는 인천광역시 전체이고, 주제 범위는 인천시민 전체의 삶이다. 시범운영 참여자는 청년이다.

### 3.1 파트 A: 접속과 자격 확인

진입 순서는 다음과 같다.

1. QR 또는 링크 접속
2. 시스템 개요 확인
3. 개인정보 수집·이용 동의
4. 본인인증 목업
5. 인터뷰 시작

개인정보 동의는 별도 화면이 아니라 시작 화면 안의 고지 문구와 체크박스로 통합했다. 참여자가 체크해야 본인인증 버튼이 활성화된다.

실서비스에서는 휴대폰 본인인증으로 1인 1회 참여를 확인하고 출생연도를 함께 받는다. 프로토타입에서는 실제 인증 없이 버튼으로 통과시키며, 출생연도 입력 필드 하나로 인증 응답값을 대신한다.

프로토타입의 목업 인증은 중복참여를 실제로 강제하지 않는다. 이는 시연용 구현의 한계다. 출생연도는 1900~2026 범위의 정수인지 여부만 검사한다.

휴대폰 신원은 중복참여 방지 게이트로만 사용한다. 인터뷰 데이터에는 출생연도만 전달하고 다른 신원 정보는 결합하지 않는다.

참여는 열린 참여이고 무보상이다. QR 홍보물에는 사업 배경과 기대효과를 적는다. 입력은 타이핑, 언어는 한국어로 한정한다.

자동저장은 없다. 이어지는 범위는 브라우저가 페이지를 백그라운드에서 유지하는 정도에 따른다.

### 3.2 파트 B: AI 인터뷰 엔진

참여자가 2040년 인천에서의 하루를 상상하도록 돕고, 꼬리질문으로 정책 수요를 구체화한다. 동시에 4개 도시가치 축을 판정할 수 있을 만큼 모든 축을 탐색한다.

출생연도는 세션 생성 시 백엔드로 전달한다. 백엔드는 연 단위 추정 나이(`2040 − 출생연도`)를 계산해 시스템 프롬프트에 주입한다.

별명, 거주 지역, 꿈 또는 현재 직업은 별도 폼으로 받지 않는다. LLM이 인터뷰 도입부에서 라포 형성을 겸해 자연스럽게 질문하며, 종료 후 구조화 단계에서 대화록으로부터 추출한다.

단일 시스템 프롬프트가 도입, 생활 장면, 정리 전 단계를 관장한다. 백엔드는 사용자 발화 뒤에 `[운영 지시: ...]` 블록을 덧붙여 그 턴에 무엇을 물을지 지정한다. `system.md`에는 이 블록이 참여자 발화가 아니라 백엔드의 운영 지시이며, 참여자의 의견이라고 해석하면 안 된다고 명시한다.

**한 턴은 두 번의 Gemini 호출로 처리한다.** 인터뷰어 호출과 증거 태깅(채점) 호출이며, 참여자 발화가 도착하는 시점에 **동시에** 출발한다. 경계는 다음과 같다.

- **인터뷰어 호출**은 `rubric.md`, `system.md`, 참여자 나이를 시스템 지시로 받는다. `axes.md`도 계획 요약본도 받지 않는다. 출력은 참여자에게 보일 대화 텍스트뿐이며 제어 토큰만 붙을 수 있다.
- **계획 요약본을 인터뷰어 호출에서 뺀 이유는 둘이다**(2026-08-12). 관측 14회에서 인용이 한 번도 없었고, 그 정책 어휘가 시민에게 행정의 답을 요구하는 질문을 키웠다. 프리픽스의 40.2%를 차지하던 자료다.
- **채점 호출**은 `axes.md`만 시스템 지시로 받는다. 입력과 출력 계약은 5.4에 있다.

**분리하는 이유는 억제로는 막히지 않기 때문이다.** 한 호출로 두 일을 시키면 인터뷰어에게 채점 기준 전문이 주어지고, 채점 기준을 읽은 인격은 그 대비를 질문으로 옮기고 싶어진다. 프롬프트가 "옮기지 마라"를 반복해 억눌러야 하는 구조 자체가 설계에서 나온다. 실측에서 억제는 유혹이 문장 형태로 떠오른 뒤에 걸러 내는 데만 기여했고 유혹의 발생 자체는 막지 못했으며, 실제 인터뷰에서 5.4가 가장 강하게 금지한 형태의 질문이 나갔고 그 답이 증거로 태깅되어 유형 판정까지 오염됐다. **읽지 않으면 옮길 수 없다는 것이 이 분리의 전부다.** 부수 효과로 인터뷰어 프리픽스가 37,155자에서 25,376자로 줄어, 남은 규칙 전부의 준수율이 함께 오른다(8.5의 제로섬 관찰).

채점 호출은 참여자 발화만 있으면 되므로 인터뷰어 응답을 기다리지 않는다. 한 턴의 소요는 두 호출 중 느린 쪽이며 그것은 인터뷰어 호출이므로 **참여자 체감 지연은 늘지 않는다.** 첫 인사 턴에는 참여자 발화가 없으므로 채점 호출을 하지 않는다.

**인터뷰어는 축도, 극도, 커버리지 임계도 영영 모른다**(2026-08-12 개편). 운영 지시가 축의 표시 이름 하나만 실어도 두 극이 역산되어 검사형 이지선다가 만들어진다는 것이 12인 실험에서 확인됐다. 축 이름과 침묵 사이의 제3의 선택지를 쓴다.

**백엔드가 검수된 질문 문장 하나를 통째로 내려보낸다.** 운영 지시에는 `ANSWER_KEY`(한 답칸의 식별자)와 그 답칸의 질문 문장이 들어가고, 인터뷰어는 그 문장을 낱말과 어순을 바꾸지 않고 그대로 한 번만 묻는다. 어느 축을 위한 질문인지는 백엔드에만 있는 값이며 프리픽스에 직렬화되지 않는다. 문맥에 맞지 않으면 인터뷰어는 새 질문을 만들지 말고 그 답칸을 건너뛴다.

질문 은행의 16문항은 `rubric.md`에 원본으로 적혀 있고 코드가 같은 문장을 들고 있다. 두 곳이 어긋나면 테스트가 실패한다.

**한 턴의 일정 전체를 백엔드가 소유한다.**

- 0턴: 첫 인사와 호칭 질문.
- 1~2턴: 도입. 거주지 하나, 근황 하나. 이 두 턴에는 은행 질문을 내보내지 않는다. 도입을 루브릭에만 맡기면 은행 질문이 1턴부터 나가 거주지 질문을 밀어낸다.
- 3턴부터 마무리 턴 전까지: 은행 질문 하나씩.
- 마무리 턴부터: 정리 4단계(우선순위 → 이유 → 요약과 추가 발언 → 감사와 종료).

**다음 답칸은 증거가 가장 적은 축의 질문으로 고른다.** 같은 수준이면 은행 순서를 따른다. 이유를 묻는 문항은 바로 앞 턴에서 그 장소나 장면을 실제로 답했을 때만 나간다. 한 번 내보낸 답칸은 다시 열지 않는다. **이것이 (C) 반복 질문 11/12의 구조적 동력을 끊는다.**

**축 커버리지는 질문 선택 신호일 뿐 종료 자격이 아니다.** 거절·무응답·빈 축은 정상 상태다. 모델이 반복 질문을 하는 이유는 빈 축을 실패로 믿기 때문이며, 그 믿음을 만든 것은 "네 축을 모두 탐색하라"는 지시였다.

한 턴은 `사용자 발화 1회 + AI 응답 1회`다.

- 목표는 15턴이고 마무리는 12턴부터다.
- 12턴 전에는 마무리하지 말라는 운영 지시를 매 턴 추가한다.
- 12턴부터는 정리 네 단계를 한 턴에 하나씩 지시한다. 각 단계에는 답칸이 하나뿐이다.
- 강제 상한은 없다. 정리 마지막 단계는 15턴 이후에도 반복된다.
- **빈 축이 남아 있어도 마무리를 미루지 않는다.** 근거 부족은 제품이 표시할 사실이지 인터뷰를 늘릴 이유가 아니다.
- 15와 12는 백엔드 설정값으로 두고 시범운영에서 튜닝할 수 있게 한다.

**참여자에게는 진행 정도를 퍼센트 하나로 보여 준다.** 끝이 보이지 않는 대화는 중간 이탈을 부른다. 표시값은 현재 턴을 목표 턴 15로 나눈 값이며 1을 넘지 않고, 소수점을 버린 정수 퍼센트다.

**커버리지를 진행률에 섞지 않는다**(2026-08-12 개편). 빈 축이 정상 상태가 되면서 커버리지는 영영 100%에 닿지 않을 수 있게 됐고, 그것을 진행률의 하한으로 쓰면 남은 대화 내내 퍼센트가 멈춰 선다. 지금은 백엔드가 턴 일정 전체를 소유하므로 턴 비율이 곧 실제 진행이다.

종료 신호를 받은 턴에는 100%로 표시한다. **축 이름도 축의 개수도 참여자에게 노출하지 않는다.** 참여자는 축의 존재를 모른다(5.1).

조기 종료 방지는 안내일 뿐 강제가 아니다. 참여자가 그만하겠다고 말하면 12턴 전이라도 종료한다.

종료 경로는 다음과 같다.

- 참여자가 대화 안에서 그만하겠다고 말하거나, 마지막으로 덧붙일 말에 대한 답을 받은 뒤 인터뷰어가 마무리 발화 뒤에 `[[END_INTERVIEW]]`를 출력한다. 백엔드는 토큰을 숨기고 세션을 종료 상태로 바꾼다.
- **종료 자격은 백엔드가 소유한다.** 정리 마지막 단계에 이르기 전이거나, 참여자가 그만두겠다고 말하지 않았다면, 인터뷰어가 종료 토큰을 내도 **문장 형태와 무관하게 제거하고** 대화를 계속한다. 자격이 있어도 그 응답에 답을 요구하는 표현이 남아 있으면 종료를 한 턴 보류한다. 12인 실험에서 4/12가 마지막 질문과 종료를 같은 응답에 담았고, 물음표 가드는 청유형에 뚫렸다. **이 상태를 모델에게 알리지 않으므로 운영 지시 이행률에 의존하지 않는다.**
- **프론트는 종료 신호를 받아도 화면을 자동으로 넘기지 않는다.** 마무리 발화 아래에 "대화 종료하기" 버튼을 띄우고, 참여자가 그 버튼을 눌러야 결과 단계로 이동한다. 마무리 인사를 읽을 틈도 없이 화면이 넘어가면 참여자는 방금 무엇이 끝났는지 알지 못한 채 다음 화면을 마주하게 된다. 또한 대화가 이미 끝났는데 무엇을 해야 할지 몰라 한 번 더 입력하게 되는 일도 막는다.
- 이 버튼은 별도의 LLM 호출이 아니라 이미 존재하는 종료 신호에 반응해 나타난다.
- 화면 상단의 "인터뷰 그만두기" 버튼은 순수 이탈 경로다. 확인 모달 후 세션을 폐기하며 어떤 데이터도 저장하지 않고 결과 단계로 가지 않는다. 인터뷰 화면에서 기기 뒤로가기를 누르면 이 확인 모달이 열린다.

**참여자는 직전 한 턴을 되돌릴 수 있다.** 잘못 보낸 답변이 그대로 남아 유형 판정과 보고서에 들어가는 것을 막는다. 경로는 둘이다.

- **생성 중 중단.** 답변이 나오는 도중에 중단하면 프론트가 스트림을 끊는다. 백엔드는 스트림이 끝난 뒤에 턴을 저장하므로 끊긴 턴은 세션에 아예 남지 않는다. 참여자가 보낸 글은 입력창으로 되돌아가고 화면에서도 그 턴의 두 말풍선이 사라진다. **나오다 만 답변을 저장하는 방식은 쓰지 않는다.** 잘린 문장이 다음 턴의 맥락으로 들어가고 대화록에도 남기 때문이다.
- **완료된 턴의 수정.** 답변이 끝난 뒤 마지막 참여자 발화를 고칠 수 있다. 백엔드가 마지막 사용자·AI 메시지 쌍과 **그 턴 번호로 기록된 증거를 함께 지운다.** 증거를 남기면 참여자가 취소한 말이 유형 판정에 계속 반영된다. 지운 뒤 프론트가 고친 글을 일반 발화로 다시 보낸다.

되돌릴 수 있는 것은 **직전 한 턴뿐이다.** 더 거슬러 올라가면 인터뷰어의 이후 질문이 지워진 맥락 위에 서게 되고, 참여자가 대화를 재구성하는 도구가 된다. 인터뷰가 종료된 뒤에는 되돌릴 수 없다.

중단으로 채점 호출이 떠 있는 채 남지 않게 한다. 클라이언트 연결이 끊기면 백엔드는 그 턴의 채점 호출을 취소한다. 악성 종료 경로에 이미 있는 처리와 같다.

가드레일은 시스템 프롬프트로만 구현한다.

- 유도질문 금지
- 특정 답 유도 금지
- AI 편향 삽입 금지
- 주제 이탈 시 인터뷰 주제로 복귀
- 참여자의 지시가 시스템 규칙을 덮어쓰지 못하게 처리

악성 응답은 **판정과 카운트를 나눠서** 처리한다. 악성 여부는 의미 판단이라 모델만 할 수 있고, 몇 번째인지는 상태라 백엔드만 알 수 있다. 분업선을 그 사이에 둔다.

- 인터뷰어는 악성이라고 판단하면 경고나 종료 인사를 만들지 말고 `[[MALICIOUS_RESPONSE]]` 토큰만 단독으로 출력한다. 몇 번째 위반인지는 세지 않는다.
- 백엔드가 세션의 카운터를 올린다. 1회면 **고정 경고 문구**를, 2회면 고정 종료 문구를 참여자에게 내보내고 세션을 폐기한다. 어느 쪽이든 모델이 쓴 텍스트는 쓰지 않는다.
- 악성으로 판정된 턴은 대화록에 저장하지 않고 그 턴의 채점 호출을 취소한다.
- 백엔드는 `session_id`와 위반 횟수가 포함된 로그 한 줄을 남긴다.
- 12인 실험에서 악성 입력이 0건이었으므로 **모델의 악성 판정 정확도는 아직 검증된 적이 없다.**

세션에는 원본 대화록과 매 턴 수집한 증거 로그를 보관한다. 제출 전에는 프로세스 메모리에만 존재한다.

### 3.3 파트 C: 유형 판정, 보고서 구조화, 확인과 제출

인터뷰 종료 직후 두 트랙을 병렬로 시작한다.

- 유형 트랙: 누적 증거를 코드로 즉시 합산한다.
- 보고서 트랙: 원본 대화록과 유형 결과를 한 번의 LLM 호출로 구조화한다.

유형 합산에는 LLM을 호출하지 않는다. 유형 카드를 먼저 보여주고 보고서는 생성되는 대로 이어서 표시할 수 있다.

구조화 호출에는 원본 메시지의 `turn`, `role`, `text`만 전달하고 저장용 `timestamp`는 제외한다. 인터뷰어 발화는 맥락으로만 사용하며 요구와 인용은 참여자 발화에서만 찾는다. 구조화 단계에서는 대화록으로부터 전체 요약, 축별 요구, 축별 판정 이유, 별명, 거주지, 꿈 또는 직업을 생성한다.

보고서 생성에는 LLM 호출이 한 번 들어가며 15초 안팎을 예상한다. 이 시간 동안 참여자를 정지된 문구 앞에 세워 두지 않는다. 부드럽게 움직이는 화면과, 지금 무엇을 하고 있는지 알려주는 안내가 순차적으로 바뀌며 기다림을 채운다.

진행 표시는 다음 규칙을 따른다. 서버가 실제 진행률을 알려 주지 않으므로, 기다리는 동안의 진행은 예상 소요를 근거로 한 연출이다. 이 구간에서는 진행바가 100%에 닿지 않는다. 끝나지 않았는데 끝난 것처럼 보이면 안 되기 때문이다.

**응답이 실제로 도착하면 진행바를 100%까지 채우고 잠깐 머문 뒤 결과 화면으로 넘어간다.** 이때의 100%는 거짓이 아니라 사실이다. 다 채우지 않은 채로 화면이 넘어가면 참여자는 무언가 덜 끝난 채로 넘어갔다고 느낀다. 완료를 눈으로 확인시켜 준 뒤에 넘긴다.

결과는 한 화면에 몰아넣지 않고 세 단계로 나눈다. 한 화면에 모두 담으면 스크롤이 지나치게 길어져 참여자가 무엇을 해야 하는지 알기 어렵다. 각 화면은 하나의 목적만 갖는다.

**결과 1단계 — 내 유형 확인**

- 유형 일러스트, 유형 별명, 한 줄 설명, 일상어 라벨 배지 4개, 축별 강도 바를 담은 유형 카드 (4글자 코드는 표시하지 않는다, 5.1)
- 공유하기 버튼과 이미지로 저장하기 버튼
- 축별 판정 이유

**결과 2단계 — 내 이야기와 요구 확인**

- 인터뷰 전체 요약
- 축별 요구 보고서
- 수정하거나 의견을 덧붙이고 싶은지 묻는 안내와 그 진입점

**결과 3단계 — 요구 수정**

- 요구 문장 선택과 참여자 의견 입력
- 4축 전체 재생성

세 단계 모두 그 화면의 행동을 **아래에 고정된 바**에 둔다. 스크롤 위치와 무관하게 항상 보인다.

- 1단계: 다음 단계로 넘어가는 버튼 하나가 폭을 채운다.
- 2단계: 이전 버튼과 제출 버튼을 나란히 둔다. 요구 수정 진입점은 본문에 남긴다. **하단 바에는 그 화면의 주된 행동 하나와 되돌아가는 길만 둔다.** 셋을 넣으면 무엇이 주된 행동인지 흐려진다.
- 3단계: 이전 버튼과 재생성 버튼을 나란히 둔다.

**헤더의 작은 이전 버튼은 없앤다.** 시험 사용에서 참여자가 제출 화면에서 결과로 돌아가려 했으나 그 버튼을 찾지 못했고, 대신 휴대폰 뒤로가기를 눌러 서비스를 통째로 빠져나갔다(2026-08-11).

기기 뒤로가기는 각 단계의 이전 버튼과 같게 동작한다. 1단계에서는 인터뷰가 이미 종료됐으므로 되돌아갈 곳이 없고 아무 일도 일어나지 않는다. 제출 완료 화면에서는 세션이 이미 폐기됐으므로 처음 화면으로 보낸다.

판정 근거로는 참여자의 발화를 다시 인용하지 않는다. 대신 각 축에 대해 **왜 그 글자로 판정했는지를 한두 문장으로 설명한다.** 다만 **증거가 한 건도 없는 축에는 설명할 근거가 존재하지 않으므로 코드가 고정 문장을 넣는다**(2026-08-12). 근거 없이 설명하게 하면 모델이 지어낸다. 방금 나눈 대화의 내용을 그대로 되돌려주는 것은 참여자에게 새로운 정보가 아니다. 참여자가 알고 싶은 것은 자기가 한 말이 아니라 그 말이 어떻게 이 판정으로 이어졌는가다.

판정 이유는 증거와 판정 결과를 근거로 LLM이 생성한다. 유형 코드와 강도가 코드로 결정된 뒤에 만들어지므로 판정과 어긋나지 않는다. 유형 결과와 마찬가지로 참여자가 수정할 수 없으며 요구를 수정해도 바뀌지 않는다.

수정 가능한 영역은 축별 요구뿐이다. 유형 코드, 축별 강도, 판정 근거, 전체 요약은 고정한다. 수정 LLM은 `axis_demands`만 출력하고, 서버가 기존 `summary`, `axis_reasons`, `self_info`와 메타데이터를 복사해 전체 개인 보고서를 조립한다.

축별 요구 설명은 문장 배열로 표시한다. 참여자가 하나 이상의 문장을 선택하고 의견을 입력하면, 선택 문장과 참여자 의견을 재생성 지시의 입력으로 전달한다. LLM은 일부 요구만 수정하지 않고 `axis_demands`의 4축 전체를 기존과 동일한 스키마로 재생성한다. `summary`와 `type_result`는 변경하지 않는다. 사용자가 텍스트를 직접 편집하지 않는다.

수정 횟수에는 제한이 없다. 15회에 도달하면 제출을 권하는 문구만 표시하고 수정은 차단하지 않는다.

제출 시 원본 대화록, 증거 로그, 유형 결과, 최종 보고서, 메타데이터를 Firestore에 영속화하고 메모리 세션을 제거한다. 한 제출 흐름은 1회 제출로 끝나며 재작성과 재제출은 허용하지 않는다. 목업 인증은 다른 세션을 통한 중복참여까지 실제로 차단하지 않는다.

공유 카드는 프론트가 결과 단계에서 받아둔 `type_result` 상태로 렌더링한다. 서버에서 다시 조회하지 않으며 `submit` 응답은 `submission_id`만 반환한다.

공유와 이미지 저장은 **제출 전 결과 1단계에서도 제공한다.** 유형 카드는 참여자가 가장 먼저 보고 싶어 하는 것이고, 그것을 보기 위해 제출을 먼저 요구하면 마찰이 생긴다. 제출하지 않고 카드만 공유한 뒤 이탈할 수 있다는 뜻이지만, 제출을 인질로 삼는 것보다 낫다고 본다. 제출 완료 화면에도 같은 공유 수단을 유지한다.

모바일에서는 Web Share API 공유 시트를 연동한다. 새로고침하거나 재접속하면 결과를 다시 볼 수 없다는 안내도 표시한다.

### 3.4 파트 D: 후처리 분석 파이프라인

담당자 웹의 "분석 실행" 버튼이 파이프라인을 한 번 실행한다. 파트 D는 참여자 흐름과 분리된 배치 작업이며, 실행 자체는 약 10건 규모의 admin HTTP 요청 안에서 동기 완료한다.

제출 응답이 끝난 뒤 백그라운드에서 Gemini가 요구 텍스트와 인용 발화의 이름·연락처를 블라인드 처리해 제출본의 비식별 사본을 채운다. 실패하면 로그만 남기고 제출 자체는 유지한다.

분석 실행 단계는 다음과 같다.

1. 비식별 사본이 없는 제출본만 Gemini로 보충하고 기존 비식별본은 재사용한다.
2. 코드가 축별 극 인원수, 극별 평균 강도, 16유형 분포를 계산한다.
3. Gemini가 각 축과 극의 요구 경향을 요약하고 대표 인용을 선정한다. **인용 ID는 코드가 부여한다.**

규칙 기반 비식별 필터, 최소 인원 임계값, 임베딩, 군집화는 사용하지 않는다. 특정 회사·지역 같은 준식별자의 처리 기준은 베타 테스트에서 정한다.

분석을 다시 실행해도 이미 있는 비식별 사본은 다시 만들지 않는다.

**비식별 호출은 텍스트만 바꾼다.** 입력은 그 제출본의 `axis_demands`이고 출력은 같은 구조에서 `title`, `description` 문장, `quotes`의 발화 텍스트만 블라인드 처리한 것이다. 축 순서, 요구 ID, 문장 개수, 인용 개수가 입력과 어긋나면 그 제출본의 비식별화를 실패로 처리한다. 축·글자·주제 태그·턴 번호는 모델에게 다시 만들게 하지 않고 서버가 원본에서 복사한다. 보고서 수정(3.3)이 `axis_demands`만 재생성하고 나머지를 서버가 복사하는 것과 같은 방식이다.

**인용 ID는 LLM이 만들지 않는다.** 서버가 비식별 사본이 준비된 시점에 그 제출본의 모든 인용에 축·요구·문장 순서로 `{submission_id 앞 8자}-Q{순번}`을 부여한다. 요약 호출에는 축마다 `quote_id`와 비식별 발화만 담은 후보 목록이 들어가고, 모델은 그중에서 대표 인용의 `quote_id`를 고른다. 후보에 없는 ID를 돌려주면 그 항목만 버린다. ID 생성을 모델에게 맡기면 형식이 어긋나 추적이 끊기고, 애초에 코드가 결정할 수 있는 일이다(8.5 기준 1).

**요약 호출은 실행당 한 번이며 네 축을 함께 처리한다.** 축마다 나누면 같은 시스템 지시를 네 번 보내면서 축 사이의 대비를 볼 수 없다. 어느 극에 해당하는 참여자가 한 명도 없으면 그 극의 요약 문장은 빈 배열이다.

**제출본이 하나도 없으면 분석을 실행하지 않고 409를 반환한다.** 집계할 것이 없는 실행 기록을 남기지 않기 위해서다.

**빈 축은 그 축의 집계와 요약 어디에도 넣지 않는다.** 어떤 제출본의 어떤 축에 증거가 한 건도 없으면(5.4의 빈 축) 그 축의 인원수·평균 강도·제출본 목록에서 그 제출본을 제외하고, 그 축의 요약 입력과 인용 후보에서도 그 제출본의 요구를 제외한다. 빈 축의 판정 글자는 기본 극 51%로 채워진 값이라 실제 선호가 아니다. 그것을 인원수에 섞으면 분포가 기본 극 쪽으로 기울고, 요약에 섞으면 그 요구가 근거 없이 기본 극의 경향으로 서술된다. `type_distribution`은 이와 달리 유형 코드 기준이므로 모든 제출본을 센다.

**평균 강도에는 5.5의 표시 클램프를 적용하지 않는다.** 클램프는 한 사람의 강도 바가 단정적으로 읽히는 것을 막는 표시 규칙이고, 여러 명을 평균한 값은 그 문제를 갖지 않는다. 평균은 저장값(51~100) 기준이며 정수로 반올림한다.

### 3.5 파트 E: 담당자 웹과 보고서

같은 Next.js 앱의 `/admin` 경로에 개발자·담당자 화면을 만든다. 실제 작동 화면이어야 하지만 로그인이나 권한 시스템은 구현하지 않는다. 링크를 외부에 공개하지 않는 것으로 접근을 통제한다.

종합 리포트에는 정확히 다음 다섯 요소가 있어야 한다.

1. 축별 분포와 극별 평균 강도
2. 16유형 분포
3. 축별·극별 정성 요구 요약
4. 인용 ID가 붙은 비식별 대표 인용
5. 축과 유형의 의미를 설명하는 담당자용 정적 해설

담당자용 축·유형 해설은 `web/src/data/admin_guide.json`에 둔다. 16유형 별명·설명은 `city_types.json`을 재사용한다.

**제출본 목록에는 삭제 모드가 있다.** 시범운영에서 시험 삼아 만든 대화와 실제 참여자의 대화가 한 목록에 섞이므로, 담당자가 시연 전에 정리할 수단이 필요하다. 다음 규칙을 지킨다.

- 삭제 모드는 토글이다. 꺼져 있으면 체크상자도 삭제 버튼도 보이지 않는다. 평소 목록을 읽다가 실수로 지우는 일이 없어야 한다.
- 켜면 행 오른쪽 끝에 체크상자가 붙고 여러 건을 고를 수 있다.
- 삭제 직전에 **접근 코드를 다시 입력받고**, 지운 대화는 영원히 사라지며 되돌릴 수 없다는 것을 그 자리에서 알린다. 코드는 서버가 다시 검사한다(6.2).
- 지운 뒤 이미 저장된 분석 결과는 고치지 않는다. 집계는 다음 "자료 업데이트"에서 새로 계산된다.

추가 화면은 다음과 같다.

- 제출본 목록
- 제출본 상세
- 유형 결과
- 개인 보고서
- 원본 대화록
- 분석 실행 버튼

종합 리포트와 대표 인용은 비식별본을 사용한다. 인용 ID로 제출본까지 근거를 추적할 수 있게 한다. 담당자가 간접 정보를 조합해 개인을 추적하지 않는다고 전제하며, 그 이상의 익명성 검증은 구현 범위에 포함하지 않는다.

### 3.6 파트 F: 담당자 대시보드 (Phase 10)

3.5의 세로로 긴 종합 리포트를 **한 화면 대시보드**로 교체한다. 담당자가 계획서를 쓰면서 옆에 띄워 두는 화면이고, 스크롤로 답을 찾는 것이 아니라 **한눈에 보고 궁금한 것을 눌러 파고드는** 방식이어야 한다. 확정 디자인은 저장소 루트의 `dashboard-mock.html`이며 이것이 화면 계약이다.

화면은 1920×1080을 기준으로 만들고 브라우저 크기에 맞춰 통째로 축소한다. 담당자 화면은 회의실 스크린이나 노트북에서 **전체가 한 번에 보여야** 하고, 반응형으로 재배치하면 카드 간의 비교 관계가 깨지기 때문이다. 참여자 화면의 모바일 우선 원칙(2.4)은 여기에 적용하지 않는다.

구성은 다음과 같다.

- 상단: 요약 수치 줄(참여자수, 수집된 세부 요구, 참여 자치구수, 참여 목적)과 AI 분석모드 토글, 제출본 목록·원자료 내려받기·자료 업데이트 버튼
- 왼쪽 열: 자치구별 참여자수 지도, 연령 구성, 조사 개요
- 가운데 열: 계획 부문별 요구, 도시가치 4축, 연령대별 요구사항 교차표, 내가 바라는 도시 유형
- 오른쪽 열: 상세 보기 패널. 아무것도 고르지 않았으면 비워 두고, 왼쪽에서 무엇을 누르면 그 내용을 펼친다

**AI 분석모드는 토글이다.** 켜면 카드마다 "AI 해석" 표시가 나타나고 누르면 그 카드의 수치가 무엇을 시사하는지를 오른쪽 패널에 펼친다. 켜져 있는 동안 상단에 AI 생성물이라는 주의 문구를 띄운다. 해석 문장은 화면을 열 때가 아니라 **분석 실행 때 한 번 생성해 저장한다**(7.7의 `ai_notes`). 화면마다 호출하면 담당자를 기다리게 하고 같은 수치에 다른 문장이 나온다.

**원자료 내려받기는 요구 전수를 CSV로 내보낸다.** 담당자가 계획서에 붙이거나 엑셀에서 다시 세는 것이 실제 업무이고, 화면이 그 길을 막으면 안 된다.

지도는 2026년 7월 개편 경계를 단순화한 형상을 쓴다. 옹진군은 본토에서 멀리 떨어져 있어 같은 축척에 넣으면 본토가 작아지므로 왼쪽 아래 별도 상자에 넣는다.

## 4. 데이터 흐름과 런타임 계약

데이터는 다음 순서로 단방향 이동한다.

1. 메모리 세션에 원본 대화록과 증거 로그 누적
2. 인터뷰 종료
3. 코드로 유형 결과 산출
4. 원본 대화록과 유형 결과로 축별 요구 보고서 생성
5. 참여자가 요구 영역 확인·수정
6. 제출
7. Firestore 영속화
8. 응답 후 백그라운드로 비식별 사본 생성
9. 분석 실행 시 누락된 비식별 사본 보충
10. 코드 집계와 LLM 정성 요약
11. 담당자 리포트 제공

제출 전 데이터는 FastAPI 프로세스 메모리의 임시 상태다. 서버 재시작, 참여자 이탈, 악성 종료로 사라져도 허용한다. 이탈·악성 종료를 포함해 제출되지 않은 인터뷰는 저장하지 않는다. 악성 종료만 이벤트 로그 한 줄을 남긴다.

### 4.1 프론트엔드와 백엔드의 책임

Next.js는 사용자가 보는 모든 화면을 담당한다.

- 진입과 동의
- 본인인증 목업
- 인터뷰 UI
- 스트리밍 표시와 타자기 효과
- 유형 카드
- 보고서 확인·수정
- 공유 카드
- `/admin`

FastAPI는 다음을 담당한다.

- 인터뷰 세션
- Gemini 호출
- 프롬프트 조립
- 증거 수집
- 유형 합산
- 대화록 구조화
- Firestore 저장
- 비식별화와 집계
- 담당자용 데이터 제공

프론트는 백엔드 HTTP API만 호출한다. Gemini 키, 프롬프트, 축 정의, 도메인 지식은 백엔드에만 둔다. 유형 별명·설명만 표시용 프론트 정적 자산으로 둔다.

### 4.2 SSE 스트리밍과 제어 토큰

FastAPI는 Gemini 스트리밍 응답을 SSE로 중계하고 Next.js는 fetch 스트림으로 소비한다.

SSE 이벤트는 두 종류다.

- `delta`: 참여자에게 표시할 응답 텍스트 조각. 데이터는 `{"text": ...}`다.
- `end`: 스트림 종료와 세션 상태 플래그. 데이터는 `{"state": ..., "progress": ...}`다. `state`는 `continue`, `ended`, `aborted` 중 하나이고, `progress`는 3.2의 산식으로 계산한 0~100 정수다. 첫 인사 턴은 0이고 `ended` 턴은 100이다. `aborted`는 세션이 폐기되므로 값에 의미가 없다.

인터뷰어 응답은 첫 인사를 포함해 다음 순서를 따른다.

1. 참여자에게 보이는 텍스트
2. 종료 턴이면 다음 줄에 `[[END_INTERVIEW]]` 또는 `[[ABORT_INTERVIEW]]`를 단독으로 둔 한 줄

정상 진행 턴에는 대화 텍스트에서 응답이 끝나며 제어 토큰을 붙이지 않는다. 제어 토큰은 대화 텍스트 다음 줄에 다른 텍스트 없이 단독으로 출력한다.

**`[[EVIDENCE]]` 머신 트레일러는 폐지했다.** 증거는 인터뷰어 응답에 실려 오지 않고 별도의 채점 호출이 돌려준다(3.2, 5.4). 인터뷰어는 증거 형식을 알 필요가 없고 `system.md`에도 그 규정이 없다.

백엔드는 스트림에서 `[[`를 감지하면 이후 출력을 버퍼링해 프론트에 보내지 않는다. 제어 토큰은 세션 상태 전환에만 쓴다.

**채점 호출의 실패는 대화를 죽이지 않는다.** 채점 호출이 실패하면 그 턴의 증거를 0건으로 두고 실패 로그만 남기며, 인터뷰어 호출과 참여자 화면은 영향을 받지 않는다. 재시도와 폴백은 만들지 않는다(9.6). 한 응답 안에 대화와 트레일러가 함께 있던 구조에서는 형식이 어긋나면 같은 스트림에서 증거가 통째로 폐기됐는데, 분리 후에는 두 실패가 서로 독립한다.

응답이 계약에 맞지 않으면 해당 증거를 버리고 실패 로그만 남긴다. 불량 증거를 합산에 포함하지 않는다.

## 5. 도시 선호 유형화 계약

### 5.1 성격과 표기

진단 대상은 참여자의 성격이 아니라 참여자가 바라는 2040년 인천의 수요와 지향이다. 심리측정 검사가 아니라 참여 흥미와 정책 전달력을 높이기 위한 진단 장치다.

일상어로 정의한 4개 축을 조합해 16개 유형을 만든다. 각 축은 **하나의 질문 차원**만 갖는다.

**MBTI 글자(E/I·S/N·T/F·J/P) 차용은 폐기했다.** 2026-07-29 개편 이전에는 MBTI 형식을 빌렸으나, "성격 MBTI와 완전히 분리한다"(2.1)면서 MBTI 글자를 쓰는 자기모순이 있었고 그 때문에 참여자 화면에서 축 라벨을 일상어로 가리는 우회를 이미 하고 있었다. 이번 개편은 그 우회를 정식 설계로 승격시킨 것이다. 구 축의 극 정의가 정책 추상어("관문·글로벌", "성장관리")여서 자유 발화에서 태깅이 어려웠던 문제도 함께 해소한다.

**4글자 유형 코드는 참여자에게 노출하지 않는다.** 결과 화면, 공유 카드, 저장 이미지, 저장 파일명, 공유 시트 제목 어디에도 코드와 개별 극 글자를 표시하지 않는다. 코드는 내부 판정·저장·집계와 담당자 화면에서만 쓰는 식별자다. 참여자 화면에서 코드가 하던 식별자 역할은 일상어 라벨 배지 4개가 대신한다(5.5).

축 설계는 인천·서울·부산의 2040 도시기본계획에서 반복되는 도시가치 대립에서 도출한다. "현재 유형"이 아니라 "바라는 유형"을 표현한다.

### 5.2 확정된 4축 v2

| 순서 | 축 ID | 축 이름 | 판별 차원 | 극 1 | 극 2 | 동률 기본 극 |
|---|---|---|---|---|---|---|
| 1 | `AC` | 생활 리듬 | 하루의 템포 | `A` 활기찬 | `C` 여유로운 | C |
| 2 | `UN` | 선호 공간 | 눈앞의 풍경 | `U` 도시적인 | `N` 자연과 가까운 | N |
| 3 | `OW` | 우선 가치 | 무엇을 먼저 챙기는가 | `O` 기회가 많은 | `W` 서로 돌보는 | W |
| 4 | `FH` | 발전 가치 | 변화를 받아들이는 방식 | `F` 새로움을 여는 | `H` 이야기를 이어가는 | H |

유형 코드는 네 자리 순서 고정으로 `AUOF`~`CNWH`, 16종이다. 여덟 극 글자가 서로 겹치지 않으므로 코드에 모호성이 없다. 이 불변식은 `api/app/axes.py`가 검증한다.

각 축이 다루는 것과 다루지 않는 것은 다음과 같다.

- **생활 리듬**: 활동과 이벤트의 빈도·속도만 다룬다. 시설의 종류나 근접성은 선호 공간, 새것의 등장은 발전 가치가 맡는다.
- **선호 공간**: 물리적 환경과 근접성만 다룬다. 그 시설을 얼마나 자주 쓰는지는 생활 리듬, 새것인지 옛것인지는 발전 가치가 맡는다.
- **우선 가치**: 성장·기회·효율과 복지·돌봄·관계 사이의 배타적 자원 배분을 다룬다. 시간·비용·번거로움을 줄여 달라는 발화는 산업이나 복지를 언급하지 않아도 `O` 증거다. 이웃·단골·아지트처럼 사람 사이의 연결을 말하는 발화는 복지 제도를 언급하지 않아도 `W` 증거다. **이 배타성은 채점자가 증거를 가르는 기준이지 참여자에게 던질 프레임이 아니다**(5.3).
- **발전 가치**: 변화를 받아들이는 방식만 다룬다. 변화의 결과로 하루가 얼마나 붐비는지는 생활 리듬, 그 공간이 도심인지 자연인지는 선호 공간, 그 변화가 누구의 이익을 위한 것인지는 우선 가치가 맡는다.

2026-08-11 팀 검수로 극 정의 서술을 갱신했다. 팀 문안에서 출발하되 위의 축 간 경계(5.3 원칙 1의 상호배타)를 침범하는 항목만 대체했다 — A극의 시설 다양성(→U)과 새 가게 등장(→F), C극의 산책길(→N)과 느린 변화(→H), U극의 새 서비스 유입(→F), F극의 창업·일자리 지원(→O), H극의 "기존 주민이 떠나지 않도록 배려함"(5.3이 명시 금지). 같은 문안을 `axes.md`의 정의 불릿과 담당자 해설(`admin_guide.json`)에 함께 써서 소개 문구와 채점 기준이 갈라지지 않게 한다. 채점 앵커와 가중치 규칙은 바꾸지 않았다.

축 계보는 다음과 같다. 담당자 해설과 발표 자료에 기록한다. 이것이 없으면 심사위원에게 "감성 테스트"로 읽힌다.

- 생활 리듬 ← 구 E/I (개방·교류·활력 vs 정주·정온·내밀)
- 발전 가치 ← 구 S/N (현실·기존 자원 vs 미래·혁신·첨단)
- 우선 가치 ← 구 T/F (효율·기능·경쟁력 vs 관계·돌봄·정서)
- 선호 공간 ← 신설. 인천 계획의 해양·녹지·수변 의제를 반영
- 구 J/P (계획·정돈 vs 유연·자생)는 **의도적으로 제외**했다. 실측에서 태깅이 가장 희소했던 축이고, 행정 개념이라 일상어 인터뷰와 궁합이 나쁘다. 운영 방식에 대한 수요는 주제 태그(7.4)와 축별 요구 보고서로 잡는다.

축 정의와 축·극·강도별 예시 발화인 채점 앵커는 `api/prompts/axes.md`를 단일 원천으로 삼는다. 인터뷰 루브릭과 증거 태깅이 이 파일을 함께 참조한다. 같은 파일의 기계 판독 JSON 블록(축 순서·극·기본 극·표시명)은 백엔드 코드 상수의 단일 원천이기도 하며 `api/app/axes.py`가 이를 파싱한다. **축 이름이나 극 글자를 다른 파일에 리터럴로 두지 않는다.** `scoring`·`trailer`·`prompts`·`session`·`report`·`main`이 모두 `axes.py`만 참조한다.

`display` 값은 축의 한국어 이름이다. **인터뷰어에게는 이 값도 전달하지 않는다**(2026-08-12 개편). 표시 이름 하나만으로도 두 극이 역산되기 때문이며, 지금은 담당자 화면과 백엔드 로그에서만 쓰인다.

### 5.3 채점 앵커 설계 원칙

축 교체의 성패는 앵커에 달려 있다. `axes.md`는 다음 원칙을 지킨다.

1. **축 간 상호배타.** 각 축의 앵커는 그 축의 판별 차원에만 반응한다. 같은 장면이 두 축의 앵커에 나타나면 안 된다. 소재가 겹칠 때는 참여자가 그것을 **어떤 문제로 말했는지**로 가른다(조용한 동네 → `C` vs `N`, 밀려나지 않는 동네 → `W` vs `H`, 새 가게 → `A` vs `U` vs `F`, 스마트 교통 → `O` vs `F`).
2. **양극 대칭.** 여덟 극 모두 가중치 1·2·3 앵커를 정확히 하나씩 갖는다. 구 축에서 T/F 쏠림의 원인은 F에만 앵커가 많고 전용 확장 노트가 있던 비대칭이었다. `O`·`W`는 같은 분량의 해설 노트를 각각 하나씩 갖는다.
3. **부정 앵커 포함.** 축마다 "증거가 아닌 발화" 예시를 3건씩 둔다. 유형은 현상 서술, 인터뷰어 표현에 대한 단순 동의, 앞 턴 부연이다. 판정 일관성은 긍정 예시보다 경계 예시에서 갈린다.
4. **교차 조합 앵커 포함.** 상관 쏠림을 끊기 위해 AN형(활기+자연: 수변 레저, 공원 축제)과 CU형(여유+도심: 조용한 콤팩트 생활권) 발화 예시를 명시한다. "활기찬 = 도시적인"이 아님을 태깅 LLM에게 보여 주는 장치다.
5. **지명 의존 축소.** 여덟 극의 가중치 1 앵커에 인천 지명이 하나도 없다. 취지가 같으면 지명이 다르거나 없어도 동일하게 태깅한다는 단서를 함께 둔다.

`H` 극에는 "기존 주민이 떠나지 않도록 배려함"을 두지 않는다. 이 장면은 `W` 극의 "소외되는 사람이 없도록 배려"와 정면으로 겹쳐, 두 축이 가장 자주 충돌하는 지점에서 원칙 1이 깨지기 때문이다. 대신 "밀려나지 않는 동네"를 취지에 따라 `W`와 `H`로 가르는 판별 규칙을 둔다.

### 5.4 증거 기반 판정

대화록 전체를 한 번에 평가하는 홀리스틱 LLM 판정은 실행마다 기준이 흔들리므로 사용하지 않는다. 개방형 대화에서 미시 증거를 추출하고 코드가 이를 결정적으로 합산한다.

**인터뷰어가 답의 후보를 만들어 내밀지 않는다.** 이것이 가장 강한 금지다. 축 이름을 말하지 않아도, 두 개든 세 개든, 마지막을 열어 두었든, 인터뷰어가 지어낸 항목 안에서 참여자가 답하는 순간 그 말은 참여자의 생각이 아니라 인터뷰어가 만든 틀에 대한 반응이 된다. 이 금지는 최소한 세 형태로 나타난다.

- 두 극을 나란히 놓는 질문 ("활기찬 동네가 좋으세요, 조용한 동네가 좋으세요?")
- **우선순위나 예산 배분을 두 항목으로 제시하는 질문** ("한정된 예산에서 일자리와 복지 중 어디가 먼저일까요?"). 문장이 길고 정중해도, 후보를 참여자가 앞서 한 말에서 가져왔더라도, 두 개로 압축하고 예산 프레임을 씌우면 같은 위반이다.
- 예시라는 이름으로 후보를 흘리는 질문

우선순위는 참여자가 이미 꺼낸 것들 **전체** 안에서 스스로 고르게 한다. 트레이드오프도 참여자가 이미 말한 바람 하나를 붙들고 그 대가를 감수하겠는지 묻는 형태여야 한다.

새 축의 극 이름이 모두 일상어라서 이 위험은 구 축보다 크다. 금지 예문 "활기찬 동네가 좋으세요, 조용한 동네가 좋으세요"는 1축의 두 극 이름과 글자 그대로 같다.

**이 위험을 다루는 주된 수단은 이제 금지 문구가 아니라 호출 분리다.** 인터뷰어가 극 이름·정의문의 대비·앵커 문장을 아예 받지 않으므로 옮길 것이 없다. `system.md`의 후보 제시 금지는 인터뷰 규범으로 남기고, 채점 기준을 질문으로 옮기지 말라는 계열의 문장은 `axes.md`에서 걷어냈다. 읽지 않는 문서를 옮기지 말라는 지시는 존재 이유가 없기 때문이다.

다만 분리가 유혹을 완전히 없애지는 못했다. 운영 지시 블록이 싣는 축의 **표시 이름 하나만으로도** 두 극이 역산되어 이지선다가 생성된다는 것이 격리 점검에서 확인됐다(14장). **2026-08-12에 이 경로를 닫았다.** 운영 지시는 이제 축 이름 대신 검수된 질문 문장 하나를 통째로 내려보낸다(3.2).

**채점은 인터뷰어와 분리된 전용 호출이 수행한다**(3.2). 그 계약은 다음과 같다.

- **시스템 지시**는 `axes.md` 단독이다.
- **입력**은 세 키를 가진 JSON이다. `transcript`(누적 대화 기록, `turn`·`role`·`text`만), `evidence_log`(누적 증거 목록), `participant_utterance`(이번에 태깅할 참여자 발화).
- **출력**은 증거 배열 하나이며 `response_schema`로 형식을 강제한다. 필드는 축, 극, 가중치, 원문 인용 넷이다.
- 축과 극의 대응은 스키마로 표현할 수 없으므로 백엔드가 항목마다 검증한다. 검증에 실패한 항목만 버리고 나머지는 살린다.

**누적 대화 기록을 함께 넘기는 이유**는 발화 하나만 떼어 놓으면 채점할 수 없는 경우가 많기 때문이다. 참여자가 "그것도 좋지만 저는 이쪽이요"라고 말했을 때 앞을 모르면 무엇을 말하는지 알 수 없다.

**누적 증거를 넘기는 목적은 중복 태깅 방지 하나로 한정한다.** 이 한정을 `axes.md`에 명시한다. 누적 증거를 본 채점자가 "이 축은 한쪽 극만 있으니 반대쪽도 찾아보자"고 균형을 맞추기 시작하면 채점이 오염된다. 단극 관측은 참여자가 실제로 일관되게 말했다는 신호여야 하며, 채점자가 인위적으로 지우면 5.5의 표시 강도 보정과 시범운영 지표가 함께 무의미해진다. 실측에서 이 충동은 실재했고 이 한정 문단이 막았다.

**채점은 증분으로 한다.** 매 턴 마지막 참여자 발화 하나만 태깅하며, 대화 전체를 다시 채점해 증거 목록을 통째로 갱신하지 않는다. 이유는 셋이다. 첫째, 매 턴 전면 재채점은 이 절이 배제한 홀리스틱 판정을 매 턴 반복하는 것에 가깝다. 둘째, 전면 갱신이면 각 증거가 어느 턴 발화인지 모델이 지정해야 하는데 이는 아래의 턴 번호 결정과 부딪친다. 셋째, 매 턴 판정이 달라져도 관측되는 것은 마지막 상태뿐이라 흔들림을 관찰할 방법이 없다.

증분의 대가도 기록해 둔다. 어떤 턴에서 인용이 한 번 느슨해지면 그것이 누적 증거에 남아 **이후 모든 턴의 선례로 작동한다.** 실측에서 채점자가 누적 증거의 느슨한 인용을 보고 원문 그대로 규칙이 실제로는 느슨하게 운용된다고 판단했다.

증거 한 건은 축, 극, 가중치, 사용자 발화 원문 인용으로 구성한다. 가중치는 1(약한 증거), 2(보통 증거), 3(강한 증거)다. 턴 번호는 백엔드가 저장 시점에 부여하며 LLM에게 요구하지 않는다. 모델이 세어 낸 값은 어차피 백엔드 값으로 덮이는데, 그 필드의 형식이 어긋나면 나머지가 멀쩡한 증거까지 통째로 폐기되기 때문이다.

**가중치 3은 참여자가 그 우선순위나 트레이드오프를 스스로 꺼냈을 때만 해당한다.** 인터뷰어가 두 가지를 나란히 놓고 물어 받아 낸 답은 가중치가 낮아지는 것이 아니라 **증거가 아니다.** 유도해서 얻은 답이 아무 값도 갖지 않아야 인터뷰어에게 그 질문이 지름길이 아니게 된다. 다만 같은 발화 안에 인터뷰어가 제시하지 않은 참여자 자신의 말이 있으면 그 부분은 태깅한다.

증거가 없는 턴은 빈 배열을 출력한다. 채점 기준은 `axes.md`의 채점 앵커로 고정한다.

인터뷰 종료 시 코드는 각 축의 두 극에 대해 증거 가중치 합을 계산한다.

- 극 점수는 해당 극 증거의 가중치 총합이다.
- 점수가 큰 극을 판정 글자로 정한다.
- 원시 강도 비율 `x`는 `승자 점수 ÷ 축 총점 × 100`으로 계산한다.
- 정수 강도는 파이썬 기본 `round`를 사용하지 않고 `floor(x + 0.5)` 방식으로 반올림한다.
- 반올림한 값이 51 미만이면 51로 클램프한다.
- 두 극의 표시 백분율 합은 100이고 동률은 표시하지 않는다.

점수가 동률이면 증거 개수가 더 많은 극, 개수도 같으면 기본 극(`C`/`N`/`W`/`H`)으로 결정한다. 해당 축의 증거가 하나도 없으면 기본 극을 51%로 판정하고 로그를 남긴다.

**축 결과에는 `evidence_count`와 `empty_axis`가 함께 실린다**(2026-08-12). 빈 축의 51과 박빙의 51은 표면값이 같아 서로 구분되지 않았고, 그 구분이 없으면 프롬프트 A/B의 기준선 자체가 오염된다. **`empty_axis`는 참여자 응답에도 실린다** — 화면이 두 51을 갈라 표시해야 하기 때문이다(5.5). `scores`와 `evidence`는 서버 전용으로 남는다.

각 축의 판정 출력은 판정 글자, 강도 51~100, 두 극의 가중치 점수, 발화 원문·턴·가중치가 포함된 증거 목록을 포함한다. 유형은 네 축의 판정 글자를 이어 붙여 만든다. 참여자는 유형, 강도, 증거를 수정할 수 없다.

증거 태깅 자체는 LLM 판단이므로 완전히 재현되지는 않는다. 다만 한 번의 큰 판단 대신 여러 미시 판단을 합산하고, 합산·반올림·동률·빈 축 처리를 코드로 고정해 변동 범위를 줄인다.

### 5.5 표시 강도 보정 (단극 관측과 빈 축)

두 축이 같은 이유로 잘못 읽힌다. 한쪽은 강도가 위로 튀고 다른 쪽은 근거 없는 값이 근거 있는 값처럼 보인다.

어떤 축에서 한쪽 극 증거가 0건이면 강도가 100%로 계산된다. 공식상 옳지만 단정적으로 읽힌다.

- **판정·저장값(51~100)은 불변이다. 표시만 보정한다.**
- 참여자·담당자 화면 공통으로, 반대 극 증거가 0건인 축은 표시 강도를 95로 클램프한다. 96~100 표시는 양극이 모두 관측된 축에서만 가능하다. 클램프 값 95는 튜닝 가능한 프론트 상수다.
- 참여자 응답에는 극 점수가 없으므로 단극 여부는 `strength === 100`으로 판별한다. 강도 공식상 반대 극 점수가 0일 때만 100에 도달하므로 정확한 판별이다. 참여자·담당자 화면이 같은 헬퍼를 쓴다.
- 담당자 제출본 상세에는 해당 축에 "단극 관측" 표기를 붙여 데이터의 성격을 알 수 있게 한다.

**증거가 0건인 축(`empty_axis`)에는 백분율을 보이지 않는다**(2026-08-12 결정). 그 축의 51은 계산 결과가 아니라 기본 극을 채운 값이고, 숫자를 그리는 순간 없는 근거를 있는 것처럼 만든다. 5.4가 이미 빈 축을 집계에서 빼고 있으므로 표시만 남아 있던 오표시다.

- 참여자 유형 카드는 그 축의 강도 바와 두 백분율을 그리지 않고 "이번 대화에서는 이 이야기가 나오지 않았어요"만 둔다. **극 라벨 배지 네 개는 그대로 둔다** — 유형은 네 글자로 성립하고, 기본 극도 판정 결과이기 때문이다.
- 담당자 제출본 상세는 강도 자리에 "강도 없음"을 두고, 저장된 51이 기본 극을 채운 값이며 집계에서 제외된다는 것을 함께 적는다.
- 판정 이유 자리는 코드가 채우고 축별 요구는 빈 배열이 된다(7.4). 표시 세 곳이 같은 사실을 말한다.

- 커버리지를 극 단위로도 축 단위로도 인터뷰어에게 내리지 않는다. 백엔드가 부족한 축을 보고 **그 축의 검수된 질문 문장**을 고를 뿐이며, 어느 축을 위한 질문인지는 전달하지 않는다.

### 5.6 산출물

참여자 산출물은 다음과 같다.

- 유형별 정형 일러스트 이미지, 유형 별명, 한 줄 설명
- 일상어 라벨 배지 4개 (예: 활기 · 도시 · 기회 · 새로움)
- 축별 강도 바 (일상어 라벨, 단극 클램프 적용, 빈 축은 백분율 없이 문구만)
- 축별 판정 이유
- 축별 요구 보고서
- 이미지로 저장하고 공유할 수 있는 유형 카드

**4글자 코드는 어디에도 포함하지 않는다**(5.1). 참여자 화면에 보이는 축 극 라벨은 일상어 표시 문구를 쓰며 `web/src/lib/city-axes.ts`가 그 원천이다. 5.2의 축 정의, `axes.md`의 채점 앵커와 증거 태깅 기준은 이 표시 문구의 영향을 받지 않는다.

유형 카드의 주인공은 유형 이미지다. 이미지 자산은 번들 동일 출처(`web/public/city-types/`)에 둔다. 개발 단계에서는 16종 모두 플레이스홀더 SVG로 진행하고 최종 일러스트는 추후 삽입한다. **이미지 로딩에 실패하면 결과 화면은 별명 타이포그래피 카드로 폴백하고 흐름을 끝내지 않는다.** 공유 카드 래스터화 시에는 폰트에 적용 중인 검증 규칙(9.3)을 이미지에도 동일 적용한다 — 카드가 이미지를 표시 중인데 그것이 실제로 로드·임베드되지 않았다면 그 공유·저장 시도를 실패로 끝내고, 이미 폴백 상태라면 임베드할 이미지가 없으므로 정상 내보낸다.

담당자 산출물은 다음과 같다.

- 축별 인원 분포
- 극별 평균 강도
- 16유형 분포
- 축별·극별 정성 요약
- 비식별 대표 인용
- 축과 유형의 의미 설명, 축 계보(5.2), 구 J/P 제외 사유

16유형 콘텐츠는 `web/src/data/city_types.json`에 둔다. 각 항목의 스키마는 `code`, `nickname`, `description`, `image` 네 필드로 고정한다.

**별명 검수 기준**: 별명만 보고 4축 극을 역산할 수 있어야 한다. 다만 카드 전면의 라벨 배지 4개가 이미 정확한 식별자이므로 역산 부담을 별명이 혼자 지지 않는다. 16개가 같은 문법 구조를 공유하는 기계적 조합이어서는 안 된다. 무보상 15턴 인터뷰의 유일한 완주 보상이자 SNS 공유 소재이기 때문이다.

2026-08-11 팀 검수로 별명 16종을 확정했다. 팀 문안 12종을 그대로 쓰고, 핵심어가 겹치거나("정원도시" 2회, "빛+기회" 2회) 별명에서 극이 역산되지 않던 4종(AUOF·ANOH·ANWH·CNOF)만 Claude 대안으로 바꿨다. 한 줄 설명은 새 별명 기조로 다시 쓴 Claude 초안이며 팀 재검수 대상이다. 배지 8종은 **활기 · 여유 · 도시 · 자연 · 기회 · 포용 · 새로움 · 이야기**로 확정했다. W극 배지를 "포용"으로 정한 이유: "복지"는 제도로 좁고 "돌봄"은 돌봄 서비스로 좁게 읽히는 반면, 포용은 제도·이웃 관계·소외 없는 배려를 한 단어로 덮고 O극 "기회"와 대비가 선다. 명칭 "도시TI" 개명안은 MBTI 분리 원칙(2.1)에 따라 채택하지 않고 "도시 유형"을 유지한다.

## 6. API 계약

운영 엔드포인트와 별개로 다음 두 개를 둔다.

- `GET /health`
  - 서비스 상태 확인
  - Cloud Run 배포 후에도 유지한다

- `POST /api/dev/echo`
  - Phase 0 검증 전용이다
  - 입력 텍스트를 Gemini에 한 번 보내고 응답과 사용 모델명을 돌려준다
  - `프론트 → 백엔드 → Gemini` 왕복이 실제로 연결되었음을 확인하는 것이 유일한 목적이다
  - Phase 1에서 `/api/sessions/*`가 생기면 제거한다

### 6.1 참여자 API

- `POST /api/sessions`
  - 입력: 본인인증 목업에서 받은 출생연도
  - 검증: 1900~2026 범위의 정수인지 검사
  - 출력: `session_id`

- `POST /api/sessions/{id}/start`
  - 기능: LLM의 첫 인사 생성
  - 채점 호출: 직전 사용자 발화가 없으므로 하지 않는다
  - 출력: SSE 스트림

- `POST /api/sessions/{id}/messages`
  - 입력: 사용자 발화 텍스트
  - 기능: 인터뷰어 호출과 채점 호출을 동시에 시작하고, 인터뷰어 스트림을 중계하는 동안 채점을 끝낸다
  - 증거는 그 턴의 `end` 이벤트 전에 세션에 누적되어 다음 턴의 커버리지 계산에 반영된다
  - 출력: SSE 스트림

- `DELETE /api/sessions/{id}/turns/last`
  - 기능: 직전 한 턴 되돌리기 (3.2)
  - 결과: 마지막 사용자·AI 메시지 쌍과 그 턴 번호의 증거를 세션에서 제거
  - 조건: 세션이 `active`이고 첫 인사 외의 턴이 하나 이상 있을 때만. 아니면 409
  - 출력: 없음(204). 되돌린 뒤의 재전송은 프론트가 `/messages`로 한다

- `DELETE /api/sessions/{id}`
  - 기능: "인터뷰 그만두기" 이탈 처리
  - 결과: 세션 폐기, 저장 없음

- `POST /api/sessions/{id}/result`
  - 기능: 유형 코드 합산과 원본 대화록 직접 구조화 트랙을 병렬 실행
  - 출력: 유형 결과와 개인 보고서 JSON
  - 참여자 응답에서는 유형 결과의 `evidence`·`scores`와 보고서 요구의 `quotes`를 제외

- `POST /api/sessions/{id}/result/revise`
  - 입력: 하나 이상의 선택 문장 위치와 참여자 의견
  - 문장 위치: `axis`, `demand_id`, 문장 인덱스
  - 기능: 선택 문장과 의견을 입력으로 사용해 `axis_demands` 4축 전체를 동일한 스키마로 재생성
  - 출력: 4축 전체의 요구 영역이 재생성된 개인 보고서 JSON
  - `summary`와 `type_result`는 불변

- `POST /api/sessions/{id}/submit`
  - 기능: Firestore 영속화와 세션 종료
  - 출력: `submission_id`만 반환
  - 공유 카드: 프론트가 결과 단계에서 보관 중인 `type_result` 상태로 렌더링

### 6.2 담당자 API

- `GET /api/admin/submissions`
  - 제출본 목록
  - 출력: 제출본 요약 배열. 각 항목은 `submission_id`, `submitted_at`, `nickname`, `region`, `type_code`, `turn_count`, `revision_count`
  - 정렬: `submitted_at` 내림차순

- `GET /api/admin/submissions/{id}`
  - 유형 결과, 개인 보고서, 원본 대화록을 포함한 제출본 상세
  - 출력: 7.5의 `submissions` 문서 완전본. 참여자 응답과 달리 `evidence`·`scores`·`quotes`를 제외하지 않는다
  - 존재하지 않는 ID는 404

- `DELETE /api/admin/submissions/{id}`
  - 제출본 한 건을 저장소에서 지운다. 되돌릴 수 없다
  - 요청 본문에 접근 코드를 담고, 서버가 `access_code`와 대조해 다르면 403이다. 헤더의 코드와 **따로** 검사한다. 헤더는 화면에 이미 들어와 있는 값이라 삭제 의사를 확인하지 못하기 때문이다(6.5, 3.5)
  - 존재하지 않는 ID는 404
  - 여러 건을 지울 때는 프론트가 이 호출을 건마다 보낸다. 목록 삭제 전용 엔드포인트를 따로 만들지 않는다

- `POST /api/admin/analysis/run`
  - 참여자 흐름과 분리된 분석 배치 파이프라인을 admin 요청 안에서 동기 실행
  - 출력: `run_id`만 반환한다. 결과 본문은 프론트가 이어서 `latest`로 받는다
  - 저장된 제출본이 하나도 없으면 409를 반환한다 (3.4)

- `GET /api/admin/analysis/latest`
  - 최신 종합 리포트 조회
  - 출력: 7.6의 `analysis_runs` 문서. 분석 결과가 하나도 없으면 404이며 프론트는 빈 상태를 표시한다

### 6.3 HTTP 오류 계약

- 존재하지 않는 세션에 대한 호출은 404를 반환한다.
- 현재 세션 상태에 맞지 않는 호출은 409를 반환한다.
- 그 외 서버 오류는 500을 반환한다.
- 자동 재시도와 복구 로직은 구현하지 않는다.

### 6.4 개발자 모드

결과 화면, 보고서, 유형 카드, 담당자 리포트를 확인하려면 그때마다 인터뷰를 완주해야 한다. 한 번에 십수 턴이 걸리므로 UI를 반복해 다듬는 동안 이 비용을 감당할 수 없다. 그래서 인터뷰 중간을 건너뛰는 개발자 모드를 둔다. 이 기능의 이름은 어디에서나 "개발자 모드"로 통일한다.

- `GET /api/dev/fixtures`
  - 사용 가능한 대화 묶음집 목록과 각 묶음집의 설명

- `POST /api/dev/sessions/{id}/load`
  - 입력: 대화 묶음집 이름
  - 기능: 진행 중인 세션의 대화록과 증거 로그를 저장된 묶음집의 것으로 교체하고, **세션을 곧바로 종료 상태로 바꾼다**
  - 출력: 주입된 원본 대화 메시지 배열. 화면이 대화 맥락을 이어서 보여주려면 백엔드가 돌려줘야 한다

개발자 모드는 세션을 새로 만들지 않고 이미 만들어진 세션에 대화를 주입한다. 참여자와 똑같이 시작 화면을 거쳐 인터뷰에 들어온 뒤, 대화 도중 원하는 시점에 완성된 대화를 불러오는 방식이다. 그래야 시작 화면과 세션 생성 경로가 개발자 모드 때문에 갈라지지 않는다.

대화 묶음집은 이미 완주된 인터뷰다. 불러온 뒤에 마무리 발화를 다시 입력하게 하는 것은 같은 일을 두 번 하는 셈이고, LLM 호출을 한 번 더 쓸 뿐 얻는 것이 없다. 그래서 불러오는 즉시 세션을 종료 상태로 두고, 3.2의 "대화 종료하기" 버튼이 곧바로 나타나게 한다. 결과 화면으로 넘어가는 시점은 여전히 사람이 그 버튼을 눌러 정한다.

진입점은 인터뷰 화면 안에 둔다. 시작 화면에는 두지 않는다. 참여자 흐름의 첫 화면이 개발용 요소로 오염되지 않게 하기 위해서다. 인터뷰 화면에서도 눈에 띄지 않는 작은 버튼으로 두어 참여자용 요소와 혼동되지 않게 한다.

대화 묶음집은 손으로 지어내지 않는다. **실제 인터뷰를 완주시킨 결과를 그대로 저장한다.** 참여자 역할은 두 방식 중 하나로 세운다. 하나는 `regen_fixtures.py`의 가상 참여자(같은 Gemini 호출)이고, 다른 하나는 **프론티어 모델이 배정된 인물로 직접 대화하는 것**이다. 후자는 2026-08-10 인터뷰 실험에서 도입했다(15장). 재현성은 전자가 낫지만 발화의 자연스러움과 결함 발견력은 후자가 확실히 낫다. 어느 쪽이든 지어낸 대화로 만든 결과는 검증에 쓸 수 없다는 원칙은 같다. 지어낸 대화로 만든 결과는 실제 결과와 다르므로 검증에 쓸 수 없다. 서로 다른 유형이 나오도록 성향이 대비되는 묶음집을 여러 개 준비하고, **축이 함께 묶여 보이는 착시를 검사할 교차형 인물을 최소 하나 포함한다**(5.3의 원칙 4).

재생성은 `api/scripts/regen_fixtures.py`가 수행한다. 서버 없이 인터뷰 엔진을 직접 구동해 시뮬레이션 참여자(별도 Gemini 호출)와 실제 인터뷰를 완주시키고, 이어서 결과 생성까지 통과시켜 묶음집과 예시 제출본을 한 번에 만든다. 인물 정의는 `api/scripts/personas.json`에 두어 코드와 분리한다. 개발자 모드 묶음집 목록이 `api/fixtures/*.json`을 전부 열거하므로 인물 파일을 그 디렉터리에 두지 않는다. 실행은 다음 한 줄이며 **실제 Gemini 크레딧을 소모한다.**

```
cd api && PYTHONPATH=. uv run python scripts/regen_fixtures.py
```

**개발 중에는 인물 한 명만 재생성한다.** 이름을 인자로 주면 그 인물만 돌아간다(`... regen_fixtures.py sion`). 프롬프트를 고칠 때마다 세 명을 전부 돌리면 크레딧이 빠르게 소모되는데, 프롬프트 변화가 인터뷰어 행동에 미치는 영향은 한 명으로도 관찰된다. **세 명 전부 재생성은 서비스가 완성된 뒤 1회로 미룬다.** 담당자 화면의 집계가 여러 인물을 필요로 하는 시점이 그때다.

축 정의나 채점 앵커를 바꾸면 기존 묶음집의 증거는 무효가 된다. `score_type`이 알 수 없는 축의 증거를 조용히 걸러내므로 에러 없이 모든 축이 기본 극·강도 51로 나온다. 축을 바꾼 뒤에는 반드시 이 스크립트를 다시 돌린다.

주입된 세션은 인터뷰를 실제로 진행한 세션과 구별되지 않는다. 따라서 이후의 종료, 유형 판정, 보고서 생성, 수정, 제출 경로를 특별 취급 없이 그대로 탄다.

담당자 웹에도 같은 성격의 지름길을 둔다. 담당자 화면을 확인하려면 그때마다 참여자 흐름을 완주해 제출해야 하는데, 이 비용 역시 반복 작업에서는 감당할 수 없기 때문이다.

- `POST /api/dev/submissions/seed`
  - 기능: `api/fixtures/submissions/*.json`의 예시 제출본 문서를 store에 적재한다
  - 각 파일의 고정 `submission_id`를 그대로 쓰므로 반복 호출해도 중복이 쌓이지 않는다
  - 출력: 적재된 `submission_id` 배열

예시 제출본 문서는 손으로 지어내지 않는다. 개발자 모드 묶음집을 실제 파이프라인(대화 주입 → 결과 생성 → 제출)에 한 번 통과시켜 store에 저장된 7.5 완전본 문서를 그대로 파일로 떠 둔 것이다. 이후 적재에는 Gemini 호출이 없다. 프론트는 dev mode에서만 `/admin/submissions`에 "예시 제출본 불러오기" 버튼을 보여 준다.

개발자 모드는 `dev_mode` 환경변수가 켜져 있을 때만 활성화한다. 꺼져 있으면 위 경로들을 아예 등록하지 않는다. **배포에서도 켠다.** 팀이 배포된 서비스로 계속 시험하고 개선안을 찾기 때문이다. 아무나 쓰지 못하게 막는 것은 이 스위치가 아니라 6.5의 접근 코드다.

### 6.5 접근 코드

개발자 모드와 담당자 웹은 **하나의 접근 코드**로 막는다. 계정도 세션도 만들지 않는다.

- 코드는 `access_code` 환경변수 하나다. 자릿수와 문자 종류를 코드가 강제하지 않으므로, 늘리고 싶으면 값만 바꾸면 된다. 운영에서는 네 자리 숫자를 쓴다.
- 값이 비어 있으면 게이트가 없다. `gcp_project_id`와 같은 방식이며, 자동화 테스트가 코드를 몰라도 돌게 하기 위해서다.
- 값이 있으면 `/api/dev/*`와 `/api/admin/*`의 모든 요청에 `X-Access-Code` 헤더가 있어야 하고 값이 일치해야 한다. 없거나 다르면 403이다.
- **참여자 API는 막지 않는다.** 참여자는 코드를 모르며 알아서도 안 된다.

**검사는 반드시 백엔드가 한다.** 프론트에서만 확인하면 아무 보호가 되지 않는다. `/api/admin/submissions`를 직접 호출하면 대화록 원문이 그대로 나오기 때문이다. 화면의 코드 입력은 편의일 뿐이고 실제 방어선은 서버다.

프론트는 다음과 같이 다룬다.

- 인터뷰 화면의 개발자 모드 진입 버튼은 항상 보인다. 누르면 코드를 묻고, 맞으면 묶음집 목록이 열린다.
- `/admin`은 코드를 입력해야 내용을 보여준다.
- 통과한 코드는 `sessionStorage`에 두고 해당 요청의 헤더로 보낸다. 탭을 닫으면 사라진다.
- **코드 확인 전용 엔드포인트를 만들지 않는다.** 각 화면이 원래 하던 첫 호출(`/api/dev/fixtures`, `/api/admin/submissions`)이 403이면 코드가 틀린 것이다.
- 빌드 시점 플래그 `NEXT_PUBLIC_DEV_MODE`는 없앤다. 버튼을 감출 이유가 사라졌고, 노출 여부를 두 곳에서 정하면 어긋난다.

**이 코드의 한계를 분명히 해 둔다.** 네 자리 숫자는 만 가지뿐이라 자동화된 시도에 오래 버티지 못한다. 지나가다 열어보는 사람은 막지만 작정한 사람은 막지 못한다. 링크를 공개하지 않는다는 전제와 함께 시연용 프로토타입 범위에서만 유효한 조치다.

## 7. 데이터 스키마

### 7.1 메모리 세션

FastAPI 프로세스의 딕셔너리에 다음을 보관한다.

- `session_id`
- 출생연도
- 연 단위 추정 나이(`2040 − 출생연도`)
- 원본 대화 메시지 배열
- 증거 로그
- 상태: 진행 중, 종료, 결과 생성, 제출됨
- 유형 결과
- 보고서 초안
- 수정 횟수
- 생성 시각

원본 대화록은 턴 배열이 아니라 메시지 배열이다. 각 메시지의 구조는 Firestore의 `raw_transcript`와 동일하다. 사용자 발화와 그에 대한 AI 응답은 같은 턴 번호를 공유한다.

서버 재시작 시 진행 중 세션이 소멸해도 허용한다. 제출 시에만 Firestore에 저장하고 메모리에서 제거한다.

### 7.2 증거

증거 한 건은 다음 필드로 구성한다.

- `axis`: `"AC"`, `"UN"`, `"OW"`, `"FH"` 중 하나
- `pole`: 해당 축의 극 글자
- `weight`: 1, 2, 3 중 하나
- `text`: 참여자 발화 원문
- `turn`: 턴 번호

### 7.3 유형 결과

`type_result`는 다음을 포함한다.

- `code`: 4글자 유형 코드. 서버·저장·담당자 전용이며 참여자 응답에도 포함되지만 화면에 표시하지 않는다 (5.1)
- `axes`: 네 축의 배열

각 축 객체는 다음을 포함한다.

- `axis`
- `letter`
- `strength`: 51~100 정수
- `scores`: 두 극의 가중치 합
- `evidence`: 해당 축의 증거 배열

모든 값은 증거 로그와 확정 합산 규칙으로 결정적으로 도출한다.

`scores`와 `evidence`는 서버 보관·저장용 완전본에 유지한다. 참여자에게 보내는 `/result` 응답에서는 두 필드를 제외하고 `axis`, `letter`, `strength`만 전달한다.

### 7.4 개인 보고서

개인 보고서는 다음 필드를 갖는다.

- `session_id`
- `self_info`
- `summary`
- `axis_reasons`
- `axis_demands`
- `participation_notes`
- `meta`

`axis_reasons`는 네 축의 배열이며 각 항목은 `axis`, `letter`, `reason`을 갖는다. `reason`은 그 축을 왜 그 글자로 판정했는지를 설명하는 한두 문장이다. 참여자의 발화를 그대로 인용하지 않고, 참여자가 무엇을 중요하게 여겼기에 이 판정에 이르렀는지를 서술한다. 유형 결과와 마찬가지로 수정할 수 없으며 요구를 수정해도 재생성하지 않는다.

**증거가 한 건도 없는 축의 `reason`은 LLM이 쓰지 않는다.** 근거가 없는 축에는 설명할 것이 존재하지 않으므로, 쓰게 하면 지어낸다. 이 자리는 코드가 고정 문장으로 채운다(2026-08-12).

`participation_notes`는 조사 자체의 신뢰나 반영 가능성에 대해 참여자가 한 말을 `text`와 `turn`으로 원문 보존한 배열이다. 없으면 빈 배열이다. 요구가 아니므로 `axis_demands`에 넣지 않으며, 참여자 응답에서는 제외하고 저장본과 담당자 화면에만 남긴다. 이전에는 이런 발화가 "주제 밖"으로 분류돼 대화록에 한 줄만 남고 보고서에서 통째로 사라졌다.

`self_info`는 다음을 포함한다.

- `nickname`
- `birth_year`
- `age_2040`
- `raw_region`
- `normalized_region`
- `region_table_version`
- `dream_or_job`

`age_2040`은 연 단위 추정 나이(`2040 − 출생연도`)다. 출생연도는 파트 A에서 받고, 별명·지역·꿈 또는 직업은 대화록에서 LLM이 추출한다.

**지역 정규화는 LLM이 아니라 코드가 한다**(2026-08-12). 행정구역 별칭은 바뀌는 데이터이고 미등록 값에서 LLM의 출력이 흔들렸다. LLM은 참여자가 말한 그대로를 `raw_region`에 담고, 백엔드가 버전이 있는 별칭 표로 `normalized_region`을 만든다. 확정할 수 없으면 빈 문자열이며 집계에서 빠진다. 참여자 화면에는 `raw_region`을, 담당자 화면과 집계에는 `normalized_region`을 쓴다.

`summary`는 인터뷰 전체 요약의 문장 배열이며 수정할 수 없다.

`axis_demands`는 네 축의 배열이다. 각 축 객체는 다음을 포함한다.

- `axis`
- `letter`: 표시용 판정 글자
- `demands`

`demands`는 **빈 배열일 수 있다**(2026-08-12). 그 축을 함의하는 인용을 찾지 못하면 비워 두는 것이 정상이며, 억지로 채운 요구는 담당자를 잘못된 곳으로 보낸다. 화면은 빈 축에 "이번 대화에서는 이 이야기가 나오지 않았어요"를 표시한다.

각 요구 항목은 다음을 포함한다.

- `id`: 예를 들어 `"AC-D1"`
- `title`
- `description`: 문장 단위 선택 UI를 위한 문장 배열
- `topics`: 정책 주제 태그 배열. 고정 어휘 9종(`일자리`, `주거`, `교통`, `문화`, `환경`, `돌봄`, `안전`, `교육`, `상권`) 중 1~3개
- `quotes`: `text`와 `turn`을 가진 근거 발화 배열

`quotes`와 `topics`는 참여자에게 보내는 `/result`와 `/revise` 응답에서 제외한다. 방금 자기가 한 말을 다시 읽는 것은 참여자에게 새로운 정보가 아니기 때문이다. 서버 세션과 Firestore 완전본에는 유지한다. 담당자가 제출본 상세에서 요구의 근거를 원문으로 확인할 수 있어야 하고, 분석 파이프라인이 대표 인용을 뽑는 재료이기도 하다.

수정 요청은 `axis`, `demand_id`, 문장 인덱스, 참여자 의견 텍스트로 구성된 배열이다. 선택 문장과 참여자 의견은 재생성 지시의 입력이 된다. LLM은 `axis_demands`의 일부가 아니라 4축 전체를 위와 동일한 스키마로 재생성하며, LLM 출력 자체에는 새 `axis_demands`만 담는다. 서버가 기존 `summary`, `axis_reasons`, `self_info`, 메타데이터를 복사해 전체 개인 보고서를 조립한다. `summary`와 별도 `type_result`는 변경하지 않는다.

`meta`는 다음을 포함한다.

- `turn_count`
- `revision_count`
- `created_at`

### 7.5 Firestore `submissions`

문서 ID는 UUID 형식의 `submission_id`다.

필드는 다음과 같다.

- `session_id`
- `submitted_at`
- `self_info`
- `raw_transcript`
- `evidence_log`
- `type_result`
- `report`
- `deidentified`

`raw_transcript`는 턴 배열이 아니라 메시지 배열이다. 각 메시지는 다음 필드를 갖는다.

- `turn`: 턴 번호
- `role`: `user` 또는 `assistant`
- `text`
- `timestamp`

한 사용자 메시지와 그에 대한 AI 메시지는 같은 `turn` 번호를 공유한다.

`deidentified`는 제출 응답 후 백그라운드에서 채우는 비식별 사본이다. 백그라운드 생성이 실패하거나 아직 끝나지 않아 값이 없으면 분석 실행 시 보충하며, 이미 있으면 재사용한다. 값이 없는 동안에는 `null`이다.

`deidentified`는 `axis_demands` 한 필드만 갖는다. 그 값은 `report.axis_demands`와 동일한 스키마이며, `title`·`description` 문장·`quotes`의 `text`만 블라인드 처리된 상태다. 나머지 필드(`axis`, `letter`, 요구 `id`, `topics`, 인용 `turn`)는 서버가 원본에서 복사하므로 원본과 항상 같다. 각 인용에는 서버가 부여한 `quote_id`가 함께 붙는다(3.4). 대화록 전체나 요약은 비식별 대상이 아니다. 담당자 리포트와 대표 인용이 쓰는 것이 요구 텍스트와 인용 발화뿐이기 때문이다.

### 7.6 Firestore `analysis_runs`

문서 ID는 UUID 형식의 `run_id`다.

최상위 필드는 다음과 같다.

- `executed_at`
- `input_submission_ids`
- `axis_stats`
- `type_distribution`
- `axis_summaries`

필드 이름은 프론트가 이미 고정한 `web/src/lib/api.ts`의 `AnalysisRun` 타입을 따른다.

`axis_stats`는 네 축의 배열이며 `axis`, `poles`, `submission_ids`를 갖는다. `poles`는 그 축의 두 극 배열이고 각 항목은 `letter`, `count`(인원수), `mean_strength`(정수 평균 강도)를 갖는다. `submission_ids`는 그 축의 집계에 포함된 제출본 목록이며, 그 축에 증거가 있는 제출본만 담는다(3.4). 인원이 0인 극의 `mean_strength`는 0이다.

`type_distribution`은 유형 코드별 인원수이며 인원이 0인 코드는 담지 않는다. 약 10명의 시범 규모에서는 참고용이고 축 단위 통계가 주력이다.

`axis_summaries`는 네 축의 배열이며 `axis`, `poles`, `quotes`를 갖는다. `poles`는 두 극 배열이고 각 항목은 `letter`와 그 극 참여자의 요구 경향을 정리한 `sentences` 문장 배열을 갖는다. 해당 극의 참여자가 없으면 `sentences`는 빈 배열이다.

`quotes`는 그 축의 대표 인용 배열이며 각 항목은 `quote_id`, 출처 `submission_id`, 비식별 발화 `text`를 갖는다. 축마다 최대 4건이고 후보가 없으면 빈 배열이다.

인용 ID 형식은 `{submission_id 앞 8자}-Q{순번}`이다. 예시는 `a1b2c3d4-Q2`다. 순번은 제출본 안에서 1부터 시작하며 축·요구·문장 순서로 부여한다. ID를 만드는 것은 서버이고 모델은 후보 중에서 고르기만 한다(3.4).

### 7.7 `analysis_runs`의 대시보드 집계 (Phase 10)

담당자 대시보드(3.6)가 쓰는 집계다. 7.6의 기존 다섯 필드는 그대로 두고 아래를 더한다. 모두 **코드 계산**이며 `ai_notes`만 Gemini가 만든다. 계산의 재료는 이미 제출본에 다 들어 있다. 새로 물어보는 항목은 없다.

- `kpi`: `participants`(제출본 수), `demands`(요구 총 건수), `regions`(참여자가 한 명 이상인 자치구 수), `age_min`, `age_max`
- `ages`: 네 연령대(`19~24`, `25~29`, `30~34`, `35~39`) 배열. 각 항목은 `band`, `male`, `female`, `unknown`, `total`
- `regions_count`: 자치구 이름별 인원수. 자치구를 확정하지 못한 참여자는 넣지 않는다
- `topics`: 계획 부문별 집계 배열. 각 항목은 `topic`, `demands`(요구 건수), `people`(그 부문을 한 번이라도 말한 사람 수). **요구 하나가 여러 부문에 걸치므로 부문 건수의 합은 요구 총 건수보다 크다.**
- `cross`: 부문별 네 연령대 요구 건수 배열
- `people`: 참여자별 항목. `submission_id`, `nickname`, `gender`, `age`, `region`, `code`, `turns`, `submitted_at`, `summary`, `demands`(각 `axis`·`title`·`topics`), `reasons`(각 `axis`·`letter`·`reason`)
- `ai_notes`: 카드별 해석 문장. 키는 `map`, `topics`, `axes`, `cross`, `types`

`people`의 요구 제목은 **비식별본에서** 가져온다(3.4). 별명·나이·자치구는 비식별 대상이 아니므로 원본을 쓴다.

**연령대는 네 구간 고정이다.** 청년 정책 대상이 19~39세이므로 그 밖의 나이는 `ages`와 `cross`에서 빼고 `kpi.participants`에는 넣는다. 나이는 `실행 연도 - self_info.birth_year`이며, 세션이 `age_2040`을 만드는 방식(7.1)과 같은 계산이다. `birth_year`가 없으면 두 집계 모두에서 뺀다. 나이를 구하지 못한 참여자의 `people[].age`는 0이며 화면은 이를 나이 미상으로 다룬다.

**자치구는 2026년 7월 개편 이름 11개로 정규화한다.** 집계는 `self_info.normalized_region`을 그대로 읽으며, 정규화 자체는 7.4의 버전 있는 별칭 표가 담당한다. 개편 전 이름은 대표 후신으로 옮기고(중구·동구 → 제물포구, 서구 → 서해구), 동·섬 이름도 표에 있으면 해당 자치구로 옮긴다. 확정하지 못하면 지도와 `regions_count`에서 뺀다. **중구는 제물포구와 영종구로 갈라졌으므로 이 매핑은 정확하지 않다.** 시범 데이터를 지도에 얹기 위한 잠정 규칙이며, 실제 운영에서는 인터뷰가 개편 후 이름을 받아야 한다.

**성별은 인터뷰에서 받지 않는다.** 지금 저장된 제출본은 전원 AI 가상 주민 표본이므로, 연령 구성 막대를 채우기 위해 **별명에서 성별을 지정한 표**를 백엔드에 둔다. 표에 없는 별명은 `unknown`이다. 이것은 시연용 표본에만 성립하는 임시 장치이며 실제 응답자에게 적용할 수 없다. 실제 운영에서 성별이 필요하면 인터뷰가 물어야 한다.

## 8. Gemini와 LLM 자산

### 8.1 모델

모든 LLM 작업에는 `gemini-3-flash-preview`를 사용하되, 모델명을 코드에 하드코딩하지 않고 설정값으로 주입한다. 개발용 묶음집 재생성 스크립트가 쓰는 가상 참여자 호출도 같은 설정값을 따르므로, 이 프로젝트가 호출하는 모델은 어디서나 하나다.

**개발과 테스트는 값이 싼 Gemini로 계속하고, 서비스가 완성된 뒤 Claude 계열로 교체한다.** 이 계획을 반드시 인지해야 한다. `TUNING.md` 7장에 "모델 교체 대기"로 분류된 항목들이 여기에 매달려 있기 때문이다. 그 항목들을 지금 프롬프트 문단으로 막으면, 모델을 바꾼 뒤 그 문단이 필요했는지 알 수 없게 되고 그때까지 다른 규칙을 계속 희석한다(8.5의 기준 2·3).

사용 대상은 다음과 같다.

- 인터뷰어 응답 생성
- 증거 태깅 (인터뷰어와 분리된 별도 호출, 3.2·5.4)
- 개인 보고서 구조화
- 비식별화
- 축별 정성 요약과 대표 인용 선정

유형 합산과 축별 통계 집계는 LLM이 아니라 코드가 수행한다. 모델·프롬프트 버전 관리 기능은 만들지 않는다.

임베딩 모델은 없다. `gemini-embedding-001`도 사용하지 않는다.

### 8.2 프롬프트 파일

모든 LLM 자산은 `api/prompts/` 아래의 마크다운 파일로 두고 런타임에 로드한다. 파일은 정확히 다음 일곱 개다.

- `system.md`
  - 인천 시민의 미래 도시 상상을 돕는 인터뷰어 페르소나
  - 존댓말
  - 상상 보조
  - 금지사항과 가드레일
  - 악성 응답 처리
  - 4축 커버리지
  - 종료·중단 제어 토큰 규칙
  - **증거 형식 규정은 두지 않는다.** 인터뷰어는 증거를 만들지 않는다(4.2)
  - `[운영 지시: ...]` 블록은 참여자 발화가 아니라 백엔드 운영 지시라는 규칙
  - 운영 지시 블록을 참여자 의견이나 증거로 해석하지 않는 규칙
  - 인터뷰어 이름은 임시로 "하늘"

- `rubric.md`
  - 도입: 라포 형성과 별명·거주지·꿈 수집, 연 단위 추정 나이 언급
  - 심화: 4축 주제 블록과 하루·동네를 구체화하는 꼬리질문
  - 정리: 요약, 확인, 마무리
  - 네 축을 모두 탐색하는 것이 인터뷰어의 책임이라는 서술. `axes.md`가 채점 전용이 되면서 이곳으로 옮겼다

- `axes.md`
  - **채점 호출 전용이다.** 인터뷰어는 이 파일을 받지 않는다(3.2)
  - 4축과 극의 정의, 축마다 다루지 않는 것(→어느 축으로 가는지)
  - 축·극·강도별 채점 앵커. 5.3의 다섯 원칙을 지킨다
  - 소재가 겹칠 때의 판별 규칙, 축이 함께 묶여 보이는 착시와 그 반례
  - 증거가 아닌 발화(축당 3건 + 유도된 답 공통 규칙)
  - 채점 호출의 입력 형식과 출력 형식, 누적 증거의 용도 한정(5.4)
  - 기계 판독 JSON 블록. 백엔드 축 상수의 단일 원천이며 `api/app/axes.py`가 파싱한다. `load_axes`가 문서의 **첫 번째** JSON 코드펜스를 파싱하므로 이 블록보다 앞에 다른 JSON 펜스를 만들면 조용히 깨진다

- `structuring.md`
  - 원본 대화록을 개인 보고서로 구조화
  - 인터뷰어 발화는 맥락으로만 사용하고 요구·인용은 참여자 발화에서만 추출
  - 자기보고 정보 추출 (지역은 인천 군·구 단위로 정규화, 7.4)
  - 축별 요구 생성과 요구별 주제 태그 부여 (고정 어휘 9종, 7.4)
  - 수정 시 선택 문장과 의견을 바탕으로 `axis_demands` 4축 전체만 재생성

- `deidentify.md`
  - 한 제출본의 `axis_demands`에서 이름과 연락처를 블라인드
  - 텍스트 세 종류(`title`, `description` 문장, 인용 발화)만 바꾸고 구조와 개수는 그대로 유지
  - 블라인드 이외의 표현 수정·요약·보완 금지

- `aggregate.md`
  - 네 축의 극별 요구 경향 요약. 참여자가 없는 극은 빈 배열
  - 주어진 후보 목록에서 축별 대표 인용의 `quote_id` 선정. ID를 새로 만들지 않는다

- `insight.md`
  - 대시보드 카드별 해석 문장(7.7의 `ai_notes`). 다섯 카드를 한 번의 호출로 함께 처리한다
  - 입력에 있는 집계 수치만 근거로 삼고 없는 수치를 만들지 않는다
  - 표본이 대표성을 갖지 않는다는 한계를 해석에 반영한다

별도 `typing.md`는 만들지 않는다. 유형 채점 기준은 `axes.md`에 통합한다.

### 8.3 도시기본계획 지식 주입

참여자는 2040 인천 도시기본계획이 실현된 미래를 상상한다. 해당 계획이 채택된 역사적 배경은 구현에 반영하지 않는다.

선행 데이터 작업은 다음과 같다.

- 텍스트 추출이 되지 않는 대용량 PDF는 코딩 에이전트의 VLM 직접 판독을 우선하고 OCR은 보조수단으로만 사용한다.
- 보고서별 독립 에이전트가 원문 순서·표·단위·출처를 보존해 전사하고, 이미지·도표·그래프는 객관적 내용과 해석을 텍스트로 함께 남긴다.
- 보고서 12개를 개별 QA한 뒤 byte-for-byte로 결합한 정본을 `data/plan2040/2040_incheon_city_plan.md`에 둔다.
- 별도의 독립 에이전트가 정본에서 `data/plan2040_summary.md`를 만들고 자동·독립 QA를 거친다. 운영 투입 전 최종 판단은 사람이 원문 포인터를 따라 표본 검증한다.

2026-07-15에 12개 보고서 687쪽의 전사·개별 QA, 단일 정본 통합, 프로젝트용 요약과 독립 QA를 완료했다. 정본은 보고서 본문을 그대로 보존하고, 요약본은 공식 계획 내용과 프로젝트 활용 시사점을 구분하며 `(보고서 NN, PDF N쪽)` 포인터를 제공한다.

지식 주입은 두 방식으로 한다.

- 검증된 파생 요약본은 시스템 프롬프트에 항상 포함한다.
- 전처리된 전체 마크다운은 Gemini API File Search Tool에 업로드한다.

File Search는 Google이 청킹, 임베딩, 저장, 검색을 관리하는 RAG 도구다. 별도 벡터 저장소를 만들지 않는다. 저장·질의 시 임베딩은 무료이고 최초 인덱싱만 과금되며 출처 인용이 자동으로 붙는다는 전제로 사용한다.

File Search 스토어는 GCP 프로젝트당 하나만 사용한다. Phase 1에서 업로드 스크립트가 스토어를 생성하고 전처리 문서를 업로드·인덱싱한다. 스토어 이름은 `api`의 환경변수로 주입하며 코드에 하드코딩하지 않는다.

매 인터뷰 턴의 `generateContent` 호출에 File Search를 부착해 현재 참여자 발화와 관련된 계획 내용을 검색하게 한다.

### 8.4 대화 맥락과 캐싱

인터뷰 도중과 종료 후 모두 대화록을 압축하지 않는다. 목표 15턴 동안 인터뷰 호출에는 전체 대화 기록을 전달하고, 종료 후 구조화·수정 호출에는 `turn`, `role`, `text`만 남긴 대화록을 전달한다.

**인터뷰어 호출**은 다음 프리픽스 구조로 조립한다.

1. 고정부. `rubric.md`, `system.md`, 참여자 나이 순서다.
2. 누적 대화 기록
3. 현재 사용자 발화와 그 뒤에 덧붙인 `[운영 지시: ...]` 블록

고정부 안의 순서에는 이유가 있다. 매 턴 반드시 지켜야 하는 행동 규범과 응답 형식이 담긴 `system.md`는 생성 직전에 오도록 자산 중 마지막에 둔다. 참여자 나이는 참여자마다 다른 유일한 값이므로 반드시 맨 뒤를 유지한다. 그래야 앞선 고정 자산 전체가 참여자 간 캐시를 공유한다.

이 배치가 의도대로 작동한다는 직접 증거가 있다. 격리 점검에서 모델이 응답 형식을 지킨 이유로 "이 절이 문서 맨 끝이라 가장 최근에 읽었기 때문"을 들었고 "문서 중간에 있었다면 틀렸을 것"이라고 답했다.

`axes.md`는 이 프리픽스에서 빠졌다. 원래 채점 기준이자 유도 질문의 유혹이 나오는 원천이라 가운데에 두었는데, 3.2의 호출 분리로 인터뷰어가 아예 받지 않게 됐다. 그 결과 프리픽스가 37,155자에서 25,376자로 줄었고, **2026-08-12에 계획 요약본 제거와 두 프롬프트 감량으로 7,626자가 됐다.** 원본 대비 79.5% 감소다.

**채점 호출**의 시스템 지시는 `axes.md` 단독이고, 사용자 콘텐츠는 5.4의 JSON 하나다. 이 호출도 시스템 지시가 참여자 간 완전히 동일하므로 캐시를 공유한다.

Gemini implicit caching을 사용한다. 앞부분을 고정해 자동 캐시 할인을 유도한다. explicit 캐시 API는 Flash 요율에서 시간당 저장비가 절감분을 상쇄하므로 사용하지 않는다.

### 8.5 프롬프트 변경의 판단 기준

프롬프트 자산을 고칠 때 상시 적용하는 네 기준이다. 앞선 라운드들이 실측으로 세웠다.

**기준 1. 코드가 결정할 수 있는 것은 프롬프트에 두지 않는다.** 프롬프트를 줄이는 방향이 항상 우선한다.

**기준 2. 정보 결손과 행동 튜닝을 가른다.** 모델이 알 수 없는 정보가 없어서 지어내는 것은 결손이며 반드시 채운다. 정보는 있는데 다르게 행동하는 것은 튜닝이며 얹을수록 희석된다. 결손은 즉시 채우고, 튜닝은 실측 재발이 두 번 이상일 때만 손댄다.

**기준 3. 예방적 패치를 넣지 않는다.** 실측 근거 없이 "이럴 수도 있으니" 넣지 않는다.

**기준 4. 효과를 재는 라운드에서는 다른 프롬프트 내용을 함께 바꾸지 않는다.** 다음 재생성에서 무엇이 무엇을 고쳤는지 가릴 수 없게 되기 때문이다. 코드 결함 수정은 이 기준의 대상이 아니지만, 그 경우에도 해당 경로가 대조군에서 실행된 적이 있는지를 확인하고 없을 때만 함께 고친다.

**프롬프트는 제로섬에 가깝다.** 한 줄을 넣으면 그 규칙은 정확해지지만 나머지 전부가 조금씩 흐려진다는 것이 실측으로 확인됐다. 기준 2와 3이 여기서 나온다. 반대로 분량을 줄이면 남은 규칙 전부의 준수율이 오른다.

**금지 규칙의 중복 배치는 줄이지 않는다.** 반복의 효과가 실측으로 확인됐다. 격리 점검에서 모델이 운영 지시 블록 규칙이 또렷하게 남은 이유로 그것이 두 곳에 중복돼 있다는 점을 꼽았고, 반대로 한 번만 적힌 선언은 응답 열 개 중 두 번만 회상됐다. 기준 1의 "줄이는 방향 우선"은 **불필요해진 규칙**을 걷어내라는 뜻이지 살아 있는 규칙의 반복을 지우라는 뜻이 아니다.

**모델의 자기점검은 신뢰하지 않는다.** 시뮬레이션들이 자기 산출물을 지시와 대조하는 단계에서 위반을 거의 잡지 못했다. 축 세 개가 뒤바뀐 결과물을 "지어낸 내용 없음, 축 순서 준수"로 통과시킨 사례가 있다. 다만 "무엇이 흐려졌는가", "규칙이 없었다면 무엇을 했겠는가" 같은 **내성 질문에 대한 답은 유용했다.** 자기 산출물의 옳고 그름을 판정하게 하지 말고 자기 상태를 보고하게 한다.

**작은 모델은 지적을 받으면 심각도를 부풀린다.** 한 검토가 사실관계 정정을 받고 같은 항목을 문제없음에서 가장 심각한 설계 오류로 뒤집었는데 새 근거는 없었다. 튜닝 판단을 모델 의견이 아니라 실측에 두어야 하는 이유다.

### 8.6 프롬프트 격리 점검 절차

프롬프트를 고칠 때마다 함께 돌린다. **저장소 전체를 읽은 검토는 프롬프트의 결손을 놓친다.** 다른 파일에서 얻은 정의를 무의식적으로 채워 넣기 때문이다. 실제로 보고서 호출의 축 정의 결손은 저장소를 읽은 검토 세 건이 모두 놓쳤고 프롬프트만 준 점검에서 즉시 드러났다.

1. 실제 호출에 들어가는 시스템 지시를 그대로 조립해 임시 파일로 쓴다. 사용자 턴도 코드가 실제로 만드는 문자열 그대로 넣는다.
2. 에이전트에 그 파일만 읽게 하고 저장소 접근을 금지한다. **코드가 이 텍스트를 어떻게 처리하는지 추측하는 것도 금지한다.** 이 금지가 없으면 코드 흐름을 낙관적으로 추정해 엉뚱한 결론을 낸다.
3. **비평이 아니라 실행을 시킨다.** 인터뷰라면 첫 인사와 참여자 발화 몇 개에 대한 응답을 형식 그대로 실제로 쓰게 한다. 채점이라면 실제 증거 배열을 만들게 한다.
4. 만든 결과를 지시와 대조해 스스로 위반을 짚게 하되, 이 자기점검은 신뢰하지 않고 사람이 직접 대조한다(8.5).
5. **"규칙이 없었다면 하고 싶었을 행동"을 적게 한다.** 금지 규칙이 실제로 작동하는지 확인하는 가장 직접적인 방법이다.
6. **분량 소감을 묻는다.** 어느 규칙이 또렷하게 남고 어느 규칙이 흐려졌는지가 배치 판단의 근거가 된다.

입력에는 함정을 심는다. 근거가 얇은 축을 하나 두어 네 축을 모두 채우라는 지시와 지어내지 말라는 지시가 부딪치게 만들고, 지역명을 군·구가 아닌 동네 이름으로 주어 정규화를 시험하고, 운영 지시의 특정 분기(예: 미탐색 축이 남은 마무리 시점)를 일부러 도착시켜 그 분기를 검사한다. 채점 쪽에는 인터뷰어가 만든 선택지와 그에 대한 답을 심어 배제 규칙을 시험한다.

## 9. 데이터베이스, 배포, 운영

### 9.1 Firestore

Cloud Firestore Native 모드를 사용한다.

- 대화록과 보고서가 JSON 문서 구조에 적합하다.
- 시범 규모는 무료 티어로 충분하다고 본다.
- Google Cloud와 Gemini 생태계에 맞춘다.
- 로컬 개발도 동일 GCP 프로젝트의 Firestore를 사용한다.
- 에뮬레이터는 사용하지 않는다.
- 애플리케이션 수준의 추가 암호화를 하지 않는다.

### 9.2 Cloud Run

- 리전은 서울 `asia-northeast3`다.
- `web`과 `api` 두 서비스를 배포한다.
- 각 서비스는 Dockerfile을 가진다.
- 이미지는 Artifact Registry에 둔다.
- `api`는 프로세스 메모리 세션 때문에 `min=1`, `max=1`로 고정한다.
- 수평 확장은 하지 않는다.
- `min=1`로 콜드스타트를 줄여 시연 안정성을 확보한다.
- 시범 규모 약 10명에 충분한 구성으로 본다.
- Gemini 키, `gemini-3-flash-preview` 모델명, File Search 스토어 이름 등은 환경변수로 주입한다.

`api`가 읽는 환경변수 이름은 다음과 같이 고정한다. 루트 `.env`에서 로드하고 Cloud Run에서는 서비스 환경변수로 주입한다. 값이 비어 있어도 기동은 되며, 해당 기능을 쓰는 시점에 필요하다.

- `gemini_api_key`: 필수
- `gemini_model`: 기본값 `gemini-3-flash-preview`
- `file_search_store_name`: Phase 1의 업로드 스크립트가 스토어를 만든 뒤 주입
- `gcp_project_id`: Phase 4의 Firestore 연결에 사용
- `interview_target_turns`: 기본값 15
- `interview_wrapup_turn`: 기본값 12
- `dev_mode`: 개발자용 대화 묶음집 경로 등록 여부. 기본값 `false`, Cloud Run에서도 켠다(6.4)
- `access_code`: 개발자 모드와 담당자 웹의 접근 코드. 비어 있으면 게이트 없음(6.5)

턴 상수를 코드가 아니라 환경변수로 두는 이유는 시범운영에서 인터뷰 길이를 재배포 없이 조정하기 위해서다.

`web`은 `NEXT_PUBLIC_API_BASE_URL` 하나만 읽는다. Gemini 키, 프롬프트, **접근 코드**는 프론트에 두지 않는다. 접근 코드는 사람이 화면에서 입력하며 빌드에 박히지 않는다.

### 9.3 공유 카드

프론트에서 유형 카드를 이미지로 변환한다. 이미지 변환 라이브러리는 `html-to-image`로 확정했다. 카드를 3배 해상도로 래스터화해 휴대폰에서 흐릿하지 않게 한다.

한글 서체를 애플리케이션에 번들하므로, 변환 시 그 서체가 이미지에 실제로 박혔는지 확인한 뒤 내보낸다. 서체가 인라인되지 않으면 시스템 서체로 조용히 대체되어 카드가 망가지므로, 그 경우에는 조용히 넘어가지 않고 그 공유·저장 시도를 실패로 끝낸다. 다만 공유·저장 실패는 결과 흐름 전체를 종료하지 않는다. 참여자는 결과 화면에 그대로 남아 다시 시도하거나 공유 없이 진행할 수 있다. 부수 기능의 실패로 제출 전 인터뷰 결과 전체가 사라지지 않게 하기 위함이다.

모바일에서는 Web Share API로 공유 시트를 열고, 파일 공유를 지원하지 않는 환경에서는 이미지를 내려받는다.

제출 이후에는 서버에서 유형 결과를 다시 조회하지 않는다. 프론트가 결과 단계에서 받아 보관 중인 `type_result` 상태로 공유 카드를 렌더링한다. `submit` 응답은 `submission_id`만 반환하며, 새로고침하거나 재접속하면 결과를 다시 볼 수 없다.

### 9.4 로깅

구조화 JSON 로그를 표준출력에 남기고 Cloud Run이 Cloud Logging으로 수집하게 한다.

로그 필드는 다음과 같다.

- 시각
- 레벨
- `session_id`
- 이벤트명
- 토큰 사용량

이벤트에는 세션 생성, 턴 진행, 종료, 이탈, 악성 중단, 결과 생성, 수정, 제출, 분석 실행 등이 포함된다.

대화 원문과 보고서 본문은 로그에 남기지 않는다. 원문은 제출 후 DB에만 존재한다.

### 9.5 테스트와 품질 도구

pytest로 다음 핵심 로직의 테스트를 기능 구현보다 먼저 작성한다.

- 세션 관리
- 제어 토큰과 트레일러 파싱
- 프롬프트 조립
- 스키마 검증
- 유형 가중치·반올림·동률·빈 축 처리
- 축별 집계 계산

테스트는 로컬 개발 자산으로만 유지하고 공개 저장소에는 포함하지 않는다. 저장소에는 시스템 구동에 필요한 코드만 둔다. 테스트를 작성하지 않는다는 뜻이 아니라 공개 범위에서 제외한다는 뜻이며, 위의 선행 작성 원칙은 그대로 지킨다.

Python은 ruff, 프론트는 ESLint와 Prettier를 사용한다.

### 9.6 에러 처리

존재하지 않는 세션은 404, 세션 상태에 맞지 않는 호출은 409, 그 외 서버 오류는 500으로 처리한다. 자동 재시도와 복구 로직은 만들지 않는다.

API 오류가 발생하면 고정 문구 "일시적인 오류가 발생했습니다"와 처음 화면으로 이동하는 버튼 하나만 있는 오류 화면을 표시하고 해당 흐름을 종료한다. 그 외 복구 안내나 복구 UI는 만들지 않는다. 시연 직전에 서비스 상태를 점검하는 것으로 대응한다.

## 10. 개인정보와 안전

수집 항목은 다음과 같다.

- 휴대폰 본인인증 통과 여부
- 출생연도
- 별명
- 거주 지역
- 꿈 또는 현재 직업
- 인터뷰 대화록
- 파생된 유형 결과와 보고서

휴대폰 신원은 중복참여 방지에만 사용하고 출생연도만 인터뷰 데이터로 전달한다. 다만 프로토타입의 목업 인증은 중복참여를 실제로 강제하지 않는다.

분석 전 Gemini가 이름과 연락처를 블라인드 처리한다. 담당자 종합 리포트와 대표 인용은 비식별본을 사용한다.

Firestore에는 애플리케이션 수준의 추가 암호화를 하지 않고 저장한다. 원본 접근은 담당자와 개발자로 한정하며, 구현 수단은 비공개 `/admin` 링크와 6.5의 접근 코드다. 코드는 서버가 검사하므로 화면을 우회해도 원문에 닿지 못한다. 계정과 권한 시스템은 만들지 않으며, 짧은 코드의 한계는 6.5에 적었다.

진입 화면에는 수집 항목, 이용 목적, 보관 기간, 파기 시점을 고지한다. 실제 문구는 Phase 7에서 작성한다.

프롬프트 인젝션은 시스템 프롬프트 지시로만 방어한다. Gemini 안전필터의 오작동은 개발 단계에서 다루지 않는다.

## 11. 저장소 구조

모노레포는 다음 네 영역으로 나눈다.

- `web/`
  - Next.js 프론트엔드
  - 진입과 동의
  - 대화 UI
  - 유형 카드와 보고서 확인·수정
  - 공유 카드
  - `/admin`
  - `city_types.json`의 16유형 정적 콘텐츠
  - 담당자용 축·유형 해설 정적 자산

- `api/`
  - FastAPI 백엔드
  - 세션과 Gemini 호출
  - 증거 수집과 유형 합산
  - 저장
  - 분석 파이프라인
  - 담당자 API
  - File Search 업로드 스크립트
  - `api/prompts/*.md`의 여섯 LLM 자산

- `data/`
  - `plan2040/`의 전처리된 도시기본계획
  - `plan2040_summary.md`의 출처 포인터가 포함된 검증용 파생 요약본

- `docs/`
  - 사례조사 등 보조 문서
  - `qr/`의 참여자 접속 QR (인쇄용 SVG와 화면용 PNG). 배포 주소가 바뀌면 다시 만들어야 한다
  - `interview-lab/`의 12인 인터뷰 실험 자료. 인물 정의, 진행 지침, 개별 기록 12건, 종합 1건. AI 튜닝의 원자료다(`TUNING.md`)

루트의 `PLAN.md`가 상세 계약을 포함한 SSOT다. 진행 중인 라운드가 있으면 루트에 그 라운드 문서를 하나 두고(현재는 AI 튜닝의 `TUNING.md`), 끝나면 이 문서로 통폐합하고 지운다. 그 밖의 세부 파일 구조는 각 Phase 착수 시 정한다.

## 12. 구현 로드맵

Phase 순서는 변경하지 않는다. 각 Phase는 검증 가능한 산출물로 끝나야 한다.

### Phase 0: 프로젝트 기반 (완료)

- 확정 스택으로 `web`과 `api` 모노레포 초기화
- Gemini 키, 모델명, File Search 스토어 이름 등 환경변수 구성
- 구조화 로그 골격
- Firestore 연결 준비
- 프론트 → 백엔드 → Gemini 1회 왕복 연결

검증: 실제 "hello" 왕복 성공.

완료 시점의 상태는 다음과 같다.

- `api`는 Python 3.11, FastAPI, uvicorn, google-genai로 구성했고 `web`은 Node.js 22 이상, Next.js App Router, TypeScript, Tailwind, shadcn/ui로 구성했다.
- `gemini-3.5-flash`가 실제로 호출 가능한 모델임을 API로 확인했다.
- 프로세스가 표준출력에 쓰는 모든 줄이 한 줄짜리 JSON이다. uvicorn과 서드파티 라이브러리 로그도 같은 포맷으로 통일했다. 대화 원문은 로그에 남기지 않는다.
- Firestore 클라이언트는 지연 생성 골격만 두었다. GCP 프로젝트가 없으므로 Phase 0에서는 연결하지 않으며 Phase 4에서 붙인다.
- 모바일 뷰포트의 실제 브라우저에서 `입력 → FastAPI → Gemini → 응답 표시` 왕복과, API 장애 시의 단일 오류 화면을 확인했다.
- 도시기본계획 12개 보고서 687쪽을 전사·검수해 `data/plan2040/2040_incheon_city_plan.md`에 통합했고, `data/plan2040_summary.md`와 QA 기록도 만들었다. 아직 실행하지 않은 단계는 외부 File Search 스토어 생성·업로드뿐이다.

### Phase 1: 인터뷰 엔진 코어 (지식 문서 완료, File Search 업로드 대기)

- 도시기본계획 PDF를 `data/plan2040/` 마크다운으로 전처리 (완료)
- `data/plan2040_summary.md`를 독립 에이전트가 작성하고 출처 기반 QA 및 인간 표본 검증이 가능한 형태로 제공 (완료)
- 업로드 스크립트 작성
- GCP 프로젝트당 하나의 File Search 스토어 생성
- 전처리 문서 업로드와 인덱싱
- 스토어 이름을 `api` 환경변수로 주입
- 4축 v1과 채점 앵커를 `axes.md`로 작성
- `system.md`, `rubric.md`, `axes.md` 구성
- 요약본과 File Search 주입
- 메모리 멀티턴 세션
- 프리픽스 캐싱 구조
- SSE 스트리밍
- 모든 응답에 적용되는 EVIDENCE 머신 트레일러와 제어 토큰 파싱
- 첫 인사의 `[[EVIDENCE]][]` 처리
- 증거 누적
- 15턴 목표, 12턴 마무리 안내
- `[운영 지시: ...]` 형식의 `ANSWER_KEY`와 검수된 질문 문장
- 운영 지시 블록과 참여자 발화의 의미 분리
- 가드레일

프론트 없이 API에서 먼저 품질을 튜닝한다.

검증: 한 세션이 자연스럽게 진행되고, 첫 인사를 포함한 모든 응답에 올바른 EVIDENCE 줄이 있으며, 4축을 모두 탐색하고, 증거가 누적되고, 종료 토큰을 감지한다.

**이 절의 EVIDENCE 트레일러 항목은 Phase 1 당시의 기록이다.** 2026-08-10에 인터뷰어 호출과 채점 호출을 분리하면서 트레일러를 폐지했다. 현재 계약은 3.2·4.2·5.4에 있다.

완료 시점의 상태는 다음과 같다.

- 엔진, 프롬프트 자산 세 개, SSE, 트레일러 파싱, 증거 누적, 커버리지 힌트, 가드레일을 모두 구현했다. 실제 Gemini로 시뮬레이션 참여자와 한 세션을 완주해 위 검증 항목을 모두 확인했다.
- 도시기본계획 정본과 요약본은 준비됐고 요약본은 코드가 즉시 로드한다. 외부 상태를 바꾸는 `data/plan2040/` 업로드와 File Search 스토어 생성은 아직 실행하지 않았다. GCP 설정 후 업로드 스크립트를 한 번 실행하고 스토어 이름을 환경변수에 넣으면 코드 변경 없이 전체 검색도 활성화된다.

시범 인터뷰에서 드러난 문제와 그 대응을 기록해 둔다. 이후 튜닝의 출발점이다.

- LLM이 4축을 한 번씩만 찍고 8턴 만에 종료했다. 축당 증거가 한두 건에 그쳐 강도가 100%로 계산되는 문제가 있었다. 커버 기준을 증거 2건으로 올리고 12턴 전 마무리 금지 안내를 넣어, 13턴·증거 16건·축당 3건 이상으로 개선했다.
- LLM이 축의 두 극을 그대로 선택지로 내미는 이지선다 질문을 했다. 5.3의 검사형 질문 금지 위반이다. 원인은 루브릭의 주제를 "A인지 B인지" 형태로 적어 둔 데 있었다. 루브릭의 모든 주제를 열린 질문으로 다시 쓰고 시스템 프롬프트에 이지선다 금지를 명시해 해소했다.
- 한 발화에 여러 축이 섞여 있을 때 한 축만 태깅하고 나머지를 놓쳤다. 축별 점수가 0이 되어 유형이 왜곡된다. 발화 하나를 네 축 모두에 대해 훑으라는 규칙과, 중복 방지 규칙은 같은 축 안에서만 적용된다는 단서를 `axes.md`에 넣어 해소했다.
- 인터뷰어의 응답이 길고 감탄과 칭찬이 과했으며 마크다운 서식을 썼다. 세 문장 제한, 과장 금지, 평문 규칙을 시스템 프롬프트에 넣었다. 완전히 해소되지는 않았으므로 시범 전 추가 튜닝 대상이다. 이후 리뷰 라운드(R9)에서 세 문장 경성 상한은 기본 두세 문장, 필요 시 다섯 문장까지 허용하는 연성 규칙으로 완화했다.

### Phase 2: 인터뷰 UI (완료)

- Next.js 대화 화면
- SSE 소비
- 타자기 효과
- 모바일 우선 UI
- "인터뷰 그만두기" 버튼과 확인 모달
- 정식 진입 게이트 전 임시 개발용 시작 화면
- 임시 화면에는 출생연도와 시작 버튼 배치
- 고정 오류 문구와 처음 화면 이동 버튼만 있는 오류 화면

검증: 사용자가 화면에서 인터뷰를 처음부터 끝까지 완주하고, 오류 시 지정된 단일 오류 화면으로 흐름이 종료된다.

완료 시점의 상태는 다음과 같다.

- 2.4의 디자인 계약을 구현했다. 인천 3색을 shadcn 시맨틱 토큰에 직접 대응시켜 버튼과 강조가 자동으로 인천 블루가 되게 했고, 파괴적 행동에도 빨강 대신 인천 그레이를 쓴다. Pretendard Variable을 번들해 외부 폰트 CDN에 의존하지 않는다.
- 데스크톱에서는 모바일 폭을 유지한 채 기기 형태로 가운데에 배치하고, 실제 휴대폰에서는 화면을 가득 채운다.
- 타자기 효과는 도착한 문자만 큐에 넣고 일정한 속도로 한 글자씩 공개한다. 스트림보다 앞서 나가지 않고, 스트림이 끝나면 남은 글자를 반드시 모두 공개한다. 밀린 글자가 많으면 글자 간 간격만 줄여 따라잡는다.
- 실제 브라우저에서 `시작 → 첫 인사 → 대화 → 그만두기 확인 → 세션 폐기 → 시작 화면 복귀`와 API 장애 시의 단일 오류 화면을 모두 확인했다.
- 결과 화면은 Phase 3의 몫이므로, 인터뷰가 정상 종료되면 완료 안내만 표시하고 멈춘다.

### Phase 3: 유형 판정, 구조화, 확인과 제출 (완료 — 핵심 MVP 도달)

백엔드:

- 유형 합산 코드
- 가중치·반올림·동률·빈 축 규칙의 선행 pytest
- `structuring.md`
- 개인 보고서 스키마
- 수정 시 `axis_demands` 4축 전체 재생성
- `summary`와 `type_result` 불변 처리

프론트:

- 3.3의 세 단계로 나뉜 결과 화면
- 유형 일러스트와 별명, 일상어 라벨 배지
- 축별 강도 바
- 축별 판정 이유
- 축별 요구 보고서
- 문장 선택과 의견 입력
- 재생성 표시
- 제출 버튼
- 결과 단계의 `type_result` 상태 유지
- 제출 전후 모두에서 이미지 저장·공유
- 새로고침 시 결과를 다시 볼 수 없다는 안내

16유형 콘텐츠 초안을 `web/src/data/city_types.json`에 `code`, `nickname`, `description`, `image` 스키마로 작성하고 팀 검수를 병행한다.

검증: 한 사용자가 `인터뷰 → 유형·보고서 확인 → 4축 요구 재생성 → 제출 → 공유 카드`를 완주하며, 제출 응답이 `submission_id`만 반환해도 기존 `type_result` 상태로 공유 카드가 렌더링된다.

이 단계 완료를 실시간 구간 완성과 핵심 인터뷰 MVP 도달로 본다.

완료 시점의 상태는 다음과 같다.

- 유형 합산은 LLM 없이 코드가 결정적으로 수행한다. 반올림은 파이썬 기본 `round`의 은행가 반올림을 피해 `floor(x + 0.5)`로 구현했고, 두 극의 표시 백분율은 승자만 반올림한 뒤 패자를 `100 − 승자`로 유도해 합이 항상 정확히 100이 되게 했다.
- 결과 화면은 3.3의 세 단계로 나뉜다. 유형 코드, 별명, 한 줄 설명, 축별 강도 바, 축별 판정 이유, 축별 요구 보고서를 보여 준다. 유형·강도·판정 이유·요약은 고정이고 수정할 수 없다.
- 요구 수정은 문장 선택과 의견 입력으로만 이루어지며, LLM이 4축 전체를 재생성한다. `summary`와 `type_result`의 불변은 프롬프트 지시가 아니라 코드로 강제한다. 재생성 결과에서 `axis_demands`만 취하고 나머지는 기존 보고서에서 복사한다.
- 제출은 `submission_id`만 반환하고 세션을 메모리에서 제거한다. Firestore 저장은 Phase 4에서 이 자리에 붙인다.
- 공유 카드는 제출 후 서버를 다시 조회하지 않고 프론트가 들고 있던 `type_result`로 렌더링한다.
- 실제 브라우저에서 `개발자 모드로 대화 불러오기 → 마무리 발화 → 종료 → 유형 카드 → 요구 수정 → 제출 → 공유 카드 PNG 생성`을 완주해 확인했다. 번들한 한글 서체가 이미지에 실제로 박히는 것도 확인했다.

시범 전에 살펴야 할 사항을 하나 기록해 둔다. 어떤 축에서 참여자가 반대 극을 한 번도 말하지 않으면 5.3의 공식상 강도가 100%로 계산된다. 데이터로는 옳지만 화면에 100%와 0%가 나란히 표시되면 단정적으로 읽힐 수 있다. 실제 참여자는 대개 양극을 섞어 말하므로 시범운영에서 실제 분포를 보고 표현 방식을 다시 판단한다.

### Phase 4: 저장소와 데이터 계약 (완료)

- 제출본을 Firestore에 저장
- `submissions` 스키마 확정
- `raw_transcript`를 메시지 배열로 저장
- 사용자 메시지와 AI 메시지의 턴 번호 공유
- 메모리 세션에서 Firestore로 전이
- 저장 성공 후 세션 정리
- 제출 응답 후 비식별화 백그라운드 작업 시작

검증: 제출본이 Firestore에 남고 다시 조회되며, 원본 대화록의 각 메시지가 확정 스키마를 따른다.

착수 시 확정한 설계는 다음과 같다.

- **바꾸는 것은 저장 계층뿐이다.** `store.py`의 두 인터페이스에 Firestore 구현을 추가한다. `main.py`, `analysis.py`, `dev.py`는 store만 부르므로 손대지 않는다.
- **어느 구현을 쓸지는 `gcp_project_id`가 정한다.** 값이 있으면 Firestore, 비어 있으면 인메모리다. 별도의 스위치를 만들지 않는 이유는 그 환경변수의 존재 자체가 이미 "이 실행에 Firestore가 있다"는 뜻이기 때문이다. 이 규칙 덕분에 테스트와 GCP 없는 환경이 지금처럼 계속 돈다.
- **문서 ID는 필드에 넣지 않는다.** 인메모리 구현이 읽을 때 ID를 병합하는 것은 Firestore의 문서 ID 의미론을 흉내 낸 것이었으므로, Firestore 구현에서는 실제 문서 ID를 그대로 병합한다. 저장 시에는 `submission_id`와 `run_id`를 본문에서 제외한다.
- **컬렉션 이름은 7.5·7.6의 이름을 그대로 쓴다.** `submissions`와 `analysis_runs`다.
- **`latest()`는 `executed_at` 내림차순 한 건이다.** 인메모리 구현은 마지막에 저장한 것을 기억하지만, Firestore에는 저장 순서라는 개념이 없으므로 시각으로 정렬한다.
- **연결 실패를 감싸지 않는다.** 자격증명이 없거나 데이터베이스가 없으면 그대로 오류가 나야 한다(9.6). 조용히 인메모리로 되돌아가면 제출본이 사라지는데도 성공한 것처럼 보인다.

2026-08-10에 완료했다. 완료 시점의 상태는 다음과 같다.

- 운영 코드 변경은 `api/app/store.py` 하나뿐이다. `main.py`·`analysis.py`·`dev.py`는 한 줄도 손대지 않았다. 저장 계층을 인터페이스 뒤에 숨겨 둔 판단이 의도대로 작동했다는 뜻이다.
- **실제 Firestore로 검증했다.** 전 흐름을 완주해 제출본 2건과 분석 실행 1건을 남기고, 데이터베이스를 직접 조회해 문서 ID가 본문에 중복되지 않았음과 대화록·증거·비식별 사본이 온전한 것을 확인했다. 이어서 **서버를 완전히 내렸다 올린 뒤에도 담당자 목록과 종합 리포트가 그대로 조회되는 것**을 확인했다. 이것이 이 Phase의 존재 이유였다.
- Firestore가 돌려주는 `DatetimeWithNanoseconds`가 목록 정렬과 admin 응답 직렬화를 그대로 통과한다.
- pytest 142개, `pnpm e2e` 54항목이 통과한다.
- 참여자 흐름 중의 세션은 여전히 프로세스 메모리에 있다. 영속화 시점은 제출 하나뿐이라는 4장의 계약은 바뀌지 않았다.

### Phase 5: 후처리 분석 파이프라인 (완료)

- `deidentify.md`를 이용한 LLM 비식별화
- 분석 실행 시 비식별본 누락분만 보충하고 기존 사본 재사용
- 코드 기반 축별 집계
- `aggregate.md`를 이용한 축별 정성 요약
- 대표 인용과 인용 ID
- `analysis_runs` 스키마
- admin HTTP 요청 안에서 분석 배치 동기 실행

참여자 흐름과 분리되어 있으므로 앞 단계와 병렬로 개발·검증할 수 있다.

**GCP 없이 착수할 수 있다.** Phase 6이 만든 `api/app/store.py`의 저장 인터페이스와 인메모리 구현이 이미 제출본을 보관하고, `POST /api/dev/submissions/seed`가 예시 제출본을 적재한다. 파이프라인은 store만 읽으므로 Firestore 없이 완성·검증할 수 있고, Phase 4에서 store 구현만 교체하면 그대로 따라온다. 이전 판에 "제출본이 있어야 하므로 Phase 4에 묶여 있다"고 적었던 것은 store 추상화가 생기기 전의 기록이다.

착수 시 알아야 할 것은 다음과 같다.

- 프롬프트 자산 `deidentify.md`와 `aggregate.md`는 아직 만들지 않았다. 8.2가 요구하는 여섯 개 중 이 둘만 없다.
- `analysis_runs`(7.6)를 담을 저장소가 없다. `store.py`에 같은 방식의 인터페이스를 하나 더 두고 인메모리로 구현한다.
- 프론트가 기대하는 구체 필드 형태는 `web/src/lib/api.ts`의 `AnalysisRun` 타입에 이미 고정돼 있다. 백엔드를 이 형태로 맞추고 7.6에 확정 반영한다.
- 리포트 화면은 이미 다섯 요소를 렌더링할 준비가 되어 있고, 지금은 `latest`가 404라 1~4요소가 빈 상태로 표시된다.
- **집계가 의미를 가지려면 제출본이 여럿이어야 한다.** 저장소의 예시 제출본은 현재 `sion` 하나뿐이다. 집계 산식 자체는 pytest로 검증하고, 실데이터 확인은 6.4의 3인 재생성 시점에 함께 한다.

착수 시 확정한 설계는 다음과 같다. 계약 본문은 3.4·7.5·7.6에 반영했다.

- **모듈 경계.** 파이프라인은 `api/app/analysis.py` 하나에 둔다. 비식별 호출, 코드 집계, 요약 호출, 실행 문서 조립이 모두 여기에 있고 `main.py`는 라우트만 갖는다. 인용 ID 부여도 이 모듈이 한다.
- **저장소.** `store.py`에 `AnalysisStore`(`save`/`latest`)를 같은 방식으로 하나 더 두고 인메모리로 구현한다. Phase 4에서 두 store를 함께 Firestore 구현으로 바꾼다.
- **비식별 시점은 두 곳이고 함수는 하나다.** 제출 직후 FastAPI 백그라운드 작업과 분석 실행의 누락분 보충이 같은 함수를 부른다. 백그라운드 실패는 로그 한 줄로 끝나고 제출은 그대로 유지된다. 다음 분석 실행이 보충하기 때문에 재시도를 만들지 않는다(9.6).
- **비식별 호출은 제출본마다 하나이고 서로 독립이므로 동시에 보낸다.** 한 건이 실패해도 나머지 제출본은 그대로 진행하며, 실패한 제출본은 그 실행의 집계와 인용 후보에서 빠지고 `deidentified`는 `null`로 남는다.
- **집계와 요약의 입력원이 다르다.** 축별 통계와 유형 분포는 원본 `type_result`로 계산하고, 요약과 대표 인용은 비식별본으로만 만든다. 비식별에 실패한 제출본이 통계에는 남고 인용에는 나타나지 않는 것은 의도한 동작이다.
- **테스트는 LLM 없이 검증할 수 있는 곳까지 판다.** 축별 인원수·평균 강도·빈 축 제외·유형 분포·인용 ID 부여·비식별 응답 구조 검증·후보에 없는 `quote_id` 폐기가 대상이다. Gemini 호출 자체는 테스트하지 않는다.

검증: 여러 샘플 제출본으로 축 분포, 요약, 대표 인용이 하나의 admin 요청 안에서 생성된다.

2026-08-10에 완료했다. 완료 시점의 상태는 다음과 같다.

- 파이프라인은 `api/app/analysis.py`에 있고 `main.py`는 라우트만 갖는다. `store.py`에 `AnalysisStore`와 인메모리 구현이 추가됐으며, 제출 직후 비식별화는 FastAPI 백그라운드 작업으로 붙었다. 프롬프트 자산은 이제 8.2가 요구한 여섯 개가 모두 있다.
- 실제 Gemini로 제출본 2건을 한 번의 admin 요청 안에서 처리해 다섯 요소가 모두 채워지는 것을 확인했다. `e2e/shots/11-admin-report-filled.png`가 그 화면이다.
- **비식별화가 실제로 이름과 연락처를 가리는지 별도로 확인했다.** 묶음집에는 개인정보가 없어 검증되지 않는 경로였으므로, 요구 문장과 인용에 사람 이름 둘·전화번호·이메일을 심어 한 번 실행했다. 넷 모두 `[이름]`·`[연락처]`로 바뀌었고 나머지 텍스트와 문장 수는 그대로였다.
- `pnpm e2e` 54항목과 pytest 133개가 통과한다. e2e는 이제 분석 실행을 실제로 수행하므로 제출본 수만큼의 비식별 호출과 요약 호출 한 번을 쓴다. 이 스크립트에서 Gemini를 쓰는 유일한 구간이다.
- 검증에 쓴 두 제출본은 같은 인물(`sion`)이라 축 분포와 대표 인용의 다양성은 확인하지 못했다. 그 확인은 6.4의 3인 재생성 시점에 함께 한다.

### Phase 6: 담당자 웹 (완료)

`/admin`에 다음을 구현한다.

- 제출본 목록
- 제출본 상세
- 유형, 보고서, 원본 대화록
- 다섯 요소의 종합 리포트
- 분석 실행 버튼
- 담당자용 축·유형 해설 정적 자산의 구체적인 파일 위치 결정

착수 시 확정한 설계는 다음과 같다.

- **제출 저장의 store 추상화.** 제출본 저장·조회를 `api/app/store.py`의 저장 인터페이스로 추상화한다. Phase 6에서는 프로세스 메모리 구현을 사용한다. `submit`은 7.5의 `submissions` 문서와 같은 구조(단, `deidentified`는 비움)로 store에 기록한 뒤 메모리 세션을 제거하며, 응답은 기존대로 `submission_id`만 반환한다. 담당자 API는 store만 읽는다. Phase 4에서 같은 인터페이스의 Firestore 구현으로 교체하면 admin 코드는 변경 없이 유지된다. 인메모리 구현은 서버 재시작 시 소멸하며 이는 제출 전 세션과 같은 기준으로 허용한다.
- **분석 API의 단계적 활성화.** `POST /api/admin/analysis/run`은 Phase 5 전까지 501을 반환한다. 종합 리포트 화면은 `latest`가 404이면 다섯 요소 중 1~4(분포·강도·요약·인용)를 빈 상태로 표시하고, 5(정적 해설)는 항상 표시한다.
- **담당자용 축·유형 해설 자산은 `web/src/data/admin_guide.json`.** 네 축 각각의 이름·의미·두 극 설명을 담당자 시점에서 서술한다. 16유형 별명·설명은 기존 `web/src/data/city_types.json`을 재사용하고 이 파일에 중복하지 않는다. 내용은 5.2의 축 정의에서 파생하되 담당자가 리포트를 읽는 데 필요한 설명으로 쓴다.
- **admin 라우트 구성.** `/admin`은 종합 리포트(담당자의 메인 화면)와 분석 실행 버튼, `/admin/submissions`는 제출본 목록, `/admin/submissions/{id}`는 제출본 상세. 담당자가 처음부터 개별 대화를 확인할 필요는 없으므로 집계 리포트를 첫 화면으로 삼는다. 데스크톱 와이드 모니터 기준 레이아웃이며 참여자용 모바일 셸을 쓰지 않는다.
- **제출본 상세의 정보 위계.** 개인 보고서(요약·판정 이유·축별 요구)를 최상단에 두고, 유형 결과(극 점수·증거)를 그 아래, 원본 대화록을 마지막에 둔다. 증거 발화는 전면에 내세우지 않고 접힘(펼쳐 보기) 형태의 보조 정보로 표시한다.
- **16유형 해설은 물음표 아이콘 뒤로.** 종합 리포트의 정적 해설 중 4축 해설은 본문에 표시하고, 16유형 별명·설명 목록은 도움말 아이콘으로 여는 오버레이 안에 둔다.

검증: 개발자 모드로 만든 제출본이 목록·상세에 나타나고, 담당자가 `분석 실행 → 축 분포 → 정성 요약 → 대표 인용 → 원본 맥락`을 추적할 수 있다. 분석 결과 화면의 실데이터 검증은 Phase 5 완료 후 이어서 한다.

2026-07-16에 완료했다. 완료 시점의 상태는 다음과 같다.

- `api/app/store.py`의 저장 인터페이스(save/list/get)와 인메모리 구현을 만들었고, 제출이 7.5 완전본 문서를 store에 기록한 뒤 세션을 지운다. 읽기 시 문서에 `submission_id`를 병합해 돌려주는 방식이 Firestore의 문서 ID 의미론과 같아, Phase 4는 이 인터페이스의 Firestore 구현만 추가하면 된다.
- 담당자 API 4개가 6.2 계약대로 동작한다. revise가 세션 보고서와 `meta.revision_count`를 함께 갱신하므로 저장본은 항상 수정 반영 최신본이다.
- `/admin` 세 화면을 구현했다. 종합 리포트의 정적 해설(다섯째 요소)은 로딩·오류 상태와 무관하게 항상 렌더링된다.
- `web/e2e/smoke.mjs`에 담당자 웹 검사(목록 반영, 상세 렌더링, 리포트 빈 상태·해설·분석 501 안내)를 추가했고, admin 구간은 데스크톱 뷰포트(1280×900)로 전환해 찍는다. 전체 39항목 통과, pytest 91개 통과를 확인했다.
- 참여자 흐름을 완주하지 않고도 담당자 화면을 채울 수 있도록 6.4의 예시 제출본 적재(`POST /api/dev/submissions/seed`, `api/fixtures/submissions/` 3건, dev 전용 "예시 제출본 불러오기" 버튼)를 함께 구현했다. 예시 문서는 실제 파이프라인을 한 번 통과시켜 뜬 것이라 지역 군·구 정규화(부평구·연수구)까지 반영돼 있다.

### Phase 7: 진입과 자격 게이트 (R6에서 사실상 완료 — QR만 Phase 8)

- QR과 링크 진입
- 시스템 개요
- 개인정보 동의
- 수집·보관·파기 고지 문구
- 휴대폰 본인인증 목업
- 출생연도 입력
- 출생연도가 1900~2026 범위의 정수인지 검증
- 목업 인증이 중복참여를 실제로 강제하지 않는 시연용 한계 반영
- Phase 2의 개발용 시작 화면 교체

개인정보 동의와 고지 문구, 시작 화면 통합은 리뷰 라운드 R6에서 시작 화면 안의 체크박스로 구현·완료했다. QR·링크 생성은 원래대로 Phase 8에 남는다.

검증: `진입 → 동의 → 목업 인증 → 인터뷰`가 자연스럽게 연결되고, 출생연도 검증이 적용된다.

### Phase 8: 배포와 시연 준비

- Cloud Run에 `web`과 `api` 배포
- 환경변수 구성
- QR코드 생성
- 외부 스마트폰 전체 흐름 테스트
- 전체 시연 리허설

착수 시 확정한 설계는 다음과 같다.

- **`api` 이미지의 빌드 컨텍스트는 저장소 루트다.** 이미지가 `api/prompts/`(없으면 `axes.py`가 import 시점에 죽는다), `api/fixtures/`(개발자 모드), 그리고 **`api/` 바깥에 있는** `data/plan2040_summary.md`를 모두 담아야 하기 때문이다. 요약본은 `knowledge.py`가 `/data/plan2040_summary.md`에서 찾는다.
- **저장소 루트에 `.gcloudignore`를 둔다.** 이것이 없으면 `gcloud`가 `.gitignore`를 그대로 적용하는데, 계획 요약본은 gitignore 대상이라 조용히 빠진다. 그러면 인터뷰어가 배경 지식 없이 돌면서도 오류는 나지 않아 알아채기 어렵다.
- **CORS 허용 출처를 환경변수로 뺀다.** 지금은 `http://localhost:3000` 리터럴이라 배포된 `web`이 `api`를 부르지 못한다. `web_origin` 환경변수로 주입하고 기본값은 지금 값으로 둔다.
- **배포 순서는 `api` → `web` → `api` 환경변수 갱신이다.** `web`은 빌드 시점에 `NEXT_PUBLIC_API_BASE_URL`이 필요하고 `api`는 `web`의 주소를 알아야 CORS를 열 수 있으므로, 서로의 주소를 아는 시점이 어긋난다. `api`를 먼저 띄워 주소를 얻고, 그 주소로 `web`을 빌드·배포한 뒤, `api`의 `web_origin`만 갱신한다.
- **`api`에 주입할 환경변수**는 `gemini_api_key`, `gcp_project_id`, `dev_mode=true`, `access_code`, `web_origin`이다. Gemini 키는 Secret Manager를 쓰지 않고 환경변수로 둔다. 프로토타입이며 프로젝트 접근자가 개발자뿐이기 때문이다.
- **Firestore 접근 권한은 서비스 계정이 갖는다.** 로컬은 사람의 애플리케이션 기본 자격증명으로 붙지만 Cloud Run에서는 서비스 계정으로 붙는다. 배포 후 실제 제출이 저장되는지로 확인한다.

**도메인을 사지 않는다. Cloud Run 기본 주소를 그대로 쓴다.** 2026-08-10에 사람이 정했다. 참여자 접속은 QR로만 이뤄지므로 주소 길이가 참여자 경험에 영향을 주지 않는다. 도메인은 연 사용료와 갱신 관리가 따르고, 구글 클라우드에서 등록하더라도 등록비는 무료 크레딧 적용 대상이 아니다. 서울 리전은 Cloud Run 도메인 연결을 지원하므로 나중에 마음이 바뀌면 서비스 중단 없이 붙일 수 있고 기존 주소도 함께 살아 있다.

이 결정의 대가는 하나다. **발표 중 주소를 말이나 글로 전달하기 어렵다.** 발표 자료에는 QR을 크게 싣고 주소를 적지 않는 방식으로 다룬다.

**2026-08-10에 두 서비스를 배포했다.** 남은 것은 도메인 연결, QR 생성, 외부 스마트폰 통주 테스트, 시연 리허설이며 앞의 셋은 도메인이 정해져야 진행한다.

- `web`: `https://web-435235910956.asia-northeast3.run.app`
- `api`: `https://api-435235910956.asia-northeast3.run.app`
- 두 서비스 모두 `asia-northeast3`, `min=1`, `max=1`, 미인증 접근 허용이다. 접근 통제는 6.5의 코드가 맡는다.
- 이미지는 Artifact Registry의 `youthplan` 저장소에 있다. **`gcloud run deploy --source`는 쓰지 못한다.** `api`는 빌드 컨텍스트가 저장소 루트여야 하고 `web`은 빌드 인자가 필요한데 그 명령이 둘 다 지원하지 않기 때문이다. 대신 루트의 `cloudbuild.api.yaml`과 `cloudbuild.web.yaml`로 빌드한 뒤 `--image`로 배포한다. 이 두 파일을 지우면 배포 방법이 사라진다.
- 기본 컴퓨트 서비스 계정에 `roles/datastore.user`(Firestore 쓰기)와 `roles/cloudbuild.builds.builder`(빌드)를 부여했다. 신규 GCP 프로젝트는 이 계정에 아무 권한도 주지 않으므로 둘 다 없으면 각각 제출 실패와 빌드 실패로 나타난다.
- **배포본으로 `pnpm e2e`를 통과시켰다**(`WEB_URL`을 배포 주소로 주면 된다). 58항목 전부 통과했고 실제 Gemini와 Firestore로 인터뷰 결과 생성, 제출, 비식별화, 분석 실행까지 돈다.
- 착수 시 발견한 이미지 결함 셋을 함께 고쳤다. `api` 이미지에 `prompts/`가 없어 기동 즉시 죽는 문제, 계획 요약본이 빌드 컨텍스트 밖이던 문제, `web` 이미지가 `public/`을 복사하지 않아 유형 일러스트 16종이 전부 404가 되는 문제다. 마지막 것은 standalone 출력이 `public/`을 포함하지 않기 때문이며 화면이 폴백으로 넘어가 조용히 지나갈 뻔했다.

검증: 외부 스마트폰이 QR로 접속해 공유 카드까지 완주한다.

### Phase 9: 참여자 UX 보완 (2026-08-11 팀 시험 사용, 완료)

팀원들이 배포본을 실제로 써 보고 지적한 것 중 **플랫폼 쪽 문제**를 처리한다. 같은 자리에서 나온 인터뷰 질문 난이도 문제는 AI 튜닝에 속하므로 `TUNING.md` 3.1 (D)에서 다룬다.

- 인터뷰 진행률 퍼센트 표시 (3.2 산식, 4.2 `end` 이벤트)
- 직전 턴 되돌리기 — 생성 중 중단과 완료된 턴 수정 (3.2, 6.1)
- 결과 화면 하단 고정 바와 기기 뒤로가기 (2.4, 3.3)

**하지 않기로 한 것: 백그라운드 이탈 복구.** 갤럭시에서 브라우저를 잠깐 벗어나면 화면이 처음으로 돌아간다는 지적이 있었다. 원인은 안드로이드가 메모리 압박에서 백그라운드 탭을 폐기하는 것이고, **웹 페이지가 자기를 메모리에 붙들어 둘 수 있는 수단은 없다.** 복구하려면 세션 식별자를 브라우저에 남기고 대화록 조회 API를 새로 만들어야 하는데, 이는 "결과는 지금 화면에만 남는다"는 개인정보 안내(10장)와 어긋나고 서버 세션이 프로세스 메모리라는 한계도 그대로 남는다. 2026-08-11에 사람이 **하지 않기로** 정했다. 같은 불만이 누적되면 그때 다시 판단한다.

검증: `pnpm e2e`에 항목을 추가하고 배포본으로 통과시킨다.

**2026-08-11에 구현·검증·배포를 마쳤다.** e2e에 열세 항목을 더해 75항목이 되었고 로컬과 배포본 모두 통과했다. 진행률·중단·수정은 개발자 모드 묶음집으로는 검증할 수 없어(묶음집은 세션을 곧바로 종료 상태로 만든다) **e2e가 실제 Gemini 턴을 한 번 쓴다.** 인터뷰를 타이핑하지 않는다는 원칙의 유일한 예외이며, 이것 없이는 되돌리기가 증명되지 않는다.

착수 후 발견해 함께 고친 것이 하나 있다. **중단 버튼이 폼을 다시 제출시켜 같은 발화가 두 번 전송됐다.** 중단 버튼과 보내기 버튼이 같은 자리에 렌더링되므로 React가 DOM 노드를 새로 만들지 않고 재사용하며 `type`만 `button`에서 `submit`으로 바꾸는데, 브라우저는 클릭의 기본 동작을 이벤트 처리가 끝난 뒤에 평가하기 때문에 그 시점의 `submit`을 보고 폼을 제출한다. `type="button"`을 붙여도 막히지 않으므로 중단 처리에서 기본 동작을 명시적으로 취소한다. **한 자리에서 버튼을 바꿔 끼우는 다른 화면을 만들 때 같은 함정이 재발할 수 있다.**

### Phase 10: 담당자 대시보드 (2026-08-11 착수)

3.5의 종합 리포트를 3.6의 한 화면 대시보드로 교체한다. 디자인은 `dashboard-mock.html`에서 확정했다.

백엔드는 7.7의 집계를 `analysis_runs`에 더한다. 새 데이터를 참여자에게 묻지 않는다. 요구마다 이미 계획 부문 태그가 붙어 있고 거주지·출생연도·유형 코드·턴 수·요약·판정 이유가 모두 제출본에 있으므로, 부문별 집계와 교차표는 **계산만으로** 나온다. 유일한 새 LLM 호출은 `ai_notes`이며 `insight.md`로 분석 실행 안에서 한 번 돈다.

프론트는 `/admin` 첫 화면을 대시보드로 바꾼다. 기존 다섯 섹션 중 축·유형 해설(3.5의 5번)은 화면에서 사라지지 않고 **오른쪽 상세 패널 안으로 들어간다.** 축을 누르면 그 축의 의미와 극별 요구 경향과 대표 인용이 함께 나오고, 유형을 누르면 그 유형으로 판정된 사람이 나온다. 제출본 목록·상세 화면은 그대로 둔다.

**기존 `analysis_runs` 문서에는 새 필드가 없다.** 프론트는 없는 집계를 빈 상태로 그리고, 담당자가 "자료 업데이트"를 누르면 새 실행이 채운다. 마이그레이션은 하지 않는다.

주의할 함정이 둘 있다.

- **자치구 이름이 개편 전 값으로 저장돼 있다.** 시범 데이터에는 "중구", "서구"뿐 아니라 "섬", "인천광역시", 빈 값도 있다. 7.7의 정규화 규칙을 따르고, 확정하지 못한 참여자는 지도에서 빼되 참여자수에는 남긴다.
- **성별 데이터가 없다.** 7.7의 임시 표로 채우며, 이것이 시연 표본 전용이라는 것을 코드 주석과 이 문서 양쪽에 남긴다.

검증: `pnpm e2e`에 대시보드 항목을 추가하고, 배포본에서 분석을 한 번 실행해 `ai_notes`까지 채워지는 것을 확인한다.

**2026-08-11에 구현·검증·배포를 마쳤다.** e2e는 옛 리포트 기준 항목을 걷어내고 대시보드 항목으로 갈아 끼워 80항목이 되었으며 로컬과 배포본 모두 통과했다. 배포본에서 분석을 실행해 참여자 18명·요구 88건 집계와 `ai_notes` 다섯 장이 채워지는 것을 확인했다.

착수 후 발견해 함께 고친 것이 넷 있다.

- **`font` 단축 속성에 `inherit`을 쓰면 선언 전체가 무효가 된다.** 목업 시절부터 버튼 글꼴·굵기·크기가 통째로 적용되지 않고 브라우저 기본값으로 그려지고 있었다. 축약형을 풀어 `font-family`로 상속받게 고쳤다. **한 자리에서 글꼴을 상속시키는 다른 CSS를 쓸 때 같은 함정이 재발한다.**
- **응답 스키마에 `extra="forbid"`를 붙이면 Gemini가 거부한다.** Pydantic이 `additionalProperties`를 내보내는데 Gemini의 `response_schema`가 그 필드를 모른다. `ai_notes`는 실패를 삼키도록 설계돼 있어(3.6) 조용히 빈 값으로 떨어졌고, 실호출로 찔러 보지 않았으면 배포 후에야 드러났을 것이다.
- **집계 수치만 주면 모델이 축 이름을 지어낸다.** 극 글자만으로는 뜻을 알 수 없어 "매력(A)", "전통(H)" 같은 없는 이름이 나왔다. 요약 호출과 같이 축 `display`를 함께 넘기고 `insight.md`에 극 글자의 뜻을 적었다.
- **고정 스테이지는 데이터가 늘면 넘친다.** 목업은 12명·9유형 기준이었고 실데이터 16명·11유형에서 23px이 넘쳤다. 유형 목록은 표본에 따라 계속 길어지므로 카드 안에서 스크롤시키고, 부문 교차표는 아홉 줄 고정이므로 줄 높이를 줄여 항상 들어가게 했다. e2e가 스테이지 넘침을 픽셀로 재는 항목을 갖는다.

### Phase 11: 지도 강조와 제출본 삭제 (2026-08-11, 완료)

**지도에서 고른 자치구의 강조를 지도의 맨 마지막에 그린다.** 그 전에는 브라우저가 그리는 기본 포커스 테두리가 강조 역할을 하고 있었는데, 이 테두리는 도형 자신의 그리기 순서에 묶여 있어 **나중에 그려지는 이웃 자치구와 이름·숫자가 그 위를 덮었다.** 앞쪽에 그려지는 자치구일수록 심하게 가려지는, 자치구마다 다른 증상으로 나타난다. 기본 테두리를 끄고 선택된 도형의 경계 상자를 모든 도형과 글자 뒤에 직접 그린다. 선택과 포커스가 함께 움직이므로 키보드 사용자도 같은 상자를 본다. SVG에서 무엇이 무엇을 덮는가는 z-index가 아니라 문서 순서로 정해진다는 것을 기억한다.

**제출본 삭제**는 3.5의 삭제 모드와 6.2의 `DELETE`로 구현했다. 접근 코드는 헤더와 본문에서 **따로** 검사하며, 코드 대조가 삭제보다 먼저 실행되므로 틀린 코드로는 어떤 경우에도 데이터가 지워지지 않는다. 없는 ID와 틀린 코드가 겹치면 403이 먼저 나가 ID의 존재 여부가 새어 나가지 않는다.

**e2e가 자기가 만든 제출본을 삭제 기능으로 지운다.** 그 전에는 e2e를 돌릴 때마다 실제 제출본이 저장소에 쌓였다(16 → 18건). 이제 마지막 단계에서 이번 실행이 제출한 건을 골라 지우므로, 삭제 기능을 검증하는 동시에 테스트 데이터가 남지 않는다. 틀린 코드를 한 번 넣어 거부되는 것까지 확인하므로 그 403은 실패 집계에서 제외한다.

검증: e2e 89항목이 로컬과 배포본에서 통과했다. 배포본에서 삭제의 네 방어 경로(올바른 코드·틀린 코드·본문 없음·없는 ID)를 실호출로 확인했고 시연용 제출본 18건은 건드리지 않았다.

## 13. 검증과 리스크 관리

시범운영에서는 다음을 측정한다.

- 질문 구성이 자연스러운가
- 막연한 상상이 구체적인 정책 수요로 발전하는가
- 4축을 모두 탐색하는가
- 빈 축 발생률
- **축 간 판정 일치율**: 네 축의 판정 글자가 특정 조합(특히 A-U, C-N)으로 묶이는 비율. 높으면 실효 유형 수가 16보다 작다는 뜻이고, 대응은 축 교체가 아니라 교차 장면 앵커 보강이다
- **단극 관측 발생률**: 축별로 반대 극 증거가 0건인 제출의 비율 (5.5)
- **유도 질문 발생률**: 인터뷰어가 답의 후보를 만들어 내민 턴의 비율 (5.4)
- 유형 별명·일러스트가 참여 보상으로 작동하는가 (공유 여부와 반응)
- 참여자가 유형 판정에 납득하는가
- 요약과 축별 분석이 정확한가
- 비식별화가 완전한가
- 인터뷰 완주율
- 확인·수정 후 제출 비율
- 공유 버튼 사용 여부

효과 검증은 청년 참여자를 A·B 그룹으로 나눠 진행한다.

- A그룹: 일반 설문조사
- B그룹: 유스플랜AI

두 결과를 요약해 전문가가 비교 평가한다.

주요 리스크와 대응은 다음과 같다.

- 인터뷰 환각·유도질문·피상성: 프롬프트 계층, 가드레일, 시범 전 반복 검증
- 빈 축·납득하기 어려운 유형: 빈 축을 정상 상태로 표시, 질문 은행 커버리지 선택, 증거 인용, 채점 앵커 튜닝
- 시연 장애: `api min=1`, 사전 상태 점검, Phase 8 통주 리허설
- 개인정보: 신원 분리, 출생연도만 전달, 비식별화, 로그 원문 배제
- 참여자 이탈: 앱 설치 없는 모바일 진입, 유형 보상, 확인·수정 경험
- 일정 압박: 좁은 MVP, 실시간·배치 분리, 프롬프트 파일 후행 튜닝

## 14. 미결 사항

**AI 품질 결함과 그 처방은 이 장에 없다. 루트의 `TUNING.md`가 SSOT다.** 결함 26건의 확정 원인(파일·행), 프롬프트에 넣을 문장, 두 구조적 딜레마의 결론, 착수 순서와 통과 판정, 검증 설계가 전부 거기 있다. **2026-08-11에 두 프론티어 모델을 전문가로 붙여 5라운드 토론을 시킨 결과이며, 원문 아홉 건이 `docs/tuning-debate/`에 있다.** 원자료는 `docs/interview-lab/`다. 라운드가 끝나면 `TUNING.md`를 이 문서로 통폐합한다.

**그 토론이 이 문서의 계약 세 곳을 고치라고 결론지었다.** 3.2의 커버리지 책임(인터뷰어 → 백엔드), 5.1의 "표시 이름에는 대비가 없어 안전하다"는 판단(틀렸다), 5.5의 강도 표시(빈 축 51과 박빙 51이 값으로 구분되지 않는다). **셋 다 2026-08-12에 적용했다.** 셋째는 `empty_axis`를 참여자 응답까지 실어 보내고 빈 축에서 백분율을 지우는 것으로 닫았다(5.5).

이 장에 남은 것은 **AI 품질이 아닌 미결**과, `TUNING.md`가 참조하는 **실측 기록**이다.

### 호출 분리의 효과 측정 (2026-08-10 구현 완료, 부분 검증)

**2026-08-10에 사람이 직접 12턴을 대화해 부분 검증했다. 아래 "규진 실측"이 결과다.** 이 라운드의 목적이었던 **유도된 답의 증거 오염은 멈췄다.** 남은 문제는 전부 인터뷰어 쪽이며 `TUNING.md` 4.1의 축 인지 문제에 속한다. 아래에 적힌 정량 항목(증거 총 건수, 가중치 분포, 총 턴 수, 캐시 적중률, 사고 토큰)은 인물이 달라 비교되지 않았으므로 **3인 재생성 때 다시 잰다.** 되돌릴 조건에 걸린 것은 없다.

인터뷰어·채점 호출 분리는 구현과 격리 점검이 끝났고 **정량 비교를 위한 재생성이 남았다.** `cd api && PYTHONPATH=. uv run python scripts/regen_fixtures.py sion` 한 줄이며 크레딧을 쓴다. 사람이 시점을 정한다.

**이 재생성은 플랫폼 완성 시점의 3인 재생성과 합쳐서 한 번에 하는 것이 경제적이다.** 6.4가 세 명 전부 재생성을 서비스 완성 후 1회로 미뤄 두었고, Phase 5의 집계 검증도 여러 인물을 필요로 하기 때문이다. 다만 그때는 분리 효과와 인물 차이가 한 번에 관측되므로, 아래 측정 항목을 인물별로 따로 기록해야 한다.

대조군은 아래 "sion 실측"이다. 재생성 후 확인할 항목은 다음과 같다.

- 인터뷰어가 만든 선택지 질문의 발생 건수. 대조군은 15턴 중 2건(8턴, 6턴)이다.
- 축 정의문의 대비가 질문에 섞이는지.
- 선택지에 대한 답이 증거로 태깅되는지. 대조군은 9턴에서 4건이 태깅됐고 그중 하나가 가중치 3이었다.
- 가중치 분포. 대조군은 37건 중 1이 1건, 2가 26건, 3이 10건으로 사실상 2에 몰려 있다.
- 증거 총 건수와 축별 분포, 그리고 **축별 첫 등장 턴**(3.2의 한 축씩 싣기가 제때 도는지).
- 채점 호출 실패율.
- 사고 토큰 총합. 대조군은 호출 15건에서 17,501이며 전체 출력 토큰의 80%를 넘는다. 호출이 둘로 늘면 이 항목이 늘 수 있다.
- 캐시 적중률. 대조군은 입력 360,867 토큰 중 261,291이 적중으로 72%다.
- **총 턴 수.** 인터뷰어가 네 축 탐색 완료를 스스로 판정할 수 없게 되어 종료가 늦어질 수 있다. 대조군은 14턴, 스크립트 상한은 20턴이다.
- `TUNING.md` 7장의 모델 교체 대기 항목들이 프리픽스 축소(-32%)의 간접 효과로 개선되는지. 관찰만 한다.

**되돌릴 조건.** 증거 건수가 대조군 37건의 절반 아래로 떨어지거나, 맥락이 필요한 발화를 놓치는 사례가 반복되거나, 커버리지 지시가 제때 반영되지 않아 축 탐색이 무너지거나, 한 세션 총비용이 대조군의 두 배를 넘으면 단일 호출 복귀를 검토한다. 첫 경우에는 채점 호출에 넘기는 맥락을 늘리는 것을 먼저 시도한다. 지연은 되돌릴 조건이 아니다 — 병렬이므로 체감 지연이 늘면 그것은 구현 오류다.

### 그 밖의 관찰 항목

- **느린 회선에서 첫 접속이 오래 걸린다.** 2026-08-10에 배포본을 실측했다. 전송량 약 2.9MB 중 **한글 서체 하나가 2.06MB**로 70%를 차지한다. 9Mbps에서는 전체 로드 2.2초지만 1.6Mbps에서는 11.3초였고, 사용자가 혼잡한 와이파이에서 겪은 지연과 일치했다(셀룰러에서는 즉시). 서버는 원인이 아니다. HTML 응답은 70밀리초이고 두 서비스 모두 상시 대기 상태다. **화면 자체는 두 회선 모두 1초 안에 뜬다**(버튼 노출 0.3초·0.9초). 서체가 `display: swap`이라 글자는 대체 서체로 먼저 보이고, 늦게 도착한 서체로 바뀌면서 브라우저 로딩 표시가 계속 도는 것이 지연으로 느껴진다. 고치려면 Pretendard 동적 서브셋으로 바꿔 필요한 글자 구간만 받게 하는 방법이 있으나, 9.3의 공유 카드 서체 임베드 검증과 부딪칠 수 있어 함께 설계해야 한다. **시연장 회선이 혼잡할 때 재발할 수 있다는 것만 알아 둔다.**

- **사용되지 않는 세션 상태 값.** `api/app/session.py`의 상태 리터럴에 `"submitted"`가 있는데 어디서도 대입되지 않는다. `submit_result`가 상태를 바꾸는 대신 세션을 즉시 폐기하기 때문이다. 죽은 값이지만 동작에 영향이 없다.
- **File Search 활성화 시의 비용.** 활성화하면 요약본과 별개로 매 턴 검색 청크가 추가되므로 그 시점에 비용을 다시 본다.
- ~~우선 가치 축의 극 이름과 글자, 동률 기본 극, 명칭 "도시 유형"은 팀 검수 대상이다~~ **2026-08-11 팀 검수 완료.** 극 이름·글자·동률 기본 극·명칭 모두 유지로 확정했다. "도시TI" 개명안은 MBTI 분리 원칙(2.1)에 따라 채택하지 않았다. 글자를 바꿀 일이 생기면 `axes.md`의 기계 판독 JSON만 고치면 백엔드는 따라온다.

### 규진 실측 (2026-08-10, 분리 이후 = 부분 검증)

가상 참여자 호출 대신 **사람이 직접 참여자 역할로 12턴을 대화**해 호출 분리의 효과를 봤다. 크레딧을 아끼려는 선택이었고, 그 대가로 대조군과 증거 건수·턴 수를 나란히 비교할 수는 없다(인물이 다르고 자연 종료까지 가지 않았다). 인물은 대조군 `sion`(`ANWH`)의 정반대인 `CUOF`로 잡았다. 여덟 극 중 `C`·`U`·`O`·`F` 네 개는 그때까지 실제 대화에서 한 번도 관측된 적이 없었기 때문이다. 결과는 `CUOF`, 증거 17건, 대화록은 이 세션의 임시 디렉터리에만 있고 묶음집으로 남기지 않았다.

**분리가 고친 것 (이 라운드의 목적).**

- **인터뷰어가 만든 선택지에 대한 답이 증거로 태깅되지 않았다.** 두 번 시험해 두 번 다 배제됐다. 한 번은 "굳이 고르라면 성장 쪽이요"였고, 한 번은 인터뷰어가 제시한 트레이드오프에 "집값 부담을 감수하더라도 해야 한다"고 답한 것이었다. 후자는 여덟 극의 가중치 3 앵커가 요구하는 명시적 포기 절을 갖췄는데도 증거 0건으로 처리됐다. 5.4가 "유도해서 얻은 답은 가중치가 낮아지는 것이 아니라 증거가 아니다"라고 정한 그대로다. 대조군에서는 같은 성격의 답 4건이 태깅됐고 그중 하나가 가중치 3이었다.
- 반대로 참여자가 **자기 항목들 안에서 스스로 고른** 우선순위는 가중치 3으로 정확히 잡혔다. 허용 형태와 금지 형태를 채점자가 실제로 가른다.
- 네 극(`C`·`U`·`O`·`F`)의 앵커가 모두 처음으로 작동했다. 태깅이 축 정의와 어긋난 사례는 없었다.
- 첫 인사가 네 문장으로 줄었고 2040년 나이를 정확히 언급했다(대조군은 여섯 문장). 프리픽스 32% 축소의 간접 효과일 수 있으나 한 번의 관측이다.
- 생활 리듬 축에서 `A` 3 대 `C` 4로 **양극이 모두 관측됐다.** 단극 관측이 구조적으로 불가피한 것은 아니라는 첫 반례다.
- 지역 정규화가 "구월동" → "남동구"로 성공했다. 판정 이유 네 개와 요약 다섯 문장에 지어낸 내용이 없었다.

**분리가 고치지 못한 것.** 아래는 모두 인터뷰어 쪽이며 `TUNING.md` 4.1의 "축 인지 문제"에 속한다.

- **커버리지 운영 지시를 두 턴 연속 이행하지 않았다.** 지시는 두 번 다 "발전 가치 축의 증거가 부족하니 더 탐색할 것"이었는데 인터뷰어는 우선 가치를 팠다.
- **축의 표시 이름을 다른 축의 대립으로 오독했다.** "발전 가치"를 받고 성장이냐 분배냐를 물었다. `axes.md` 분리 시점에 예측한 것은 표시 이름에서 그 축의 두 극이 역산되는 것이었는데, 실제로는 **엉뚱한 축의 이지선다**가 생성됐다. 지시가 커버리지를 돕는 것이 아니라 오도한다.
- **이지선다 생성 자체는 재발했다.** 6턴에서 "복지나 나눔에도 비슷한 무게를 두어야 한다는 의견에 어떻게 생각하시나요, 격차를 감수해야 한다고 보시는지 아니면 다른 생각이 있으신지"를 물었다. 축 정의 어휘가 질문에 그대로 나왔다. 다만 그 답이 증거가 되지 않았으므로 판정 오염으로 이어지지는 않았다.
- **인터뷰어가 스스로 반대 극을 캐물었다.** 세 턴 연속으로 우선 가치의 `W` 쪽을 유도했다("도움이 꼭 필요한 순간이 온다면"). 채점자에게는 균형 맞추기 충동을 5.4의 한정 문단으로 막아 두었는데 인터뷰어에게는 같은 장치가 없다. 5.5가 커버리지 힌트를 극 단위로 내리지 않기로 한 이유가 그대로 인터뷰어의 자발 행동으로 나타났다.
- **한 턴 두 질문**이 12턴 중 최소 6턴이었다. "혹은"으로 두 질문을 잇는 형태가 반복된다.
- **참여자가 쓰지 않은 표현을 되짚기에 넣었다.** 이번에는 `'조용하고 방해받지 않는 주말'`처럼 **따옴표까지 붙여** 참여자의 말인 것처럼 인용했다. 참여자는 "조용한"이라고 말한 적이 없다. 대조군보다 나빠진 형태다.
- 루브릭 주말 블록 문항의 유도성이 재발했다("그대로 남아있었으면 하는 것이 있을까요").

**새로 관찰된 것.**

- 같은 한 문장이 서로 다른 두 축에 동시에 태깅됐다(행정 앱 발화가 `OW O`와 `FH F`로). 5.3 원칙 1(축 간 상호배타)과 부딪친다. 두 축의 판별 규칙이 이 장면을 가르지 못한다.
- 명백히 `F` 취지인 발화("옛 정취는 관광객한테나 좋은 거지")가 태깅되지 않았다. 누적 증거 중복 방지의 부작용인지 단순 누락인지는 한 번의 관측으로 판단할 수 없다.

### sion 실측 (2026-08-10, 분리 이전 = 대조군)

모델 `gemini-3-flash-preview`, 프리픽스 37,155자, 목표 15턴으로 실행했다. 14턴에 정상 종료, 증거 37건, 유형 코드 `ANWH`.

- 생활 리듬 `A` 100% (A 22 : C 0), 선호 공간 `N` 100% (U 0 : N 23), 우선 가치 `W` 76% (O 4 : W 13), 발전 가치 `H` 81% (F 4 : H 17). 인물이 우선·발전 가치에서 양가적으로 설계되어 있었고 실제로 그 두 축에서만 양극이 관측됐다.
- 토큰은 위 "최우선" 절에 적었다. 가상 참여자 호출은 `log_event`를 거치지 않아 집계에 없으므로 실제 비용은 이보다 크다.
- **잘 작동한 것**: "턴" 같은 내부 용어가 참여자에게 노출되지 않았다. 트레일러 형식이 15턴 내내 깨지지 않았다. 보고서의 축 대응이 네 축 모두 정확했다(다만 이 참여자는 발화 안에 대비를 직접 담았으므로 모델이 축의 반대편을 지어낼 필요가 없었다). 2040년 나이 계산이 정확했다.
- **운영 지시의 미탐색 축 분기는 발동하지 않았다.** 4턴 만에 네 축이 다 채워졌다. 이 경로가 대조군에서 실행된 적이 없다는 것이 3.2의 한 축씩 싣기를 같은 라운드에서 함께 고쳐도 측정이 오염되지 않는다고 판단한 근거다.
- **위반**: 선택지 제시 2건, 그에 대한 답의 증거 태깅 4건, 한 턴 두 질문 8턴 이상, 감탄·칭찬 다수, 첫 인사 여섯 문장, 지역 정규화 오류 1건.

이 실측으로 유형화 개편(2026-07-29) 이후의 미결 세 가지가 해소되거나 확인됐다. 묶음집 무효 상태는 `sion` 재생성으로 해소됐고(구 파일 여섯 개 삭제), 2라운드 프롬프트 수정분은 검증됐으며(감탄·덕담과 선택지 제시가 모두 재발), 단극 관측이 구조적으로 잦다는 우려는 두 축에서 확인됐다. 발전 가치 축 블록 교체의 예방적 판단만 여전히 미확인이다.

### 그 밖

- **최종 일러스트 16종은 사용자가 직접 제작해 삽입한다.** 개발은 `web/public/city-types/*.svg` 플레이스홀더로 완결했다.
- 16유형 별명은 2026-08-11 팀 검수로 확정했다(5.6). 한 줄 설명은 새 별명 기조의 Claude 초안이며 팀 재검수 대상이다.
- 주제 태그 고정 어휘 9종(7.4)은 초안이며 시범운영에서 실제 요구 분포를 보고 조정한다.
- 인터뷰어 이름은 임시로 "하늘"을 사용하며 추후 마크다운 파일에서 교체한다. 시뮬레이션 인물 이름이 이와 겹치지 않게 한다.
- 특정 회사·세부 지역 같은 준식별자 처리 기준은 베타 테스트에서 정한다.
- 개인정보 수집·이용 고지 문구는 시작 화면에 초안을 넣었으며 팀 회의에서 확정한다.
- 담당자용 축·유형 해설의 위치는 `web/src/data/admin_guide.json`으로 확정했다(Phase 6). 내용 문구는 팀 검수 대상이다.
- 참여자가 브라우저를 닫으면 이탈 세션이 프로세스 메모리에 남는다(DELETE 미수신). 시범 규모(약 10명)에서는 실해가 없으므로 TTL 스윕은 만들지 않고, Cloud Run 장기 운영 시점에 재평가한다.
- `api/app/report.py`, `api/app/tagging.py`, `web/src/app/globals.css`가 포매터 검사(`ruff format --check`, `prettier --check`)에 걸린다. 유형화 개편 이전부터 있던 것이라 무관한 포매팅을 이번 변경에 섞지 않으려고 그대로 두었다. 게이트인 `ruff check`와 `eslint`는 통과한다.

## 15. 현재 진행 상황

새 세션이 이 절만 읽고도 이어받을 수 있도록 적는다. 기준일은 2026-08-12이다.

### 2026-08-12 AI 품질 튜닝 라운드

12인 실험(`docs/interview-lab/`)에서 나온 결함 26건의 처방을 적용했다. 원인 분석과 근거는 `TUNING.md`에 있고, 여기에는 **무엇이 바뀌었고 무엇이 남았는지**만 적는다.

**설계에서 바뀐 것 여섯 가지.**

1. **인터뷰어는 축을 영영 모른다.** 운영 지시가 축 표시 이름을 싣던 것을 없애고, 검수된 질문 문장 하나를 통째로 내려보낸다(3.2, 5.2, 5.5).
2. **백엔드가 턴 일정 전체를 소유한다.** 도입 3턴, 은행 질문 구간, 정리 4단계가 코드에 있다. 커버리지는 질문 선택 신호일 뿐 종료 조건이 아니다(3.2).
3. **종료 자격은 백엔드 소유다.** 자격 없는 `[[END_INTERVIEW]]`는 문장 형태와 무관하게 제거된다(3.2).
4. **악성 처리는 판정과 카운트를 나눴다.** 모델이 신호만 내고 횟수와 고정 문구는 백엔드가 소유한다(3.2).
5. **빈 축이 정상 상태가 됐다.** 보고서의 축별 요구가 빈 배열일 수 있고, 근거 없는 축의 판정 이유는 코드가 채운다(3.3, 7.4).
6. **지역 정규화가 코드로 내려왔다.** LLM은 참여자가 말한 그대로만 돌려주고 버전 있는 별칭 표가 자치구를 정한다(7.4, 7.7).

**측정된 것.**

- 인터뷰어 프리픽스 25,376자 → **7,626자(70.0% 감소)**. 계획 요약본 제거가 절반, 두 프롬프트 감량이 나머지다.
- `system.md` 152행 → 110행, `rubric.md` 97행 → 71행. 합계 249행 → 181행.
- 정적 검사(`api/scripts/tuning/prefix_check.py`)에서 채점 어휘·축 이름·삭제 대상 예문 잔존 0건.
- 수정 전 기준선은 `docs/interview-lab/baseline-metrics.json`에 얼려 두었다. 인터뷰어 181턴 기준 복합 답칸 55.3%(사람 라벨 50.8%), 되짚기 80.1%, 극 어휘 54.1%, 추상어 42.0%, 거절 뒤 재질문 27건, 마지막 답 기회 6/12.

**튜닝 도구는 `api/scripts/tuning/`에 있다.** 라운드마다 재사용한다.

- `corpus.py`: 대화록 기록 12건을 기계 판독 코퍼스로 변환
- `metrics.py`: 호출 0회로 인터뷰어 행동 지표 산출
- `prefix_check.py`: 프리픽스 크기와 금지 어휘 잔존 검사
- `rehearse.py`: 가상 주민 페르소나와 전 구간 리허설. 백엔드 계약은 실제 코드이고 세 모델 호출만 로컬 CLI로 우회하므로 인터뷰 제공자 크레딧을 쓰지 않는다

### 어디까지 됐나

**Phase 0부터 3까지 완료했다.** 핵심 인터뷰 MVP가 실제로 동작한다. 참여자가 인터뷰를 완주하면 코드가 유형을 판정하고, LLM이 개인 보고서를 만들고, 참여자가 요구를 수정하고 제출한 뒤 유형 카드를 이미지로 저장하거나 공유하는 흐름이 실제 Gemini로 끝까지 돌아간다.

Phase 3 이후 실사용 검토에서 나온 개선도 모두 반영했다. 각각의 계약은 해당 절에 적혀 있다.

- 인터뷰가 끝나도 화면이 자동으로 넘어가지 않고 "대화 종료하기" 버튼을 기다린다 (3.2)
- 결과 화면을 세 단계로 나눴다. 한 화면에 몰아넣으니 스크롤이 지나치게 길었다 (3.3)
- 판정 근거로 참여자 발화를 다시 인용하지 않고, 축마다 왜 그 글자인지를 설명한다 (3.3, 7.4)
- 보고서 생성은 압축 없이 원본 대화록을 한 번에 구조화한다 (3.3, 8.2)
- 참여자 응답에서는 서버 보관용 증거·점수·인용 원문을 제외한다 (6.1, 7.3, 7.4)
- 시작 화면에 서비스 설명과 본인인증 버튼을 두었다 (2.4)
- 개발자 모드는 대화를 불러오는 즉시 세션을 종료 상태로 둔다 (6.4)

**2026-07-15 리뷰/리팩토링 라운드도 완료했다.** 새 세션이 반드시 알아야 할 바뀐 점은 다음과 같다.

- 결과 생성이 압축 없이 원본 대화록(`turn`·`role`·`text`만)을 **단일 구조화 호출**로 처리한다. `compression.md`는 삭제됐고 프롬프트 자산은 여섯 개다. 결과 대기는 약 15초.
- 참여자에게 가는 `/result`·`/revise` 응답은 슬림 사본이다. `type_result`의 `evidence`·`scores`와 보고서의 `quotes`는 서버 세션·Firestore 완전본에만 있고 응답에서는 빠진다 (6.1, 7.3, 7.4).
- 축·극·기본극·표시명 상수의 단일 원천은 `api/prompts/axes.md`의 기계 판독 JSON 블록이며 `api/app/axes.py`가 파싱한다. `scoring`·`trailer`·`prompts`·`session`이 이 모듈만 참조한다 (5.2).
- 참여자 화면의 축 극 라벨은 일상어이고 `web/src/lib/city-axes.ts`가 원천이다. 5.2의 축 정의·채점 앵커와는 별개의 표시 문구다 (5.4).
- revise LLM 출력은 `axis_demands`만 담고 나머지는 서버가 기존 보고서에서 복사한다. 재생성 입력도 슬림하다 (7.4).
- 비식별화는 분석 실행 시점이 아니라 제출 직후 서버 백그라운드로 수행해 `deidentified`를 채우고, 분석 실행은 누락분만 보충한다 — 계약만 확정했고 구현은 Phase 4·5다 (3.4, 7.5).
- 로딩 화면은 응답 도착 시 진행바가 실제로 100%까지 찬 뒤 결과로 넘어가고 중앙 별이 스텝 회전한다 (3.3).
- **Phase 7 진입 게이트가 사실상 완료됐다.** 시작 화면에 서비스 소개·출생연도 입력·개인정보 수집·이용 고지와 동의 체크박스·본인인증 목업이 있고, 체크해야 버튼이 활성화된다 (3.1). 남은 것은 QR·링크 생성(Phase 8)과 고지 문구 확정(2026-07-17 회의)뿐이다.

**2026-07-29에 도시 유형화 파트를 전면 개편했다.** 새 세션이 반드시 알아야 할 내용이며 상세 계약은 5장에 있다.

- MBTI 글자 차용(E/I·S/N·T/F·J/P)을 폐기하고 일상어 4축으로 교체했다: 생활 리듬(`AC`), 선호 공간(`UN`), 우선 가치(`OW`), 발전 가치(`FH`). 유형 코드는 `AUOF`~`CNWH` 16종이다.
- **참여자에게 4글자 코드를 완전히 숨긴다.** 결과 화면·공유 카드·저장 이미지·파일명·공유 시트 어디에도 코드와 극 글자가 없다. 대신 유형 일러스트 + 별명 + 일상어 라벨 배지 4개가 카드의 주인공이다. 담당자 화면과 내부 저장은 코드 기준을 그대로 유지한다.
- 축 상수의 하드코딩을 전부 걷어냈다. `axes.py`의 고정 축 순서, `report.py`의 축 `Literal`과 순서 검사, `main.py`의 축 `Literal`이 모두 `axes.md`의 기계 판독 JSON에서 파생된다. 여덟 극 글자의 전역 유일성 검증을 새로 넣었다.
- 요구 항목에 정책 주제 태그(`topics`)를 추가했다. 고정 어휘 9종이며 참여자 응답에서는 `quotes`와 함께 제외된다. 집계는 Phase 5의 몫이다.
- 단극 관측 표시 보정(5.5)을 넣었다. 저장값은 불변이고 표시만 95로 클램프하며, 담당자 상세에는 "단극 관측" 표기가 붙는다.
- **실제 Gemini로 세 인물을 완주시켜 실측했다.** 교차형 인물이 `ANWH`로 나와 "활기찬 = 도시적인" 상관 쏠림이 끊기는 것을 확인했고, 구 T극 블라인드스팟이던 행정 효율 발화가 `O` 증거로 잡히는 것도 확인했다. 반면 우선 가치 축의 이지선다가 3건 중 2건에서 재발해 프롬프트를 두 라운드에 걸쳐 고쳤다.

**2026-08-10에 AI 파트를 두 라운드에 걸쳐 손봤다. 이것이 가장 최근 작업이다.**

먼저 프롬프트 자산을 정리하고 묶음집을 새 축으로 재생성했다. 구 4축 묶음집 여섯 개를 지우고 `sion` 하나를 실제 Gemini로 완주시켰다(14장 "sion 실측"). 그 실측에서 인터뷰어가 답의 후보를 지어내 내미는 위반이 재발했고, **그 답이 증거로 태깅되어 유형 판정까지 오염되는 것**이 확인됐다. 격리 점검은 유혹이 네 축 전부에서 일어나며 억제 선언은 응답 열 개 중 두 번만 회상된다고 보고했다.

그래서 **인터뷰 한 턴을 인터뷰어 호출과 채점 호출 둘로 나눴다.** 새 세션이 반드시 알아야 할 것은 다음과 같다.

- 인터뷰어는 `axes.md`를 받지 않는다. 프리픽스는 `rubric.md` · `system.md` · 참여자 나이 순서이며 37,155자에서 25,376자로, 다시 7,626자로 줄었다(3.2, 8.4).
- **`[[EVIDENCE]]` 머신 트레일러는 폐지했다.** 인터뷰어 응답은 대화 텍스트와 종료 제어 토큰뿐이다(4.2).
- 채점은 `axes.md`만 시스템 지시로 받는 별도 호출이 하며, `api/app/tagging.py`가 담당한다. 입력은 `transcript`·`evidence_log`·`participant_utterance` 세 키를 가진 JSON이고 출력은 `response_schema`로 강제한 증거 배열이다(5.4).
- 두 호출은 참여자 발화가 도착하는 순간 **동시에** 출발한다. 체감 지연은 늘지 않는다. 채점 실패는 그 턴 증거 0건과 로그 한 줄로 끝나며 대화를 죽이지 않는다.
- 누적 증거를 채점자에게 넘기는 목적은 **중복 태깅 방지 하나로 한정**하고 그 한정을 `axes.md`에 명시했다. 균형 맞추기 충동이 실측으로 확인됐기 때문이다(5.4).
- `[운영 지시: ...]` 블록은 이제 **한 턴에 축을 하나만** 싣고, 같은 수준의 축이 여럿이면 턴 번호로 회전한다(3.2).
- 프롬프트 작업의 상시 규칙(8.5)과 격리 점검 절차(8.6)를 계약으로 승격했다. 프롬프트를 고칠 때 반드시 읽는다.

**아직 실제 Gemini 재생성으로 검증하지 않았다. 14장 "호출 분리의 효과 측정"이 무엇을 재야 하는지와 되돌릴 조건을 적어 두었다.**

**2026-08-10에 Phase 5(후처리 분석 파이프라인)를 완료했다. 이것이 가장 최근 작업이다.** 담당자 종합 리포트의 다섯 요소가 전부 실데이터로 찬다. 새 세션이 알아야 할 것은 다음과 같다.

- 파이프라인은 `api/app/analysis.py` 하나에 있다. 비식별 호출, 코드 집계, 요약 호출, 인용 ID 부여, 실행 문서 조립이 모두 여기에 있고 `main.py`는 라우트만 갖는다.
- **인용 ID는 코드가 부여한다.** 모델은 주어진 후보 중에서 고르기만 하고, 후보에 없는 ID를 돌려주면 그 항목만 버린다(3.4).
- **빈 축은 그 축의 집계와 요약 어디에도 들어가지 않는다.** 기본 극 51%는 판정이 아니라 채움값이기 때문이다. `type_distribution`만 모든 제출본을 센다(3.4).
- 비식별화는 제출 직후 백그라운드와 분석 실행의 누락분 보충 두 곳에서 같은 함수를 부른다. 한 건이 실패해도 로그 한 줄로 끝나고 나머지 제출본과 실행은 그대로 진행된다. 실패한 제출본은 통계에는 남고 인용 후보에서만 빠진다.
- `pnpm e2e`가 이제 분석 실행까지 검사한다. 이 스크립트에서 Gemini를 쓰는 유일한 구간이며 제출본 수만큼의 비식별 호출과 요약 호출 한 번이 든다.

**같은 날 GCP 준비와 Phase 4(Firestore)까지 마쳤다. 이것이 가장 최근 작업이다.**

- GCP 프로젝트 준비 상태는 아래 "막혀 있는 것"에 적었다. 프로젝트 ID는 `.env`의 `gcp_project_id`에 있다.
- 저장소 구현은 `gcp_project_id`가 있으면 Firestore, 없으면 인메모리다. 그래서 GCP 자격증명이 없는 환경에서도 테스트와 개발이 지금처럼 돈다. 별도 스위치는 만들지 않았다(12장 Phase 4).
- **개발 서버를 띄우면 이제 실제 Firestore에 쓴다.** 개발자 모드로 만든 제출본과 예시 제출본도 그대로 남으므로, 지저분해지면 사람이 콘솔이나 스크립트로 지운다. 자동 정리는 만들지 않았다.

**이어서 6.5의 접근 코드를 구현했다.** 개발자 모드와 담당자 웹이 하나의 코드로 열린다. 검사는 `main.py`의 HTTP 미들웨어가 하므로 나중에 두 접두사 아래에 라우트를 더 붙여도 자동으로 보호된다. 프론트는 `web/src/lib/api.ts`의 `call` 하나로 모든 호출을 모아 경로를 보고 헤더를 붙이므로 어느 화면도 붙이는 것을 잊을 수 없다. 코드는 `sessionStorage`에만 있고 403이 오면 지워진다. `NEXT_PUBLIC_DEV_MODE`는 없앴다. 실제 코드 값은 `.env`에만 있고 이 문서에 적지 않는다. `curl`로 게이트를 직접 확인했고(코드 없음·틀린 코드 403, 맞는 코드 200, 참여자 API는 코드 없이 200), `pnpm e2e`는 58항목으로 늘었다.

**2026-07-16에 Phase 6(담당자 웹)을 완료했다.** 제출이 `api/app/store.py`의 저장 인터페이스(Phase 6은 인메모리, Phase 4에서 Firestore로 교체)에 완전본을 기록하고, `/admin`(종합 리포트, 담당자 메인)·`/admin/submissions`(목록)·`/admin/submissions/{id}`(상세)가 담당자 API 4개를 소비한다. 분석 실행은 Phase 5 전까지 501이고 리포트 1~4요소는 빈 상태, 5요소(축 해설은 본문, 16유형은 도움말 오버레이)는 항상 표시된다. 같은 날 사용자 피드백 라운드로 리포트 메인화, 와이드 폭, 상세 위계 재배치(보고서 → 유형 → 대화록, 증거 접힘), `structuring.md`의 지역 군·구 정규화(7.4)까지 반영했다. 자세한 완료 상태는 12장 Phase 6을 본다.

### 시작하는 법

개발 서버는 두 개를 띄운다. 루트의 `./dev.sh` 한 줄이 둘 다 띄운다(시작 전에 8000·3000 포트를 정리하고, Ctrl+C 한 번에 함께 종료).

```
./dev.sh
```

개별로 띄우려면 다음과 같다.

```
cd api && uv run uvicorn app.main:app --port 8000
cd web && pnpm dev
```

브라우저에서 `http://localhost:3000`으로 접속한다. UI를 확인할 때는 인터뷰를 직접 타이핑하지 말고 6.4의 개발자 모드를 쓴다. 검증 방법은 `CLAUDE.md`의 "UI 검증 방법"에 적혀 있다. `cd web && pnpm e2e`가 전 흐름을 검사하고 `web/e2e/shots/`에 스크린샷을 남긴다. 백엔드 로직은 `cd api && uv run pytest`로 검증한다(테스트는 gitignore된 로컬 자산이며 9.5 참조).

### 다음에 할 일

**2026-08-10에 12인 인터뷰 실험을 마쳤다.** 배포 서버에 실데이터 12건이 쌓였고 담당자 리포트가 9개 유형·여덟 극 전부로 채워졌다. 결함 목록은 `docs/interview-lab/00-종합.md`에 있으며 **하나도 고치지 않았다.** 같은 조건에서 비교하기 위해 기록만 했고, 결함을 정리한 것이 `TUNING.md`다. 예시 대화에 `taeyoung`을 추가했다.

**2026-08-11에 팀원들이 배포본을 써 보고 여섯 가지를 지적했다.** 다섯은 플랫폼 문제라 Phase 9로 묶었고(12장), 하나는 인터뷰 질문이 배경지식을 요구한다는 것이라 `TUNING.md` 3.1 (D)로 넘겼다. 백그라운드 이탈 복구는 하지 않기로 정했으며 이유는 Phase 9에 적혀 있다. **Phase 9는 같은 날 구현·검증·배포까지 끝났다.**

**배포까지 끝났다. 남은 것은 시연 준비다.**

1. 배포된 `web` 주소로 QR을 만든다. 도메인은 사지 않기로 했다(12장 Phase 8).
2. 실제 스마트폰으로 QR 접속부터 공유 카드까지 통주한다. 지금까지의 검증은 전부 헤드리스 브라우저였다.
3. 시연 리허설.

3인 묶음집 재생성을 돌리면 호출 분리의 정량 비교와 집계의 실데이터 검증이 한 번에 처리되는데, **프롬프트를 고치고 나면 그 비교는 불가능해진다.** 순서를 정하는 판단이 `TUNING.md` 6장에 있다.

**재배포 방법**은 12장 Phase 8에 있다. `gcloud run deploy --source`가 아니라 `cloudbuild.*.yaml`을 거친다는 점을 반드시 확인한다.

### AI 튜닝 재개 (2026-08-11 결정)

**2026-08-10에 보류했던 AI 품질 튜닝을 재개한다.** 보류 이유는 일정이었고, 플랫폼을 먼저 끝내기로 했던 조건(Phase 5 완료, 배포)이 전부 충족됐다.

**결함 목록·구조적 딜레마·착수 순서의 SSOT는 루트의 `TUNING.md`다.** 12인 인터뷰 실험에서 나온 것과 이전 라운드의 미해결 항목을 합쳐 원인별로 묶었고, 이미 해결된 것과 API 조건 때문에 생긴 착시는 걷어냈다. 라운드가 끝나면 이 문서로 통폐합한다.

두 가지를 반드시 함께 인지한다.

- **호출 분리는 구현이 끝났지만 실제 Gemini로 한 세션을 완주시킨 적이 없다.** 코드 게이트와 격리 점검은 모두 통과했다. 시연 전에는 반드시 한 번 돌려야 한다. 개발자 모드로 결과·보고서·담당자 화면을 확인하는 흐름은 저장된 묶음집을 쓰므로 영향받지 않는다.
- **프롬프트를 고치면 호출 분리의 정량 비교는 영영 못 한다.** 대조군이 지금 프롬프트를 전제하기 때문이다. 재생성을 먼저 돌릴지 비교를 포기할지는 사람이 정하며 판단 재료가 `TUNING.md` 6장에 있다.

### 막혀 있는 것

- ~~GCP 프로젝트~~ **2026-08-10에 준비를 마쳤다.** 프로젝트를 만들고 결제를 연결했으며, `gcloud` CLI 설치와 두 가지 로그인(명령어용과 애플리케이션 기본 자격증명)을 끝냈다. Firestore·Cloud Run·Artifact Registry·Cloud Build API를 켰고, Firestore Native 데이터베이스를 `asia-northeast3`에 만들었다(무료 등급). 프로젝트 ID는 `.env`의 `gcp_project_id`에 있다. `api/app/firestore.py`의 클라이언트로 쓰기·읽기·삭제가 실제로 되는 것까지 확인했다. **Phase 4와 8을 막는 것은 이제 없다.**
- **도시기본계획 File Search 외부 활성화**가 남았다. 정본과 요약본은 준비됐으며 요약본은 즉시 로드된다. GCP 설정 후 `api/scripts/upload_plan.py`를 실행하고 스토어 이름을 `.env`에 넣으면 정본 검색도 코드 변경 없이 활성화된다.
- **README**는 아직 한 줄짜리다. 마지막에 작성하기로 했다.

## 16. 문서 유지 규칙

- 이 문서는 상세 API, 스키마, 상수, 축 정의를 포함한 SSOT다.
- 기능 추가·변경·의사결정은 코드보다 먼저 이 문서에 반영한다.
- 각 Phase 종료 시 문서 상태와 관련 계약을 갱신한다.
